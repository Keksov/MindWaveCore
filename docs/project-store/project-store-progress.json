{
  "schemaVersion": 1,
  "plan": "project-store",
  "planDoc": "docs/project-store/project-store-plan.md",
  "title": "Project entity: per-file editor data (curve colours, waves, undo journal) in a per-project folder (.scp.json) -- service class + API first",
  "status": "in-progress",
  "createdAt": "2026-07-15",
  "updatedAt": "2026-07-17",
  "decisions": [
    {
      "id": "PR-D1",
      "text": "Core is a server-side service ProjectStore in MindWaveCore/server/project-store.ts (host-level infrastructure, not Gnaural-specific), following the GnauralEditorStore archetype: interface + Impl + createProjectStore factory, ProjectStoreError extends Error {status} + mapProjectStoreError, singleton wired in server.ts next to archiveStore/gnauralEditorStore. UI client lives with its consumers: GnauralCore/ui/project-api.ts + composables/use-project.ts, modelled on audio-api.ts."
    },
    {
      "id": "PR-D2",
      "text": "Project identity: normalized absolute source path -> deterministic folder name <slug>-<hash8>. slug = sanitized basename without extension (Windows-forbidden chars -> _, ~40 chars cap); hash8 = first 8 hex of sha1(resolve(path).toLowerCase()) -- same normalization as withFileLock. project.scp.json stores source.path + fingerprint (size, mtime) for status checks and re-link. No global index: listing = scan of projects/*/project.scp.json."
    },
    {
      "id": "PR-D3",
      "text": "REVISED owner 2026-07-15 (req 11): Project folder layout: <userDataRoot>/projects/<slug>-<hash8>/ containing project.scp.json (the main file, text, pretty JSON, req 4) and undo.json (text, separate file because it is large and written often). NO cache/ subfolder -- caches stay central (Q4), so the project folder is text-only and req 5 holds trivially. Main file name is fixed; human readability is carried by the folder name and source.path, which survives source renames."
    },
    {
      "id": "PR-D4",
      "text": "project.scp.json schema: { schemaVersion: 1, kind: 'SoundCoreProject', source, createdAt, updatedAt, sections: { <name>: opaque JSON } }. The core does not know section contents -- each section is owned by its subsystem (req 2); unknown sections are preserved on rewrite (forward compatibility). Writes are atomic (tmp+rename); reads are tolerant: a corrupt file is renamed to project.scp.json.broken-<ts>, store restarts empty, incident is logged -- never silently lost."
    },
    {
      "id": "PR-D5",
      "text": "API-first (req 7). Phase 1 delivers the full core interface: openProject (find-or-create + folder provisioning; falls back to a source.path scan when the hash dir does not match, e.g. after relink), getProject, listProjects (scan + source status), getSection/putSection (atomic, coalesced), getUndoJournal/putUndoJournal (limits), deleteProject, relinkProject; exportProject/importProject arrive in Phase 5. No cache API (PR-D9 revised). On top: REST /api/projects* with the {error} envelope, Project* types in SharedPasCore/ts/protocol.ts, UI client project-api.ts, composable use-project.ts (reactive sections: debounce, flush on file switch). Server subsystems call ProjectStore directly (DI of the singleton)."
    },
    {
      "id": "PR-D6",
      "text": "LOCKED owner 2026-07-15 (req 8): User data folder: key user_data_root in the SQLite app_settings table, default on Windows %LOCALAPPDATA%\\KKSoundCore (fallback without LOCALAPPDATA: ~/.kksoundcore for non-Windows dev); projects under <root>/projects/. Editable in editor settings (req 3), validated like presetsRoot (directory must exist); changing the folder migrates projects (PR3.2)."
    },
    {
      "id": "PR-D7",
      "text": "Write discipline: client debounces ~500ms per section + immediate flush on file switch/unload; server keeps a per-project sequential write queue (withFileLock analogue). No cross-tab conflict detection: last-write-wins per section (sections are small and independent -- risk R2, accepted)."
    },
    {
      "id": "PR-D8",
      "text": "Undo journal (req 1): GTrackSchedule snapshots are already JSON-serializable (signature() = JSON.stringify), so the journal persists to undo.json with limits (~50 entries and/or ~5 MB), deferred writes (on activity pause), restore on file open. The core ships only the get/put transport (PR1.3); GTrackModel integration is PR2.2."
    },
    {
      "id": "PR-D9",
      "text": "REVISED owner 2026-07-15 (Q4/req 11): caches stay CENTRAL (tmp/ + manifest) -- otherwise the UI may not know about them when projects live in arbitrary places. The Project entity has NO cache API and no cache/ folder; per-file caches keep using the central manifest keyed by source fingerprint (sha1(path+size+mtime+kind)). Waveform peaks cache (PR6.1) also goes to the central cache; PR6.2 (moving the render cache into projects) is DROPPED."
    },
    {
      "id": "PR-D10",
      "text": "LOCKED owner 2026-07-15 (Q3/req 10): Import/export without zip dependencies (none exist in the workspace): export = a single text bundle <slug>.scpexport.json (schemaVersion + project.scp.json + undo.json). One portable file, stays text (req 5); caches are central and not exported. Import: schema validation, conflict with an existing project -> 409 + explicit choice, re-link to the local source path."
    },
    {
      "id": "PR-D11",
      "text": "REVISED owner 2026-07-15 (Q2/req 9): Migration of existing per-file localStorage keys (mindwave-gtrack-lanes, mindwave-gtrack-lane-spectrum, mindwave-gtrack-mix-excluded) into project sections, one-time on first open; the localStorage copy is NOT deleted in this plan (transition safety net, risk R5). Voice colour/hidden/muted MOVE OUT of the .gnaural file into the voiceState section (step PR2.4): values imported from the file once on first open, after which the editor stops writing them to the file (the voice-state endpoint is deprecated); mute/solo reach playback and render via the XML transform mechanism of gnaural-solo-render.ts. Stale attributes in existing .gnaural files are not scrubbed -- the project is the source of truth after migration (risk R9)."
    },
    {
      "id": "PR-D12",
      "text": "Tests and verification: project-store.test.ts co-located, bun:test, mkdtemp fixture (modelled on audio-cache-manifest.test.ts). There is no CI -- each step's verify is run by hand (plan section 6); prefer driving the real app over a green typecheck."
    },
    {
      "id": "PR-D13",
      "text": "Owner 2026-07-15 (Q5/req 12): editor defaults + project overrides pattern. The editor keeps global default settings; a project stores only its own values in its section and restores them on open; an absent section/key means the editor default applies. First consumer: waveform settings (section waveform, PR2.3); the pattern is mandatory for future per-file settings (req 2)."
    }
  ],
  "steps": [
    {
      "id": "PR0.1",
      "phase": 0,
      "title": "План + леджер",
      "status": "done",
      "dependsOn": [],
      "artifacts": [
        "MindWaveCore/docs/project-store/project-store-plan.md",
        "MindWaveCore/docs/project-store/project-store-progress.json"
      ],
      "verify": "ledger JSON parses; dependsOn DAG acyclic; plan + ledger committed",
      "startedAt": "2026-07-15",
      "finishedAt": "2026-07-15",
      "blockedReason": null,
      "evidence": [
        "Plan written from a three-way codebase survey: docs/ledger conventions (9 existing feature pairs), per-file data inventory (GnauralCore/ui + MindWaveCore/ui), server architecture (service archetype GnauralEditorStore, app_settings, protocol.ts, cache manifest).",
        "Requirements 1-7 numbered from the owner's verbatim request of 2026-07-15; decisions PR-D1..D12; 5 owner questions queued at the PR0.2 PAUSE."
      ]
    },
    {
      "id": "PR0.2",
      "phase": 0,
      "title": "PAUSE: owner утвердил план и ответил на Q1-Q5 (2026-07-15)",
      "status": "done",
      "dependsOn": [
        "PR0.1"
      ],
      "artifacts": [],
      "verify": "owner answers recorded in the plan (decision annotations) and in this ledger; contested decisions revised before Phase 1",
      "startedAt": "2026-07-15",
      "finishedAt": "2026-07-15",
      "blockedReason": null,
      "evidence": [
        "Q1: userDataRoot on Windows = %LOCALAPPDATA%\\KKSoundCore (PR-D6 locked).",
        "Q2: move EVERYTHING out of the .gnaural file -- voice colour/hidden/muted go to the voiceState project section (PR-D11 revised, new step PR2.4).",
        "Q3: text export bundle approved (PR-D10 locked).",
        "Q4: caches stay central so the UI can always enumerate them; no cache in the project folder (PR-D9 revised; PR6.2 dropped; cache API removed from PR1.3).",
        "Q5: waveform settings become project property: editor keeps defaults, each project restores its own on open (new PR-D13; PR2.3 reworded).",
        "Answers folded into the plan as reqs 8-12."
      ]
    },
    {
      "id": "PR1.1",
      "phase": 1,
      "title": "Идентичность и раскладка: slug+hash8, resolve папки, ProjectStoreError",
      "status": "done",
      "dependsOn": [
        "PR0.2"
      ],
      "artifacts": [
        "MindWaveCore/server/project-store.ts",
        "MindWaveCore/server/project-store.test.ts"
      ],
      "verify": "bun test project-store (Windows paths, case-insensitivity, cyrillic/forbidden chars, long names)",
      "startedAt": "2026-07-15",
      "finishedAt": "2026-07-15",
      "blockedReason": null,
      "evidence": [
        "project-store.ts: normalizeSourcePath (400 on empty), projectIdentityKey = resolve().toLowerCase() (same normalization as the editor-store write locks), sha1 hash8, projectSlug, projectDirName = <slug>-<hash8>, resolveProjectDir = <root>/projects/<dirName>; layout constants PROJECT_FILE_NAME / UNDO_FILE_NAME / PROJECTS_DIR_NAME / PROJECT_KIND.",
        "Slug is lowercased like the identity key: the first test run caught that a case-preserving slug made the folder name depend on the casing a path arrived with (hash matched, name did not -- two dirs for one file on POSIX).",
        "Sanitization checks char codes instead of an escape-range regex: a u0000-u001f regex literal embedded real control bytes into the file and re-created the server.ts NUL trap (caught and rewritten during this step; verified 0 control bytes in the final file).",
        "bun test project-store: 7 pass / 13 expect (case-insensitive identity; same basename in different dirs differs; cyrillic kept; forbidden chars replaced; 40-char cap; degenerate names fall back; 400 on empty path)."
      ]
    },
    {
      "id": "PR1.2",
      "phase": 1,
      "title": "Хранилище project.scp.json: атомарная запись, толерантное чтение, unknown sections, очередь записи",
      "status": "done",
      "dependsOn": [
        "PR1.1"
      ],
      "artifacts": [
        "MindWaveCore/server/project-store.ts",
        "MindWaveCore/server/project-store.test.ts"
      ],
      "verify": "bun test project-store (tmp+rename; corrupt file -> .broken-<ts> + empty restart; unknown sections survive rewrite; concurrent puts serialized)",
      "startedAt": "2026-07-15",
      "finishedAt": "2026-07-15",
      "blockedReason": null,
      "evidence": [
        "ProjectFileData { schemaVersion: 1, kind: 'SoundCoreProject', source fingerprint, createdAt/updatedAt, sections } with an index signature so unknown top-level fields AND unknown sections written by a future version survive a rewrite (tested: hand-crafted file with futureTopLevel + fromFutureSubsystem section round-trips intact).",
        "writeProjectFile: mkdir -p, pretty JSON + trailing newline into .project.scp.json.tmp-<uuid>, rename over, temp cleaned in finally (tested: no .tmp- leftovers). readProjectFile: missing -> null with no side effects; corrupt JSON or foreign shape (e.g. schemaVersion 99) -> renamed to project.scp.json.broken-<ts> with original bytes preserved + console.error, then null.",
        "SerialQueues: withFileLock pattern generalized by key -- same key strictly sequential, different keys interleave, a rejected action does not poison the queue (all three asserted in one test with explicit gates).",
        "bun test project-store: 13 pass / 33 expect."
      ]
    },
    {
      "id": "PR1.3",
      "phase": 1,
      "title": "Полный API ядра: open/list/get, секции, undo-транспорт, delete, relink",
      "status": "done",
      "dependsOn": [
        "PR1.2"
      ],
      "artifacts": [
        "MindWaveCore/server/project-store.ts",
        "MindWaveCore/server/project-store.test.ts"
      ],
      "verify": "bun test project-store (find-or-create; source.path scan fallback when the hash dir does not match; section round-trip; undo limits; delete guarded to projects/; relink updates source + folder stays)",
      "startedAt": "2026-07-15",
      "finishedAt": "2026-07-15",
      "blockedReason": null,
      "evidence": [
        "interface ProjectStore + ProjectStoreImpl + createProjectStore({resolveUserDataRoot}) -- root resolved per operation (a runtime settings change needs no restart); store is stateless between calls (every op reads scp.json inside the per-project SerialQueues lane), so there is no cache to go stale.",
        "openProject: fingerprint (404 when the source is missing/not a file) -> locateProject (hash dir first, then a source.path identity scan so a relinked project whose folder is named after the OLD path is still found) -> queued find-or-create; a case-only path variation neither duplicates the project nor rewrites the stored pretty path; fingerprint refresh writes only when size/mtime/path actually changed.",
        "Sections: get/putSection with name validation (400), null value deletes the section; undo: get/putUndoJournal in a wrapped {schemaVersion, updatedAt, journal} undo.json, null clears, >5MB -> 413 (UNDO_JOURNAL_MAX_BYTES); deleteProject: isSafeProjectId (400) + inside-projects guard (403) + 404 when absent; relinkProject keeps id/folder and swaps the source fingerprint.",
        "listProjects reads with recover=false so listing never renames other projects' broken files (asserted: bytes untouched), sorts by updatedAt desc; getProject reports sourceStatus ok|missing.",
        "bun test project-store: 21 pass / 68 expect. Server typecheck via bun x tsc@5.7.3 --ignoreDeprecations 5.0 (bunx pitfall from [[verify-typecheck-workflow]]): only the known pre-existing audio-cache-manifest.ts TS2540."
      ]
    },
    {
      "id": "PR1.4",
      "phase": 1,
      "title": "Протокол + REST /api/projects* + wiring синглтона + user_data_root",
      "status": "done",
      "dependsOn": [
        "PR1.3"
      ],
      "artifacts": [
        "SharedPasCore/ts/protocol.ts",
        "MindWaveCore/server/server.ts"
      ],
      "verify": "bun test + bun run typecheck (MindWaveCore/server); NB: server.ts contains a NUL byte -- edit via Read, never search it with Grep",
      "startedAt": "2026-07-15",
      "finishedAt": "2026-07-15",
      "blockedReason": null,
      "evidence": [
        "protocol.ts (SharedPasCore 639a71a): ProjectSourceStatus/ProjectSourceInfo/ProjectInfo, ProjectOpenRequest, ProjectListResponse, ProjectSection{Response,PutRequest} (value null removes a section), ProjectUndo{Response,PutRequest} (journal null clears), ProjectRelinkRequest, ProjectSettings { userDataRoot }.",
        "project-store.ts now imports ProjectInfo/ProjectSourceInfo from ./protocol (single source of truth, no drift) and adds defaultUserDataRoot(): %LOCALAPPDATA%\\KKSoundCore, fallback ~/.kksoundcore for non-Windows dev (owner req 8).",
        "log-db.ts: public getProjectSettings() reading app_settings key user_data_root ('' = not configured -> server falls back to the default root).",
        "server.ts: projectStore singleton next to gnauralEditorStore -- the root is re-resolved per operation so a settings change needs no restart; mapProjectStoreError mirrors mapGnauralEditorError; routes GET/DELETE /api/projects, POST /api/projects/open, GET /api/projects/info, GET+POST /api/projects/section, GET+POST /api/projects/undo (204 on write), POST /api/projects/relink; startup endpoints log line extended.",
        "bun test project-store: 22 pass / 69 expect. tsc@5.7.3 --ignoreDeprecations 5.0: only the known pre-existing audio-cache-manifest.ts TS2540."
      ]
    },
    {
      "id": "PR1.5",
      "phase": 1,
      "title": "UI-клиент project-api.ts + composable use-project.ts + openProject из потока выбора файла",
      "status": "done",
      "dependsOn": [
        "PR1.4"
      ],
      "artifacts": [
        "GnauralCore/ui/project-api.ts",
        "GnauralCore/ui/composables/use-project.ts",
        "GnauralCore/ui/stores/audio.ts"
      ],
      "verify": "bun run typecheck + bun run build (MindWaveCore/ui); bun test ui server (GnauralCore); manual: opening a .gnaural creates the project folder with project.scp.json",
      "startedAt": "2026-07-15",
      "finishedAt": "2026-07-15",
      "blockedReason": null,
      "evidence": [
        "project-api.ts: requestJson wrapper mirrored from audio-api.ts; openProject/fetchProjects/fetchProject/fetchSection/putSection/fetchUndoJournal/putUndoJournal/relinkProject/deleteProject; putSection and putUndoJournal use keepalive:true so the unload-time flush still lands.",
        "composables/use-project.ts: module-singleton currentProject; openProjectForFile flushes the previous file's pending writes first and never throws (a project failure must not break file opening -- error kept in projectOpenError); readProjectSection short-circuits to a pending debounced value; writeProjectSection debounces 500ms per section (last-write-wins, PR-D7); beforeunload flush.",
        "stores/audio.ts: selectPath and selectExternalPath call openProjectForFile (deselect -> closeCurrentProject) -- every opened file, wav/flac/gnaural alike, gets its project.",
        "Verify: vue-tsc clean; quasar build ok; GnauralCore bun test ui server 252 pass / 850 expect.",
        "Live smoke against the real server (bun run server.ts --ui-only --port=3417, LOCALAPPDATA redirected to a scratch dir): POST /api/projects/open for 'Smoke Тест.gnaural' -> id smoke_тест-9afabf60, sourceStatus ok; project.scp.json provisioned under <LOCALAPPDATA>/KKSoundCore/projects/<id>/; section round-trip zoom=2.5; list count=1. UI click-through remains for the owner at the PR1.6 PAUSE."
      ]
    },
    {
      "id": "PR1.6",
      "phase": 1,
      "title": "PAUSE: owner смотрит ядро (папка, scp.json, интерфейс) до интеграций",
      "status": "done",
      "dependsOn": [
        "PR1.5"
      ],
      "artifacts": [],
      "verify": "owner approves naming/layout/sections; Phase 2+ does not start before the answer",
      "startedAt": "2026-07-15",
      "finishedAt": "2026-07-16",
      "blockedReason": null,
      "evidence": [
        "Owner 2026-07-16: «ok / go» -- core approved as shipped (folder naming, fixed project.scp.json name, schema, ProjectStore API, REST surface); Phase 2 integrations unblocked."
      ]
    },
    {
      "id": "PR2.1",
      "phase": 2,
      "title": "Лейны в проект: gtrackLanes/laneSpectrum/mixExcluded + одноразовая миграция из localStorage",
      "status": "done",
      "dependsOn": [
        "PR1.6"
      ],
      "artifacts": [
        "GnauralCore/ui/composables/use-gtrack-lanes.ts"
      ],
      "verify": "bun test ui server (GnauralCore); manual: lane order/solo/heights survive an app restart; old localStorage values migrated once",
      "startedAt": "2026-07-16",
      "finishedAt": "2026-07-16",
      "blockedReason": null,
      "evidence": [
        "use-project.ts grew path-addressed primitives: getProjectForPath (reuses the current project / in-flight open, else an idempotent openProject), readProjectSectionFor (short-circuits to a pending debounced value), writeProjectSectionFor; queueSectionWrite factored out of writeProjectSection.",
        "use-gtrack-lanes.ts: persist()/persistSpectrum()/persistMix() now dual-write -- localStorage stays as the transition safety net (PR-D11), the project sections gtrackLanes/laneSpectrum/mixExcluded are the source of truth; restore is two-stage: synchronous localStorage paint (previous behaviour, no flash), then restoreFromProject applies the section over it, guarded by a request id against fast file switches.",
        "One-time migration: a file whose project lacks a section gets it seeded from the localStorage value; an existing-but-EMPTY section is intentional state and is never reseeded.",
        "Verify: GnauralCore bun test ui server 277 pass / 927 expect; vue-tsc clean. The live restart walk-through is the owner's PR2.5 PAUSE check."
      ]
    },
    {
      "id": "PR2.2",
      "phase": 2,
      "title": "Undo-журнал в undo.json: персист снапшотов GTrackModel, лимиты, восстановление",
      "status": "done",
      "dependsOn": [
        "PR1.6"
      ],
      "artifacts": [
        "GnauralCore/ui/composables/gtrack-model.ts",
        "GnauralCore/ui/composables/use-gtrack-lanes.ts"
      ],
      "verify": "bun test ui server (GnauralCore); manual: undo history survives an app restart; journal size stays within limits",
      "startedAt": "2026-07-17",
      "finishedAt": "2026-07-17",
      "blockedReason": null,
      "evidence": [
        "gtrack-model.ts: GTrackUndoJournal { currentSig, undo, redo } + exportUndoJournal(maxEntries) + adoptUndoJournal -- adoption is signature-gated: the journal only applies onto the EXACT state it was exported from, so a file changed outside the editor (or lost unsaved edits) silently keeps a clean history; isGTrackUndoJournal validates parsed JSON.",
        "use-project.ts: read/writeProjectUndoJournalFor with a 1500ms debounce, a pending-write short-circuit on read, and inclusion in flushPendingProjectWrites (file switch + beforeunload, keepalive).",
        "use-gtrack-lanes.ts: every user-edit path already funnels through refreshEditState(), which now doubles as the persist hook (rebuild/adopt paths pass false -- persisting there would overwrite the stored journal with an empty baseline before adoption); the journal is byte-budgeted (~4MB) by estimating one snapshot's size instead of stringifying the whole journal per edit; restoreFromProject adopts the stored journal after the model rebuild, incl. after saveEdits' reload (undo now survives Save).",
        "Verify: GnauralCore bun test ui server 281 pass / 942 expect (4 new journal tests: round-trip restores undo/redo across models, different-state journal rejected, maxEntries bound, shape guard); vue-tsc clean. Live restart walk-through is the owner's PR2.5 PAUSE check."
      ]
    },
    {
      "id": "PR2.3",
      "phase": 2,
      "title": "Per-file view: зум/вьюпорт + секция waveform (переопределения поверх дефолтов редактора, PR-D13)",
      "status": "done",
      "dependsOn": [
        "PR1.6"
      ],
      "artifacts": [
        "GnauralCore/ui/composables/spectrogram-viewport.ts",
        "GnauralCore/ui/pages/AudioPage.vue"
      ],
      "verify": "bun run typecheck + bun run build (MindWaveCore/ui); manual: zoom/viewport and waveform overrides restored per file after restart, editor defaults apply where the project has no value; selection intentionally not persisted",
      "startedAt": "2026-07-17",
      "finishedAt": "2026-07-17",
      "blockedReason": null,
      "evidence": [
        "use-project-view-state.ts: bindProjectViewState wires a surface's shared time/freq windows to a project section with validation, a restore request-id, and a restorePending guard -- the component's own null-reset on file change can no longer clobber the stored section before the async restore lands (watch-registration ordering contract documented). AudioPage binds 'viewAudio', TracksPanel binds 'viewTracks' (the two surfaces are isolated by design, GT-D10). Selection stays ephemeral.",
        "TracksPanel waveform prefs (mode/scales/colors/opacities/minimap/link/both): the save watch now dual-writes localStorage (the EDITOR DEFAULT, keeping today's behaviour) + the project 'waveform' section; on file open the section is applied over the defaults, and a file WITHOUT a section falls back to the defaults instead of inheriting the previous file's look (PR-D13). Programmatic applies are suppressed from the save watch.",
        "Survey correction folded in: waveform prefs live ONLY in TracksPanel since the owner's WS/VS rework -- AudioPage has no waveform key; AudioPage got only the viewport binding.",
        "vue-tsc clean; quasar build ok; GnauralCore bun test ui server 281 pass. Live restart walk-through is the owner's PR2.5 PAUSE check."
      ]
    },
    {
      "id": "PR2.4",
      "phase": 2,
      "title": "Voice-state (цвет/hidden/muted) из .gnaural в секцию voiceState; файл больше не мутируем",
      "status": "done",
      "dependsOn": [
        "PR1.6"
      ],
      "artifacts": [
        "GnauralCore/ui/audio-api.ts",
        "GnauralCore/ui/stores/audio.ts",
        "GnauralCore/server/gnaural-solo-render.ts",
        "GnauralCore/server/spectrogram-audio-source.ts"
      ],
      "verify": "bun test ui server (GnauralCore); manual: changing colour/mute does not touch the .gnaural mtime; mute is audible in playback and render via the XML transform; one-time import from file attributes on first open; everything restored after restart",
      "startedAt": "2026-07-17",
      "finishedAt": "2026-07-17",
      "blockedReason": null,
      "evidence": [
        "Client: voice-state-model.ts (pure merge/seed/patch, 3 unit tests) + use-voice-state.ts IO wrappers; setGnauralSchedule overlays the project's voiceState asynchronously over every dump (both the REST load and the WS schedule_loaded path) and seeds the section from the file's own values on first open (one-time migration); AudioPage's central patch chokepoint now writes the section + applies locally (applyVoiceStateLocally) instead of POSTing voice-state -- no file rewrite, no reload-from-disk, no editor-mutation guard; the save-first dirty guard stays (the cache update still rebuilds the gtrack model).",
        "Live playback: the CLI already had a mute_voice stdio command (survey B.6) -- new POST /api/audio/voice-mute pushes mute by voice INDEX to the active session only; on audio_schedule_loaded the server pushes the project's overrides where they differ from the file's stale tags.",
        "Renders: applyVoiceMuteMap (gnaural-solo-render.ts, same mechanics as muteNonSoloVoices, 2 tests) stamps explicit <voice_mute> values; renderGnauralSpectrogramWav applies it to the single-loop temp copy and renderGnauralAudioFile stages a transformed copy next to the source; the mute set is part of the sha1 cache key (voiceMuteFingerprint) so a changed project mute never serves a stale render.",
        "Old files keep their stale tags (R9, not scrubbed); the voice-state endpoint stays server-side but the UI no longer calls it (audio-api wrappers marked deprecated).",
        "Verify: GnauralCore bun test ui server 286 pass / 953 expect; tsc@5.7.3 (known TS2540 only); vue-tsc clean; quasar build ok. Audible mute + restart walk-through is the owner's PR2.5 PAUSE check."
      ]
    },
    {
      "id": "PR2.5",
      "phase": 2,
      "title": "PAUSE: owner проверяет, что редактор всё вспоминает после перезапуска",
      "status": "done",
      "dependsOn": [
        "PR2.1",
        "PR2.2",
        "PR2.3",
        "PR2.4"
      ],
      "artifacts": [],
      "verify": "owner confirms: lanes, undo, zoom, voice-state restored for a real file; .gnaural no longer rewritten by view changes",
      "startedAt": "2026-07-17",
      "finishedAt": "2026-07-17",
      "blockedReason": null,
      "evidence": [
        "Owner 2026-07-17: «ok» -- live walk-through passed; Phase 4/5 unblocked."
      ]
    },
    {
      "id": "PR3.1",
      "phase": 3,
      "title": "Настройка user_data_root: GET/PATCH /api/project-settings + поле в настройках редактора",
      "status": "done",
      "dependsOn": [
        "PR1.6"
      ],
      "artifacts": [
        "MindWaveCore/server/log-db.ts",
        "MindWaveCore/server/server.ts",
        "SharedPasCore/ts/protocol.ts"
      ],
      "verify": "bun test (server); manual: setting visible in editor settings, rejects a non-existing directory",
      "startedAt": "2026-07-17",
      "finishedAt": "2026-07-17",
      "blockedReason": null,
      "evidence": [
        "log-db.ts updateProjectSettings (app_settings key user_data_root); server.ts GET/PATCH /api/project-settings mirroring audio-settings: existing-directory validation, response carries userDataRoot + effectiveUserDataRoot (configured or the LOCALAPPDATA default); protocol ProjectSettingsPatchRequest/Response (SharedPasCore 5defbf8).",
        "GnauralSettingsTab.vue: new 'Projects' block above the cache -- q-input with the effective root as placeholder/hint, save button, error surface; projectApi.fetch/updateProjectSettings; i18n keys ru/en (staged via index blobs while the owner's parallel i18n work was uncommitted, then the owner's commit landed first).",
        "Live REST smoke on the real server (port 3418): GET shows the default effective root; PATCH to a scratch dir switches it and a subsequent /api/projects/open provisions under the NEW root without restart (per-operation resolution); PATCH to a non-existing dir -> 400; PATCH '' resets to the default. Setting restored to '' after the smoke.",
        "tsc@5.7.3: only the known pre-existing TS2540; vue-tsc clean."
      ]
    },
    {
      "id": "PR3.2",
      "phase": 3,
      "title": "Смена папки данных с переносом проектов (copy, отчёт, без авто-удаления старой)",
      "status": "done",
      "dependsOn": [
        "PR3.1"
      ],
      "artifacts": [
        "MindWaveCore/server/project-store.ts"
      ],
      "verify": "manual scenario: change the folder -> projects copied, editor keeps working against the new root; old folder left intact",
      "startedAt": "2026-07-17",
      "finishedAt": "2026-07-17",
      "blockedReason": null,
      "evidence": [
        "project-store.ts copyProjectsTree: copies <from>/projects/* recursively into <to>/projects/, skips project folders already present at the destination, never touches the source (old root stays as a manual fallback); same-root and missing-source are no-ops. Tests: 2 projects (one with undo.json) copied intact, re-run reports skipped=2, no-op cases.",
        "PATCH /api/project-settings gained migrate: the copy runs from the PREVIOUS effective root into the next one before the setting is stored; response carries {migrated: {copied, skipped}}. Protocol: ProjectsMigrationSummary + migrate flag (SharedPasCore 8179132).",
        "GnauralSettingsTab: saving the folder now asks 'copy the existing projects?' (copy / do not copy; dismiss aborts the save); the success toast reports copied/skipped counts.",
        "bun test project-store 24 pass / 76 expect; tsc@5.7.3 (known TS2540 only); vue-tsc clean."
      ]
    },
    {
      "id": "PR4.1",
      "phase": 4,
      "title": "Список проектов + статус источника (ok/moved/missing) + re-link",
      "status": "done",
      "dependsOn": [
        "PR1.6"
      ],
      "artifacts": [
        "MindWaveCore/server/project-store.ts",
        "SharedPasCore/ts/protocol.ts"
      ],
      "verify": "bun test project-store; manual: rename the source file -> status moved -> re-link restores the connection",
      "startedAt": "2026-07-17",
      "finishedAt": "2026-07-17",
      "blockedReason": null,
      "evidence": [
        "Server side (listProjects with sourceStatus ok|missing, relinkProject, the openProject scan fallback that finds a relinked project) shipped back in PR1.3/PR1.4 with tests; this step adds the UI: a 'Projects on disk' list in the settings tab -- source basename + full path, a red 'file not found' badge for missing sources, refresh, and a re-link action (prompt dialog seeded with the stored path) that reloads the list on success.",
        "GnauralCore 9f84458 (panel bundle commit PR4.1-PR4.3+PR5.1-PR5.2); vue-tsc + quasar build clean."
      ]
    },
    {
      "id": "PR4.2",
      "phase": 4,
      "title": "Удаление проекта (с защитой projects/), ручной GC осиротевших",
      "status": "done",
      "dependsOn": [
        "PR4.1"
      ],
      "artifacts": [
        "MindWaveCore/server/project-store.ts"
      ],
      "verify": "bun test project-store; manual: delete removes only a folder inside projects/",
      "startedAt": "2026-07-17",
      "finishedAt": "2026-07-17",
      "blockedReason": null,
      "evidence": [
        "deleteProject (isSafeProjectId 400 + inside-projects/ 403 + 404, tested in PR1.3) is now reachable from the panel: per-project delete with a named confirm, plus 'Delete orphans (N)' -- a confirmed batch delete of every project whose source file is missing (the manual GC from the plan; the button only appears when orphans exist)."
      ]
    },
    {
      "id": "PR4.3",
      "phase": 4,
      "title": "UI «Проекты»: список, статус, удалить/re-link/открыть папку",
      "status": "done",
      "dependsOn": [
        "PR4.1",
        "PR4.2"
      ],
      "artifacts": [
        "GnauralCore/ui/settings/GnauralSettingsTab.vue"
      ],
      "verify": "bun run typecheck + bun run build (MindWaveCore/ui); manual walk-through of every action",
      "startedAt": "2026-07-17",
      "finishedAt": "2026-07-17",
      "blockedReason": null,
      "evidence": [
        "The panel lives in GnauralSettingsTab between the user-data folder and the cache block: list, missing-source badge, re-link, export, reveal (POST /api/projects/reveal spawns explorer.exe on the project dir; 501 off-Windows), delete, orphan GC, import. Owner walk-through of every action remains open -- flagged for the next session."
      ]
    },
    {
      "id": "PR5.1",
      "phase": 5,
      "title": "Экспорт проекта: текстовый бандл .scpexport.json (cache по умолчанию исключён)",
      "status": "done",
      "dependsOn": [
        "PR1.6"
      ],
      "artifacts": [
        "MindWaveCore/server/project-store.ts",
        "SharedPasCore/ts/protocol.ts"
      ],
      "verify": "bun test project-store; manual: exported bundle is valid JSON and readable",
      "startedAt": "2026-07-17",
      "finishedAt": "2026-07-17",
      "blockedReason": null,
      "evidence": [
        "exportProject returns { schemaVersion: 1, kind: 'SoundCoreProjectExport', exportedAt, project: <scp.json data>, undo: <journal|null> } -- one text JSON, no zip dependency (PR-D10); GET /api/projects/export?id serves it with a content-disposition attachment <id>.scpexport.json; the panel's download button saves it via a Blob link.",
        "bun test project-store 26 pass / 88 expect (export->import round-trip restores sections + undo on another root)."
      ]
    },
    {
      "id": "PR5.2",
      "phase": 5,
      "title": "Импорт проекта: валидация, конфликты -> 409 + выбор, re-link на локальный источник",
      "status": "done",
      "dependsOn": [
        "PR5.1"
      ],
      "artifacts": [
        "MindWaveCore/server/project-store.ts"
      ],
      "verify": "bun test project-store; manual: export on one path -> import on another -> settings restored",
      "startedAt": "2026-07-17",
      "finishedAt": "2026-07-17",
      "blockedReason": null,
      "evidence": [
        "importProject validates the bundle shape (400 on junk), derives the project folder from bundle.project.source.path, 409s on an existing project unless overwrite, writes scp.json + undo.json atomically under the project's write queue; POST /api/projects/import { bundle, overwrite }.",
        "Panel: Import button -> file picker -> parse -> POST; on the 409 message a confirm dialog offers the overwrite retry (the explicit choice from PR-D10). Tested: conflict keeps the existing project intact until overwrite=true replaces it."
      ]
    },
    {
      "id": "PR6.1",
      "phase": 6,
      "title": "Кеш пиков волновой формы в центральном кеше (tmp/ + манифест; req 11)",
      "status": "pending",
      "dependsOn": [
        "PR0.2"
      ],
      "artifacts": [
        "GnauralCore/ui/composables/audio-model.ts",
        "MindWaveCore/server/audio-cache-manifest.ts",
        "MindWaveCore/server/server.ts"
      ],
      "verify": "bun test; manual: reopening a large file is visibly faster; cache invalidated after the source changes; peaks cache visible in the cache-management UI",
      "startedAt": null,
      "finishedAt": null,
      "blockedReason": null,
      "evidence": []
    },
    {
      "id": "PR6.2",
      "phase": 6,
      "title": "Центральный рендер-кеш → проекты — СНЯТ (owner 2026-07-15, Q4: кеши остаются центральными)",
      "status": "deferred",
      "dependsOn": [],
      "artifacts": [],
      "verify": "n/a -- step dropped",
      "startedAt": null,
      "finishedAt": null,
      "blockedReason": "Owner decided 2026-07-15 (Q4/req 11): caches stay central so the cache-management UI can always enumerate them regardless of where projects live; there is nothing to move.",
      "evidence": []
    }
  ],
  "log": [
    {
      "at": "2026-07-17",
      "text": "LEDGER REPAIR: fourteen shipped steps (PR1.4..PR5.2) were sitting with stale status pending/active -- they had been 'completed' by editing only the verify/evidence block, and the status line above the anchor never flipped (the exact trap from the ledger-status-field-slip note). A parallel session read the ledger honestly and concluded the foundation (PR1.4/PR1.5) was still pending. All fourteen are in fact committed (MindWaveCore 0730140/172815b + docs chain to 0f0fe72; GnauralCore c296548..9f84458; SharedPasCore 639a71a..cfc12d4). Statuses fixed to done per the commit record; the validator is hardened (dates/evidence with a pending status are now a hard error) and committed to the workspace root as validate-ledger.js with a binding rule in AGENTS.md."
    },
    {
      "at": "2026-07-17",
      "text": "PR2.5 PAUSE passed (owner «ok») and Phases 4-5 shipped the same day: the settings tab gained a 'Projects on disk' panel (list with missing-source badges, re-link via a path prompt, per-project delete + confirmed orphan GC, reveal-in-Explorer, one-file .scpexport.json export/import with a 409->overwrite conflict flow); export/import core landed in ProjectStore with round-trip tests (26 project-store tests total). Remaining: PR6.1 (waveform peaks in the central cache) -- deliberately deferred to its own session: it needs a real design pass (peaks pyramid format, client aggregation replacing per-zoom recompute from the decoded buffer) and none of it touches the Project entity; the plan's function set for the Project itself is complete. Owner: click through the new panel actions when convenient."
    },
    {
      "at": "2026-07-17",
      "text": "Phases 2 and 3 complete (PR2.1-PR2.4, PR3.1-PR3.2), each step with its own per-repo commits and green verify (GnauralCore 286 tests, server tsc, vue-tsc, quasar build, REST smokes for project-settings). Everything the owner listed in req 1 now lives in the project: lanes/laneSpectrum/mixExcluded sections with one-time localStorage migration, the undo journal (signature-gated adopt, byte-budgeted), per-file view state (spectrogram time/freq windows per surface + waveform settings as overrides over editor defaults), and voice colour/hidden/muted moved OUT of the .gnaural (voiceState section; renders take a mute-fingerprinted cache key + staged transformed copy; live mute via the CLI's mute_voice command, pushed on schedule load). The user-data folder is configurable with an offer to copy the projects tree. Stopped at the PR2.5 PAUSE: the owner walks through a real file -- lanes/undo/zoom/waveform/voice-state must survive a restart and the .gnaural must stay untouched by view changes."
    },
    {
      "at": "2026-07-15",
      "text": "Phase 1 (the priority: service class + API) complete in one session -- PR1.1..PR1.5 done, each with its own commit and green verify (project-store bun tests 22, GnauralCore ui+server 252, vue-tsc, quasar build, tsc@5.7.3 server typecheck). End-to-end smoke on the real server: opening a file provisions <LOCALAPPDATA>/KKSoundCore/projects/<slug>-<hash8>/project.scp.json and sections round-trip over REST. Stopped at the PR1.6 PAUSE for the owner to click through the live app and approve naming/layout/sections before Phase 2 integrations."
    },
    {
      "at": "2026-07-15",
      "text": "PR0.2 PAUSE passed -- owner answered Q1-Q5: (1) userDataRoot default = %LOCALAPPDATA%\\KKSoundCore; (2) move everything out of the .gnaural file -- voice colour/hidden/muted become the voiceState project section, new step PR2.4, the old PAUSE renumbered to PR2.5; (3) text export bundle approved; (4) caches stay CENTRAL (UI must always see them) -- cache API removed from the core, PR6.1 retargeted to the central manifest, PR6.2 dropped; (5) waveform settings become project-owned overrides on top of editor defaults -- new decision PR-D13. Plan reqs 8-12 added; decisions PR-D3/D5/D6/D9/D10/D11 revised. Phase 1 (service class + API) starts."
    },
    {
      "at": "2026-07-15",
      "text": "Plan + ledger created for the Project entity (owner req. 2026-07-15): per-file editor data -- curve colours, waves, undo journal -- moves into a per-project folder under a configurable user-data root; main file project.scp.json, everything text except bin under cache/; cache data lives in the project folder too. Survey found today's state scattered: voice colours are written INTO the .gnaural, lanes/solo live in per-file localStorage keys, undo and zoom are memory-only and lost on reload, caches are central under server/tmp. Full function set planned (open/sections/undo/cache/list/relink/delete/import/export/settings), with the service class + API first (PR-D5, Phase 1). Decisions PR-D1..D12 proposed; five owner questions (default data folder, fate of in-file voice colours, export format, render-cache move, per-file waveform colours) queued at the PR0.2 PAUSE."
    }
  ]
}
