{
  "schemaVersion": 1,
  "plan": "spectrogram-settings-scope",
  "planDoc": "docs/spectrogram-settings-scope/spectrogram-settings-scope-plan.md",
  "title": "spectrogram-settings-scope: the PROGRAM-LEVEL spectrogram «Параметры» move into the general Settings page as a tab; the floating/detachable «Параметры» @panel is retired from the Audio page; and (end goal) each spectrum graph gets a header button for per-graph LEFT/RIGHT channel settings, mirroring the waveform mechanism",
  "status": "in-progress",
  "createdAt": "2026-07-16",
  "updatedAt": "2026-07-16",
  "decisions": [
    {
      "id": "SG-D1",
      "text": "REVISED 2026-07-16 (owner correction — WRONG SURFACE the first time). The target «общий диалог настроек, который вызывается из меню Настройки и отображается в отдельном окне» is the AUDIO-WINDOW's own Меню→Настройки dialog, NOT the top-level MainLayout /settings route. Owner: «Настройки спектрограммы нужно было добавить в окно Настройки, которое открывается через Меню->Настройки в Аудио окне». That dialog is a page-level q-dialog in AudioPage.vue (GT10.29, `settingsDialogOpen` :1295, template :54-86): two-pane — a `settingsSubsystems` list on the left (today only {id:'cache'} -> GnauralSettingsTab) driving the content on the right; its comment literally says «the list is set up to grow». So the integration is a NEW SUBSYSTEM in that list ({id:'spectrogram', icon:'tune', label audio.settingsSubsystemSpectrogram}) rendering the spectrogram settings form in the content pane. --- WHY THE FIRST CUT WAS WRONG (the load-bearing-premise check MISSED it): I investigated whether the MainLayout nav /settings is a separate window, correctly found it is a plain route, and asked the owner, who — reading my framing — answered «оставить как вкладку». Both of us were talking past the real object: I never looked for a SECOND settings surface, the Audio page's own Меню→Настройки dialog, which IS the separate window the task named. Lesson: when a stated premise (\"opens in a separate window\") contradicts the first surface you find, the resolution may be a DIFFERENT surface you haven't found yet — enumerate all «Настройки» entry points before concluding, don't just correct the premise on the one you found. --- SUPERSEDED rev 1: «Оставить как вкладку» on the MainLayout /settings page via a CoreModuleSettingsTabContribution (the BodyMonitor «Devices» seam). Built in SG1.1 and then reverted — it was the wrong window."
    },
    {
      "id": "SG-D2",
      "text": "The tab REUSES the finished form (from the spectrum-settings-panel feature), it does not rebuild it. New SpectrogramSettingsTab.vue (in GnauralCore/ui/settings/, modelled on BodyMonitorSettingsTab.vue) renders <SpectrogramSettingsPanel/> — the ~23-line in-window adapter that already talks to useSpectrogramStore and applies actions via applySpectrumSettingsAction — and RE-PROVIDES «Сброс» (store.reset()) and presets access (SpectrogramPresetsDialog), because those lived in the retired PanelWindow chrome (SpectrumSettingsDialog: footer Reset + title-bar bookmarks icon), NOT inside the form. Registered through gnauralModule.settingsTabs with defineAsyncComponent (laziness — R2; SettingsPage wraps each tab in <Suspense>). This re-populates settingsTabs, which GT10.17 had emptied — but GT10.17 moved AUDIO-DEVICE settings into the Audio tab; the spectrogram «Параметры» is a different concern whose owner-chosen home is the general Settings (confirm framing + label/icon + Reset/presets placement at the SG0.1 PAUSE)."
    },
    {
      "id": "SG-D3",
      "text": "REVISED 2026-07-16 (owner clarification after SG0.1 PAUSE): «после того, как у спектра появится своя кнопка с индивидуальными настройками, панель настроек должна выглядеть В ТОЧНОСТИ КАК СЕЙЧАС — в виде докабл/детачбл окна». So the @panel/PanelWindow machinery is NOT deleted — it is PRESERVED and REPURPOSED: the per-graph individual settings (SG-D5, phase 3) are shown through the SAME dockable/detachable panel that «Параметры» is today. What phase 2 actually does is NARROWER than the superseded plan: remove the panel's GLOBAL binding + its Audio-toolbar `tune` opener (the program level moves to the Settings tab, SG-D2/SG-D4), so the panel is no longer opened as the global editor; its NEW opener becomes a per-graph button in each spectrum header, and its content binds to that graph's individual L/R override (phase 3). The reusable pieces stay ALIVE by construction: PanelWindow/SpectrumSettingsDialog/SpectrogramSettingsRemote/stores/spectrum-settings-panel become the TEMPLATE for the per-graph panel (likely parameterized by a graph/channel key rather than the single global singleton), not dead code. The SS-D11 titlebar-actions slot stays. What still goes away in phase 2: only the Audio-toolbar `tune` toggle as the GLOBAL entry point (TracksPanel.vue:125-132) — the panel itself lives on for phase 3. Orphaned localStorage keys, if any, are harmless (best-effort reads). --- SUPERSEDED rev 1 (owner Q2 «кнопка не нужна, её нужно убрать», read too broadly as 'delete the whole apparatus'): (a) TracksPanel.vue:125-132 tune toggle + useSpectrumSettingsPanelState (:1168,2616); (b) AudioPage.vue:156 host + import (:462) + state (:461,626); (c) delete SpectrumSettingsDialog.vue; (d) delete SpectrogramSettingsRemote.vue + the 'spectrum-settings' manifest entry (module.ts:38-47); (e) delete stores/spectrum-settings-panel.ts. That rev was wrong about DELETING the panel machinery — the owner wants exactly that dockable/detachable window kept for the per-graph editor. The `tune` toolbar button removal (Q2) still holds; the file DELETIONS do not."
    },
    {
      "id": "SG-D6",
      "text": "NEW 2026-07-16 (owner clarification). TWO surfaces for TWO levels: (1) PROGRAM-LEVEL settings live in the general Settings page as a plain TAB (SG-D1/SG-D2/SG-D4) — always a tab, no window chrome. (2) PER-GRAPH INDIVIDUAL settings (SG-D5, phase 3) live in the DOCKABLE/DETACHABLE @panel — «в точности как сейчас» — opened from a button in each spectrum graph's header. So the current «Параметры» dockable/detachable panel is not retired but re-homed onto the per-graph level; the program level it used to edit is taken over by the Settings tab. This settles a design question phase 3 raises: the per-graph editor's form factor is FIXED by the owner (dock/float/detach), and its content is the same SpectrogramSettings form (View/Fields/presets) already reused by the tab — only its data binding differs (per-graph individual override vs the global program store). OPEN for phase-3 design: one panel retargeted to the active graph vs one panel instance per graph (multiple @panel states); and how the per-channel L/R split maps onto the panel (a scope selector like the waveform dialog's both/L/R, or two sub-forms)."
    },
    {
      "id": "SG-D4",
      "text": "PROGRAM-LEVEL semantics. The global Pinia store stores/spectrogram.ts stays the PROGRAM-LEVEL default: a single settings object (spectrogram.ts:96) that already applies to every spectrogram (both overall L/R SpectrogramView instances read spectrogramStore.renderOptions, TracksPanel.vue:492). The Settings tab edits exactly this object. This matches the owner's model: program-level settings apply to all spectrograms UNLESS individual per-graph params are set. Individual per-graph override is SG-D5 (phase 3), not this."
    },
    {
      "id": "SG-D7",
      "text": "NEW 2026-07-16 (owner phase-3 design). Two decisions, both owner-picked at the SG3.0 design PAUSE. (1) MODEL = HYBRID: for the overall spectrogram group there are per-CHANNEL overrides [L,R], each either an individual SpectrogramSettings or null=inherit the PROGRAM level (the global store edited via Меню→Настройки). The panel gains a SCOPE selector «Оба | Левый | Правый» + a per-scope «Индивидуальные / наследовать программные» toggle. «Левый»/«Правый» edit that channel's override; «Оба» is a BULK editor writing both channels at once (its toggle sets/clears both). Resolution per rendered channel: channelOverride[ch] ?? programLevel — this is the 'program-level applies unless individual set' the owner named, expressed with the waveform's L/R scope UX (so it honours BOTH of the owner's phrasings; the waveform itself has no program-level inherit, so this ADDS that dimension). (2) PLACEMENT = ONE gear in the overall spectrogram GROUP header (TracksPanel.vue:441-472, today only fold/eye/title — no gear), mirroring the waveform group-header gear (:387-395). It opens the SAME dockable/detachable @panel that «Параметры» used (SG-D3 rev/SG-D6 — machinery kept in SG2.1), now bound to the active scope instead of the global store. ONE panel instance suffices — the overall spectrogram group is singular (gtrack solo spectra keep their own GTrackSpectrumSettings). This replaces the removed global `tune` opener (SG2.1) with a per-group gear."
    },
    {
      "id": "SG-D8",
      "text": "NEW 2026-07-16 (owner, phase-3 UX micro-decisions for SG3.2). (1) INHERIT→EDIT = AUTO-INDIVIDUAL: while a scope inherits the program level, its fields stay ACTIVE (showing the inherited values); the first edit auto-enables «индивидуальные» for that scope (seeding the override from the program level) and applies the edit. The toggle reflects state and lets the user revert to inherit. Not the 'disable fields until toggled' variant. (2) PRESETS APPLY TO THE ACTIVE SCOPE: the presets affordance stays in the per-graph panel; applying a preset writes the active scope's override (turning it individual). The preset LIBRARY is the same shared one (no per-graph preset store). Consequence for the action routing: every mutating action (set/apply-preset/save-as-from-current) targets the active scope's channel override(s) via the overrides store (setChannel for L/R, setBoth for «Оба», ensure-on-first-write), NOT the global program store. «Reset» in this panel = revert the active scope to inherit (clearChannel/clearBoth), i.e. drop the override — distinct from the program-level dialog's «Сброс» which resets the program defaults."
    },
    {
      "id": "SG-D5",
      "text": "END GOAL (phase 3, GATED — decompose only after the SG0.1 scope PAUSE). A gear in each spectrum graph's HEADER opens per-graph individual LEFT/RIGHT channel settings, mirroring the waveform mechanism. North star (waveform, all in TracksPanel.vue, no store/protocol type): per-channel [L,R] arrays + a separate «Оба канала» group + a waveformLinkChannels toggle + resolvers wfScale/wfColor/wfOpacity(ch) (linked => the «both» group, else the per-channel entry) + a two-pane q-dialog opened from the group-header gear (:387-395). NOTE the waveform semantics are NOT «override a global default» — «Оба канала» is a peer group (WS-D9). A CLOSER precedent already exists for the spectrogram itself: the gtrack laneSpectrum override (use-gtrack-lanes.ts:246-296) with individual-else-global resolvers (TracksPanel.vue:2186-2197) and the GTrackSpectrumSettings.vue editor — but it is keyed per lane id and wired only for gtrack solo spectra, not the overall L/R stack. Mirroring for the overall stack = a per-channel [L,R] SpectrogramSettings override (falling back to the program-level global store, SG-D4) feeding the two SpectrogramView instances, plus a gear in the currently gear-less spectrogram group header (TracksPanel.vue:452-483). Substantial and design-heavy; its steps + an owner PAUSE precede any code."
    }
  ],
  "steps": [
    {
      "id": "SG0.1",
      "phase": 0,
      "title": "План + леджер (этот файл + spectrogram-settings-scope-plan.md) + инвентаризация (разведка механизма волны, архитектуры спектра/стора, точного списка сноса). PAUSE ПРОЙДЕНА 2026-07-16: scope = ВСЕ фазы 1-3 в этой задаче («Всё сразу»); панель сохраняется под пер-график (SG-D3 rev/SG-D6); UX таба — дефолты приняты без возражений",
      "status": "done",
      "dependsOn": [],
      "artifacts": [
        "docs/spectrogram-settings-scope/spectrogram-settings-scope-plan.md (owner reqs verbatim, inventory §2 from two Explore passes, decisions SG-D1..SG-D5, phases, risks, open questions §7)",
        "docs/spectrogram-settings-scope/spectrogram-settings-scope-progress.json (this ledger)"
      ],
      "verify": "PASSED — owner settled §7 across three messages: (1) «Оставить как вкладку» (SG-D1, premise corrected); (2) «Кнопка не нужна, её нужно убрать» + program-level applies to all unless per-graph individual set + end goal = per-graph header button for L/R mirroring the waveform; (3) «панель настроек должна выглядеть в точности как сейчас — докабл/детачбл окно» => panel preserved & repurposed (SG-D3 rev/SG-D6); (4) «Всё сразу» => all phases 1-3 are in THIS task. Tab-UX defaults (label «Спектрограмма», icon tune, Reset+presets in the tab header) proposed and drew no objection — final call at SG1.2.",
      "startedAt": "2026-07-16",
      "finishedAt": "2026-07-16",
      "blockedReason": null,
      "evidence": [
        "THE LOAD-BEARING PREMISE WAS FALSE AND WAS RECONCILED BEFORE PLANNING (SG-D1): the task described the general Settings as shown «в отдельном окне», but /settings is a plain in-app route (MindWaveCore/ui/src/router/routes.ts:28), navigated by router.push in MainLayout.vue — no window.open, no dialog, no detached @panel. Verified by an Explore pass across MindWaveCore/ui (menu, routes, layouts, panel system). Rather than plan on the stated-but-wrong fact, it was surfaced to the owner (AskUserQuestion), who chose «Оставить как вкладку». This repo has punished shape/premise-based planning before — see [[verify-the-load-bearing-premise]].",
        "THE SEAM (SG-D2): the general Settings consumes CoreModuleSettingsTabContribution {name, icon, labelKey, component} (src/modules/index.ts:17-22) and renders each tab.component in a <Suspense> (SettingsPage.vue:24-39). BodyMonitor is the working model — one tab «Devices» via defineAsyncComponent (BodyMonitorCore/ui/module.ts:17-24). Gnaural's settingsTabs is [] (module.ts:14-16, emptied by GT10.17). So the integration is a manifest contribution, no routing/window work.",
        "THE FORM IS ALREADY REUSABLE (SG-D2): the spectrum settings form is split View + store-adapter + store. SpectrogramSettingsPanel.vue (~23 lines) pulls useSpectrogramStore, builds the snapshot, and applies each emitted action via applySpectrumSettingsAction — it is the store-facing piece and drops straight into a tab. «Сброс»/presets are NOT in it (they were in the retired PanelWindow chrome), so the tab must re-provide them.",
        "THE RETIREMENT LIST IS EXACT (SG-D3): grep pinned every consumer — tune toggle TracksPanel.vue:125-132 (+state :1168,2616), host AudioPage.vue:156 (+import :462, +state :461,626), and the detach-only files SpectrumSettingsDialog / SpectrogramSettingsRemote (+ manifest entry module.ts:38-47) / stores/spectrum-settings-panel. The reused pieces (Panel/View/Fields, model/actions, snapshot, PresetsDialog) were confirmed to survive because SpectrogramSettingsPanel keeps referencing them.",
        "THE END GOAL HAS A PRECEDENT (SG-D5): the waveform per-channel mechanism (TracksPanel.vue: model :1872-1934, header gear :387-395, dialog :721-809, resolvers :1912-1925) is the north star; and the spectrogram already has an individual-else-global override for gtrack lanes (use-gtrack-lanes.ts:246-296 + TracksPanel.vue:2186-2197 + GTrackSpectrumSettings.vue). The overall spectrogram group header (TracksPanel.vue:452-483) exists but has no gear yet — the exact spot for the future button. So phase 3 is a mirror + extension, not a greenfield.",
        "Step ids use the SG prefix — free after checking all existing ledgers (taken: AP/BK/FB/GT/MR/PR/PW/R/RF/SB/SF/SS/TP/TT/WS via grep over docs/*/*-progress.json).",
        "NOT decided by me, deliberately: §7 (phase-3 scope, tab UX, GT10.17 reversal) is put to the owner at this PAUSE rather than settled by proposal — phase-3 scope changes the size of the whole task, and the GT10.17 reversal is a direction call."
      ]
    },
    {
      "id": "SG1.1",
      "phase": 1,
      "title": "Программные настройки как ПОДСИСТЕМА диалога Меню→Настройки в Аудио-окне (SG-D1 rev.): новый SpectrogramSettingsTab.vue (форма SpectrogramSettingsPanel + «Сброс» + триггер пресетов) как правая панель диалога settingsDialogOpen рядом с «Кеш»/GnauralSettingsTab; запись в settingsSubsystems + i18n audio.settingsSubsystemSpectrogram. Плавающая панель НЕ трогается",
      "status": "done",
      "dependsOn": ["SG0.1"],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramSettingsTab.vue (new; toolbar with presets `bookmarks` + «Сброс» → store.reset(), body = <SpectrogramSettingsPanel/>, lazy SpectrogramPresetsDialog mounted on first open; name mirrors the sibling GnauralSettingsTab)",
        "GnauralCore/ui/pages/AudioPage.vue (settingsSubsystems += {id:'spectrogram', icon:'tune', labelKey:'audio.settingsSubsystemSpectrogram'}; settingsSubsystem ref widened to 'cache'|'spectrogram'; lazy SpectrogramSettingsTab via defineAsyncComponent; content pane <SpectrogramSettingsTab v-if=\"settingsSubsystem === 'spectrogram'\">) [CRLF file — \\r\\n-preserving script]",
        "GnauralCore/ui/i18n/en.json + ru.json (audio.settingsSubsystemSpectrogram = «Spectrogram» / «Спектрограмма»)",
        "REVERTED: the first (wrong) cut — MindWaveCore /settings MainLayout tab via gnauralModule.settingsTabs + settings.spectrogramTab i18n — was undone (module.ts restored to `settingsTabs: []` via git checkout of its pre-SG1.1 blob; the settings.spectrogramTab keys removed from both locales, 0 dangling refs)"
      ],
      "verify": "DONE (corrected surface) — vue-tsc 0 errors; quasar build succeeded; bun test ui server 269 pass / 0 fail. Runtime-visual is SG1.2 (owner PAUSE — no browser driver here).",
      "startedAt": "2026-07-16",
      "finishedAt": "2026-07-16",
      "blockedReason": null,
      "evidence": [
        "WRONG SURFACE FIRST, then corrected (SG-D1 rev.): the first cut put the settings on the MainLayout /settings route as a module settingsTab. The owner corrected: the real target is the AUDIO window's own Меню→Настройки dialog (AudioPage `settingsDialogOpen`, GT10.29), a page-level two-pane q-dialog whose left list `settingsSubsystems` («the list is set up to grow») drives the right content pane. Fixed by adding a {id:'spectrogram'} subsystem rendering the SAME SpectrogramSettingsTab component in the pane, and fully reverting the MainLayout tab (module.ts back to `settingsTabs: []` via git checkout of the pre-SG1.1 blob — byte-exact original, CRLF intact; settings.spectrogramTab removed from both locales).",
        "LAZINESS PROVEN by bundle: the build emits SpectrogramSettingsTab-*.js as its OWN chunk (4.18 KB), loaded via defineAsyncComponent only when the user opens Меню→Настройки and selects «Спектрограмма» — so AudioPage's EAGER chunk did NOT absorb the 18-field form / preset manager (SpectrogramSettingsPanel + SpectrogramPresetsDialog remain their own chunks). GnauralSettingsTab (the «Кеш» pane) is a static import; the heavier spectrogram pane is deliberately lazy.",
        "REUSE, not rebuild (SG-D2): the pane renders <SpectrogramSettingsPanel/> — the ~23-line store-adapter that already drives the whole 18-field form off useSpectrogramStore — and re-provides «Сброс» (store.reset()) + presets (SpectrogramPresetsDialog) that lived in the retired PanelWindow chrome, not in the form.",
        "THE FLOATING PANEL IS UNTOUCHED (SG-D3 rev.): the Audio `tune` button + SpectrumSettingsDialog still work over the SAME global store as the dialog pane — two surfaces, one source of truth. Phase 2 removes the `tune` global opener; phase 3 re-homes the panel onto per-graph.",
        "i18n: audio.settingsSubsystemSpectrogram added to both locales (parallel to audio.settingsSubsystemCache), key parity held; the reverted settings.spectrogramTab has 0 dangling refs workspace-wide (audio.spectrogramTab / spectrogramTabHint are unrelated pre-existing keys, left alone).",
        "LINE ENDINGS via the AUTHORITATIVE byte-count method (`tr -cd '\\r' | wc -c`, after `grep -c '\\r'` gave FALSE CRLF positives): SpectrogramSettingsTab.vue LF (component sibling); i18n en/ru LF; AudioPage.vue is CRLF and was edited via a \\r\\n-preserving node script (CR===LF, no mixed lines); module.ts restored via git checkout (CRLF intact).",
        "Staged only my files; the owner's parallel work and runtime artifacts left alone."
      ]
    },
    {
      "id": "SG1.2",
      "phase": 1,
      "title": "PAUSE — owner-проверка подсистемы «Спектрограмма» в диалоге Меню→Настройки Аудио-окна: форма, tiles/masonry, «Сброс», пресеты (применение/управление), правки применяются к спектрограмме",
      "status": "done",
      "dependsOn": ["SG1.1"],
      "artifacts": [],
      "verify": "owner drives the real app",
      "startedAt": "2026-07-16",
      "finishedAt": "2026-07-16",
      "blockedReason": null,
      "evidence": [
        "Owner approved («ok», 2026-07-16) AFTER the surface correction — the spectrogram settings now live as the «Спектрограмма» subsystem of the Audio window's Меню→Настройки dialog (the correct window). Phase 1 accepted. Proceed to phase 2 (remove the redundant global `tune` opener; the program level is now covered by this dialog)."
      ]
    },
    {
      "id": "SG2.1",
      "phase": 2,
      "title": "Убрать ГЛОБАЛЬНЫЙ вход в панель (SG-D3 rev.): удалена tune-кнопка тулбара TracksPanel (+ мёртвые import/const useSpectrumSettingsPanelState). Панель @panel НЕ удалена: AudioPage по-прежнему хостит SpectrumSettingsDialog + стор — шаблон пер-графикового редактора (фаза 3). Панель временно бездействует, пока фаза 3 не добавит вход из шапки графика",
      "status": "done",
      "dependsOn": ["SG1.2"],
      "artifacts": [
        "GnauralCore/ui/components/TracksPanel.vue (removed the `tune` toolbar button + its SS1.1 comment; removed the now-dead `import { useSpectrumSettingsPanelState }` and `const spectrumSettingsPanel = useSpectrumSettingsPanelState()` + its comment) [CRLF file — \\r\\n-preserving script]"
      ],
      "verify": "DONE — vue-tsc 0 errors; quasar build succeeded; bun test ui server 269 pass / 0 fail. 0 remaining `spectrumSettingsPanel` refs in TracksPanel; CR===LF (no mixed endings). The @panel machinery still builds (AudioPage keeps hosting SpectrumSettingsDialog + the store for phase 3).",
      "startedAt": "2026-07-16",
      "finishedAt": "2026-07-16",
      "blockedReason": null,
      "evidence": [
        "The `tune` button was the ONLY template user of spectrumSettingsPanel in TracksPanel (grep: lines 128-131), so removing it orphaned the import (:1168) and the const (:2616) — removed with their user (the <script setup> unused-binding blind spot means vue-tsc would not have flagged them). AudioPage remains the store's live consumer (it hosts SpectrumSettingsDialog + the floating teleport), so stores/spectrum-settings-panel.ts stays alive for phase 3 — nothing else broke.",
        "TRANSIENT STATE, intended (SG-D3 rev.): with the global opener gone and phase 3 not yet wired, the floating «Параметры» panel is currently unreachable — but its program-level function is fully covered by the Меню→Настройки dialog subsystem (SG1.1). Default panel `open` is false, so it does not appear on load; phase 3 gives it a per-graph opener. This is why the panel machinery was NOT deleted (owner: it must look «в точности как сейчас»).",
        "Line endings: TracksPanel.vue is CRLF; edited via a \\r\\n-preserving node script (each of the 3 removals asserted to match exactly once), CR===LF after."
      ]
    },
    {
      "id": "SG2.2",
      "phase": 2,
      "title": "PAUSE — owner подтверждает Audio-страницу без tune (программные настройки только в диалоге Меню→Настройки). Проверяется вместе с фазой 3, т.к. фаза 3 снова меняет Audio-страницу (кнопка в шапке графика)",
      "status": "pending",
      "dependsOn": ["SG2.1"],
      "artifacts": [],
      "verify": "owner drives the real app",
      "startedAt": null,
      "finishedAt": null,
      "blockedReason": null,
      "evidence": []
    },
    {
      "id": "SG3.0",
      "phase": 3,
      "title": "DESIGN PAUSE (SG-D5/SG-D6) — индивидуальные L/R настройки графика спектра. Заземлено в коде; развилка в словах владельца вынесена на owner-решение. РЕШЕНО (SG-D7): модель=Гибрид (scope Оба/L/R + per-scope тумблер individual/inherit-программные), кнопка=шестерёнка в шапке группы, одна панель",
      "status": "done",
      "dependsOn": ["SG0.1"],
      "artifacts": [],
      "verify": "DONE — owner picked at the design PAUSE: «Гибрид (реком.)» for the L/R model and «В шапке группы (реком.)» for the button. SG-D7 records both. Decomposed into SG3.1-3.3.",
      "startedAt": "2026-07-16",
      "finishedAt": "2026-07-16",
      "blockedReason": null,
      "evidence": [
        "Grounded in code BEFORE asking (the lesson from the SG-D1 miss): read the overall spectrogram group header (TracksPanel.vue:441-472 — fold/eye/title, NO gear) and confirmed L/R render as two SpectrogramView on the shared spectrogramStore.renderOptions. Surfaced the genuine tension in the owner's own words (mirror-the-waveform, whose model has no program-level inherit, vs program-level-applies-unless-individual) rather than guessing. Owner resolved: Hybrid + group-header gear."
      ]
    },
    {
      "id": "SG3.1",
      "phase": 3,
      "title": "Модель + резолверы (SG-D7): пер-канальные оверрайды [L,R] общего спектра (individual SpectrogramSettings или null=наследовать программный стор) + сеттеры (setChannel/clearChannel/setBoth) + персист + резолверы render/analysis (override ?? программный), заведённые в общий спектр-стек TracksPanel. Юнит-тесты резолюции. UI пока нет",
      "status": "done",
      "dependsOn": ["SG3.0"],
      "artifacts": [
        "GnauralCore/ui/composables/overall-spectrum-overrides.ts (new; PURE model: SpectrumOverrides {0,1} + channelOverride/isChannelIndividual/set/clear/ensure/setBoth/clearBoth/loadOverrides)",
        "GnauralCore/ui/composables/overall-spectrum-overrides.test.ts (new; 8 tests — independence, clone, ensure-keeps-existing, setBoth/clearBoth, junk-tolerant load+merge)",
        "GnauralCore/ui/stores/overall-spectrum-overrides.ts (new; Pinia wrapper: reactive ref + getChannel/isIndividual/set/ensure/clear/setBoth/clearBoth + localStorage persist mindwave-spectrogram-channel-overrides)",
        "GnauralCore/ui/components/TracksPanel.vue (overallSpectrumAnalysis/overallSpectrumRender resolvers; overall spectrogram stack + status-bar minimap render/analysis now resolve per channel; kept SEPARATE from spectrogramAnalysisForChannel used by waveform/gtrack) [CRLF file — \\r\\n-preserving edits]"
      ],
      "verify": "DONE — vue-tsc 0 errors; quasar build succeeded; bun test ui server 277 pass / 0 fail (was 269, +8 model tests). Default = both channels inherit (no override), so the overall spectrogram is byte-for-byte unchanged until an override is set.",
      "startedAt": "2026-07-16",
      "finishedAt": "2026-07-16",
      "blockedReason": null,
      "evidence": [
        "MODEL IS PURE + TESTED (matching the repo's test style — pure fns, no Pinia/DOM): composables/overall-spectrum-overrides.ts holds the state transforms; the store is a thin reactive+persist wrapper. A test I wrote caught my OWN wrong expectation before commit: slot() maps any non-1 index to the L slot (defensive), not 'everything else = R' — corrected the assertion to the real semantics (channels 0/1 independent).",
        "ISOLATION verified by reading usages (the lesson from the SG-D1 miss — don't assume): spectrogramLeftAnalysis/RightAnalysis + spectrogramAnalysisForChannel feed the WAVEFORM tracks (TracksPanel:393) and the gtrack lane fallback (:2176) too, so I did NOT make those override-aware. Instead a SEPARATE overallSpectrumAnalysis/overallSpectrumRender is used ONLY on the overall spectrogram stack (:481) and the status-bar minimap (:845). A spectrum override cannot leak into the waveform's analysis.",
        "INERT BY DEFAULT: with no override, overallSpectrumRender returns spectrogramStore.renderOptions and overallSpectrumAnalysis returns spectrogramAnalysisForChannel(ch) — identical to before. The minimap's analysis already followed spectrogramTracks[0] (now override-aware for the primary channel); its render was updated to match (same channel) for consistency.",
        "DETACH-AWARE BY DESIGN (SS-D3): the store is main-window-authoritative; SG3.2's detached panel will bridge actions to it rather than run its own copy (documented in the store header).",
        "Line endings: 3 new files LF (composable siblings); TracksPanel.vue CRLF, edited via \\r\\n-preserving node scripts (additions) + single-line Edits (bindings) — CR===LF, no mixed. One Edit collided with a concurrent linter/rewrite and was reapplied via script."
      ]
    },
    {
      "id": "SG3.2a",
      "phase": 3,
      "title": "UI ядро (SG-D7/SG-D8), IN-WINDOW: шестерёнка в шапке группы общего спектра открывает @panel; SpectrogramOverridesPanel — селектор scope Оба/L/R + тумблер individual/inherit + пресеты-apply к scope + «Сброс»=вернуть к наследованию; форма (SpectrogramSettingsView) привязана к эффективным настройкам scope, правки в оверрайд. Detach ВЫКЛ (:allow-detach=false) до SG3.2b. i18n scope-подписей",
      "status": "done",
      "dependsOn": ["SG3.1"],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramOverridesPanel.vue (new; the in-window per-graph adapter: scope Оба/L/R + individual toggle + presets-apply + reset=revert, over the overrides store, reusing SpectrogramSettingsView for the form)",
        "GnauralCore/ui/components/SpectrumSettingsDialog.vue (rewritten: hosts SpectrogramOverridesPanel lazily; :allow-detach=\"false\" for SG3.2a; dropped the global presets btn/footer/bridge)",
        "GnauralCore/ui/components/TracksPanel.vue (re-added useSpectrumSettingsPanelState + a `tune` gear in the overall spectrogram group header toggling panel.open) [CRLF]",
        "GnauralCore/ui/i18n/en.json + ru.json (audio.spectrumScopeBoth/Left/Right/Individual/InheritHint/Reset)"
      ],
      "verify": "DONE — vue-tsc 0 errors; quasar build succeeded (SpectrogramOverridesPanel its own 3.59 KB lazy chunk); bun test ui server 277 pass / 0 fail. Runtime-visual is SG3.3 (owner PAUSE). Dock/float work; detach is deferred to SG3.2b.",
      "startedAt": "2026-07-16",
      "finishedAt": "2026-07-16",
      "blockedReason": null,
      "evidence": [
        "REUSE: the form is SpectrogramSettingsView (unchanged), fed a snapshot built from the active scope's effective settings (override ?? program level). Field edits emit 'set'; the adapter seeds+writes the scope override (SG-D8 auto-individual). Presets-apply writes the active scope from allPresets[id].settings. «Сброс» here = clear the scope override (revert to inherit), distinct from the program dialog's reset.",
        "The gear mirrors the waveform group-header gear (title flex:1 pushes it right); it re-uses the panel-state singleton removed from the toolbar in SG2.1 — so the SAME @panel now opens per-graph from the header instead of globally from the toolbar.",
        "Laziness kept: SpectrumSettingsDialog defineAsyncComponent-loads the panel, so AudioPage's eager chunk is unaffected (panel is its own 3.59 KB chunk).",
        "A TS overlap error my typecheck caught: casting SpectrogramSettings to Record<string,string|number> needs `as unknown as` (the interface has boolean/enum fields, no index signature) — fixed."
      ]
    },
    {
      "id": "SG3.2b",
      "phase": 3,
      "title": "Detach (SG-D6, SS-D3): расщепить per-graph панель в PW5.7-«пульт» — pure SpectrogramOverridesView (snapshot+action, scope локально) + in-window adapter + child SpectrogramOverridesRemote + сериализуемая модель (оба канала + программный + пресеты) и scope-квалифицированные действия; включить detach; manifest на новый remote. Владелец: панель «в точности как сейчас» (докабл/детачбл)",
      "status": "pending",
      "dependsOn": ["SG3.2a"],
      "artifacts": [],
      "verify": "typecheck + build + tests; detach opens a child window rendering the per-graph panel; edits in the detached window repaint THIS window's spectrogram; dock/float/detach all work; only the main window writes the override store (no clobbering)",
      "startedAt": null,
      "finishedAt": null,
      "blockedReason": null,
      "evidence": []
    },
    {
      "id": "SG3.3",
      "phase": 3,
      "title": "PAUSE — owner-проверка: шестерёнка в шапке группы, панель (докабл/детачбл «как сейчас»), scope Оба/L/R, наследование программного уровня, индивидуальная правка канала перерисовывает только его; заодно SG2.2 (Audio без глобального tune)",
      "status": "pending",
      "dependsOn": ["SG3.2b"],
      "artifacts": [],
      "verify": "owner drives the real app",
      "startedAt": null,
      "finishedAt": null,
      "blockedReason": null,
      "evidence": []
    }
  ],
  "log": [
    {
      "at": "2026-07-16",
      "step": "SG0.1",
      "text": "Feature opened. Two Explore passes mapped: (1) the general Settings dialog — CONFIRMED it is a plain in-app /settings route, NOT a separate window (the stated premise was false), and the settingsTab seam; (2) the waveform per-channel north star + the spectrogram global-store architecture + the gtrack laneSpectrum individual-else-global precedent + the reusable form pieces. Plan + ledger written. Owner clarified via AskUserQuestion: keep the general Settings as an in-app tab (not @panel); program-level spectrogram settings apply to all spectrograms unless per-graph individual params are set; end goal is a per-graph header button for individual L/R channel settings, mirroring the waveform. Decisions SG-D1..SG-D5 recorded. PAUSE at SG0.1 for the owner to settle §7 (phase-3 scope, tab UX, GT10.17 reversal) before any code."
    },
    {
      "at": "2026-07-16",
      "step": "SG0.1",
      "text": "Owner clarification at the SG0.1 PAUSE reshaped the retirement: «после того, как у спектра появится своя кнопка с индивидуальными настройками, панель настроек должна выглядеть в точности как сейчас — в виде докабл/детачбл окна». => The @panel/PanelWindow machinery is PRESERVED and repurposed for the per-graph individual editor (phase 3), NOT deleted. SG-D3 REVISED (superseded rev 1's file deletions; only the Audio-toolbar `tune` global opener still goes); SG-D6 ADDED (two surfaces: program-level = Settings tab; per-graph = dockable/detachable panel). SG2.1/SG3.0 re-scoped."
    },
    {
      "at": "2026-07-16",
      "step": "SG0.1",
      "text": "Sequencing settled: owner picked «Всё сразу» => all phases (1 tab, 2 repoint the Audio-toolbar opener, 3 per-graph L/R via the dockable/detachable panel) are in THIS task. SG0.1 PAUSE PASSED and marked done. SG3.0 un-gated (still to be decomposed into SG3.x after phase 2, once the panel-state singleton and graph identity are examined closely — the SG-D6 design questions: one retargeted panel vs per-graph instances, and the L/R scope mapping). Starting SG1.1 (program-level Settings tab); the floating panel is left working over the global store until phase 2."
    },
    {
      "at": "2026-07-16",
      "step": "SG1.1",
      "text": "SG1.1 DONE — the program-level spectrogram «Параметры» are now a tab («Спектрограмма») on the general Settings page. New SpectrogramSettingsTab.vue reuses SpectrogramSettingsPanel + re-provides Reset/presets; registered as a lazy settingsTab in gnauralModule; i18n settings.spectrogramTab added (both locales, deep-merge safe). Verify green: vue-tsc 0, quasar build ok, GnauralCore 269 pass/0 fail; laziness proven by separate chunks. The floating panel is untouched (still edits the same store). Now at SG1.2 PAUSE — owner drives the app to accept the tab before phase 2 (removing the Audio `tune` global opener)."
    },
    {
      "at": "2026-07-16",
      "step": "SG1.1",
      "text": "WRONG SURFACE, corrected. Owner: «Настройки спектрограммы нужно было добавить в окно Настройки, которое открывается через Меню->Настройки в Аудио окне» — the target is the AUDIO window's Меню→Настройки dialog (AudioPage `settingsDialogOpen`, GT10.29), NOT the MainLayout /settings route I used. SG-D1 REVISED with the miss recorded (I corrected the premise on the first «Настройки» surface I found instead of enumerating all of them). Reverted the MainLayout tab (module.ts git-checked-out to its pre-SG1.1 blob → `settingsTabs: []`; settings.spectrogramTab removed from both locales) and re-implemented on the correct surface: a new {id:'spectrogram'} subsystem in AudioPage's settings dialog rendering the same SpectrogramSettingsTab.vue in the content pane, lazy (defineAsyncComponent), with i18n audio.settingsSubsystemSpectrogram. Re-verified green: vue-tsc 0, quasar build ok (tab its own 4.18 KB chunk), GnauralCore 269 pass/0 fail. Still at SG1.2 PAUSE — owner now verifies the CORRECT surface."
    },
    {
      "at": "2026-07-16",
      "step": "SG1.2 / SG2.1",
      "text": "SG1.2 PASSED — owner approved («ok») the «Спектрограмма» subsystem in the Audio Меню→Настройки dialog. Phase 1 accepted. SG2.1 DONE — removed the global `tune` toolbar opener from TracksPanel (+ its dead import/const); the @panel machinery is kept (AudioPage still hosts SpectrumSettingsDialog + the store) as the phase-3 template. Verify green (vue-tsc 0, build ok, 269 pass/0 fail). The floating panel is now dormant (no opener) until phase 3 gives it a per-graph header button. Next: phase-3 DESIGN — the SG-D6 open forks (one retargeted panel vs per-graph instances; L/R scope mapping) go to the owner before any code."
    },
    {
      "at": "2026-07-16",
      "step": "SG3.0 / SG3.1",
      "text": "Phase-3 DESIGN settled (SG-D7): owner picked Hybrid (scope Оба/L/R + per-scope individual/inherit toggle) + one gear in the overall spectrogram group header. Grounded in code before asking (post-SG-D1-miss discipline). SG3.1 DONE — pure per-channel override model (composables/overall-spectrum-overrides + 8 tests) + Pinia store + TracksPanel resolvers (overallSpectrumAnalysis/Render) on the overall stack + minimap, kept SEPARATE from the waveform/gtrack analysis so overrides don't leak. Inert by default (both inherit). Verify green (277 pass/0 fail). Now SG3.2 (active): the gear + the scope/inherit panel UI + rebinding the form to the active scope + detach-remote."
    },
    {
      "at": "2026-07-16",
      "step": "SG3.2a",
      "text": "SG-D8 recorded (owner UX): auto-individual on edit; presets apply to the active scope. SG3.2 SPLIT into SG3.2a (in-window core) + SG3.2b (detach) — validate the per-graph UX before building the detach remote around it (the agreed a/b phasing). SG3.2a DONE — SpectrogramOverridesPanel (scope Оба/L/R + individual toggle + presets-apply + reset=revert) over the overrides store, reusing SpectrogramSettingsView; a `tune` gear in the overall spectrogram group header opens the @panel (replacing the removed toolbar button); SpectrumSettingsDialog hosts it lazily with detach OFF for now. Verify green (vue-tsc 0, build ok, 277 pass/0 fail). Handing SG3.2a to the owner to try (dock/float) before SG3.2b enables detach."
    }
  ]
}
