{
  "schemaVersion": 1,
  "plan": "spectrogram-fixes",
  "planDoc": "docs/spectrogram-fixes/spectrogram-fixes-plan.md",
  "title": "Spectrogram fixes (loading indicator, only-last-frame render, settings overlay 'Параметры')",
  "status": "active",
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-02T01:30:00Z",
  "decisions": [
    { "id": "SF-D1", "text": "Scope & process: 3 items -> 3 phases. Per-step atomic commits (step-id prefixed, Co-Authored-By); NO push; phase-boundary pauses; vue-tsc + bun green each step. Code in GnauralCore; ledger in MindWaveCore." },
    { "id": "SF-D2", "text": "Loading indicator: surface worker/tile loading in SpectrogramView as an overlay spinner driven by spec.loading (analysis open + tiles pending); keep audio.spectrogramLoading (decode) in AudioPage so an indicator covers the whole prepare span (decode -> open -> first tiles)." },
    { "id": "SF-D3", "text": "Only-last-frame fix: make the rendered tile set persist/accumulate during load: (a) guard setView/applyView to no-op when the effective view (time window + zoom + viewBinCount) is unchanged (kill viewSeq churn); (b) re-assembleVisibleTiles on every tile arrival against the current cache (not only when pending.seq===viewSeq); (c) final assemble+redraw when loading clears. Root cause confirmed at execution via instrumentation; add a composable unit test for tile accumulation across a binCount/seq change. Confirmed repro (owner): happens on OPEN, no playback; a one-tile strip runs L->R and only the last tile stays." },
    { "id": "SF-D4", "text": "Settings panel = overlay named 'Параметры': convert the fixed 264px side column into a toggleable overlay mirroring the schedule settings panel (absolute aside over the plot, no main-content resize, backdrop, <transition>); header title 'Параметры' (audio.spectrogramSettingsTitle: en 'Parameters' / ru 'Параметры') + close (x). Default closed." },
    { "id": "SF-D5", "text": "Toggle placement: the 'Параметры' toggle lives in the SpectrogramView toolbar (next to zoom/fit); open/close state + overlay are self-contained in the view; state local (not persisted) unless owner asks otherwise." },
    { "id": "SF-D7", "text": "Owner addition 2026-07-01 (Audacity-like UI #1): give the spectrogram plot a taller default height matching the Audacity reference (per-channel ~240px; stereo split ~2x) instead of the current min-height:280px flex fill." },
    { "id": "SF-D8", "text": "Owner addition (Audacity-like UI #2): a drag handle on the bottom border of the spectrogram track resizes its height via pointer drag (like Audacity); persist the chosen height (localStorage)." },
    { "id": "SF-D11", "text": "Owner addition 2026-07-02: performance optimization approach A->B->C, each measured + UI-verified before the next. A (SF6.2): skip computing/storing phase + unwrapped-phase unless the data mode needs them (default magnitude doesn't) -> removes ~350M ArcTan2 + per-frame unwrap + 2 of 3 ~1.4GB matrices. B (SF6.3): multithread the independent per-frame FFT loop (per-thread FFTW plan/buffers). C (SF6.4): lazy/progressive per-visible STFT with a bounded cache (Audacity-style). All are worker (Pascal) changes -> rebuild + re-bundle + backend restart; per-step commit; pause for manual UI verification each. Owner: plan all three, implement A first with a UI-verification pause." },
    { "id": "SF-D10", "text": "Owner addition 2026-07-02 (SF2.1 real concern = SPEED): the only-last-frame visual bug is resolved; the remaining SF2.1 problem is spectrogram GENERATION SPEED (Audacity is much faster). PROFILE FIRST, optimize second (approval gate before any optimization). Measure each pipeline stage on d:\\bin\\Presets\\1_Orientation.flac: worker open-analysis, per-tile get-tile (sample-load/FLAC-decode vs FFT), tile payload bytes + JSON serialize/parse, WS transfer, UI render. Suspects to confirm/refute: single-threaded worker FFT; JSON (not binary) tile transport of large float arrays; repeated per-request FLAC decode (worker reads FLAC directly, no cached PCM)." },
    { "id": "SF-D9", "text": "Owner addition (Audacity-like UI #3): split stereo into stacked Left-over-Right panes, each analysing one channel (channel:0/channel:1, its own worker analysis); mono = single pane. Detect channel count from the decoded AudioBuffer.numberOfChannels (audio.spectrogramBuffer); label panes L/R. PREREQUISITE discovered at execution: the server kept one SpectrogramSession per WS and it was single-analysis (a 2nd open closed the 1st; get-tile used the session's stored analysisId). Refactored SpectrogramSession to hold multiple concurrent analyses keyed by analysisId (worker already multiplexes; client msgs already carry analysisId), so two channel analyses coexist over one socket. Backend change -> server restart." },
    { "id": "SF-D6", "text": "Owner addition 2026-07-01: prepare the spectrum when the Спектрограмма tab is opened. The tab renders only when audio.spectrogramBuffer!=null; RF4.1 decodes on tree selection, but selecting a file on the Воспроизведение tab then switching to Спектрограмма (or a selected-but-undecoded file) leaves the stale 'start local playback' message with nothing happening. Fix: when the Спектрограмма view becomes active for a selected LOCAL audio file (wav/flac) with no decoded buffer and no decode in flight, trigger audio.ensureLocalAudioReady(selectedPath, kind) (same non-playback decode as RF4.1); update the obsolete audio.noSpectrogram wording to cover only the no-file-selected case. .gnaural unchanged." }
  ],
  "steps": [
    {
      "id": "SF0.1",
      "phase": 0,
      "title": "Plan & ledger",
      "status": "done",
      "dependsOn": [],
      "artifacts": ["docs/spectrogram-fixes/spectrogram-fixes-plan.md", "docs/spectrogram-fixes/spectrogram-fixes-progress.json"],
      "verify": "ledger JSON parses; dependsOn DAG acyclic; plan + ledger committed",
      "startedAt": "2026-07-01T18:55:00Z", "finishedAt": "2026-07-01T18:55:00Z", "blockedReason": null,
      "evidence": [
        "Grounded in code: item1 = spec.loading exists in use-spectrogram.ts but SpectrogramView never surfaces it; AudioPage only shows audio.spectrogramLoading (decode). item2 = tiling/cache sound, analysisParams/renderOptions stable computeds; owner repro = on OPEN, one-tile strip runs L->R, only last tile stays -> rendered set holds one tile at a time; leading cause = view/seq/viewBinCount churn (ResizeObserver + double applyView) so assembleVisibleTiles matches only the newest tile. item3 = SpectrogramSettingsPanel is a fixed 264px column in AudioPage; mirror GnauralScheduleView settings overlay.",
        "DAG acyclic: SF0.1 -> SF0.2 -> {SF1.1, SF2.1, SF3.1}."
      ]
    },
    {
      "id": "SF0.2",
      "phase": 0,
      "title": "Approval gate (SF-D2..SF-D5)",
      "status": "done",
      "dependsOn": ["SF0.1"],
      "artifacts": ["docs/spectrogram-fixes/spectrogram-fixes-plan.md"],
      "verify": "owner confirms SF-D2 (loading overlay), SF-D3 (accumulate-tiles fix + unit test), SF-D4 (overlay 'Параметры'), SF-D5 (toggle in view toolbar). PAUSE before Phase 1.",
      "startedAt": "2026-07-01T18:55:00Z", "finishedAt": "2026-07-01T19:00:00Z", "blockedReason": null,
      "evidence": ["Owner approved ('go'); SF-D2..SF-D5 locked. Also confirmed the bug-2 repro (on open, no playback: one-tile strip runs L->R, only last stays)."]
    },
    {
      "id": "SF1.1",
      "phase": 1,
      "title": "Loading indicator during spectrogram preparation",
      "status": "done",
      "dependsOn": ["SF0.2"],
      "artifacts": ["GnauralCore/ui/components/SpectrogramView.vue", "GnauralCore/ui/composables/use-spectrogram.ts", "GnauralCore/ui/i18n/en.json", "GnauralCore/ui/i18n/ru.json"],
      "verify": "an overlay spinner shows while spec.loading (analysis open/reconfigure + tiles pending) and clears when ready; vue-tsc clean",
      "startedAt": "2026-07-01T19:00:00Z", "finishedAt": "2026-07-01T19:25:00Z", "blockedReason": null,
      "evidence": [
        "SpectrogramView: added isPreparing = computed(spec.loading) and an absolute overlay (spectrogram-view__loading) with q-spinner-hourglass + label shown while preparing; container set position:relative. i18n audio.spectrogramPreparing added (en 'Preparing spectrogram…' / ru 'Подготовка спектрограммы…').",
        "FOLLOW-UP FIX (owner: spinner never appeared): spec.loading originally reflected only pending tiles, so it stayed false during the slow worker open()/reconfigure() analysis and tiles arrived too fast to show. use-spectrogram.ts now tracks an `opening` flag set at the start of open()/reconfigure() (updateLoading -> loading=true) and cleared when refetch() starts (then loading = pending tiles) or on error/close. The indicator now covers the whole prepare span (open analysis -> first tiles).",
        "vue-tsc clean; GnauralCore bun 56/0. Live-verify in quasar dev (owner)."
      ]
    },
    {
      "id": "SF2.1",
      "phase": 2,
      "title": "Fix only-last-frame render (persist/accumulate tiles)",
      "status": "done",
      "dependsOn": ["SF0.2"],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramView.vue",
        "GnauralCore/ui/composables/use-spectrogram.ts",
        "GnauralCore/ui/composables/spectrogram-tiles.ts",
        "GnauralCore/ui/composables/spectrogram-tiles.test.ts"
      ],
      "verify": "opening a file renders the whole spectrogram and it stays (no single-tile strip / only-last-frame); pan/zoom still work; new composable unit test (tiles accumulate across binCount/seq change) green; vue-tsc + bun green",
      "startedAt": "2026-07-01T20:00:00Z", "finishedAt": "2026-07-01T20:20:00Z", "blockedReason": null,
      "evidence": [
        "Root cause (reasoned): tiles were cached keyed on viewBinCount and the render only used the EXACT-key tile for the current view; during initial load the canvas height settles (ResizeObserver + double applyView), so viewBinCount/tile-keys churn (viewSeq bumps) and assembleVisibleTiles matched only the newest-fetched tile -> a single strip that runs L->R and leaves only the last tile.",
        "Fix (SF-D3): (a) SpectrogramTileCache.getByTileIndex(analysisId, zoom, tileIndex) returns any cached tile for that tile regardless of binCount; assembleVisibleTiles now uses exact key OR this fallback so already-fetched tiles keep rendering across a binCount change. (b) tile arrivals ALWAYS re-assemble (dropped the pending.seq===viewSeq gate for assembly; seq still used for loading). (c) setView no-ops when the view (time window + zoom + viewBinCount) is unchanged, killing redundant applyView/ResizeObserver churn. (d) SpectrogramView rounds viewBinCount to a 32-step so small height jitter doesn't change keys.",
        "Guarded by unit tests: SpectrogramTileCache.getByTileIndex (fallback, MRU preference, prefix-boundary, and an explicit only-last-frame regression case where tiles 0..2 fetched at binCount 128 all still resolve when the plan asks for 256). vue-tsc clean; GnauralCore bun 60 pass / 0 fail (+4). Final visual confirmation = owner (quasar dev).",
        "FOLLOW-UP (owner screenshot: still a strip running L->R that vanishes, spinner stuck): the REAL root cause was planVisibleTiles nartitioning by a FIXED 256 full-res frames per tile REGARDLESS of zoom, so a full view of a long file (25 min) plans hundreds of full-res tiles that overflow the 128-entry LRU cache -> earlier tiles evicted, only the most-recent (rightmost) survive to render, and tiles never stop fetching (spinner stuck). Fix: planVisibleTiles now spans tileFrames*2^zoom full-res frames per tile, so a zoomed-out view needs only a handful of tiles (e.g. ~4-8) that fit the cache and all render/persist. Added a unit test (zoom 3 over a 100-tile file -> 13 bounded tiles, stepping by 256*8). vue-tsc clean; bun 61/0."
      ]
    },
    {
      "id": "SF3.1",
      "phase": 3,
      "title": "Settings panel overlay named 'Параметры'",
      "status": "not-started",
      "dependsOn": ["SF0.2"],
      "artifacts": [
        "GnauralCore/ui/pages/AudioPage.vue",
        "GnauralCore/ui/components/SpectrogramView.vue",
        "GnauralCore/ui/components/SpectrogramSettingsPanel.vue",
        "GnauralCore/ui/i18n/en.json",
        "GnauralCore/ui/i18n/ru.json"
      ],
      "verify": "settings panel is a toggleable overlay over the plot (no main-content resize), titled 'Параметры', with toolbar toggle + backdrop + close; i18n en/ru; vue-tsc + bun green",
      "startedAt": null, "finishedAt": null, "blockedReason": null, "evidence": []
    },
    {
      "id": "SF5.1",
      "phase": 5,
      "title": "Audacity-like default track height",
      "status": "done",
      "dependsOn": ["SF0.2"],
      "artifacts": ["GnauralCore/ui/components/SpectrogramView.vue", "GnauralCore/ui/pages/AudioPage.vue"],
      "verify": "spectrogram plot default height matches the Audacity reference; vue-tsc clean",
      "startedAt": "2026-07-01T22:00:00Z", "finishedAt": "2026-07-01T22:10:00Z", "blockedReason": null,
      "evidence": [
        "SpectrogramView gained a `height?: number` prop -> fixed track height via rootStyle (height + minHeight); canvas min-height 240px -> 0 so it flex-fills the fixed track. AudioPage passes :height=spectrogramTrackHeight (ref, default SPECTROGRAM_TRACK_HEIGHT_DEFAULT=260px) and the spectrogram row is items-start (no vertical stretch). This becomes the per-pane height reused by SF5.2 (resize) + SF5.3 (stereo L/R).",
        "vue-tsc clean. Visual confirm = owner (quasar dev)."
      ]
    },
    {
      "id": "SF5.2",
      "phase": 5,
      "title": "Drag-resize track height",
      "status": "done",
      "dependsOn": ["SF5.1"],
      "artifacts": ["GnauralCore/ui/components/SpectrogramView.vue", "GnauralCore/ui/pages/AudioPage.vue", "GnauralCore/ui/i18n/en.json", "GnauralCore/ui/i18n/ru.json"],
      "verify": "a bottom-border drag handle resizes the track height (pointer drag); height persisted in localStorage; vue-tsc + bun green",
      "startedAt": "2026-07-01T22:15:00Z", "finishedAt": "2026-07-01T22:30:00Z", "blockedReason": null,
      "evidence": [
        "SpectrogramView: bottom-edge drag handle (spectrogram-view__resize-handle, ns-resize, pointer-capture drag) emits update:height clamped to [120,1200]. AudioPage uses v-model:height=spectrogramTrackHeight, loads the initial value from localStorage (mindwave-audio-spectrogram-track-height, clamped) and persists on change. i18n audio.spectrogramResizeHandle (en/ru).",
        "vue-tsc clean; GnauralCore bun 61/0; i18n JSON parses."
      ]
    },
    {
      "id": "SF5.3",
      "phase": 5,
      "title": "Split stereo into left/right tracks",
      "status": "done",
      "dependsOn": ["SF5.1"],
      "artifacts": ["GnauralCore/ui/pages/AudioPage.vue", "GnauralCore/ui/components/SpectrogramView.vue", "GnauralCore/server/spectrogram-session.ts"],
      "verify": "stereo files render stacked Left/Right per-channel panes (channel 0/1, separate analyses); mono renders one pane; channel count from AudioBuffer.numberOfChannels; panes labelled L/R; vue-tsc + bun green",
      "startedAt": "2026-07-01T22:35:00Z", "finishedAt": "2026-07-01T23:00:00Z", "blockedReason": null,
      "evidence": [
        "SERVER (enabling change): SpectrogramSession now keeps a Map<analysisId,{wavHandle,params}> instead of a single analysis — onOpen no longer closes the previous (acquires its own source; releases on failure); get-tile/point-query/area-query use the message's analysisId (requireAnalysis(id) validates against the map); reconfigure targets the message's analysisId (close+reopen -> new id, keeps the source); close/dispose close per-analysis. server tests 17/0.",
        "UI: AudioPage renders two stacked SpectrogramViews for stereo (isSpectrogramStereo = spectrogramBuffer.numberOfChannels>=2) with per-channel analysis computeds (spectrogramLeftAnalysis channel:0 / spectrogramRightAnalysis channel:1, stable computeds so no reconfigure churn), labelled L/R; mono renders one pane. Both share v-model:height (resize both). SpectrogramView gained a `label` prop shown in its toolbar.",
        "Backend change -> requires bun server restart. vue-tsc clean; GnauralCore bun 61/0.",
        "FOLLOW-UP: the WORKER was also single-analysis (EnsureCurrent errored on a non-current id -> owner saw 'get-tile analysisId mismatch' on the L channel). SpectrumCoreFftwWorkerProbe.pas now keeps a registry of analyses; EnsureCurrent selects by id + restores that analysis's cache token; open registers (no longer frees the previous), close unregisters, shutdown frees all. Rebuilt + re-bundled (manifest updated; exe gitignored). Verified: two opens then get-tile on the first -> ok; bridge test 15/0."
      ]
    },
    {
      "id": "SF6.1",
      "phase": 6,
      "title": "Profile the spectrogram generation pipeline",
      "status": "done",
      "dependsOn": ["SF0.2"],
      "artifacts": ["SpectrumCore or a scratch harness (profiling)", "spectrogram-fixes ledger (findings)"],
      "verify": "stage-by-stage timing + payload breakdown on d:\\bin\\Presets\\1_Orientation.flac (open-analysis, get-tile sample-load vs FFT, tile bytes + serialize/parse, WS, UI render); bottleneck named; owner approval gate on the fix",
      "startedAt": "2026-07-02T00:30:00Z", "finishedAt": "2026-07-02T01:20:00Z", "blockedReason": null, "evidence": [
        "Recon: for wav/flac the source path passes STRAIGHT to the worker (worker decodes flac natively). Tiles transfer as JSON.",
        "PROFILED d:\\bin\\Presets\\1_Orientation.flac (33 min, 171144 frames, 2049 bins, 44.1k) via a stdio harness against a temporarily-instrumented worker: open-analysis = 22.2 s TOTAL = FLAC decode 3.4 s + STFT loop 18.8 s (DOMINANT). Full-view get-tile set (3 tiles) = 2.3 s wall, payload 1.59 MB, JSON parse 7 ms. Re-fetch not cached (~0.8 s).",
        "BOTTLENECK = the eager full-file STFT loop in TFftwAnalysis.Create (SpectrumCoreFftwAnalysis.pas). It computes + stores magnitude AND phase AND unwrapped-phase for EVERY frame (ArcTan2 per bin ~350M calls + UnwrapFramePhases per frame) into 3 x ~1.4 GB matrices, EVEN when data=magnitude (default) -> phase/uphase work is entirely unused. Transport (JSON/WS) and get-tile are NOT the bottleneck.",
        "Instrumentation was temporary (StdErr timers) and has been reverted; worker rebuilt + re-bundled clean.",
        "PROPOSED FIXES (owner gate): (A) low-risk quick win: skip phase/unwrapped-phase compute+store unless data mode needs them (phase/uphase) -> removes ~350M ArcTan2 + unwrap + 2/3 of the ~4GB memory writes for the default magnitude mode; expected large cut of the 18.8 s. (B) multithread the frame loop (frames independent) -> ~/cores. (C) architectural: lazy/progressive per-visible STFT (Audacity-style). Recommend A first (biggest bang/lowest risk), then measure, then consider B."
      ]
    },
    {
      "id": "SF6.2",
      "phase": 6,
      "title": "Optimization A: skip unused phase work",
      "status": "in-progress",
      "dependsOn": ["SF6.1"],
      "artifacts": ["SpectrumCore/src/core/SpectrumCoreFftwAnalysis.pas", "MindWaveCore bundle (rebuild+rebundle)"],
      "verify": "worker computes/stores phase + unwrapped-phase only when the data mode needs them; magnitude/reassign/pitch modes skip ArcTan2 + unwrap + the 2 phase matrices; point/area-query phase reads guarded; open-analysis time drops (re-profile on 1_Orientation.flac); PAUSE for manual UI verification (spectrum still correct)",
      "startedAt": "2026-07-02T01:30:00Z", "finishedAt": null, "blockedReason": null, "evidence": []
    },
    {
      "id": "SF6.3",
      "phase": 6,
      "title": "Optimization B: multithread the STFT loop",
      "status": "not-started",
      "dependsOn": ["SF6.2"],
      "artifacts": ["SpectrumCore/src/core/SpectrumCoreFftwAnalysis.pas"],
      "verify": "per-frame FFT loop parallelized across threads (per-thread plan/buffers); output identical; open-analysis time ~/cores (re-profile); PAUSE for manual UI verification",
      "startedAt": null, "finishedAt": null, "blockedReason": null, "evidence": []
    },
    {
      "id": "SF6.4",
      "phase": 6,
      "title": "Optimization C: lazy/progressive per-visible STFT",
      "status": "not-started",
      "dependsOn": ["SF6.2"],
      "artifacts": ["SpectrumCore (worker analysis)", "GnauralCore (protocol/session if needed)"],
      "verify": "open-analysis cheap; STFT computed on demand for the visible window with a bounded cache; cost scales with what is shown; re-profile; PAUSE for manual UI verification",
      "startedAt": null, "finishedAt": null, "blockedReason": null, "evidence": []
    },
    {
      "id": "SF5.4",
      "phase": 5,
      "title": "Match Audacity default spectrogram settings",
      "status": "done",
      "dependsOn": ["SF0.2"],
      "artifacts": ["GnauralCore/ui/composables/spectrogram-settings.ts"],
      "verify": "DEFAULT_SPECTROGRAM_SETTINGS matches the Audacity defaults screenshot; vue-tsc + bun green",
      "startedAt": "2026-07-02T00:15:00Z", "finishedAt": "2026-07-02T00:20:00Z", "blockedReason": null,
      "evidence": [
        "Set DEFAULT_SPECTROGRAM_SETTINGS to Audacity defaults per the owner screenshot: fscale log, startHz 80, stopHz 4000, gain 1 (0 dB), drange 120, frequencyGain 0, data magnitude (Frequencies), window 2048, winFunc hann, zeroPaddingFactor 2, scale log. Changed from prior defaults: zeroPaddingFactor 1->2, startHz 0->80, stopHz 0->4000, palette intensity->rainbow. Aligned the 'audacity' preset to match. vue-tsc clean; bun 61/0.",
        "NOTES: (1) palette = 'rainbow' is our closest COLOURED option to Audacity's 'Color (pink)' — we don't have the exact Audacity pink colormap. (2) Existing localStorage settings override defaults, so a user with saved settings must Reset / pick the Audacity preset to see the new defaults."
      ]
    },
    {
      "id": "SF4.1",
      "phase": 4,
      "title": "Auto-prepare spectrum when the Спектрограмма tab is opened",
      "status": "done",
      "dependsOn": ["SF0.2"],
      "artifacts": [
        "GnauralCore/ui/pages/AudioPage.vue",
        "GnauralCore/ui/i18n/en.json",
        "GnauralCore/ui/i18n/ru.json"
      ],
      "verify": "switching to the Спектрограмма tab with a selected local audio file (no decoded buffer) triggers ensureLocalAudioReady so the spectrum prepares (spinner -> spectrum) instead of the stale 'start local playback' message; noSpectrogram wording updated; .gnaural unchanged; vue-tsc + bun green",
      "startedAt": "2026-07-01T19:40:00Z", "finishedAt": "2026-07-01T19:55:00Z", "blockedReason": null,
      "evidence": [
        "AudioPage: added ensureSpectrogramPrepared() — when the player Спектрограмма view is active and a local audio file (wav/flac) is selected with no decoded buffer and no decode in flight, it calls audio.ensureLocalAudioReady(path, kind) (same non-playback decode as RF4.1). Wired via watch([activePlayerViewTab, activeContentTab, () => audio.selectedPath], immediate) guarded to the player content tab. Guard skips when already loading/decoded, so it won't double-fetch with RF4.1's selection decode (wavLoading is set synchronously first).",
        "Updated stale audio.noSpectrogram wording (a selected file now auto-prepares): en 'Select an audio file to view its spectrum.' / ru 'Выберите аудиофайл, чтобы посмотреть его спектр.'. .gnaural (noGnauralSpectrogram) unchanged.",
        "Verify: vue-tsc --noEmit clean; GnauralCore bun 56/0; i18n JSON parses. Live-verify in quasar dev (owner): select on Воспроизведение -> switch to Спектрограмма -> spinner then spectrum (no dead message)."
      ]
    }
  ],
  "log": [
    { "ts": "2026-07-01T18:55:00Z", "step": "SF0.1", "event": "done", "note": "Spectrogram-fixes plan + ledger created. 3 items: (1) loading indicator during prepare; (2) fix only-last-frame render (tiles don't accumulate; owner repro = on open, one-tile strip runs L->R, only last stays); (3) settings side panel -> overlay named 'Параметры' (no main-content resize). Decisions SF-D1..SF-D5 drafted; next = SF0.2 approval gate." },
    { "ts": "2026-07-01T19:00:00Z", "step": "SF0.2", "event": "done", "note": "Owner approved ('go'). SF-D2..SF-D5 locked. Proceeding to Phase 1." },
    { "ts": "2026-07-01T19:05:00Z", "step": "SF1.1", "event": "done", "note": "Added a loading overlay (spinner + 'Подготовка спектрограммы…') in SpectrogramView driven by spec.loading (analysis open + tiles pending). vue-tsc clean." },
    { "ts": "2026-07-01T19:10:00Z", "step": "-", "event": "note", "note": "Owner: none of the session's UI changes were visible -> the prod app served the stale built server/public. Rebuilt UI (quasar build). Owner then switched to quasar dev for live HMR." },
    { "ts": "2026-07-01T19:25:00Z", "step": "SF1.1", "event": "fixed", "note": "Owner: spinner still not appearing. Root cause: spec.loading only counted pending tiles, so it was false during the slow worker open()/reconfigure() and tiles arrived too fast. Added an `opening` flag (open/reconfigure -> loading true; cleared at refetch/error/close) so the indicator covers the whole prepare span. vue-tsc + bun green." },
    { "ts": "2026-07-01T19:40:00Z", "step": "SF4.1", "event": "planned", "note": "New owner item (SF-D6): on the Воспроизведение tab select a file, switch to Спектрограмма -> stale 'start local playback' message and nothing happens. Add Phase 4: auto-decode (ensureLocalAudioReady) when the Спектрограмма tab opens for a selected undecoded local file + refresh noSpectrogram wording. Awaiting owner 'go'." },
    { "ts": "2026-07-01T19:55:00Z", "step": "SF4.1", "event": "done", "note": "AudioPage now auto-prepares the spectrum when the Спектрограмма tab is opened for a selected undecoded local file (ensureSpectrogramPrepared + watch); stale noSpectrogram wording refreshed (en/ru). vue-tsc + bun 56/0." },
    { "ts": "2026-07-01T20:20:00Z", "step": "SF2.1", "event": "done", "note": "Fixed only-last-frame render: tiles now persist/accumulate. Cache getByTileIndex binCount-fallback + assembleVisibleTiles uses it; always re-assemble on tile arrival; setView no-op guard against churn; viewBinCount rounded to 32-step. +4 unit tests (incl. explicit regression). vue-tsc clean; GnauralCore bun 60/0. Owner visual confirm pending." },
    { "ts": "2026-07-01T20:45:00Z", "step": "SF2.1", "event": "fixed", "note": "Owner screenshot: bug persisted (strip runs L->R then vanishes; spinner stuck). REAL root cause: planVisibleTiles used a fixed 256 full-res frames/tile regardless of zoom -> a long file's full view plans hundreds of tiles that overflow the 128-LRU cache -> only rightmost survive. Fix: tile span = tileFrames*2^zoom full-res frames, so zoomed-out views need only a handful of tiles that fit the cache and all render. +1 unit test. vue-tsc clean; bun 61/0." },
    { "ts": "2026-07-01T21:05:00Z", "step": "SF2.1", "event": "reopened", "note": "Owner screenshot after the zoom fix: spinner clears but the plot is fully blank (no tiles AND no axes). Since the spinner clears when pending tiles drain (incl. on error), the wide/high-zoom get-tile requests are most likely erroring -> pending drained -> empty. Added diagnostics/robustness (no behavioural fix yet): (1) SpectrogramView draws axes whenever an analysis is present (blank plot != no analysis); (2) surfaces spec.error as an on-screen overlay; (3) close() resets currentView so the no-op guard can't skip the post-open setView. Need the on-screen error text / browser console to pin the worker failure before the real fix. vue-tsc clean; bun 61/0." },
    { "ts": "2026-07-01T21:25:00Z", "step": "SF2.1", "event": "fixed", "note": "Owner error surfaced: 'worker request timed out after 15000ms' on a 25-min FLAC (d:\\bin\\Presets\\01 - Elation Galaxy.flac). Root cause of the blank: heavy worker ops (full-file STFT open-analysis / wide overview get-tile) exceed the flat 15s SpectrogramWorkerManager timeout, which poisons + restarts the worker -> analysis/tiles never arrive -> blank. Fix: spectrogram-session.ts now passes generous per-command timeouts (open-analysis/reconfigure 120s, get-tile 60s). BACKEND change -> requires bun server restart (not UI HMR). server tests 17/0." },
    { "ts": "2026-07-01T21:45:00Z", "step": "SF5", "event": "planned", "note": "Owner addition (attached Audacity reference): new Phase 5 'Make the UI Audacity-like' — SF5.1 default track height, SF5.2 drag-resize via bottom border, SF5.3 stereo left/right split. Decisions SF-D7/D8/D9. Awaiting owner 'go'." },
    { "ts": "2026-07-01T22:00:00Z", "step": "-", "event": "note", "note": "Owner: prioritize the Audacity-like UI (Phase 5) first so the remaining spectrum-render issue can be described in detail afterwards. SF2.1 left in-progress, SF3.1 deferred; proceeding with Phase 5." },
    { "ts": "2026-07-01T22:10:00Z", "step": "SF5.1", "event": "done", "note": "SpectrogramView takes a height prop (fixed track height); AudioPage sets default 260px per track (items-start row). Reused by SF5.2/SF5.3. vue-tsc clean." },
    { "ts": "2026-07-01T22:30:00Z", "step": "SF5.2", "event": "done", "note": "Bottom-edge drag handle in SpectrogramView resizes the track (pointer-capture, clamp 120-1200) via v-model:height; AudioPage persists the height to localStorage (loaded on init). i18n added. vue-tsc clean; bun 61/0." },
    { "ts": "2026-07-01T23:00:00Z", "step": "SF5.3", "event": "done", "note": "Stereo L/R split. Enabling server change: SpectrogramSession now supports multiple concurrent analyses keyed by analysisId (was single-analysis; blocked two-channel view). UI: AudioPage stacks two per-channel SpectrogramViews (channel 0/1, labelled L/R) for stereo (numberOfChannels>=2), one pane for mono; shared v-model:height. server 17/0, ui 61/0, vue-tsc clean. Requires bun server restart." },
    { "ts": "2026-07-02T00:30:00Z", "step": "SF2.1", "event": "done", "note": "Only-last-frame render RESOLVED (tile-count-by-zoom + worker timeouts + worker multi-analysis + WS multicast); owner moved on to speed, so the frames render. Remaining SF2.1 concern = generation SPEED, re-scoped to Phase 6 (SF-D10)." },
    { "ts": "2026-07-02T01:20:00Z", "step": "SF6.1", "event": "profiled", "note": "Bottleneck found: open-analysis 22.2s = FLAC decode 3.4s + eager full-file STFT loop 18.8s (dominant). The loop computes phase+unwrapped-phase (350M ArcTan2 + unwrap) + 3x ~1.4GB matrices EVEN for data=magnitude (unused). JSON/WS transport + get-tile are fine (full view 2.3s / 1.6MB / parse 7ms). Proposed: (A) skip phase/uphase unless needed [quick win], (B) multithread the loop, (C) lazy per-visible STFT. Awaiting owner gate. Profiling timers reverted; worker rebuilt+rebundled clean." },
    { "ts": "2026-07-02T01:25:00Z", "step": "SF6.1", "event": "note", "note": "Owner saw an error popup 'EInOutError: File not open' during profiling — caused by my temporary StdErr timers in the GUI-subsystem worker (no console). Reverted the timers, rebuilt + re-bundled the clean worker, killed stray worker processes. LATENT ISSUE noted: the worker is a GUI app that shows a message box on unhandled exceptions (e.g. a broken stdio pipe on kill/timeout) — worth a hardening follow-up (global handler / console subsystem)." },
    { "ts": "2026-07-02T00:30:00Z", "step": "SF6.1", "event": "planned", "note": "Owner: main problem is generation SPEED (Audacity much faster). Suspects: single-threaded worker FFT, JSON-not-binary tile transport, repeated per-request FLAC decode. Work via plan+ledger; profile the pipeline on d:\\bin\\Presets\\1_Orientation.flac first, then optimize at a gate." },
    { "ts": "2026-07-02T00:20:00Z", "step": "SF5.4", "event": "done", "note": "Set default spectrogram settings to match Audacity (owner screenshot): log scale 80-4000 Hz, 0 dB gain, 120 dB range, HF boost 0, Frequencies/magnitude, window 2048 Hann, zero-pad 2; palette rainbow (closest coloured to Audacity 'Color pink'). audacity preset aligned. vue-tsc + bun 61/0. NB: existing saved settings override defaults (Reset/preset to apply). Next: SF2.1." },
    { "ts": "2026-07-02T00:05:00Z", "step": "SF5.3", "event": "fixed", "note": "Owner: left channel loaded but the RIGHT channel spun forever (no data, no error). Root cause: ws.ts dispatch() stops at the first handler returning true, and every spectrogram composable's onSpectrogram handler returned true -> the first-registered (left) view consumed ALL spectrogram messages (filtering by requestId), so the second (right) view's handler was never called and its open/tile responses never arrived. Fix: multicast spectrogram messages to a dedicated Set of subscribers (all views receive every spectrogram message); regular ServerEvents keep single-consumer dispatch. UI-only change (HMR / page reload; no backend restart). vue-tsc clean; ui 61/0." },
    { "ts": "2026-07-01T23:40:00Z", "step": "SF5.3", "event": "fixed", "note": "Owner error on the left channel: 'get-tile failed: get-tile analysisId mismatch'. The WORKER (SpectrumCoreFftwWorkerProbe) was single-analysis too (one global Current; EnsureCurrent erroring on a different id) — the server multi-analysis change alone wasn't enough. Made the worker keep a registry of analyses; EnsureCurrent(id) now SELECTS the analysis and restores its TileCacheAnalysisToken (which keys both tile caches, so caching stays correct per analysis); open registers instead of freeing the previous; close unregisters; shutdown frees all. Rebuilt the probe + re-bundled into MindWave (manifest.json updated; exe gitignored). Verified via direct stdio probe: open AAA + BBB, get-tile AAA -> ok (frames=33). Bridge integration test 15/0. Requires bun server restart to pick up the new bundled worker." }
  ]
}
