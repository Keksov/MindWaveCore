{
  "schemaVersion": 1,
  "plan": "spectrogram-fixes",
  "planDoc": "docs/spectrogram-fixes/spectrogram-fixes-plan.md",
  "title": "Spectrogram fixes (loading indicator, only-last-frame render, settings overlay 'Параметры')",
  "status": "active",
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-06T00:41:00Z",
  "decisions": [
    {
      "id": "SF-D1",
      "text": "Scope & process: 3 items -> 3 phases. Per-step atomic commits (step-id prefixed, Co-Authored-By); NO push; phase-boundary pauses; vue-tsc + bun green each step. Code in GnauralCore; ledger in MindWaveCore."
    },
    {
      "id": "SF-D2",
      "text": "Loading indicator: surface worker/tile loading in SpectrogramView as an overlay spinner driven by spec.loading (analysis open + tiles pending); keep audio.spectrogramLoading (decode) in AudioPage so an indicator covers the whole prepare span (decode -> open -> first tiles)."
    },
    {
      "id": "SF-D3",
      "text": "Only-last-frame fix: make the rendered tile set persist/accumulate during load: (a) guard setView/applyView to no-op when the effective view (time window + zoom + viewBinCount) is unchanged (kill viewSeq churn); (b) re-assembleVisibleTiles on every tile arrival against the current cache (not only when pending.seq===viewSeq); (c) final assemble+redraw when loading clears. Root cause confirmed at execution via instrumentation; add a composable unit test for tile accumulation across a binCount/seq change. Confirmed repro (owner): happens on OPEN, no playback; a one-tile strip runs L->R and only the last tile stays."
    },
    {
      "id": "SF-D4",
      "text": "Settings panel = overlay named 'Параметры': convert the fixed 264px side column into a toggleable overlay mirroring the schedule settings panel (absolute aside over the plot, no main-content resize, backdrop, <transition>); header title 'Параметры' (audio.spectrogramSettingsTitle: en 'Parameters' / ru 'Параметры') + close (x). Default closed."
    },
    {
      "id": "SF-D5",
      "text": "Toggle placement: the 'Параметры' toggle lives in the SpectrogramView toolbar (next to zoom/fit); open/close state + overlay are self-contained in the view; state local (not persisted) unless owner asks otherwise."
    },
    {
      "id": "SF-D7",
      "text": "Owner addition 2026-07-01 (Audacity-like UI #1): give the spectrogram plot a taller default height matching the Audacity reference (per-channel ~240px; stereo split ~2x) instead of the current min-height:280px flex fill."
    },
    {
      "id": "SF-D8",
      "text": "Owner addition (Audacity-like UI #2): a drag handle on the bottom border of the spectrogram track resizes its height via pointer drag (like Audacity); persist the chosen height (localStorage)."
    },
    {
      "id": "SF-D21",
      "text": "Owner addition 2026-07-02 (Phase 10 item 1): render one shared header bar above the first track holding ALL control buttons (zoom in/out, fit); buttons leave the per-track toolbar and exist once for the whole spectrogram."
    },
    {
      "id": "SF-D22",
      "text": "Owner addition 2026-07-02 (Phase 10 item 2): the L label sits in the SAME place as R - a top-left corner overlay on the plot, not in the toolbar. Both channel labels become plot overlays."
    },
    {
      "id": "SF-D23",
      "text": "Owner addition 2026-07-02 (Phase 10 item 3): the horizontal time ruler is drawn only above the first track and below the last track (middle tracks carry no time ruler when >2). The vertical frequency axis stays on every track."
    },
    {
      "id": "SF-D24",
      "text": "Owner addition 2026-07-02 (Phase 10 item 4) DECIDED: move the timespan selector out of the header to a fixed bar at the very bottom of the spectrogram window (position fixed). Owner chose a FULL minimap-style overview (canvas overview + draggable/resizable visible-window handle) modelled on the schedule view 68px bottom minimap - NOT a relocated q-range slider. Readout moves to a cursor tooltip (SF-D25) so the header holds only buttons."
    },
    {
      "id": "SF-D26",
      "text": "Owner addition 2026-07-02 (Phase 10 item 5): remove the 'Спектрограмма' subtitle (audio.spectrogramTitle, text-subtitle2) shown between the Старт/transport toolbar and the plot in the spectrogram tab."
    },
    {
      "id": "SF-D30",
      "text": "Owner addition 2026-07-03 (Phase 11 item 3, step 2) DESIGN awaiting go: Audacity-style display-resolution STFT. Root cause (measured): the worker computes the FULL-res STFT for a tile's whole span (e.g. 171144 FFTs for a 33-min overview) then max-pools -> cold overview ~3.9s; zoom/pan recompute. Audacity SpecCache::Populate/CalculateOneSpectrum computes ONE windowSize FFT per DISPLAY column at from=where[0]+xx*samplesPerPixel (samplesPerPixel=sampleRate/pixelsPerSecond), so FFT count = display width, independent of clip length -> instant zoom. Plan: add a display-resolution STFT path to the worker (compute emittedColumns FFTs at stride spanSamples/emittedColumns, reuse Opt-B parallel pool + Opt-A magnitude-only, map to viewBinCount display bins, drop full-res EnsureRange+pyramid for zoomed views; zoom0/full-res path unchanged); keep W3 + SF11.2 caches + pan copy-range reuse. Tradeoff accepted: zoomed-out views approximate (skip samples between columns), exactly Audacity's behaviour. Expected cold overview ~3.9s -> <~100ms. Worker rebuild+rebundle+tests+re-profile+UI verify. APPROVAL GATE before implementing."
    },
    {
      "id": "SF-D38",
      "text": "Owner addition 2026-07-04 (Phase 14 item 1): 'Channel mode' (Режим каналов) is a NO-OP. Owner saw 'Combined' yet both channels draw. Investigation: the worker only parses mode -> Config.ChannelMode and echoes it (responses/matrix manifest); it does NOT affect the STFT (always analyses the single ChannelIdx). The stereo L/R split is UI-driven (AudioPage opens channel 0 + channel 1 as two analyses, SF5.3), independent of mode. Owner decision needed: (a) REMOVE the field (matches our always-separate L/R split), or (b) IMPLEMENT it (Combined = one mixed track, Separate = current L/R split; bigger UI change). Awaiting decision + go."
    },
    {
      "id": "SF-D42",
      "text": "Owner addition 2026-07-04 (Phase 15 item 1): match Audacity's track mouse navigation (Ctrl/Alt/Shift + wheel/drag combos for time zoom/scroll + frequency/vertical zoom). RESEARCH the vendored Audacity 3.7.8 sources first: src/tracks/ui/CommonTrackPanelCell.cpp (HandleWheelRotation: plain wheel=scroll, Ctrl=time zoom about the pointer, Shift=horizontal scroll, + combos), and SpectrumVZoomHandle.cpp / WaveChannelVZoomHandle.cpp / SpectrumVRulerControls.cpp (freq-ruler drag/wheel vertical zoom). Enumerate ALL modifier+mouse gestures, then map onto SpectrogramView (currently wheel=time zoom, drag=area select, click=seek; minimap=pan/zoom). Deliverable = gesture table + implementation. Research + go."
    },
    {
      "id": "SF-D43",
      "text": "Owner addition 2026-07-04 (Phase 15 item 2): the panel 'Channel' param is VESTIGIAL. Owner: says 'Left' yet both channels draw. Investigation: AudioPage always opens channel 0 (L) + channel 1 (R) as two tracks (spectrogramLeftAnalysis/spectrogramRightAnalysis spread {...analysisParams, channel:0/1}), IGNORING the panel value; a mono file has only channel 0 (so 'Right' is invalid). So the Channel field does nothing useful (like the removed 'mode'). Proposal: REMOVE the Channel field. Recommend remove; awaiting go."
    },
    {
      "id": "SF-D41",
      "text": "Owner addition 2026-07-04 (Phase 14 variant b) DECIDED: overlap is the frame-step control (Audacity/ffmpeg style); drop the redundant hop field. Removed hop from the SpectrogramSettings model; toAnalysisParams derives hop = max(1, round(window * (1 - overlap))) (same formula as the worker's NormalizeSpectrumAnalysisConfig). Default unchanged (overlap 0.75 -> hop 512)."
    },
    {
      "id": "SF-D40",
      "text": "Owner addition 2026-07-04 (Phase 14 item 3): audit ALL spectrogram parameters for real use (motivated by the mode no-op, SF-D38). Trace each analysis param (window, zeroPaddingFactor, hop, overlap, channel, winFunc, data, fscale, startHz, stopHz, mode) into the worker STFT, and each render param (scale, gain, frequencyGain, drange, limit, saturation, palette) into the client tile render. Deliverable = per-parameter verdict (used/partial/no-op + code path) + a proposal (remove/wire/fix) for each dead one. Known suspects: mode (dead), overlap vs hop (redundancy?), limit/saturation (confirm). Awaiting go."
    },
    {
      "id": "SF-D39",
      "text": "Owner addition 2026-07-04 (Phase 14 item 2): change the 'Channel' field from a raw 0/1 number to a localized Left/Right select (value 0/1). Note: for stereo this is overridden per track (L=0, R=1) by the SF5.3 split, so mostly a display/clarity change. Awaiting go."
    },
    {
      "id": "SF-D35",
      "text": "Owner addition 2026-07-04 (Phase 13 item 1): regroup SpectrogramSettingsPanel into Audacity-style sections (owner attach): Масштаб (freq scale / min Hz / max Hz), Цвет (intensity scale, gain, range, limit, HF-boost, saturation, palette), FFT-фильтр (algorithm/data, window size, window type, zero-padding, hop, overlap, channel mode, channel)."
    },
    {
      "id": "SF-D36",
      "text": "Owner addition 2026-07-04 (Phase 13 item 2): i18n for parameter VALUES - localize dropdown option labels (window functions, data/algorithm, freq scale, intensity scale, channel mode, palette) like Audacity ('Тип окна: Ханна', 'Алгоритм: Частоты', 'Шкала: Логарифмическая', 'Тема: Цвет (розовый)'), en/ru."
    },
    {
      "id": "SF-D37",
      "text": "Owner addition 2026-07-04 (Phase 13 item 3): a '?' help icon next to each parameter opening a short help popover for that parameter; help text written en/ru."
    },
    {
      "id": "SF-D34",
      "text": "Owner addition 2026-07-04 (Phase 12): fix the TSpecFft real-FFT mirror bug. TSpecFft.RealFFTf + ReorderToFreq (SpectrumCore/src/core/SpectrumCoreFft.pas, ported from Audacity hfft.cpp) produce a spurious mirror peak at bin N/2-k for a real cosine at bin k (up to ~80% of the true peak, bin-dependent), folding the magnitude spectrum about N/4; also affects SpectrumCoreStft.pas. SCOPE: INDEPENDENT of the MindWave spectrogram (which uses the FFTW path TFftwAnalysis/fftwf_*, NOT affected); distinct SpectrumCore core bug found by SoundCore (worked around there by band-limiting <=10kHz). Root-cause hypotheses: bit-reversal indexing (FBitReversed) in ReorderToFreq, or the RealFFTf conjugate-symmetric massage; compare bin-by-bin vs original Audacity RealFFTf/ReorderToFreq. Verify: 10 cases (cosine/sine sweep, impulse, DC, Nyquist, two-tone, FFTW/NumPy reference <1e-3, Parseval, inverse round-trip) across N in {256,512,1024,2048,4096}; clean = every non-signal bin < 0.1% of the true peak. Ref: SpectrumCore/TSPECFFT-MIRROR-BUG.md."
    },
    {
      "id": "SF-D27",
      "text": "Owner addition 2026-07-03 (Phase 11 item 1): audit SpectrogramSettingsPanel (and spectrogram controls) for hardcoded/untranslated strings and route them through i18n (en/ru) so the whole Параметры panel is localized."
    },
    {
      "id": "SF-D28",
      "text": "Owner addition 2026-07-03 (Phase 11 item 2): spectrogram caching is not actually working - switching A->B->A recomputes A from scratch (SF7.3 warm LRU not reused). Find why the reuse path misses (cache key mismatch, close/dispose evicting, per-socket session lifecycle, or UI reopening with changed params) and make returning to a recent file skip the re-decode/STFT."
    },
    {
      "id": "SF-D29",
      "text": "Owner addition 2026-07-03 (Phase 11 item 3): speed up tile generation (Audacity is faster on the same files). Step 1: profile OUR get-tile pipeline (worker STFT-on-demand + tile build + JSON/WS + UI raster) and apply wins, then owner verifies. Step 2 (only if step 1 insufficient): study Audacity 3.7.8 sources at c:\\projects\\KKMindWave\\SpectrumCore\\lib\\vendor\\audacity\\3.7.8 to learn why it is faster and port the approach."
    },
    {
      "id": "SF-D25",
      "text": "Owner addition 2026-07-02 (Phase 10): the area/point readout (peak dB / value under the pointer) is shown as a tooltip at the mouse position on the plot, not in the header or the bottom bar."
    },
    {
      "id": "SF-D18",
      "text": "Owner addition 2026-07-02 (Phase 9 item 1): remove border-radius on the spectrogram track block (.spectrogram-view) -> square corners like Audacity."
    },
    {
      "id": "SF-D19",
      "text": "Owner addition 2026-07-02 (Phase 9 item 2): a 2px horizontal divider between adjacent tracks; dragging it MUTUALLY resizes the two (track above and below change in opposite directions, combined height ~constant). Requires per-track heights (not one shared value). NOTE: owner wrote 'одна уменьшается, вторая уменьшается' - interpreted as one shrinks / other grows (standard boundary drag); confirm if different."
    },
    {
      "id": "SF-D20",
      "text": "Owner addition 2026-07-02 (Phase 9 item 3): the resize area at the bottom of the LAST track resizes ALL tracks by the same amount (equal heights), unlike the divider (mutual). Move resize handling to the AudioPage stack (per-track dividers + one bottom handle); SpectrogramView takes height as a plain prop, retiring its SF5.2 self-resize handle."
    },
    {
      "id": "SF-D15",
      "text": "Owner addition 2026-07-02 (Phase 8 item 1): Audacity-style multi-track chrome. For >1 track (stereo): control icons/toolbar (zoom/fit/range/readout) on the FIRST track only; per-track bottom resize handle; UNIFIED span/area selector across all tracks (selection on any track applies to + renders on all -> lift selection state to parent/shared store); remove inter-track chrome/gap so tracks abut. Guard: per-track click->seek + hover readout still correct."
    },
    {
      "id": "SF-D16",
      "text": "Owner addition 2026-07-02 (Phase 8 item 2): adaptive frequency-axis tick density. Reveal more level labels as the track height grows (fewer when short), like Audacity, instead of a fixed ~6 ticks; derive the tick target from plot height."
    },
    {
      "id": "SF-D17",
      "text": "Owner addition 2026-07-02 (Phase 8 item 3) DECIDED = option (a): build only on explicit user action - tree file click (RF4.1) or switching TO the Спектрограмма sub-tab - NOT on tab/page mount for a restored selection. Implementation: drop immediate:true from the SF4.1 ensureSpectrogramPrepared watch so it fires only on subsequent changes, not on mount."
    },
    {
      "id": "SF-D12",
      "text": "Owner addition 2026-07-02 (item 1): non-blocking load progress. On file select, render the tracks (spectrogram/schedule) view immediately + a small Audacity-style progress dialog OVERLAY while decode/prepare runs, instead of the full-panel 'Загрузка…' placeholder that hides the tracks."
    },
    {
      "id": "SF-D13",
      "text": "Owner addition 2026-07-02 (item 2) DECIDED = option (b) 'never recompute on resize'. Make viewBinCount a fixed constant (decoupled from track height; e.g. 512): a height-only resize leaves the view (time window+zoom+viewBinCount) unchanged so the setView no-op guard skips refetch, and the raster just scales to the new height (drawImage already stretches to plotH). Tradeoff accepted: enlarging shows fixed-resolution raster stretched (slightly softer). Width/time zoom still refetch."
    },
    {
      "id": "SF-D14",
      "text": "Owner addition 2026-07-02 (item 3): cache spectrograms across file switches. Opt C made analyses lazy (samples-only ~350MB, no matrix), so keep an LRU of a few open worker analyses keyed by file+params (switching back reuses; evict oldest, bounded by memory). Alt: client-side tile cache keyed by file+params. Recommend worker-analysis LRU; cap refined at execution."
    },
    {
      "id": "SF-D11",
      "text": "Owner addition 2026-07-02: performance optimization approach A->B->C, each measured + UI-verified before the next. A (SF6.2): skip computing/storing phase + unwrapped-phase unless the data mode needs them (default magnitude doesn't) -> removes ~350M ArcTan2 + per-frame unwrap + 2 of 3 ~1.4GB matrices. B (SF6.3): multithread the independent per-frame FFT loop (per-thread FFTW plan/buffers). C (SF6.4): lazy/progressive per-visible STFT with a bounded cache (Audacity-style). All are worker (Pascal) changes -> rebuild + re-bundle + backend restart; per-step commit; pause for manual UI verification each. Owner: plan all three, implement A first with a UI-verification pause."
    },
    {
      "id": "SF-D10",
      "text": "Owner addition 2026-07-02 (SF2.1 real concern = SPEED): the only-last-frame visual bug is resolved; the remaining SF2.1 problem is spectrogram GENERATION SPEED (Audacity is much faster). PROFILE FIRST, optimize second (approval gate before any optimization). Measure each pipeline stage on d:\\bin\\Presets\\1_Orientation.flac: worker open-analysis, per-tile get-tile (sample-load/FLAC-decode vs FFT), tile payload bytes + JSON serialize/parse, WS transfer, UI render. Suspects to confirm/refute: single-threaded worker FFT; JSON (not binary) tile transport of large float arrays; repeated per-request FLAC decode (worker reads FLAC directly, no cached PCM)."
    },
    {
      "id": "SF-D9",
      "text": "Owner addition (Audacity-like UI #3): split stereo into stacked Left-over-Right panes, each analysing one channel (channel:0/channel:1, its own worker analysis); mono = single pane. Detect channel count from the decoded AudioBuffer.numberOfChannels (audio.spectrogramBuffer); label panes L/R. PREREQUISITE discovered at execution: the server kept one SpectrogramSession per WS and it was single-analysis (a 2nd open closed the 1st; get-tile used the session's stored analysisId). Refactored SpectrogramSession to hold multiple concurrent analyses keyed by analysisId (worker already multiplexes; client msgs already carry analysisId), so two channel analyses coexist over one socket. Backend change -> server restart."
    },
    {
      "id": "SF-D6",
      "text": "Owner addition 2026-07-01: prepare the spectrum when the Спектрограмма tab is opened. The tab renders only when audio.spectrogramBuffer!=null; RF4.1 decodes on tree selection, but selecting a file on the Воспроизведение tab then switching to Спектрограмма (or a selected-but-undecoded file) leaves the stale 'start local playback' message with nothing happening. Fix: when the Спектрограмма view becomes active for a selected LOCAL audio file (wav/flac) with no decoded buffer and no decode in flight, trigger audio.ensureLocalAudioReady(selectedPath, kind) (same non-playback decode as RF4.1); update the obsolete audio.noSpectrogram wording to cover only the no-file-selected case. .gnaural unchanged."
    },
    {
      "id": "SF-D44",
      "text": "SF16.1 — Numeric spinbox fields for render dials (gain/drange/limit/frequencyGain/saturation) replacing sliders; q-input type=number with +/- arrows (or explicit +/- buttons + keyboard). Client-only render dials. OPEN: 'default 0' is degenerate for a linear gain multiplier and for drange/saturation; proposed resolution = switch gain to dB (0 dB neutral, mult=10^(dB/20)); frequencyGain already dB/dec (0), limit already 0 dBFS -> those three default 0; drange stays 120, saturation stays 1. Awaiting owner confirmation of the mapping/defaults."
    },
    {
      "id": "SF-D45",
      "text": "SF16.2 — Full Audacity palette set. Add 4 schemes: Розовый=Color(default/New) porting specColormap[256][3] LUT from Audacity 3.7.8 lib-theme/AColorResources.h (magma pink->orange->yellow); Классика=Color(classic) HSV blue->red (~current rainbow); Оттенки серого=Grayscale (~current intensity); Инверсия оттенков серого=1-v. Extend SpectrogramPalette + paletteColor + SPECTROGRAM_PALETTES + palette i18n + coverage test. Client-only."
    },
    {
      "id": "SF-D46",
      "text": "SF16.3 — Zoom preset popover opened on right-click (Win context menu; macOS ctrl/right-click same handler): presets x1 x2 x3 x4 x8 x16 + a % field that shows current zoom and accepts an exact value (Illustrator-style). Zoom = time-axis zoom; x1/100%=whole-clip fit, xN=N x into clip about view centre. New popover wired to SpectrogramView zoom state. OPEN: confirm x1='fit whole clip' vs '1px/sample'."
    },
    {
      "id": "SF-D47",
      "text": "SF16.4 — Home/End/arrow keyboard navigation matching Audacity: Home->view start, End->view end, Left/Right->scroll step (page vs nudge modifiers pinned from Audacity key table at execution). Extends SF15.1 nav with a keydown handler + canvas tabindex/focus. Client-only."
    },
    {
      "id": "SF-D48",
      "text": "SF16.5 — Investigate why our end-of-recording image differs from Audacity's (owner's two screenshots). Known contributor palette (absorbed by SF16.2); trace remaining structural diffs (stronger vertical transient striations / low-level detail visibility) to gain/range/gamma mapping, window/overlap, or SF11.5 display-res max-pool vs Audacity per-pixel render. Deliverable = written finding + any config/render fix it justifies. Best done AFTER SF16.2 (same palette)."
    },
    {
      "id": "SF-D49",
      "text": "SF16.6 — Denser time ruler with minor ticks in SpectrogramView.drawAxes: more frequent labelled marks on a nice 1/2/5 step chosen from visible span + pixel budget, plus short unlabelled minor ticks between labels. Client-only draw-layer change."
    },
    {
      "id": "SF-D50",
      "text": "SF17.1 — Zoom popover polish: add a close ✕ button, close on Esc, and make Применить read clearly as a button (filled/bordered, or a full-width q-btn below the field). Client-only in SpectrogramView.vue. q-menu closes on Esc/outside-click by default — verify or rely on the ✕."
    },
    {
      "id": "SF-D51",
      "text": "SF17.2/17.3 — Sharpen at high zoom. OWNER DECISION (2026-07-05): implement ALL variants, UI-configurable via a new «Резкость» settings group. Controls: imageScaling (Сглаженное bilinear | Резкое nearest, render-only live), highZoomMode (Выкл | Меньшее окно | Переназначение/reassign), highZoomThreshold (×N, default 8), highZoomWindow (default 512). SF17.2 = imageScaling toggle (easy, render only). SF17.3 = high-zoom analysis profile: effective analysis params become zoom-reactive (window->highZoomWindow or data->reassign above threshold), triggering worker reconfigure on threshold crossing (bounded, SF11.2 cache); applies to both L/R tracks (shared view)."
    },
    {
      "id": "SF-D52",
      "text": "SF17.4 — Per-tile window override (owner picked variant C). Add optional windowOverride to get-tile. Worker: route override tiles to a lazy sibling TFftwAnalysis built with the override window (decode cached SF11.7; lazy=no whole-track STFT; only visible columns computed); WriteTileResponse works unchanged; Signature.WindowSize keys tile caches. Siblings cached per (baseAnalysisId,window), freed on base close. No reconfigure/close+reopen/whole-track. Scope: covers smallWindow; Переназначение stays whole-track (reassignment scatter-writes whole file). Client: for smallWindow stop mutating analysisParams, send windowOverride on tile requests above threshold (hysteresis kept), fold into tile cache key."
    },
    {
      "id": "SF-D53",
      "text": "Phase 18 preset manager (full, owner). Client-only. Built-in presets read-only; user presets = {id,name,settings}[] persisted to a new localStorage key, validated via mergeStoredSettings. Track activePresetId + modified flag. Actions: apply, saveAs, rename, delete, duplicate (incl. from built-in), export/import JSON. UI: quick-apply dropdown (active check + modified '*' + Save as… + Manage…) + a preset-manager dialog component."
    },
    {
      "id": "SF-D54",
      "text": "SF19.1 (resolved) — Left/Right = seek playhead ±1s (Audacity); Shift+Left/Right = ±5s; Alt+Left/Right = pan view (current 15%-span behavior); Home/End keep view-edges (unchanged); view follows the playhead if it scrolls off-screen. emit('seek', clamp(playhead±step))."
    },
    {
      "id": "SF-D55",
      "text": "SF19.2 (resolved) — raise refetch idle debounce ~80ms -> ~200ms (trailing) so fast scroll/zoom bursts coalesce; SF11.9 fallback keeps it responsive. Same value for pan + zoom."
    },
    {
      "id": "SF-D56",
      "text": "SF19.3 (resolved, main) — split composable loading: `preparing` (analysis opening, nothing to show) vs `fetchingTiles` (tiles pending for an already-rendered analysis). Full overlay/dim only for `preparing` (initial load). On a view-change refetch: keep current/fallback tiles, show only a TINY corner spinner while the new frame loads in the background, then swap in seamlessly."
    },
    {
      "id": "SF-D57",
      "text": "Phase 20 — no auto-load on Audio tab + recent files. Store: selectedPath inits null (no restore-into-selection); drop refreshPresets re-select; add recentFiles (paths, most-recent first, cap 5, persisted new localStorage key), recorded in selectPath for valid files; remove old single last-path persistence. UI: when selectedPath===null the player body shows a 'Recent files' list (basename + path tooltip), click -> audio.selectPath(path) -> prepares as usual. Any file kind. Client-only."
    },
    {
      "id": "SF-D58",
      "text": "SF20.3 — Sub-second time-axis labels at high zoom. At large zoom ruler ticks round to the same second -> duplicate labels (1:05,1:05,...). formatTimeSec rounds to whole seconds when minutes>0. Make label precision follow the major tick step: when step<1s append the fraction of a second after a comma (1:05,3 / 1:05,25); decimals = step<0.1?2:step<1?1:0. Thread step/decimals from timeAxisTicksWithMinor into drawAxes label formatting. Draw-layer only; unit-tested."
    }
  ],
  "steps": [
    {
      "id": "SF0.1",
      "phase": 0,
      "title": "Plan & ledger",
      "status": "done",
      "dependsOn": [],
      "artifacts": [
        "docs/spectrogram-fixes/spectrogram-fixes-plan.md",
        "docs/spectrogram-fixes/spectrogram-fixes-progress.json"
      ],
      "verify": "ledger JSON parses; dependsOn DAG acyclic; plan + ledger committed",
      "startedAt": "2026-07-01T18:55:00Z",
      "finishedAt": "2026-07-01T18:55:00Z",
      "blockedReason": null,
      "evidence": [
        "Grounded in code: item1 = spec.loading exists in use-spectrogram.ts but SpectrogramView never surfaces it; AudioPage only shows audio.spectrogramLoading (decode). item2 = tiling/cache sound, analysisParams/renderOptions stable computeds; owner repro = on OPEN, one-tile strip runs L->R, only last tile stays -> rendered set holds one tile at a time; leading cause = view/seq/viewBinCount churn (ResizeObserver + double applyView) so assembleVisibleTiles matches only the newest tile. item3 = SpectrogramSettingsPanel is a fixed 264px column in AudioPage; mirror GnauralScheduleView settings overlay.",
        "DAG acyclic: SF0.1 -> SF0.2 -> {SF1.1, SF2.1, SF3.1}."
      ]
    },
    {
      "id": "SF0.2",
      "phase": 0,
      "title": "Approval gate (SF-D2..SF-D5)",
      "status": "done",
      "dependsOn": [
        "SF0.1"
      ],
      "artifacts": [
        "docs/spectrogram-fixes/spectrogram-fixes-plan.md"
      ],
      "verify": "owner confirms SF-D2 (loading overlay), SF-D3 (accumulate-tiles fix + unit test), SF-D4 (overlay 'Параметры'), SF-D5 (toggle in view toolbar). PAUSE before Phase 1.",
      "startedAt": "2026-07-01T18:55:00Z",
      "finishedAt": "2026-07-01T19:00:00Z",
      "blockedReason": null,
      "evidence": [
        "Owner approved ('go'); SF-D2..SF-D5 locked. Also confirmed the bug-2 repro (on open, no playback: one-tile strip runs L->R, only last stays)."
      ]
    },
    {
      "id": "SF1.1",
      "phase": 1,
      "title": "Loading indicator during spectrogram preparation",
      "status": "done",
      "dependsOn": [
        "SF0.2"
      ],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramView.vue",
        "GnauralCore/ui/composables/use-spectrogram.ts",
        "GnauralCore/ui/i18n/en.json",
        "GnauralCore/ui/i18n/ru.json"
      ],
      "verify": "an overlay spinner shows while spec.loading (analysis open/reconfigure + tiles pending) and clears when ready; vue-tsc clean",
      "startedAt": "2026-07-01T19:00:00Z",
      "finishedAt": "2026-07-01T19:25:00Z",
      "blockedReason": null,
      "evidence": [
        "SpectrogramView: added isPreparing = computed(spec.loading) and an absolute overlay (spectrogram-view__loading) with q-spinner-hourglass + label shown while preparing; container set position:relative. i18n audio.spectrogramPreparing added (en 'Preparing spectrogram…' / ru 'Подготовка спектрограммы…').",
        "FOLLOW-UP FIX (owner: spinner never appeared): spec.loading originally reflected only pending tiles, so it stayed false during the slow worker open()/reconfigure() analysis and tiles arrived too fast to show. use-spectrogram.ts now tracks an `opening` flag set at the start of open()/reconfigure() (updateLoading -> loading=true) and cleared when refetch() starts (then loading = pending tiles) or on error/close. The indicator now covers the whole prepare span (open analysis -> first tiles).",
        "vue-tsc clean; GnauralCore bun 56/0. Live-verify in quasar dev (owner)."
      ]
    },
    {
      "id": "SF2.1",
      "phase": 2,
      "title": "Fix only-last-frame render (persist/accumulate tiles)",
      "status": "done",
      "dependsOn": [
        "SF0.2"
      ],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramView.vue",
        "GnauralCore/ui/composables/use-spectrogram.ts",
        "GnauralCore/ui/composables/spectrogram-tiles.ts",
        "GnauralCore/ui/composables/spectrogram-tiles.test.ts"
      ],
      "verify": "opening a file renders the whole spectrogram and it stays (no single-tile strip / only-last-frame); pan/zoom still work; new composable unit test (tiles accumulate across binCount/seq change) green; vue-tsc + bun green",
      "startedAt": "2026-07-01T20:00:00Z",
      "finishedAt": "2026-07-01T20:20:00Z",
      "blockedReason": null,
      "evidence": [
        "Root cause (reasoned): tiles were cached keyed on viewBinCount and the render only used the EXACT-key tile for the current view; during initial load the canvas height settles (ResizeObserver + double applyView), so viewBinCount/tile-keys churn (viewSeq bumps) and assembleVisibleTiles matched only the newest-fetched tile -> a single strip that runs L->R and leaves only the last tile.",
        "Fix (SF-D3): (a) SpectrogramTileCache.getByTileIndex(analysisId, zoom, tileIndex) returns any cached tile for that tile regardless of binCount; assembleVisibleTiles now uses exact key OR this fallback so already-fetched tiles keep rendering across a binCount change. (b) tile arrivals ALWAYS re-assemble (dropped the pending.seq===viewSeq gate for assembly; seq still used for loading). (c) setView no-ops when the view (time window + zoom + viewBinCount) is unchanged, killing redundant applyView/ResizeObserver churn. (d) SpectrogramView rounds viewBinCount to a 32-step so small height jitter doesn't change keys.",
        "Guarded by unit tests: SpectrogramTileCache.getByTileIndex (fallback, MRU preference, prefix-boundary, and an explicit only-last-frame regression case where tiles 0..2 fetched at binCount 128 all still resolve when the plan asks for 256). vue-tsc clean; GnauralCore bun 60 pass / 0 fail (+4). Final visual confirmation = owner (quasar dev).",
        "FOLLOW-UP (owner screenshot: still a strip running L->R that vanishes, spinner stuck): the REAL root cause was planVisibleTiles nartitioning by a FIXED 256 full-res frames per tile REGARDLESS of zoom, so a full view of a long file (25 min) plans hundreds of full-res tiles that overflow the 128-entry LRU cache -> earlier tiles evicted, only the most-recent (rightmost) survive to render, and tiles never stop fetching (spinner stuck). Fix: planVisibleTiles now spans tileFrames*2^zoom full-res frames per tile, so a zoomed-out view needs only a handful of tiles (e.g. ~4-8) that fit the cache and all render/persist. Added a unit test (zoom 3 over a 100-tile file -> 13 bounded tiles, stepping by 256*8). vue-tsc clean; bun 61/0."
      ]
    },
    {
      "id": "SF3.1",
      "phase": 3,
      "title": "Settings panel overlay named 'Параметры'",
      "status": "done",
      "dependsOn": [
        "SF0.2"
      ],
      "artifacts": [
        "GnauralCore/ui/pages/AudioPage.vue",
        "GnauralCore/ui/i18n/en.json",
        "GnauralCore/ui/i18n/ru.json"
      ],
      "verify": "settings panel is a toggleable overlay over the plot (no main-content resize), titled 'Параметры', with toolbar toggle + backdrop + close; i18n en/ru; vue-tsc + bun green",
      "startedAt": "2026-07-03T01:05:00Z",
      "finishedAt": "2026-07-03T01:30:00Z",
      "blockedReason": null,
      "evidence": [
        "AudioPage: removed the fixed 264px settings side column (which shrank the plot). SpectrogramSettingsPanel now lives in a toggleable OVERLAY mirroring the schedule settings pattern: an absolutely-positioned <aside> (right/top/bottom, 300px, dark #0f172a) over the .audio-page__output-section--spectrogram (now position:relative), a click backdrop, and enter/leave <transition>s (fade + slide-in). Header = title 'Параметры' + close (x). Default CLOSED.",
        "Toggle = a 'tune' q-btn in the SF10.1 common header (after fit, pushed right with q-space); active color when open; aria-expanded/aria-controls wired. Backdrop click, the close button, and Escape (handlePlayerKeyDown) all close it. i18n audio.spectrogramSettingsTitle (en 'Parameters' / ru 'Параметры') + spectrogramSettingsClose. vue-tsc + prod build + bun 61/0."
      ]
    },
    {
      "id": "SF5.1",
      "phase": 5,
      "title": "Audacity-like default track height",
      "status": "done",
      "dependsOn": [
        "SF0.2"
      ],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramView.vue",
        "GnauralCore/ui/pages/AudioPage.vue"
      ],
      "verify": "spectrogram plot default height matches the Audacity reference; vue-tsc clean",
      "startedAt": "2026-07-01T22:00:00Z",
      "finishedAt": "2026-07-01T22:10:00Z",
      "blockedReason": null,
      "evidence": [
        "SpectrogramView gained a `height?: number` prop -> fixed track height via rootStyle (height + minHeight); canvas min-height 240px -> 0 so it flex-fills the fixed track. AudioPage passes :height=spectrogramTrackHeight (ref, default SPECTROGRAM_TRACK_HEIGHT_DEFAULT=260px) and the spectrogram row is items-start (no vertical stretch). This becomes the per-pane height reused by SF5.2 (resize) + SF5.3 (stereo L/R).",
        "vue-tsc clean. Visual confirm = owner (quasar dev)."
      ]
    },
    {
      "id": "SF5.2",
      "phase": 5,
      "title": "Drag-resize track height",
      "status": "done",
      "dependsOn": [
        "SF5.1"
      ],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramView.vue",
        "GnauralCore/ui/pages/AudioPage.vue",
        "GnauralCore/ui/i18n/en.json",
        "GnauralCore/ui/i18n/ru.json"
      ],
      "verify": "a bottom-border drag handle resizes the track height (pointer drag); height persisted in localStorage; vue-tsc + bun green",
      "startedAt": "2026-07-01T22:15:00Z",
      "finishedAt": "2026-07-01T22:30:00Z",
      "blockedReason": null,
      "evidence": [
        "SpectrogramView: bottom-edge drag handle (spectrogram-view__resize-handle, ns-resize, pointer-capture drag) emits update:height clamped to [120,1200]. AudioPage uses v-model:height=spectrogramTrackHeight, loads the initial value from localStorage (mindwave-audio-spectrogram-track-height, clamped) and persists on change. i18n audio.spectrogramResizeHandle (en/ru).",
        "vue-tsc clean; GnauralCore bun 61/0; i18n JSON parses."
      ]
    },
    {
      "id": "SF5.3",
      "phase": 5,
      "title": "Split stereo into left/right tracks",
      "status": "done",
      "dependsOn": [
        "SF5.1"
      ],
      "artifacts": [
        "GnauralCore/ui/pages/AudioPage.vue",
        "GnauralCore/ui/components/SpectrogramView.vue",
        "GnauralCore/server/spectrogram-session.ts"
      ],
      "verify": "stereo files render stacked Left/Right per-channel panes (channel 0/1, separate analyses); mono renders one pane; channel count from AudioBuffer.numberOfChannels; panes labelled L/R; vue-tsc + bun green",
      "startedAt": "2026-07-01T22:35:00Z",
      "finishedAt": "2026-07-01T23:00:00Z",
      "blockedReason": null,
      "evidence": [
        "SERVER (enabling change): SpectrogramSession now keeps a Map<analysisId,{wavHandle,params}> instead of a single analysis — onOpen no longer closes the previous (acquires its own source; releases on failure); get-tile/point-query/area-query use the message's analysisId (requireAnalysis(id) validates against the map); reconfigure targets the message's analysisId (close+reopen -> new id, keeps the source); close/dispose close per-analysis. server tests 17/0.",
        "UI: AudioPage renders two stacked SpectrogramViews for stereo (isSpectrogramStereo = spectrogramBuffer.numberOfChannels>=2) with per-channel analysis computeds (spectrogramLeftAnalysis channel:0 / spectrogramRightAnalysis channel:1, stable computeds so no reconfigure churn), labelled L/R; mono renders one pane. Both share v-model:height (resize both). SpectrogramView gained a `label` prop shown in its toolbar.",
        "Backend change -> requires bun server restart. vue-tsc clean; GnauralCore bun 61/0.",
        "FOLLOW-UP: the WORKER was also single-analysis (EnsureCurrent errored on a non-current id -> owner saw 'get-tile analysisId mismatch' on the L channel). SpectrumCoreFftwWorkerProbe.pas now keeps a registry of analyses; EnsureCurrent selects by id + restores that analysis's cache token; open registers (no longer frees the previous), close unregisters, shutdown frees all. Rebuilt + re-bundled (manifest updated; exe gitignored). Verified: two opens then get-tile on the first -> ok; bridge test 15/0."
      ]
    },
    {
      "id": "SF6.1",
      "phase": 6,
      "title": "Profile the spectrogram generation pipeline",
      "status": "done",
      "dependsOn": [
        "SF0.2"
      ],
      "artifacts": [
        "SpectrumCore or a scratch harness (profiling)",
        "spectrogram-fixes ledger (findings)"
      ],
      "verify": "stage-by-stage timing + payload breakdown on d:\\bin\\Presets\\1_Orientation.flac (open-analysis, get-tile sample-load vs FFT, tile bytes + serialize/parse, WS, UI render); bottleneck named; owner approval gate on the fix",
      "startedAt": "2026-07-02T00:30:00Z",
      "finishedAt": "2026-07-02T01:20:00Z",
      "blockedReason": null,
      "evidence": [
        "Recon: for wav/flac the source path passes STRAIGHT to the worker (worker decodes flac natively). Tiles transfer as JSON.",
        "PROFILED d:\\bin\\Presets\\1_Orientation.flac (33 min, 171144 frames, 2049 bins, 44.1k) via a stdio harness against a temporarily-instrumented worker: open-analysis = 22.2 s TOTAL = FLAC decode 3.4 s + STFT loop 18.8 s (DOMINANT). Full-view get-tile set (3 tiles) = 2.3 s wall, payload 1.59 MB, JSON parse 7 ms. Re-fetch not cached (~0.8 s).",
        "BOTTLENECK = the eager full-file STFT loop in TFftwAnalysis.Create (SpectrumCoreFftwAnalysis.pas). It computes + stores magnitude AND phase AND unwrapped-phase for EVERY frame (ArcTan2 per bin ~350M calls + UnwrapFramePhases per frame) into 3 x ~1.4 GB matrices, EVEN when data=magnitude (default) -> phase/uphase work is entirely unused. Transport (JSON/WS) and get-tile are NOT the bottleneck.",
        "Instrumentation was temporary (StdErr timers) and has been reverted; worker rebuilt + re-bundled clean.",
        "PROPOSED FIXES (owner gate): (A) low-risk quick win: skip phase/unwrapped-phase compute+store unless data mode needs them (phase/uphase) -> removes ~350M ArcTan2 + unwrap + 2/3 of the ~4GB memory writes for the default magnitude mode; expected large cut of the 18.8 s. (B) multithread the frame loop (frames independent) -> ~/cores. (C) architectural: lazy/progressive per-visible STFT (Audacity-style). Recommend A first (biggest bang/lowest risk), then measure, then consider B."
      ]
    },
    {
      "id": "SF6.2",
      "phase": 6,
      "title": "Optimization A: skip unused phase work",
      "status": "done",
      "dependsOn": [
        "SF6.1"
      ],
      "artifacts": [
        "SpectrumCore/src/core/SpectrumCoreFftwAnalysis.pas",
        "MindWaveCore/server/vendor/spectrumcore/win64/manifest.json"
      ],
      "verify": "worker computes/stores phase + unwrapped-phase only when the data mode needs them; magnitude/reassign/pitch modes skip ArcTan2 + unwrap + the 2 phase matrices; point/area-query phase reads guarded; open-analysis time drops (re-profile on 1_Orientation.flac); PAUSE for manual UI verification (spectrum still correct)",
      "startedAt": "2026-07-02T01:30:00Z",
      "finishedAt": "2026-07-02T02:00:00Z",
      "blockedReason": null,
      "evidence": [
        "TFftwAnalysis.Create: added NeedPhase = (DataMode in [admPhase, admUnwrappedPhase]); phase/unwrapped-phase arrays are allocated + the ArcTan2 + PhaseDest/UnwrappedPhaseDest stores + UnwrapFramePhases run only when NeedPhase (magnitude/reassign/pitch skip them). Magnitude (sqrt) is computed unchanged. Nil-guarded PhaseAt/UnwrappedPhaseAt (return 0 when the array wasn't allocated) so point-query still fills phase fields safely.",
        "RE-PROFILE on 1_Orientation.flac: open-analysis 22.2s -> 6.2s (~3.6x); the ~16s of phase work removed (decode 3.4s + magnitude loop ~2.8s). get-tile unchanged (~2.2s), magnitude tiles byte-identical.",
        "Correctness: magnitude point-query valid db/displayDb/magnitude (phase now 0, acceptable in magnitude mode); phase-mode get-tile still returns real phase values. GnauralCore bridge + session integration tests 17/0 against the re-bundled worker. Worker rebuilt + re-bundled (manifest refreshed). Requires backend restart. Owner UI verification = the pause."
      ]
    },
    {
      "id": "SF6.3",
      "phase": 6,
      "title": "Optimization B: multithread the STFT loop",
      "status": "done",
      "dependsOn": [
        "SF6.2"
      ],
      "artifacts": [
        "SpectrumCore/src/core/SpectrumCoreFftwAnalysis.pas",
        "MindWaveCore/server/vendor/spectrumcore/win64/manifest.json"
      ],
      "verify": "per-frame FFT loop parallelized across threads (per-thread plan/buffers); output identical; open-analysis time ~/cores (re-profile); PAUSE for manual UI verification",
      "startedAt": "2026-07-02T02:10:00Z",
      "finishedAt": "2026-07-02T02:40:00Z",
      "blockedReason": null,
      "evidence": [
        "Added TStftFrameWorker(TThread) + ComputeStandardStftParallel: the standard STFT (magnitude + optional phase) is split into contiguous frame ranges across TThread.ProcessorCount workers (capped 32), each owning its own FFTW plan + in/out buffers (plans created serially in the main thread; execution on distinct plans/buffers is thread-safe; frame output rows are disjoint). Reassignment stays SERIAL (it scatter-writes to arbitrary CorrectedFrame -> races); pitch EAC pass unchanged.",
        "RE-PROFILE on 1_Orientation.flac (16 logical CPUs): open-analysis 6.2s -> ~3.06s (now decode-bound; STFT loop ~near-zero). Cumulative A+B: 22.2s -> 3.06s (~7x). Tile payloads byte-identical to the serial build (606.1/608.8/373.3 KB) -> magnitude output unchanged.",
        "get-tile rose ~2.2s -> ~3.5s in the same runs, but that is environmental (the app + quasar dev + backend all competing for CPU during the test); open-analysis is stable across repeats. get-tile does no FFT (pools the precomputed matrix), and B only changes how that matrix is filled (identical data).",
        "Correctness: GnauralCore bridge + session integration tests 17/0 against the re-bundled worker. Worker rebuilt + re-bundled (manifest refreshed). Requires backend restart. Owner UI verification = the pause."
      ]
    },
    {
      "id": "SF6.4",
      "phase": 6,
      "title": "Optimization C: lazy/progressive per-visible STFT",
      "status": "done",
      "dependsOn": [
        "SF6.2"
      ],
      "artifacts": [
        "SpectrumCore/src/core/SpectrumCoreFftwAnalysis.pas",
        "SpectrumCore/src/apps/SpectrumCoreFftwWorkerProbe.pas",
        "MindWaveCore bundle"
      ],
      "verify": "open-analysis no longer allocates the full-file matrix (no OOM on 1_Orientation.flac stereo); STFT computed on demand for the requested frame range (parallel) into a transient buffer; magnitude output identical; re-profile memory + speed; PAUSE for manual UI verification",
      "startedAt": "2026-07-02T03:00:00Z",
      "finishedAt": "2026-07-02T04:00:00Z",
      "blockedReason": null,
      "evidence": [
        "IMPLEMENTED. TFftwAnalysis is lazy for magnitude/phase/uphase/pitch (reassignment stays eager): Create keeps the decoded samples + window LUT + eager FFrameTimes and allocates NO full matrix. EnsureRange(start,count) computes that frame range in parallel (ComputeStandardStftParallel refactored to write a frame range into caller buffers) into a transient FRangeMag(+phase); FMagnitudeData/FPhaseData/FUnwrappedPhaseData are set to BIASED pointers (buf - start*spec) so all absolute-frame readers work unchanged. WriteTileResponse / PointQuery / AreaQuery / export-matrix call EnsureRange then ClearRange (transient, freed after). Destroy frees the range buffers + samples for lazy (biased pointers are not separately owned).",
        "RE-PROFILE on 1_Orientation.flac: open-analysis 2.6s (decode-only, no matrix); full-view tiles ~2.7s (computed on demand, parallel); tile payloads byte-identical (magnitude correct). Two concurrent channel analyses (stereo) open with NO matrix -> OOM path removed (open no longer allocates ~1.4GB/analysis). Peak memory ~= samples + one transient range (freed) instead of persistent full matrices.",
        "GnauralCore bridge + session integration tests 17/0 against the re-bundled lazy worker. Requires backend restart. Owner UI verification (open 1_Orientation stereo -> no OOM) = the pause.",
        "Chosen fix (owner): C. OOM cause = eager full-file magnitude matrix (~1.4GB/analysis; stereo 2x + browser decode -> OOM). Design: TFftwAnalysis becomes LAZY for magnitude/phase/uphase/pitch (reassignment stays eager - it scatter-writes across all frames). open keeps the decoded samples + window LUT + eager FFrameTimes but allocates NO matrix. EnsureRange(start,count) computes that frame range in parallel (reuses the B worker pool) into a transient FRangeMag(+phase) buffer; FMagnitudeData is set to a BIASED pointer (FRangeMag - start*spec) so all existing absolute-frame readers work unchanged. WriteTileResponse / PointQuery / AreaQuery call EnsureRange for their span then ClearRange. Peak memory ~= samples + one transient range (freed after) instead of a persistent full matrix."
      ]
    },
    {
      "id": "SF5.4",
      "phase": 5,
      "title": "Match Audacity default spectrogram settings",
      "status": "done",
      "dependsOn": [
        "SF0.2"
      ],
      "artifacts": [
        "GnauralCore/ui/composables/spectrogram-settings.ts"
      ],
      "verify": "DEFAULT_SPECTROGRAM_SETTINGS matches the Audacity defaults screenshot; vue-tsc + bun green",
      "startedAt": "2026-07-02T00:15:00Z",
      "finishedAt": "2026-07-02T00:20:00Z",
      "blockedReason": null,
      "evidence": [
        "Set DEFAULT_SPECTROGRAM_SETTINGS to Audacity defaults per the owner screenshot: fscale log, startHz 80, stopHz 4000, gain 1 (0 dB), drange 120, frequencyGain 0, data magnitude (Frequencies), window 2048, winFunc hann, zeroPaddingFactor 2, scale log. Changed from prior defaults: zeroPaddingFactor 1->2, startHz 0->80, stopHz 0->4000, palette intensity->rainbow. Aligned the 'audacity' preset to match. vue-tsc clean; bun 61/0.",
        "NOTES: (1) palette = 'rainbow' is our closest COLOURED option to Audacity's 'Color (pink)' — we don't have the exact Audacity pink colormap. (2) Existing localStorage settings override defaults, so a user with saved settings must Reset / pick the Audacity preset to see the new defaults."
      ]
    },
    {
      "id": "SF14.1",
      "phase": 14,
      "title": "Channel mode: remove or implement",
      "status": "done",
      "dependsOn": [
        "SF13.1"
      ],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramSettingsPanel.vue"
      ],
      "verify": "per the owner-chosen path (remove the field, or implement a real combined/separate mode)",
      "blockedReason": null,
      "startedAt": "2026-07-04T16:00:00Z",
      "finishedAt": "2026-07-04T16:20:00Z",
      "evidence": [
        "INVESTIGATED: worker parses 'mode' -> Config.ChannelMode (SpectrumCoreFftwWorkerProbe.pas:1197) and only echoes it (AnalysisChannelModeName in responses / matrix manifest); the STFT ignores it and analyses the single ChannelIdx. AudioPage does the L/R split by opening channel 0 + channel 1, independent of mode. So 'Combined' draws both channels because the UI splits, not because mode does anything.",
        "RESOLUTION (owner 'go' + recommendation): REMOVED the 'Channel mode' field from the panel (dead control). Kept the model field `mode` (defaulted 'combined', harmlessly sent+echoed) to avoid a protocol/store ripple; a full protocol removal can be a follow-up. Removed the now-unused SPECTROGRAM_CHANNEL_MODES import. vue-tsc + bun 63/0."
      ]
    },
    {
      "id": "SF15.1",
      "phase": 15,
      "title": "Audacity-style mouse navigation (Ctrl/Alt/Shift)",
      "status": "done",
      "dependsOn": [
        "SF8.1"
      ],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramView.vue",
        "GnauralCore/ui/pages/AudioPage.vue"
      ],
      "verify": "Ctrl/Alt/Shift + wheel/drag gestures match Audacity (time zoom/scroll + frequency zoom); gesture table documented; vue-tsc + bun green; owner verify",
      "startedAt": "2026-07-04T17:35:00Z",
      "finishedAt": "2026-07-04T18:20:00Z",
      "blockedReason": null,
      "evidence": [
        "RESEARCH: Audacity 3.7.8 MouseWheelHandler.cpp -> track area: plain wheel = vertical scroll, Shift+wheel = horizontal (time) scroll, Ctrl+wheel = time zoom about the pointer. SpectrumVRulerControls.cpp / SpectrumVZoomHandle.cpp -> frequency (vertical) zoom on the freq ruler (Ctrl+wheel over ruler = freq zoom, Shift+wheel = freq scroll; drag = zoom to a freq range, Shift+click = zoom out, reset via menu).",
        "IMPLEMENTED (mapped onto our single-canvas SpectrogramView): wheel / Ctrl+wheel = TIME zoom about the pointer; Shift+wheel = TIME pan (scroll left/right); Alt+wheel = FREQUENCY zoom about the pointer; Alt+Shift+wheel = FREQUENCY pan (scroll up/down); double-click = reset time + freq to full. Drag = area select, click = seek (unchanged).",
        "NEW CAPABILITY = frequency zoom. Added a shared FreqWindow {lo,hi} (bin-axis fractions, default full) alongside the time view (AudioPage spectrogramShared.freqView, reset per file). draw() crops each tile vertically to the freq window; drawAxes labels the sliced freq sub-range; point/area queries remap the plot y-fraction through the freq window (freqTopFraction) so the cursor readout + selection stay accurate; a freqView watch redraws (no refetch — pure client crop). Stacked stereo tracks freq-zoom together via the shared window.",
        "vue-tsc + prod build + GnauralCore ui 63/0. UI-only. Phase 15 complete. Owner UI verify = the gate."
      ]
    },
    {
      "id": "SF15.2",
      "phase": 15,
      "title": "Channel param: clarify/remove (vestigial)",
      "status": "done",
      "dependsOn": [
        "SF14.2"
      ],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramSettingsPanel.vue",
        "GnauralCore/ui/composables/spectrogram-settings.ts",
        "GnauralCore/ui/composables/spectrogram-settings.test.ts"
      ],
      "verify": "per owner (recommend remove the Channel field since the L/R split overrides it); vue-tsc + bun green",
      "startedAt": "2026-07-04T17:15:00Z",
      "finishedAt": "2026-07-04T17:30:00Z",
      "blockedReason": null,
      "evidence": [
        "INVESTIGATED: AudioPage spectrogramLeftAnalysis={...analysisParams, channel:0} / right channel:1 -> the L/R tracks always override the panel channel; mono uses analysisParams (channel 0 only). So the panel 'Channel' field is vestigial (like the removed 'mode').",
        "REMOVED (owner go): dropped the Channel field from the panel + the channelOptions helper; removed `channel` from the SpectrogramSettings model (interface + DEFAULT + mergeStoredSettings + toAnalysisParams). The L/R split still sets channel 0/1 per track; mono omits channel so the worker defaults to 0. Updated the capability-coverage test (dropped 'channel'). vue-tsc + bun 63/0. (Unused i18n channel keys left in place, harmless.)"
      ]
    },
    {
      "id": "SF14.4",
      "phase": 14,
      "title": "Overlap as the frame-step control (hop derived)",
      "status": "done",
      "dependsOn": [
        "SF14.3"
      ],
      "artifacts": [
        "GnauralCore/ui/composables/spectrogram-settings.ts",
        "GnauralCore/ui/components/SpectrogramSettingsPanel.vue",
        "GnauralCore/ui/composables/spectrogram-settings.test.ts"
      ],
      "verify": "the panel exposes Overlap (not Hop); toAnalysisParams derives hop from overlap; default unchanged (0.75 -> 512); vue-tsc + bun green",
      "startedAt": "2026-07-04T16:30:00Z",
      "finishedAt": "2026-07-04T16:45:00Z",
      "blockedReason": null,
      "evidence": [
        "Owner chose variant (b): overlap is the frame-step control (Audacity/ffmpeg style), the redundant Hop field is dropped. Removed `hop` from the SpectrogramSettings model (interface + DEFAULT + mergeStoredSettings); toAnalysisParams now derives hop = max(1, round(window * (1 - overlap))) — the same formula the worker's NormalizeSpectrumAnalysisConfig uses. Removed the Hop field from the panel (Overlap slider stays). Default unchanged (overlap 0.75 -> hop 512); ffmpeg preset overlap 0 -> hop 2048.",
        "Updated 2 settings tests (dropped the 'hop' key from the capability-coverage set + the merge default assertion -> overlap). vue-tsc + prod build + GnauralCore ui 63/0. UI-only. Phase 14 complete."
      ]
    },
    {
      "id": "SF14.3",
      "phase": 14,
      "title": "Audit all parameters for real use",
      "status": "done",
      "dependsOn": [
        "SF3.1"
      ],
      "artifacts": [
        "SpectrumCore worker",
        "GnauralCore server + ui (render)",
        "spectrogram-fixes ledger (findings)"
      ],
      "verify": "per-parameter verdict (used / partial / no-op with code path) for all analysis + render params; a remove/wire/fix proposal for each dead one; owner reviews",
      "startedAt": "2026-07-04T16:00:00Z",
      "finishedAt": "2026-07-04T16:20:00Z",
      "blockedReason": null,
      "evidence": [
        "AUDIT (all 18 params). USED: window (FWindowSize), zeroPaddingFactor (FFftLength), hop (FHopSize), channel (FChannelIdx), winFunc (window LUT), data (DataMode), fscale (FrequencyScale), startHz/stopHz (EffectiveStart/StopFrequencyHz) -> all traced into the worker STFT. RENDER params scale/gain/frequencyGain/drange/limit/saturation/palette all consumed in spectrogram-render.ts (magnitudeToScaled + paletteColor + frequencyGainFactor). saturation confirmed (rainbow HSV saturation).",
        "DEAD/NO-OP found: (1) mode -> echoed only, never affects the STFT (removed from the panel, SF14.1). (2) overlap -> EFFECTIVELY DEAD: NormalizeSpectrumAnalysisConfig only derives HopSize from Overlap when HopSize<=0, but the UI always sends hop=512, so overlap is ignored and then RE-COMPUTED from hop (SpectrumCoreAnalysisConfig.pas:381-386). The panel's Overlap slider does nothing while hop is set.",
        "PROPOSAL for overlap (owner decision): (a) remove the Overlap slider (hop is the real control); OR (b) make overlap the control -> UI sends overlap + hop=0 so the worker derives hop (Audacity/ffmpeg-style). Not applied yet — presented to owner. Everything else confirmed working."
      ]
    },
    {
      "id": "SF14.2",
      "phase": 14,
      "title": "Channel -> Left/Right select",
      "status": "done",
      "dependsOn": [
        "SF13.1"
      ],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramSettingsPanel.vue",
        "GnauralCore/ui/i18n/en.json",
        "GnauralCore/ui/i18n/ru.json"
      ],
      "verify": "the Channel field is a localized Left/Right select (value 0/1); vue-tsc + bun green",
      "startedAt": "2026-07-04T16:00:00Z",
      "finishedAt": "2026-07-04T16:20:00Z",
      "blockedReason": null,
      "evidence": [
        "The Channel field is now a localized Left/Right select (channelOptions() -> [{Left,0},{Right,1}]) instead of a raw 0/1 number. i18n spectrogramChannelLeft/Right (en Left/Right, ru Левый/Правый). vue-tsc + bun 63/0. (For stereo this is overridden per track by the SF5.3 split; mainly a clarity change.)"
      ]
    },
    {
      "id": "SF13.1",
      "phase": 13,
      "title": "Audacity-style parameter grouping",
      "status": "done",
      "dependsOn": [
        "SF3.1"
      ],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramSettingsPanel.vue",
        "GnauralCore/ui/i18n/en.json",
        "GnauralCore/ui/i18n/ru.json"
      ],
      "verify": "settings panel grouped into Масштаб / Цвет / FFT-фильтр like Audacity; vue-tsc + bun green",
      "startedAt": "2026-07-04T14:05:00Z",
      "finishedAt": "2026-07-04T14:55:00Z",
      "blockedReason": null,
      "evidence": [
        "Rewrote SpectrogramSettingsPanel data-driven: a `groups` computed = 3 Audacity-style sections. Масштаб (fscale, startHz, stopHz); Цвет (intensity scale, gain, drange, limit, frequencyGain, saturation, palette); FFT-фильтр (algorithm/data, window, winFunc, zeroPaddingFactor, hop, overlap, channel mode, channel). Each field renders a label-row + the right control (select/number/slider). vue-tsc + bun 63/0."
      ]
    },
    {
      "id": "SF13.2",
      "phase": 13,
      "title": "i18n for parameter values",
      "status": "done",
      "dependsOn": [
        "SF13.1"
      ],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramSettingsPanel.vue",
        "GnauralCore/ui/i18n/en.json",
        "GnauralCore/ui/i18n/ru.json"
      ],
      "verify": "dropdown option values localized (en/ru); vue-tsc + bun green",
      "startedAt": "2026-07-04T14:05:00Z",
      "finishedAt": "2026-07-04T14:55:00Z",
      "blockedReason": null,
      "evidence": [
        "Dropdown option VALUES localized via nested i18n namespaces (audio.spectrogram*Opt.<value>): fscale (6), intensity scale (6), data/algorithm (5), channel mode (2), palette (2, incl 'Цвет (розовый)'), window functions (24, e.g. Ханна/Хэмминга/Блэкмана). enumOptions()/numberOptions() build {label:t(...),value}; the groups computed re-resolves on locale change. window sizes + zero-pad stay numeric (with 'x'). vue-tsc + bun 63/0."
      ]
    },
    {
      "id": "SF13.3",
      "phase": 13,
      "title": "Per-parameter help icon + text",
      "status": "done",
      "dependsOn": [
        "SF13.1"
      ],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramSettingsPanel.vue",
        "GnauralCore/ui/i18n/en.json",
        "GnauralCore/ui/i18n/ru.json"
      ],
      "verify": "a ? icon per parameter opens a bilingual help popover; vue-tsc + bun green",
      "startedAt": "2026-07-04T14:05:00Z",
      "finishedAt": "2026-07-04T14:55:00Z",
      "blockedReason": null,
      "evidence": [
        "Each field's label-row has a 'help_outline' q-icon with a q-menu popover showing that parameter's help text (one bilingual key per field, audio.spectrogram*Help; 17 params). Icon has cursor:help, hover highlight, aria-label 'Help for {field}' + tabindex. vue-tsc + prod build + bun 63/0. Phase 13 complete. Owner UI verify = gate."
      ]
    },
    {
      "id": "SF12.1",
      "phase": 12,
      "title": "Fix the TSpecFft real-FFT mirror bug",
      "status": "done",
      "dependsOn": [],
      "artifacts": [
        "SpectrumCore/src/core/SpectrumCoreFft.pas",
        "SpectrumCore/tests/SpectrumCoreFftTests.pas",
        "SpectrumCore/scripts/build_fft_tests_x64.bat",
        "SpectrumCore/TSPECFFT-MIRROR-BUG.md"
      ],
      "verify": "cosine/sine sweep + impulse/DC/Nyquist + two-tone pass across N in {256,512,1024,2048,4096}; every non-signal bin < 0.1% of the true peak (no N/2-k mirror). SpectrumCore-only; no MindWave rebundle.",
      "blockedReason": null,
      "startedAt": "2026-07-04T13:00:00Z",
      "finishedAt": "2026-07-04T13:45:00Z",
      "evidence": [
        "ROOT CAUSE (confirmed vs the vendored Audacity RealFFTf.cpp): a pointer-deref mistranslation. C reads the cos twiddle as SinTable[*br1 + 1] (the ADJACENT slot FBitReversed[i]+1; the twiddle is stored as a [sin,cos] pair there per the constructor). The Pascal port wrote FSinTable[br1[1]] = FSinTable[FBitReversed[i+1]] (a DIFFERENT bin's twiddle) -> the bin-dependent N/2-k mirror fold. Same mistranslation in 3 spots: RealFFTf massage cos, the center-bin conjugate (buffer + br1[1] should be buffer + *br1 + 1), and the InverseRealFFTf massage cos. Fixed all three to br1[0]+1 / br1^ + 1. The twiddle-table build + ReorderToFreq were already faithful.",
        "SCOPE: independent of the FFTW spectrogram path (TFftwAnalysis/fftwf_*, unaffected); this fixes TSpecFft (used by SpectrumCoreStft + some backends). No MindWave rebundle needed for the spectrogram.",
        "VERIFIED: new dependency-free regression tests/SpectrumCoreFftTests.pas (build+run via scripts/build_fft_tests_x64.bat) -> before the fix a probe reproduced the report's mirror table (16->79.8% etc.); after the fix, cosine + sine sweep over EVERY bin at N in {256,512,1024,2048,4096} plus impulse/DC/Nyquist/two-tone are all clean (every non-signal bin < 0.1% of the true peak). Exit 0. (Direct RealFFTf->InverseRealFFTf round-trip intentionally not asserted: the two use different buffer orderings, bit-reversed vs normal, so composing them needs an explicit reorder protocol out of scope; the inverse still got the symmetric cos fix.) Bug report marked FIXED."
      ]
    },
    {
      "id": "SF11.9",
      "phase": 11,
      "title": "Responsiveness: cross-zoom tile fallback (backlog B1)",
      "status": "done",
      "dependsOn": [
        "SF11.6",
        "SF11.8"
      ],
      "artifacts": [
        "GnauralCore/ui/composables/spectrogram-tiles.ts",
        "GnauralCore/ui/composables/use-spectrogram.ts",
        "GnauralCore/ui/composables/spectrogram-tiles.test.ts"
      ],
      "verify": "on zoom/pan the plot shows instant coarse content (a cached covering tile from another zoom) instead of blank, refined as exact tiles arrive; vue-tsc + ui green",
      "startedAt": "2026-07-04T11:40:00Z",
      "finishedAt": "2026-07-04T12:10:00Z",
      "blockedReason": null,
      "evidence": [
        "Owner chose the responsiveness variant of B1 (the original fast->detailed was superseded: SF11.5 made the detailed max-pool the fast default, SF11.6 base64 made it fast). Implemented client-only: SpectrogramTileCache.findBest(scorer) picks the best cached tile by a caller score. assembleVisibleTiles: when the exact-zoom tile (and binCount fallback) is missing, fall back to a cached tile from ANOTHER zoom that fully covers the range (prefer the finest = smallest span), rendered stretched. Final tiles sorted by span DESC so coarse paints under the sharp exact tiles (draw() iterates in order).",
        "Effect: zoom/pan no longer flashes blank when the exact tiles aren't cached yet — it shows instant coarse content from the whole-clip overview (B7/minimap) or a previous zoom, refined in ~100ms as exact-zoom tiles arrive. No regression: the fallback only triggers when an exact tile is missing AND a covering one is cached; otherwise identical to before. No worker/protocol/server change.",
        "+2 unit tests (findBest: highest score / null-skip / none; finest-covering preference). vue-tsc clean; GnauralCore ui 63/0 (was 61 + 2). UI-only. Closes backlog B1. Owner UI verify = the gate."
      ]
    },
    {
      "id": "SF11.8",
      "phase": 11,
      "title": "Minimap spectrogram thumbnail (backlog B7)",
      "status": "done",
      "dependsOn": [
        "SF10.4",
        "SF11.6"
      ],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramMinimap.vue",
        "GnauralCore/ui/pages/AudioPage.vue"
      ],
      "verify": "the bottom minimap shows a whole-clip spectrogram thumbnail behind the navigator; reuses the primary track's warm analysis; vue-tsc + build + ui green; owner verify",
      "startedAt": "2026-07-04T11:00:00Z",
      "finishedAt": "2026-07-04T11:30:00Z",
      "blockedReason": null,
      "evidence": [
        "SpectrogramMinimap gained its own useSpectrogram: openForPath(props.filePath) + applyMinimapView() sets a FIXED whole-clip view at a minimap-width zoom (viewportZoomTier, viewBinCount ~= minimap height) -> one overview fetch (per file/params/width), not tied to the interactive view. draw() renders spec.tiles as the thumbnail behind the dim/window/ticks navigator (tileToImage + offscreen drawImage positioned by tile time), falling back to the plain baseline until tiles arrive. reconfigure on analysis-param change; close on unmount.",
        "AudioPage passes :file-path, :analysis=spectrogramTracks[0].analysis (primary channel), :render. Because the params match the first track, the server's SF11.2 findByKey REUSES the primary track's warm analysis (same analysisId) -> no extra analysis/decode (SF11.7 shared decode also applies), and the overview tile is cached (SF11.2). So the thumbnail is near-free.",
        "UI-only. vue-tsc clean; GnauralCore ui 61/0; prod build succeeds. Closes backlog B7. Owner UI verify = the gate."
      ]
    },
    {
      "id": "SF11.7",
      "phase": 11,
      "title": "Shared decode for stereo",
      "status": "done",
      "dependsOn": [
        "SF11.6"
      ],
      "artifacts": [
        "SpectrumCore/src/core/SpectrumCoreAudioReader.pas",
        "MindWaveCore/server/vendor/spectrumcore/win64/manifest.json"
      ],
      "verify": "opening a file's 2nd channel skips the FLAC decode (reuses the once-decoded PCM); stereo first-load ~halved on decode; bridge/session tests green; owner verify",
      "startedAt": "2026-07-04T10:10:00Z",
      "finishedAt": "2026-07-04T10:45:00Z",
      "blockedReason": null,
      "evidence": [
        "Implemented a 1-file shared-decode cache INSIDE SpectrumCoreAudioReader.AudioLoadChannel (sndfile/flac/ogg path) - no analysis/worker changes. New SndfileLoadAllChannels decodes all channels in one libsndfile pass into per-channel buffers (same indexing as SndfileLoadChannel). SndfileLoadChannelCached: cache hit for the same file returns a not-yet-taken channel (transfers ownership); miss frees any prior cache, decodes all, caches the siblings, returns the requested channel. Cache cleared when all channels taken / on the next different-file miss / finalization.",
        "MEMORY-SAFE (no OOM regression): each analysis already keeps its channel's samples; the cache only holds the not-yet-taken SIBLING buffers for ONE file, transiently (freed as consumed). Peak memory unchanged. Single-threaded (open-analysis is serial stdio).",
        "PROFILED (fresh worker, 1_Orientation.flac stereo): open ch0 2934ms (decode all), open ch1 1ms (from cache) [was ~2665ms]; different file re-decodes, its ch1 also 1-3ms. So stereo first-load decode ~8.3s -> ~2.9s (one decode). In production a returning file is served by the SF11.2 warm-LRU (0ms, decode skipped entirely).",
        "Rebuilt worker + rebundled (manifest refreshed). GnauralCore bridge+session integration tests 31/0 against the rebundled worker. B2 closed as obsolete (decode, not STFT, was the bottleneck). Backend restart. Owner UI verify (stereo file opens noticeably faster) = the gate."
      ]
    },
    {
      "id": "SF11.5",
      "phase": 11,
      "title": "Bounded-oversample max-pool (quality fix)",
      "status": "done",
      "dependsOn": [
        "SF11.4"
      ],
      "artifacts": [
        "SpectrumCore/src/core/SpectrumCoreFftwAnalysis.pas",
        "SpectrumCore/src/apps/SpectrumCoreFftwWorkerProbe.pas",
        "MindWaveCore/server/vendor/spectrumcore/win64/manifest.json"
      ],
      "verify": "display-res path computes K=min(Factor,32) FFTs/column max-pooled; overview looks close to the old full-res quality; still fast; bridge/session tests green; owner UI verify",
      "startedAt": "2026-07-03T06:45:00Z",
      "finishedAt": "2026-07-03T07:30:00Z",
      "blockedReason": null,
      "evidence": [
        "Added TFftwAnalysis.EnsureColumnsMaxPooled(ABaseFrame, ASubStride, AGroups, AGroupSize): computes AGroups*AGroupSize decimated sub-frames at ASubStride then MAX-POOLs each group of AGroupSize RAW magnitude spectra into one column, in place at the front of FRangeMag (forward in-place pass is safe: output slot G <= its inputs). Magnitude-only (phase/uphase fall back to 1 col/group). Presents AGroups pooled columns as contiguous frames (bias 0).",
        "Worker WriteTileResponse display-res branch: KPool = min(Factor, DISPLAY_RES_MAX_OVERSAMPLE=32), SubStride = Factor div KPool; EnsureColumnsMaxPooled + ComputeTileValues(0, EmitFrames). CRUCIAL: pooling the RAW spectra BEFORE the frequency interpolation keeps interpolation at one pass per OUTPUT column (not per sub-frame) -> a first naive attempt (pool after interpolation) was 3-5x slower.",
        "PROFILED (1_Orientation.flac): overview zoom=10 272ms (single-FFT SF11.4 was 545ms; old full-res 3949ms); zoom=8 (one 669-col tile) 1062ms; 120s zoom=5 530ms; 2s zoom=0 220ms. Brightness restored: overview max~0.10 (vs the quiet 2s window max~0.008) confirms the max-pool captures peaks again -> quality close to old full-res, ~14x faster. zoom=0/full-res + cursor readout unchanged.",
        "Rebuilt worker + rebundled (manifest refreshed). GnauralCore bridge+session integration tests 31/0 against the rebundled worker. Backend restart to pick it up. Owner UI verify (overview looks good AND fast) = the gate. JSON emit is still the floor -> SF11.6 (binary transport) next."
      ]
    },
    {
      "id": "SF11.6",
      "phase": 11,
      "title": "Binary tile transport",
      "status": "done",
      "dependsOn": [
        "SF11.5"
      ],
      "artifacts": [
        "SharedPasCore/ts/spectrogram-protocol.ts",
        "SpectrumCore/src/apps/SpectrumCoreFftwWorkerProbe.pas",
        "GnauralCore/server/spectrogram-session.ts",
        "GnauralCore/ui/composables/spectrogram-render.ts",
        "MindWaveCore/server/vendor/spectrumcore/win64/manifest.json"
      ],
      "verify": "tile bins sent as float32 blob (not JSON text); per-tile time drops from the ~500ms JSON floor; tests green; owner verify",
      "startedAt": "2026-07-03T07:35:00Z",
      "finishedAt": "2026-07-03T08:30:00Z",
      "blockedReason": null,
      "evidence": [
        "Tile bins now travel as a base64 little-endian Float32 blob (row-major [frame][bin], length emittedFrameCount*binCount) instead of per-bin JSON numbers. Protocol: added SpectrogramTile.binsB64 (frames[].bins made optional). Worker: WriteTileResponse gathers the values into a Single buffer and emits binsB64 via EncodeStringBase64 (base64 unit); 'frames' emitted as [] -> NO FloatToStrF over the bulk data. Server: toTile passes binsB64 straight through (no per-value parse). Client: spectrogram-render.tileToImage decodes binsB64 -> Float32Array (atob, memoized in a WeakMap per tile) and indexes it; falls back to frames[].bins for legacy/tests.",
        "PROFILED (worker, 1_Orientation.flac): overview zoom=10 272ms->76ms; zoom=8 1062ms->217ms; 120s zoom=5 530ms->111ms; 2s zoom=0 220ms->17ms. Decode verified: float count == emittedFrameCount*binCount, values identical (max/mean match). base64 payload (~460KB overview) is also SMALLER than the old JSON (~770KB). The FloatToStrF/JSON-number floor is gone at every hop (worker emit, server parse, WS stringify, client parse).",
        "Tests updated (bridge + session contract now assert binsB64 decodes to emittedFrameCount*binCount*4 bytes; render tests keep the frames[] fallback path). vue-tsc clean; GnauralCore server 31/0, ui 61/0. Rebuilt worker + rebundled (manifest refreshed). Cumulative Phase-11 speed: overview 3949ms -> 76ms (~52x) at restored quality. Backend restart. Owner UI verify = the gate."
      ]
    },
    {
      "id": "SF11.4",
      "phase": 11,
      "title": "Audacity-style display-resolution STFT",
      "status": "done",
      "dependsOn": [
        "SF11.3"
      ],
      "artifacts": [
        "SpectrumCore/src/core/SpectrumCoreFftwAnalysis.pas",
        "SpectrumCore/src/apps/SpectrumCoreFftwWorkerProbe.pas",
        "MindWaveCore/server/vendor/spectrumcore/win64/manifest.json"
      ],
      "verify": "get-tile computes one FFT per display column at samplesPerPixel stride (not full-res + max-pool); cold overview ~3.9s -> <~100ms; instant zoom; SpectrumCore + bridge/session tests green; owner UI verify",
      "blockedReason": null,
      "startedAt": "2026-07-03T05:00:00Z",
      "finishedAt": "2026-07-03T06:10:00Z",
      "evidence": [
        "IMPLEMENTED. Analysis (SpectrumCoreFftwAnalysis.pas): generalized the Opt-B parallel STFT worker + ComputeStandardStftParallel to be COLUMN-based with an AStride param (column c -> absolute frame ABaseFrame + c*Stride, output row c; Stride=1 = the unchanged contiguous full-res path). Added EnsureColumns(ABaseFrame, AStride, AColumns): computes AColumns DECIMATED frames at stride AStride into the range buffer and presents them as contiguous frames 0..N-1 (bias 0) so tile readers index one column per emitted pixel; magnitude(+phase when needed); returns actual columns (clamped); IsLazy property added.",
        "Worker (SpectrumCoreFftwWorkerProbe.pas WriteTileResponse): for zoom>0 lazy magnitude/phase/uphase modes (NOT pitch-EAC, NOT reassignment), take the DISPLAY-RESOLUTION path: Factor=1<<zoom, EmitFrames=ceil(FrameCount/Factor), EnsureColumns(FrameStart, Factor, EmitFrames) then ComputeTileValues(0, EmitFrames) -> emit columns directly (no max-pool pyramid). zoom=0/narrow/eager keep the full-res path (W3 cache + EnsureRange + pyramid) unchanged. Emit loop unchanged (columns by index, timeSec via Factor).",
        "PROFILED (fresh worker, 1_Orientation.flac 33min): whole-clip overview zoom=10 COLD 3949ms -> 545ms (~7x); the STFT is now negligible and JSON emit (~500ms for 770KB / 86k floats) is the new floor (backlog B4 = binary transport). Byte sizes identical to the old path (same output shape); zoom=0 full-res path unchanged. Cursor readout unaffected (point/area-query stay full-res).",
        "TRADEOFF (as designed / Audacity-equivalent): zoomed-out columns are one FFT at stride Factor*hop -> samples between columns are skipped (approximation; loses the old max-pool peak preservation on overviews). Rebuilt worker + rebundled into MindWave (manifest refreshed; exe gitignored). GnauralCore bridge+session integration tests 31/0 against the rebundled worker. Backend restart to pick it up. Owner UI verify (instant zoom/overview; overview looks acceptable) = the gate."
      ]
    },
    {
      "id": "SF11.1",
      "phase": 11,
      "title": "i18n for the Параметры panel",
      "status": "done",
      "dependsOn": [
        "SF3.1"
      ],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramSettingsPanel.vue",
        "GnauralCore/ui/i18n/en.json",
        "GnauralCore/ui/i18n/ru.json"
      ],
      "verify": "no hardcoded user-facing strings left in the settings panel; en/ru localized; vue-tsc + bun green",
      "startedAt": "2026-07-03T01:50:00Z",
      "finishedAt": "2026-07-03T02:05:00Z",
      "blockedReason": null,
      "evidence": [
        "SpectrogramSettingsPanel: routed all field labels through i18n (Window/Zero-pad/Hop/Window func/Data/Freq scale/Channel mode/Channel/Start Hz/Stop Hz/Intensity scale/Palette) and the 6 slider labels (Overlap/Gain/Frequency gain/Dynamic range/Limit/Saturation). Slider list is now a computed so labels stay reactive on language switch. Added 18 audio.spectrogram* keys in en/ru.",
        "Left as-is (intentional): dropdown OPTION values (winFunc/data/fscale/mode/scale/palette) are technical algorithm identifiers (hann, magnitude, log, rainbow, ...) and preset names ('Audacity', 'ffmpeg showspectrumpic') are proper names -> not translated. vue-tsc + bun 61/0; i18n JSON parses."
      ]
    },
    {
      "id": "SF11.2",
      "phase": 11,
      "title": "Fix spectrogram caching across file switches",
      "status": "done",
      "dependsOn": [
        "SF7.3"
      ],
      "artifacts": [
        "GnauralCore/server/spectrogram-session.ts",
        "GnauralCore/server/spectrogram-session.test.ts"
      ],
      "verify": "A->B->A reuses the warm analysis (no full recompute); root cause identified; server tests green; owner verifies",
      "startedAt": "2026-07-03T02:10:00Z",
      "finishedAt": "2026-07-03T02:40:00Z",
      "blockedReason": null,
      "evidence": [
        "ROOT CAUSE: SF7.3's warm-analysis LRU DID work (skips re-decode; the reuse test passes), but Opt C (SF6.4) made the STFT lazy -> every get-tile recomputes the STFT for the requested range. On a file switch A->B->A the client re-fetches all tiles (its per-composable tile cache is cleared on unmount), and the worker recomputes each -> the spectrum visibly rebuilds. So the *analysis* was cached but the *tiles* were not.",
        "FIX: added a per-warm-analysis server-side computed-tile cache in SpectrogramSession (CachedAnalysis.tiles Map keyed by zoom|timeStart|timeEnd|viewBinCount, rounded; insertion-order LRU, cap TILE_CACHE_MAX_PER_ANALYSIS=64). onGetTile serves a cached tile without touching the worker; on miss it fetches + stores + bounds. The cache lives with the warm analysis (kept across close; freed on eviction/reconfigure/dispose), so returning to a file (same analysisId) or re-panning to a seen view is instant.",
        "New unit test (mock worker manager + audio source, no exe needed): two identical get-tile requests -> the worker computes only once (getTileCalls==1); a different view range recomputes (==2). GnauralCore server tests 31/0 (was 30 + new). Backend change -> restart. Owner UI verification (A->B->A returns without rebuilding) = the gate."
      ]
    },
    {
      "id": "SF11.3",
      "phase": 11,
      "title": "Profile + speed up tile generation",
      "status": "done",
      "dependsOn": [
        "SF6.4"
      ],
      "artifacts": [
        "SpectrumCore worker / GnauralCore server (get-tile path)",
        "spectrogram-fixes ledger (findings)"
      ],
      "verify": "get-tile pipeline profiled, bottleneck named, wins applied; owner verifies speed; if insufficient, Audacity 3.7.8 study is the next step",
      "startedAt": "2026-07-03T02:45:00Z",
      "finishedAt": "2026-07-03T03:20:00Z",
      "blockedReason": null,
      "evidence": [
        "PROFILED both by code analysis AND a live stdio harness on 1_Orientation.flac (33min, 171144 frames, bins 2049). MEASURED single-tile get-tile (viewBinCount 512): open-analysis(decode) 2685ms; whole-clip overview zoom=10 (emit 168 cols) COLD 3949ms / worker-W3-repeat 863ms; 120s window zoom=5 (emit 324) 668ms / 455ms; 2s window zoom=0 (emit 174) 247ms / 218ms. NOTE: a naive zoom=0 whole-clip tile emits 171144x512=87M JSON floats and hangs for minutes - but the client never does that (viewportZoomTier picks a high zoom for wide views), so it is not a real bug.",
        "BOTTLENECK = per-tile STFT recompute on a cache miss. HandleGetTile maps the tile's (timeStart,timeEnd) to FULL-RESOLUTION frames (FrameCount = whole span at hop resolution); WriteTileResponse calls EnsureRange(frameStart, frameCount) which computes every one of those FFTs (Opt C lazy), then the TOverviewPyramid max-pools to the zoom tier. So a full-view overview recomputes the ENTIRE file's STFT. Interpolation + JSON emit are secondary.",
        "KEY INSIGHT from the numbers: the worker's own whole-tile cache (TileResultCache/W3) 'repeat' is STILL 863ms for the overview because it caches FULL-RESOLUTION values (AFrameCount x bins) and REBUILDS the max-pool pyramid + re-emits JSON every call. APPLIED WIN (SF11.2): a server-side cache of the FINAL emitted tile (168 cols) in front of the worker -> a re-fetch skips the worker round-trip, the pyramid re-pool AND the JSON entirely (instant). So SF11.2 removes the ~0.9s/tile re-pool the user saw as 'rebuilding', not just the decode.",
        "FIRST-GEN (cold) of an unseen overview = ~3.9s = the whole-file STFT (EnsureRange over all 171144 frames) - already A+B+C optimized (phase-skipped, multithreaded, lazy). REDUNDANCY vs Audacity: because EnsureRange computes per-tile over full-res frame ranges, different zoom levels / overlapping pans recompute the SAME underlying frames repeatedly; Audacity computes each frame once into a persistent per-clip SpecCache and never recomputes. So the deeper win = a bounded PERSISTENT per-frame STFT cache (cache computed full-res frames by frame index; EnsureRange only computes the missing ones) - a middle ground between Opt C (recompute-all) and the old eager full matrix (OOM).",
        "RECOMMENDATION / NEXT (SF-D29 step 2, owner-gated): owner verifies SF11.2 first (A->B->A + re-pan should be instant now; the 863ms/tile re-pool is gone). If cold first-gen of fresh regions is still too slow vs Audacity, implement the persistent per-frame STFT cache (informed by Audacity 3.7.8 SpecCache at c:\\projects\\KKMindWave\\SpectrumCore\\lib\\vendor\\audacity\\3.7.8). Not applied now (it's a worker rearchitecture with a memory-budget design that needs its own step + owner go)."
      ]
    },
    {
      "id": "SF10.1",
      "phase": 10,
      "title": "Common header above the track stack",
      "status": "done",
      "dependsOn": [
        "SF8.1"
      ],
      "artifacts": [
        "GnauralCore/ui/pages/AudioPage.vue",
        "GnauralCore/ui/components/SpectrogramView.vue"
      ],
      "verify": "one shared header bar above the first track holds the zoom/fit buttons (once for all tracks); vue-tsc clean",
      "startedAt": "2026-07-02T08:35:00Z",
      "finishedAt": "2026-07-02T08:45:00Z",
      "blockedReason": null,
      "evidence": [
        "AudioPage: a common header (.audio-page__spectrogram-header) above the stack holds the zoom in / zoom out / fit buttons ONCE for all tracks. They drive the shared time window directly (spectrogramShared.view via zoomWindow/fullWindow); duration from audio.spectrogramBuffer.duration; disabled until a view exists / fit disabled when full.",
        "SpectrogramView: removed the zoom/fit q-btns from its toolbar (and the now-dead zoomIn/zoomOut/resetView fns + isFull computed + isFullWindow import). The primary toolbar still hosts the q-range + readout for now (moved to the bottom minimap + cursor tooltip in SF10.4). vue-tsc clean."
      ]
    },
    {
      "id": "SF10.2",
      "phase": 10,
      "title": "L/R labels as top-left overlays",
      "status": "done",
      "dependsOn": [
        "SF10.1"
      ],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramView.vue"
      ],
      "verify": "L label overlays the top-left corner matching R; labels removed from the toolbar; vue-tsc clean",
      "startedAt": "2026-07-02T08:50:00Z",
      "finishedAt": "2026-07-02T08:55:00Z",
      "blockedReason": null,
      "evidence": [
        "SpectrogramView: the channel label now renders as a top-left plot overlay (.spectrogram-view__label-overlay, left:50px/top:4px) for EVERY track (dropped the !primary guard); removed the toolbar label span + its .spectrogram-view__label CSS. L and R now sit in the same place. (The primary q-range toolbar is retired in SF10.4, after which the overlay sits at the plot top with no toolbar above it.) vue-tsc clean."
      ]
    },
    {
      "id": "SF10.3",
      "phase": 10,
      "title": "Time axis top-of-first + bottom-of-last only",
      "status": "done",
      "dependsOn": [
        "SF8.1"
      ],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramView.vue",
        "GnauralCore/ui/pages/AudioPage.vue"
      ],
      "verify": "time ruler drawn only above the first and below the last track; frequency axis stays per-track; vue-tsc clean",
      "startedAt": "2026-07-02T09:00:00Z",
      "finishedAt": "2026-07-02T09:15:00Z",
      "blockedReason": null,
      "evidence": [
        "SpectrogramView: added showTimeAxisTop/showTimeAxisBottom props (defaults false/true = standalone shows only a bottom ruler). AXIS_MARGIN split into fixed left/right + dynamic marginTop()/marginBottom() (18px when that edge shows a ruler, else 6px); draw() + plotFractions() use them so the plot mapping/hit-testing stay correct. drawAxes draws the time ruler above the plot when showTimeAxisTop and below when showTimeAxisBottom; the frequency axis still draws on every track.",
        "AudioPage passes :show-time-axis-top=\"index===0\" and :show-time-axis-bottom=\"index===last\" -> stereo shows the ruler above L and below R only (no ruler between); mono (first & last) shows both. vue-tsc clean."
      ]
    },
    {
      "id": "SF10.4",
      "phase": 10,
      "title": "Fixed bottom minimap-overview selector",
      "status": "done",
      "dependsOn": [
        "SF10.1"
      ],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramMinimap.vue",
        "GnauralCore/ui/pages/AudioPage.vue",
        "GnauralCore/ui/components/SpectrogramView.vue",
        "GnauralCore/ui/i18n/en.json",
        "GnauralCore/ui/i18n/ru.json"
      ],
      "verify": "canvas minimap-overview bar fixed at the bottom (draggable/resizable visible window), removed from header; readout as cursor tooltip (SF-D25); vue-tsc + bun green",
      "blockedReason": null,
      "startedAt": "2026-07-02T09:20:00Z",
      "finishedAt": "2026-07-02T09:50:00Z",
      "evidence": [
        "New SpectrogramMinimap.vue: a canvas overview of the whole clip [0,duration] with a highlighted, draggable/resizable visible-window handle (player-minimap style, SF-D24). Drag the window body to pan, its left/right edges to zoom (ew-resize), click outside to recenter; dimmed outside the window + bottom time ticks. Drives the shared time window via v-model:view. Placed at the very bottom of the stack column in AudioPage (:duration-sec=spectrogramDuration).",
        "SF-D25: the point/area readout is now a cursor tooltip on the plot (.spectrogram-view__tooltip at the pointer) instead of a toolbar readout. SpectrogramView's whole primary toolbar (q-range + readout) was removed (q-range superseded by the minimap); removed the now-dead rangeModel/rangeStep + clampWindow import + toolbar/range/readout CSS. tooltipText = area summary on the primary (when a selection is committed) else the hover point; tooltipPos tracks the cursor.",
        "i18n audio.spectrogramTimespan (en 'Visible time range' / ru 'Видимый диапазон времени'). vue-tsc clean; GnauralCore bun 61/0; i18n JSON parses. Phase 10 complete. UI-only (HMR). NOTE: the minimap shows the visible-window navigator over a plain overview track (no rendered spectrogram thumbnail yet) — a real thumbnail would need a separate low-zoom tile fetch; can be a follow-up if wanted."
      ]
    },
    {
      "id": "SF10.5",
      "phase": 10,
      "title": "Remove the 'Спектрограмма' subtitle under the transport",
      "status": "done",
      "dependsOn": [
        "SF0.2"
      ],
      "artifacts": [
        "GnauralCore/ui/pages/AudioPage.vue"
      ],
      "verify": "the audio.spectrogramTitle text-subtitle2 line between the transport toolbar and the plot is removed; vue-tsc clean",
      "startedAt": "2026-07-02T07:40:00Z",
      "finishedAt": "2026-07-02T07:42:00Z",
      "blockedReason": null,
      "evidence": [
        "AudioPage.vue: removed <div class=\"text-subtitle2 q-mb-sm\">{{ t('audio.spectrogramTitle') }}</div> in the spectrogram q-tab-panel (the 'Спектрограмма' subtitle directly under the Старт/transport toolbar). i18n key left in place (still used as the tab/canvas label elsewhere)."
      ]
    },
    {
      "id": "SF9.1",
      "phase": 9,
      "title": "Square track corners",
      "status": "done",
      "dependsOn": [
        "SF0.2"
      ],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramView.vue"
      ],
      "verify": "no border-radius on the spectrogram track block (square corners); vue-tsc clean",
      "startedAt": "2026-07-02T07:50:00Z",
      "finishedAt": "2026-07-02T07:52:00Z",
      "blockedReason": null,
      "evidence": [
        "SpectrogramView.vue: removed border-radius:12px from .spectrogram-view -> square corners like Audacity. overflow:hidden retained. vue-tsc clean."
      ]
    },
    {
      "id": "SF9.2",
      "phase": 9,
      "title": "Per-track heights + 2px mutual-resize divider",
      "status": "done",
      "dependsOn": [
        "SF8.1"
      ],
      "artifacts": [
        "GnauralCore/ui/pages/AudioPage.vue",
        "GnauralCore/ui/components/SpectrogramView.vue"
      ],
      "verify": "AudioPage owns per-track heights; a 2px divider between adjacent tracks drags to trade height (one up / one down); vue-tsc + bun green",
      "startedAt": "2026-07-02T07:55:00Z",
      "finishedAt": "2026-07-02T08:10:00Z",
      "blockedReason": null,
      "evidence": [
        "AudioPage: replaced the single spectrogramTrackHeight ref with spectrogramTrackHeights: number[] (persisted as JSON at mindwave-audio-spectrogram-track-heights). Added spectrogramTracks computed (mono=1 / stereo L,R=2 -> {key,analysis,label,primary}); a watch keeps the heights array length synced to the track count (new tracks inherit the first track's height or the 260px default). The stack renders v-for track + a 2px divider between adjacent tracks.",
        "Divider = MUTUAL resize (SF-D19): onSpectrogramDividerPointerDown/Move/Up. Drag down -> the track above grows, the one below shrinks; combined height fixed (top clamped to [MIN, total-MIN]=120..). Writes a fresh array each move (reactive) + persists via the deep watch.",
        "SpectrogramView: retired its own SF5.2 bottom resize handle (template + onResize* handlers + MIN/MAX consts + update:height emit + .spectrogram-view__resize-handle CSS all removed); height is now a plain prop. vue-tsc clean; GnauralCore bun 61/0."
      ]
    },
    {
      "id": "SF9.3",
      "phase": 9,
      "title": "Bottom handle uniform-resizes all tracks",
      "status": "done",
      "dependsOn": [
        "SF9.2"
      ],
      "artifacts": [
        "GnauralCore/ui/pages/AudioPage.vue",
        "GnauralCore/ui/components/SpectrogramView.vue"
      ],
      "verify": "bottom handle below the last track resizes every track equally; resize handling moved to the AudioPage stack (per-view SF5.2 handle retired); vue-tsc + bun green",
      "startedAt": "2026-07-02T08:15:00Z",
      "finishedAt": "2026-07-02T08:25:00Z",
      "blockedReason": null,
      "evidence": [
        "AudioPage: added an 8px bottom handle below the last track in the stack (onSpectrogramBottomPointerDown/Move/Up). It captures all track heights at drag start and applies the SAME delta to every track (h+dy), clamping dy so no track leaves [120,1200] -> all tracks change equally (SF-D20). For mono (1 track) it acts like a single-track resize. Persisted via the deep watch.",
        "Resize handling is now fully in the AudioPage stack (divider = mutual, bottom handle = uniform); the per-view SF5.2 handle was already retired in SF9.2. vue-tsc clean; GnauralCore bun 61/0. Phase 9 complete. UI-only (HMR)."
      ]
    },
    {
      "id": "SF8.1",
      "phase": 8,
      "title": "Audacity-style multi-track chrome",
      "status": "done",
      "dependsOn": [
        "SF5.3"
      ],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramView.vue",
        "GnauralCore/ui/pages/AudioPage.vue",
        "GnauralCore/ui/composables/spectrogram-viewport.ts"
      ],
      "verify": "stereo: toolbar/icons on first track only; per-track resize handle; unified span/area selector across tracks; no inter-track chrome/gap; click/readout correct; vue-tsc + bun green",
      "startedAt": "2026-07-02T06:30:00Z",
      "finishedAt": "2026-07-02T07:00:00Z",
      "blockedReason": null,
      "evidence": [
        "Stacked tracks now share one time window + area selection via provide/inject (AudioPage provides `spectrogramShared` = {view, selection, commitSeq} reactive refs; SpectrogramView injects and uses them, falling back to per-view internal state when absent). Zoom/pan/range + drag-select on any track apply to + render on all synchronously (no v-model async lag). Shared view/selection reset per file (AudioPage watch on displayFilePath).",
        "Chrome: `primary` prop (L / mono = true, R = false). Toolbar (zoom/fit/range/readout + label) renders only on the primary; non-primary shows just a small label overlay on the plot -> no inter-track chrome. Column gap reduced 8px->1px so tracks abut. Each track keeps its own bottom resize handle. Area-query readout runs on the primary when any track commits a selection (shared.commitSeq); cleared when the selection clears. Per-track click->seek + hover unchanged (map via the time window).",
        "SpectrogramSelection type added to spectrogram-viewport.ts (shared by the component + AudioPage). vue-tsc clean; GnauralCore bun 61/0. UI-only (HMR / page reload). Owner visual verification = the pause."
      ]
    },
    {
      "id": "SF8.2",
      "phase": 8,
      "title": "Adaptive frequency-axis tick density",
      "status": "done",
      "dependsOn": [
        "SF0.2"
      ],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramView.vue"
      ],
      "verify": "frequency-axis shows more level labels as track height grows (fewer when short); vue-tsc + bun green",
      "startedAt": "2026-07-02T05:50:00Z",
      "finishedAt": "2026-07-02T05:55:00Z",
      "blockedReason": null,
      "evidence": [
        "SpectrogramView.drawAxes: frequency tick count is now Math.max(3, Math.min(20, floor(plotH/42))) instead of a fixed 6 -> more level labels as the track grows, fewer when short (Audacity-like). frequencyAxisTicks already yields N evenly-spaced ticks. vue-tsc clean; bun 61/0."
      ]
    },
    {
      "id": "SF8.3",
      "phase": 8,
      "title": "Don't auto-build spectrum on Audio-tab open",
      "status": "done",
      "dependsOn": [
        "SF4.1"
      ],
      "artifacts": [
        "GnauralCore/ui/pages/AudioPage.vue"
      ],
      "verify": "opening the Audio tab with a pre-selected file does NOT auto-build; build only on explicit action - file click or switch to Спектрограмма sub-tab (SF-D17 option a: drop immediate from the ensureSpectrogramPrepared watch); vue-tsc + bun green",
      "startedAt": "2026-07-02T05:45:00Z",
      "finishedAt": "2026-07-02T05:50:00Z",
      "blockedReason": null,
      "evidence": [
        "AudioPage: removed `{ immediate: true }` from the ensureSpectrogramPrepared watch. It now fires only on subsequent changes to activePlayerViewTab/activeContentTab/selectedPath (user switches to the Спектрограмма view or selects a file) -> opening/returning to the Audio tab with a restored selection no longer auto-builds. RF4.1's on-click decode is unchanged. vue-tsc clean; bun 61/0."
      ]
    },
    {
      "id": "SF7.1",
      "phase": 7,
      "title": "Immediate tracks + load-progress dialog overlay",
      "status": "done",
      "dependsOn": [
        "SF0.2"
      ],
      "artifacts": [
        "GnauralCore/ui/pages/AudioPage.vue"
      ],
      "verify": "tracks view shows immediately on select; a small non-blocking progress dialog overlays while decode/prepare runs (replacing the full-panel loading placeholder); vue-tsc clean",
      "startedAt": "2026-07-02T05:00:00Z",
      "finishedAt": "2026-07-02T05:10:00Z",
      "blockedReason": null,
      "evidence": [
        "AudioPage: removed the full-panel 'audio.spectrogramLoading -> spectrogramLoadingLabel' placeholder that hid the spectrogram tab; the tab frame (transport + title + plot area) now stays visible during load. noSpectrogram hint only shows when NOT loading. Added a small persistent q-dialog (audio-page__load-dialog, spinner + spectrogramLoadingLabel + indeterminate q-linear-progress) bound to :model-value=audio.spectrogramLoading, at the page root so it overlays regardless of tab (Audacity-style). vue-tsc clean."
      ]
    },
    {
      "id": "SF7.2",
      "phase": 7,
      "title": "Track-height resize without full recompute",
      "status": "done",
      "dependsOn": [
        "SF0.2"
      ],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramView.vue"
      ],
      "verify": "dragging the track height does not recompute the spectrogram (option b: fixed viewBinCount, view unchanged -> setView guard skips refetch; raster scales to height); readout/click mapping stays correct; vue-tsc + bun green",
      "startedAt": "2026-07-02T05:10:00Z",
      "finishedAt": "2026-07-02T05:15:00Z",
      "blockedReason": null,
      "evidence": [
        "SpectrogramView.applyView: viewBinCount is now a fixed constant (MAX_VIEW_BINS) instead of derived from canvas height. A height-only resize keeps the view (time window + zoom + viewBinCount) identical -> the SF2.1 setView no-op guard skips the refetch; the raster scales to the new height via the existing drawImage-to-plotH. Width/time zoom still refetch (columns). Readout/click map via the time window + fractions (independent of tile bin count), so they stay correct. vue-tsc clean; GnauralCore bun 61/0."
      ]
    },
    {
      "id": "SF7.3",
      "phase": 7,
      "title": "Cache spectrograms across file switches",
      "status": "done",
      "dependsOn": [
        "SF0.2"
      ],
      "artifacts": [
        "GnauralCore/server/spectrogram-session.ts",
        "GnauralCore/server/spectrogram-session.test.ts"
      ],
      "verify": "switching back to a previously-opened file reuses the cached analysis (no full recompute); memory bounded (LRU eviction); server tests green; owner verification",
      "startedAt": "2026-07-02T06:00:00Z",
      "finishedAt": "2026-07-02T06:20:00Z",
      "blockedReason": null,
      "evidence": [
        "SpectrogramSession now keeps analyses WARM as an LRU keyed by file+fileKind+params: onOpen reuses a matching warm analysis (returns its analysisId + cached info -> skips the ~seconds full-file decode/open); onClose keeps the entry alive (no free); requireAnalysis bumps LRU on get-tile/point/area. Eviction on open past caps (ANALYSIS_CACHE_MAX=4 OR ANALYSIS_CACHE_BUDGET_BYTES=1.5GB of kept samples, whichever first), never evicting the just-opened. dispose frees all. Because Opt C made analyses lazy (samples-only), keeping a few warm is affordable and bounded.",
        "Behavior change: close no longer frees the worker analysis (kept warm). Updated the contract test accordingly; added a reuse test (reopen same file+params -> same analysisId; different params -> new). GnauralCore server tests 18/0 (bridge + session, real worker). Backend change -> restart (worker binary unchanged, no re-bundle). Owner UI verification (A -> B -> back to A returns fast) = the pause."
      ]
    },
    {
      "id": "SF4.1",
      "phase": 4,
      "title": "Auto-prepare spectrum when the Спектрограмма tab is opened",
      "status": "done",
      "dependsOn": [
        "SF0.2"
      ],
      "artifacts": [
        "GnauralCore/ui/pages/AudioPage.vue",
        "GnauralCore/ui/i18n/en.json",
        "GnauralCore/ui/i18n/ru.json"
      ],
      "verify": "switching to the Спектрограмма tab with a selected local audio file (no decoded buffer) triggers ensureLocalAudioReady so the spectrum prepares (spinner -> spectrum) instead of the stale 'start local playback' message; noSpectrogram wording updated; .gnaural unchanged; vue-tsc + bun green",
      "startedAt": "2026-07-01T19:40:00Z",
      "finishedAt": "2026-07-01T19:55:00Z",
      "blockedReason": null,
      "evidence": [
        "AudioPage: added ensureSpectrogramPrepared() — when the player Спектрограмма view is active and a local audio file (wav/flac) is selected with no decoded buffer and no decode in flight, it calls audio.ensureLocalAudioReady(path, kind) (same non-playback decode as RF4.1). Wired via watch([activePlayerViewTab, activeContentTab, () => audio.selectedPath], immediate) guarded to the player content tab. Guard skips when already loading/decoded, so it won't double-fetch with RF4.1's selection decode (wavLoading is set synchronously first).",
        "Updated stale audio.noSpectrogram wording (a selected file now auto-prepares): en 'Select an audio file to view its spectrum.' / ru 'Выберите аудиофайл, чтобы посмотреть его спектр.'. .gnaural (noGnauralSpectrogram) unchanged.",
        "Verify: vue-tsc --noEmit clean; GnauralCore bun 56/0; i18n JSON parses. Live-verify in quasar dev (owner): select on Воспроизведение -> switch to Спектрограмма -> spinner then spectrum (no dead message)."
      ]
    },
    {
      "id": "SF16.1",
      "phase": 16,
      "title": "Numeric spinbox fields for render dials (gain/drange/limit/frequencyGain/saturation)",
      "status": "done",
      "dependsOn": [],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramSettingsPanel.vue",
        "GnauralCore/ui/composables/spectrogram-render.ts",
        "GnauralCore/ui/composables/spectrogram-settings.ts"
      ],
      "verify": "vue-tsc exit 0; bun test ui green; owner confirms field UX + default mapping",
      "startedAt": "2026-07-05T00:45:00Z",
      "finishedAt": "2026-07-05T01:10:00Z",
      "blockedReason": null,
      "evidence": [
        "GnauralCore b3e532c",
        "vue-tsc exit 0",
        "bun ui 66/0",
        "quasar build ok",
        "gain->dB (0 default); 5 dials now spinbox with -/+ steppers"
      ]
    },
    {
      "id": "SF16.2",
      "phase": 16,
      "title": "Full Audacity palette set (Розовый/Классика/Оттенки серого/Инверсия)",
      "status": "done",
      "dependsOn": [],
      "artifacts": [
        "GnauralCore/ui/composables/spectrogram-render.ts",
        "GnauralCore/ui/composables/spectrogram-settings.ts",
        "GnauralCore/ui/composables/spectrogram-settings.test.ts",
        "GnauralCore/ui/i18n/en.json",
        "GnauralCore/ui/i18n/ru.json"
      ],
      "verify": "vue-tsc exit 0; bun coverage test updated+green; Розовый visually matches Audacity",
      "startedAt": "2026-07-05T00:10:00Z",
      "finishedAt": "2026-07-05T00:40:00Z",
      "blockedReason": null,
      "evidence": [
        "GnauralCore a7990bd",
        "vue-tsc exit 0",
        "bun ui 66/0",
        "Roseus LUT ported from Audacity 3.7.8 AColorResources.h specColormap[256][3]"
      ]
    },
    {
      "id": "SF16.3",
      "phase": 16,
      "title": "Zoom preset popover (right-click): x1..x16 + % field",
      "status": "done",
      "dependsOn": [],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramView.vue"
      ],
      "verify": "vue-tsc exit 0; bun green; right-click opens presets + % edits/reflects zoom",
      "startedAt": "2026-07-05T02:30:00Z",
      "finishedAt": "2026-07-05T02:55:00Z",
      "blockedReason": null,
      "evidence": [
        "GnauralCore c1bcb3d",
        "q-menu context-menu popover, presets x1..x16 + % field",
        "x1=whole clip; right/middle button no longer drags",
        "vue-tsc exit 0",
        "bun ui 68/0",
        "quasar build ok"
      ]
    },
    {
      "id": "SF16.4",
      "phase": 16,
      "title": "Home/End/arrow keyboard navigation like Audacity",
      "status": "done",
      "dependsOn": [
        "SF15.1"
      ],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramView.vue"
      ],
      "verify": "vue-tsc exit 0; bun green; Home/End/arrows move view per Audacity",
      "startedAt": "2026-07-05T02:10:00Z",
      "finishedAt": "2026-07-05T02:25:00Z",
      "blockedReason": null,
      "evidence": [
        "GnauralCore 6eef7db",
        "canvas tabindex + onKeyDown",
        "Home/End edges, arrows pan 15%, Shift+arrow page",
        "vue-tsc exit 0",
        "bun ui 68/0"
      ]
    },
    {
      "id": "SF16.5",
      "phase": 16,
      "title": "Diagnose our-vs-Audacity image difference (written finding + any fix)",
      "status": "done",
      "dependsOn": [
        "SF16.2"
      ],
      "artifacts": [
        "docs/spectrogram-fixes/spectrogram-fixes-plan.md"
      ],
      "verify": "written finding recorded; comparison on identical palette/settings; any fix justified by finding",
      "startedAt": "2026-07-05T01:15:00Z",
      "finishedAt": "2026-07-05T01:40:00Z",
      "blockedReason": null,
      "evidence": [
        "GnauralCore 72607a4",
        "Finding: difference is config not bug (palette+gain/range+mel/range)",
        "Audacity preset now = Audacity 3.7.8 defaults",
        "vue-tsc exit 0",
        "bun ui 66/0"
      ]
    },
    {
      "id": "SF16.6",
      "phase": 16,
      "title": "Denser time ruler with minor ticks (drawAxes)",
      "status": "done",
      "dependsOn": [],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramView.vue"
      ],
      "verify": "vue-tsc exit 0; bun green; ruler shows denser 1/2/5 labels + minor ticks",
      "startedAt": "2026-07-05T01:45:00Z",
      "finishedAt": "2026-07-05T02:05:00Z",
      "blockedReason": null,
      "evidence": [
        "GnauralCore 7e76b0c",
        "timeAxisTicksWithMinor() major+minor",
        "~1 label/90px adaptive",
        "vue-tsc exit 0",
        "bun ui 68/0"
      ]
    },
    {
      "id": "SF17.1",
      "phase": 17,
      "title": "Zoom popover polish — close ✕ + Esc + clearer Применить button",
      "status": "done",
      "dependsOn": [
        "SF16.3"
      ],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramView.vue"
      ],
      "verify": "vue-tsc exit 0; bun green; popover has ✕, closes on Esc, Применить reads as a button",
      "startedAt": "2026-07-05T04:00:00Z",
      "finishedAt": "2026-07-05T04:15:00Z",
      "blockedReason": null,
      "evidence": [
        "GnauralCore 0a29e50",
        "close ✕ + Esc + full-width primary Применить + autofocus",
        "vue-tsc exit 0",
        "bun ui 68/0"
      ]
    },
    {
      "id": "SF17.2",
      "phase": 17,
      "title": "Image-scaling toggle (smooth/sharp) + «Резкость» settings group",
      "status": "done",
      "dependsOn": [
        "SF16.3"
      ],
      "artifacts": [
        "GnauralCore/ui/composables/spectrogram-settings.ts",
        "GnauralCore/ui/composables/spectrogram-render.ts",
        "GnauralCore/ui/components/SpectrogramSettingsPanel.vue",
        "GnauralCore/ui/components/SpectrogramView.vue"
      ],
      "verify": "vue-tsc exit 0; bun green; Резкое=nearest-neighbour draw (crisp), Сглаженное=bilinear",
      "startedAt": "2026-07-05T04:20:00Z",
      "finishedAt": "2026-07-05T04:45:00Z",
      "blockedReason": null,
      "evidence": [
        "GnauralCore f7b4b0d",
        "imageScaling smooth/sharp -> ctx.imageSmoothingEnabled",
        "new Резкость group",
        "vue-tsc exit 0",
        "bun ui 68/0",
        "quasar build ok"
      ]
    },
    {
      "id": "SF17.3",
      "phase": 17,
      "title": "High-zoom analysis profile (smaller window / reassign) — zoom-reactive params",
      "status": "done",
      "dependsOn": [
        "SF17.2"
      ],
      "artifacts": [
        "GnauralCore/ui/composables/spectrogram-settings.ts",
        "GnauralCore/ui/components/SpectrogramSettingsPanel.vue",
        "GnauralCore/ui/pages/AudioPage.vue",
        "GnauralCore/ui/components/SpectrogramView.vue"
      ],
      "verify": "vue-tsc exit 0; bun green; crossing zoom threshold switches window/data and sharpens; reverts below",
      "startedAt": "2026-07-05T04:50:00Z",
      "finishedAt": "2026-07-05T05:25:00Z",
      "blockedReason": null,
      "evidence": [
        "GnauralCore 7b690c3",
        "zoom-reactive analysis params w/ hysteresis",
        "smallWindow (re-derived hop) + reassign modes",
        "applies to mono+L/R",
        "vue-tsc exit 0",
        "bun ui 68/0",
        "quasar build ok"
      ]
    },
    {
      "id": "SF17.4a",
      "phase": 17,
      "title": "Worker get-tile windowOverride via lazy sibling analysis",
      "status": "done",
      "dependsOn": [],
      "artifacts": [
        "SpectrumCore/src/apps/SpectrumCoreFftwWorkerProbe.pas",
        "MindWaveCore/server/vendor/spectrumcore/win64/"
      ],
      "verify": "build + rebundle; direct stdio probe: open -> get-tile w/ & w/o windowOverride -> distinct binCount/binFrequenciesHz; base unaffected",
      "startedAt": "2026-07-05T06:20:00Z",
      "finishedAt": "2026-07-05T07:10:00Z",
      "blockedReason": null,
      "evidence": [
        "SpectrumCore 6e4e92b",
        "MindWave 269fe57 (rebundle)",
        "lazy sibling analysis + FreeSiblingsFor",
        "probe: base 108 cols, override512 431 cols, base after 108, clean"
      ]
    },
    {
      "id": "SF17.4b",
      "phase": 17,
      "title": "Protocol + server passthrough for windowOverride",
      "status": "done",
      "dependsOn": [
        "SF17.4a"
      ],
      "artifacts": [
        "SharedPasCore or protocol types",
        "GnauralCore/server/spectrogram-session.ts"
      ],
      "verify": "bridge/session integration tests green; get-tile forwards windowOverride",
      "startedAt": "2026-07-05T06:20:00Z",
      "finishedAt": "2026-07-05T07:10:00Z",
      "blockedReason": null,
      "evidence": [
        "SharedPasCore 3ab0dc2",
        "GnauralCore 95ca67e",
        "protocol windowOverride + server passthrough + cache key",
        "server tests 19/0"
      ]
    },
    {
      "id": "SF17.4c",
      "phase": 17,
      "title": "Client fetch-time windowOverride (replace smallWindow reconfigure)",
      "status": "done",
      "dependsOn": [
        "SF17.4b"
      ],
      "artifacts": [
        "GnauralCore/ui/composables/use-spectrogram.ts",
        "GnauralCore/ui/composables/spectrogram-tiles.ts",
        "GnauralCore/ui/components/SpectrogramView.vue",
        "GnauralCore/ui/pages/AudioPage.vue"
      ],
      "verify": "vue-tsc + bun green; smallWindow high zoom fetches override tiles w/o reconfigure; cache key includes override",
      "startedAt": "2026-07-05T06:20:00Z",
      "finishedAt": "2026-07-05T07:10:00Z",
      "blockedReason": null,
      "evidence": [
        "GnauralCore 6499d61",
        "smallWindow now per-tile override (no reconfigure); reassign stays whole-track",
        "vue-tsc 0; bun ui+server 99/0; quasar build ok"
      ]
    },
    {
      "id": "SF18.1",
      "phase": 18,
      "title": "Preset store + model: user presets, persistence, active/modified, actions",
      "status": "done",
      "dependsOn": [],
      "artifacts": [
        "GnauralCore/ui/composables/spectrogram-settings.ts",
        "GnauralCore/ui/stores/spectrogram.ts",
        "GnauralCore/ui/composables/spectrogram-settings.test.ts"
      ],
      "verify": "vue-tsc + bun green; validator + apply/saveAs/rename/delete/duplicate/import semantics tested",
      "startedAt": "2026-07-05T08:05:00Z",
      "finishedAt": "2026-07-05T08:55:00Z",
      "blockedReason": null,
      "evidence": [
        "GnauralCore 5828cbb",
        "user-preset model+validator+settingsEqual; store actions",
        "bun ui 71/0; vue-tsc 0"
      ]
    },
    {
      "id": "SF18.2",
      "phase": 18,
      "title": "Preset manager UI: dropdown + save-as + manager dialog + i18n",
      "status": "done",
      "dependsOn": [
        "SF18.1"
      ],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramSettingsPanel.vue",
        "GnauralCore/ui/components/SpectrogramPresetManager.vue",
        "GnauralCore/ui/i18n/en.json",
        "GnauralCore/ui/i18n/ru.json"
      ],
      "verify": "vue-tsc + bun green; quasar build ok; apply/save-as/rename/delete/duplicate/export/import work; active+modified shown",
      "startedAt": "2026-07-05T08:05:00Z",
      "finishedAt": "2026-07-05T08:55:00Z",
      "blockedReason": null,
      "evidence": [
        "GnauralCore 785ebde",
        "MindWave ecb8404 (Dialog plugin)",
        "dropdown + save-as + manager dialog (rename/delete/duplicate/export/import)",
        "vue-tsc 0; bun 71/0; quasar build ok"
      ]
    },
    {
      "id": "SF19.1",
      "phase": 19,
      "title": "Arrows move playhead (seek); Alt+arrows pan view",
      "status": "done",
      "dependsOn": [],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramView.vue"
      ],
      "verify": "vue-tsc+bun green; Left/Right seek playhead, Alt+Left/Right pan view; playhead stays visible",
      "startedAt": "2026-07-05T09:30:00Z",
      "finishedAt": "2026-07-05T10:05:00Z",
      "blockedReason": null,
      "evidence": [
        "GnauralCore 3b5e0e8",
        "arrows seek ±1s/±5s; Alt=pan; followPlayhead",
        "vue-tsc 0; bun 71/0"
      ]
    },
    {
      "id": "SF19.2",
      "phase": 19,
      "title": "Idle debounce for fast scroll/zoom",
      "status": "done",
      "dependsOn": [],
      "artifacts": [
        "GnauralCore/ui/composables/use-spectrogram.ts",
        "GnauralCore/ui/components/SpectrogramView.vue"
      ],
      "verify": "vue-tsc+bun green; fast scroll/zoom coalesces, intermediate frames not fetched",
      "startedAt": "2026-07-05T09:30:00Z",
      "finishedAt": "2026-07-05T10:05:00Z",
      "blockedReason": null,
      "evidence": [
        "GnauralCore 8871393",
        "refetchDebounceMs 80->200"
      ]
    },
    {
      "id": "SF19.3",
      "phase": 19,
      "title": "Seamless zoom: no preparing overlay/dim on view-change refetch; background swap",
      "status": "done",
      "dependsOn": [],
      "artifacts": [
        "GnauralCore/ui/composables/use-spectrogram.ts",
        "GnauralCore/ui/components/SpectrogramView.vue"
      ],
      "verify": "vue-tsc+bun green; zoom/pan keeps current frame, no dim/message, new frame swaps in when ready; initial load still shows overlay",
      "startedAt": "2026-07-05T09:30:00Z",
      "finishedAt": "2026-07-05T10:05:00Z",
      "blockedReason": null,
      "evidence": [
        "GnauralCore 3f2345e",
        "split preparing/fetchingTiles; full overlay only on initial prepare; tiny corner spinner + background swap on view-change",
        "vue-tsc 0; bun 71/0; quasar build ok"
      ]
    },
    {
      "id": "SF20.1",
      "phase": 20,
      "title": "Store: recent-files tracking + stop auto-load on mount",
      "status": "done",
      "dependsOn": [],
      "artifacts": [
        "GnauralCore/ui/stores/audio.ts"
      ],
      "verify": "vue-tsc green; opening Audio tab loads nothing; selecting a file records it in recentFiles (cap 5, dedupe, persisted)",
      "startedAt": "2026-07-06T00:00:00Z",
      "finishedAt": "2026-07-06T00:40:00Z",
      "blockedReason": null,
      "evidence": [
        "GnauralCore ca98d26",
        "selectedPath inits null; recentFiles+recordRecentFile; no refreshPresets re-select",
        "vue-tsc 0"
      ]
    },
    {
      "id": "SF20.2",
      "phase": 20,
      "title": "UI: recent-files quick list when no file selected",
      "status": "done",
      "dependsOn": [
        "SF20.1"
      ],
      "artifacts": [
        "GnauralCore/ui/pages/AudioPage.vue",
        "GnauralCore/ui/i18n/en.json",
        "GnauralCore/ui/i18n/ru.json"
      ],
      "verify": "vue-tsc + quasar build; recent list shows 5 entries, click loads; empty-state hint",
      "startedAt": "2026-07-06T00:00:00Z",
      "finishedAt": "2026-07-06T00:40:00Z",
      "blockedReason": null,
      "evidence": [
        "GnauralCore 6559229",
        "toolbar q-btn-dropdown of recent files; removed sessionFilePath",
        "vue-tsc 0; build ok"
      ]
    },
    {
      "id": "SF20.3",
      "phase": 20,
      "title": "Sub-second time-axis labels at high zoom (comma fraction)",
      "status": "done",
      "dependsOn": [
        "SF16.6"
      ],
      "artifacts": [
        "GnauralCore/ui/composables/spectrogram-axes.ts",
        "GnauralCore/ui/composables/spectrogram-axes.test.ts",
        "GnauralCore/ui/components/SpectrogramView.vue"
      ],
      "verify": "vue-tsc + bun green; duplicate second labels get a comma-fraction (1:05,3) when step<1s",
      "startedAt": "2026-07-06T00:00:00Z",
      "finishedAt": "2026-07-06T00:40:00Z",
      "blockedReason": null,
      "evidence": [
        "GnauralCore 713b7bd",
        "formatTimeSec(step) comma-fraction; timeAxisTicksWithMinor returns majorStep",
        "bun ui 72/0; vue-tsc 0; build ok"
      ]
    }
  ],
  "log": [
    {
      "ts": "2026-07-04T15:40:00Z",
      "step": "SF14",
      "event": "planned",
      "note": "Owner added Phase 14 (channel params): SF14.1 'Channel mode' is a NO-OP (investigation: worker only echoes mode; L/R split is UI-driven) -> owner to decide remove vs implement combined/separate (SF-D38, blocked on decision); SF14.2 'Channel' field -> localized Left/Right select instead of 0/1 (SF-D39). Plan-only ('Добавь в план', no go) — awaiting go + the SF14.1 decision."
    },
    {
      "ts": "2026-07-04T18:20:00Z",
      "step": "SF15.1",
      "event": "done",
      "note": "Audacity-style mouse navigation (researched MouseWheelHandler.cpp + SpectrumVZoomHandle.cpp). Mapped to our single canvas: wheel/Ctrl+wheel=time zoom about pointer, Shift+wheel=time pan, Alt+wheel=FREQ zoom about pointer, Alt+Shift+wheel=freq pan, double-click=reset time+freq. NEW capability = frequency zoom (shared FreqWindow {lo,hi}, tile vertical crop + axis slice + query remap; stereo tracks share it). vue-tsc + build + ui 63/0. Phase 15 complete. Owner verify = gate."
    },
    {
      "ts": "2026-07-04T17:30:00Z",
      "step": "SF15.2",
      "event": "done",
      "note": "Removed the vestigial 'Channel' field (overridden by the L/R split): dropped from panel + model (interface/DEFAULT/mergeStoredSettings/toAnalysisParams); L/R split still sets channel 0/1 per track, mono omits it (worker defaults 0). vue-tsc + bun 63/0. Now doing SF15.1 (Audacity navigation)."
    },
    {
      "ts": "2026-07-04T17:05:00Z",
      "step": "SF15",
      "event": "planned",
      "note": "Owner added Phase 15: SF15.1 Audacity-style mouse navigation (Ctrl/Alt/Shift + wheel/drag; research Audacity 3.7.8 HandleWheelRotation + SpectrumVZoomHandle first) (SF-D42); SF15.2 the 'Channel' param is vestigial (overridden by the L/R split) -> recommend remove (SF-D43). Plan-only ('Добавь в план', no go) — awaiting go."
    },
    {
      "ts": "2026-07-04T16:45:00Z",
      "step": "SF14.4",
      "event": "done",
      "note": "Owner chose variant (b) for the overlap finding: overlap is now the frame-step control (Audacity/ffmpeg style); removed the redundant `hop` field from the model + panel; toAnalysisParams derives hop = max(1, round(window*(1-overlap))). Default unchanged (0.75->512). +2 settings-test updates. vue-tsc + build + ui 63/0. Phase 14 complete."
    },
    {
      "ts": "2026-07-04T16:20:00Z",
      "step": "SF14",
      "event": "done",
      "note": "Phase 14: SF14.3 audit done -> all params USED except 'mode' (dead, echoed only) and 'overlap' (effectively dead: hop always wins, overlap re-computed from hop). SF14.1 removed the dead 'Channel mode' field (kept the harmless model default). SF14.2 'Channel' -> localized Left/Right select. vue-tsc + bun 63/0. OPEN: owner decision on 'overlap' (remove the slider vs make it the control). UI-only."
    },
    {
      "ts": "2026-07-04T15:55:00Z",
      "step": "SF14.3",
      "event": "planned",
      "note": "Owner added SF14.3 (SF-D40): audit ALL spectrogram parameters for real use (motivated by the mode no-op). Trace each analysis param into the worker STFT + each render param into the client render; deliver a per-parameter used/partial/no-op verdict + remove/wire/fix proposals. Plan-only, awaiting go."
    },
    {
      "ts": "2026-07-04T15:20:00Z",
      "step": "SF11.8",
      "event": "fixed",
      "note": "BUG (owner: switching preset to ffmpeg -> 'no open analysis to reconfigure', though a spectrum still draws). Root cause = B7 regression: the minimap REUSES the primary track's analysis (same analysisId via the warm-LRU key) but had its OWN reconfigure watch; a preset switch fires reconfigure from both the track and the minimap on the same id, and reconfigure = close+reopen-with-a-NEW-id (deletes the old), so whichever fires second throws. Fix: the minimap no longer reconfigures — on a params change it RE-OPENS (openForPath) with the new params; the server serializes per session so the track reconfigures first (150ms debounce) and the minimap re-open (300ms) reuses the reconfigured analysis via findByKey (no id conflict, no duplicate). vue-tsc + ui 63/0."
    },
    {
      "ts": "2026-07-04T14:55:00Z",
      "step": "SF13",
      "event": "done",
      "note": "Phase 13 done in one data-driven rewrite of SpectrogramSettingsPanel: SF13.1 3 Audacity groups (Масштаб/Цвет/FFT-фильтр); SF13.2 localized dropdown VALUES (nested audio.spectrogram*Opt namespaces incl 24 window funcs, en/ru); SF13.3 per-field '?' help icon (q-menu) with bilingual help text (17 params). vue-tsc + prod build + bun 63/0. UI-only. Owner verify = gate."
    },
    {
      "ts": "2026-07-04T14:00:00Z",
      "step": "SF13",
      "event": "planned",
      "note": "Owner added Phase 13 (settings panel Audacity parity): SF13.1 regroup into Масштаб/Цвет/FFT-фильтр (SF-D35); SF13.2 i18n for parameter VALUES / dropdown options (SF-D36); SF13.3 per-parameter '?' help icon + bilingual help text (SF-D37). Executing. (Push: all repos already in sync; SF12.1 FFT bug already done in a prior session.)"
    },
    {
      "ts": "2026-07-01T18:55:00Z",
      "step": "SF0.1",
      "event": "done",
      "note": "Spectrogram-fixes plan + ledger created. 3 items: (1) loading indicator during prepare; (2) fix only-last-frame render (tiles don't accumulate; owner repro = on open, one-tile strip runs L->R, only last stays); (3) settings side panel -> overlay named 'Параметры' (no main-content resize). Decisions SF-D1..SF-D5 drafted; next = SF0.2 approval gate."
    },
    {
      "ts": "2026-07-01T19:00:00Z",
      "step": "SF0.2",
      "event": "done",
      "note": "Owner approved ('go'). SF-D2..SF-D5 locked. Proceeding to Phase 1."
    },
    {
      "ts": "2026-07-01T19:05:00Z",
      "step": "SF1.1",
      "event": "done",
      "note": "Added a loading overlay (spinner + 'Подготовка спектрограммы…') in SpectrogramView driven by spec.loading (analysis open + tiles pending). vue-tsc clean."
    },
    {
      "ts": "2026-07-01T19:10:00Z",
      "step": "-",
      "event": "note",
      "note": "Owner: none of the session's UI changes were visible -> the prod app served the stale built server/public. Rebuilt UI (quasar build). Owner then switched to quasar dev for live HMR."
    },
    {
      "ts": "2026-07-01T19:25:00Z",
      "step": "SF1.1",
      "event": "fixed",
      "note": "Owner: spinner still not appearing. Root cause: spec.loading only counted pending tiles, so it was false during the slow worker open()/reconfigure() and tiles arrived too fast. Added an `opening` flag (open/reconfigure -> loading true; cleared at refetch/error/close) so the indicator covers the whole prepare span. vue-tsc + bun green."
    },
    {
      "ts": "2026-07-01T19:40:00Z",
      "step": "SF4.1",
      "event": "planned",
      "note": "New owner item (SF-D6): on the Воспроизведение tab select a file, switch to Спектрограмма -> stale 'start local playback' message and nothing happens. Add Phase 4: auto-decode (ensureLocalAudioReady) when the Спектрограмма tab opens for a selected undecoded local file + refresh noSpectrogram wording. Awaiting owner 'go'."
    },
    {
      "ts": "2026-07-01T19:55:00Z",
      "step": "SF4.1",
      "event": "done",
      "note": "AudioPage now auto-prepares the spectrum when the Спектрограмма tab is opened for a selected undecoded local file (ensureSpectrogramPrepared + watch); stale noSpectrogram wording refreshed (en/ru). vue-tsc + bun 56/0."
    },
    {
      "ts": "2026-07-01T20:20:00Z",
      "step": "SF2.1",
      "event": "done",
      "note": "Fixed only-last-frame render: tiles now persist/accumulate. Cache getByTileIndex binCount-fallback + assembleVisibleTiles uses it; always re-assemble on tile arrival; setView no-op guard against churn; viewBinCount rounded to 32-step. +4 unit tests (incl. explicit regression). vue-tsc clean; GnauralCore bun 60/0. Owner visual confirm pending."
    },
    {
      "ts": "2026-07-01T20:45:00Z",
      "step": "SF2.1",
      "event": "fixed",
      "note": "Owner screenshot: bug persisted (strip runs L->R then vanishes; spinner stuck). REAL root cause: planVisibleTiles used a fixed 256 full-res frames/tile regardless of zoom -> a long file's full view plans hundreds of tiles that overflow the 128-LRU cache -> only rightmost survive. Fix: tile span = tileFrames*2^zoom full-res frames, so zoomed-out views need only a handful of tiles that fit the cache and all render. +1 unit test. vue-tsc clean; bun 61/0."
    },
    {
      "ts": "2026-07-01T21:05:00Z",
      "step": "SF2.1",
      "event": "reopened",
      "note": "Owner screenshot after the zoom fix: spinner clears but the plot is fully blank (no tiles AND no axes). Since the spinner clears when pending tiles drain (incl. on error), the wide/high-zoom get-tile requests are most likely erroring -> pending drained -> empty. Added diagnostics/robustness (no behavioural fix yet): (1) SpectrogramView draws axes whenever an analysis is present (blank plot != no analysis); (2) surfaces spec.error as an on-screen overlay; (3) close() resets currentView so the no-op guard can't skip the post-open setView. Need the on-screen error text / browser console to pin the worker failure before the real fix. vue-tsc clean; bun 61/0."
    },
    {
      "ts": "2026-07-01T21:25:00Z",
      "step": "SF2.1",
      "event": "fixed",
      "note": "Owner error surfaced: 'worker request timed out after 15000ms' on a 25-min FLAC (d:\\bin\\Presets\\01 - Elation Galaxy.flac). Root cause of the blank: heavy worker ops (full-file STFT open-analysis / wide overview get-tile) exceed the flat 15s SpectrogramWorkerManager timeout, which poisons + restarts the worker -> analysis/tiles never arrive -> blank. Fix: spectrogram-session.ts now passes generous per-command timeouts (open-analysis/reconfigure 120s, get-tile 60s). BACKEND change -> requires bun server restart (not UI HMR). server tests 17/0."
    },
    {
      "ts": "2026-07-01T21:45:00Z",
      "step": "SF5",
      "event": "planned",
      "note": "Owner addition (attached Audacity reference): new Phase 5 'Make the UI Audacity-like' — SF5.1 default track height, SF5.2 drag-resize via bottom border, SF5.3 stereo left/right split. Decisions SF-D7/D8/D9. Awaiting owner 'go'."
    },
    {
      "ts": "2026-07-01T22:00:00Z",
      "step": "-",
      "event": "note",
      "note": "Owner: prioritize the Audacity-like UI (Phase 5) first so the remaining spectrum-render issue can be described in detail afterwards. SF2.1 left in-progress, SF3.1 deferred; proceeding with Phase 5."
    },
    {
      "ts": "2026-07-01T22:10:00Z",
      "step": "SF5.1",
      "event": "done",
      "note": "SpectrogramView takes a height prop (fixed track height); AudioPage sets default 260px per track (items-start row). Reused by SF5.2/SF5.3. vue-tsc clean."
    },
    {
      "ts": "2026-07-01T22:30:00Z",
      "step": "SF5.2",
      "event": "done",
      "note": "Bottom-edge drag handle in SpectrogramView resizes the track (pointer-capture, clamp 120-1200) via v-model:height; AudioPage persists the height to localStorage (loaded on init). i18n added. vue-tsc clean; bun 61/0."
    },
    {
      "ts": "2026-07-01T23:00:00Z",
      "step": "SF5.3",
      "event": "done",
      "note": "Stereo L/R split. Enabling server change: SpectrogramSession now supports multiple concurrent analyses keyed by analysisId (was single-analysis; blocked two-channel view). UI: AudioPage stacks two per-channel SpectrogramViews (channel 0/1, labelled L/R) for stereo (numberOfChannels>=2), one pane for mono; shared v-model:height. server 17/0, ui 61/0, vue-tsc clean. Requires bun server restart."
    },
    {
      "ts": "2026-07-02T00:30:00Z",
      "step": "SF2.1",
      "event": "done",
      "note": "Only-last-frame render RESOLVED (tile-count-by-zoom + worker timeouts + worker multi-analysis + WS multicast); owner moved on to speed, so the frames render. Remaining SF2.1 concern = generation SPEED, re-scoped to Phase 6 (SF-D10)."
    },
    {
      "ts": "2026-07-02T01:20:00Z",
      "step": "SF6.1",
      "event": "profiled",
      "note": "Bottleneck found: open-analysis 22.2s = FLAC decode 3.4s + eager full-file STFT loop 18.8s (dominant). The loop computes phase+unwrapped-phase (350M ArcTan2 + unwrap) + 3x ~1.4GB matrices EVEN for data=magnitude (unused). JSON/WS transport + get-tile are fine (full view 2.3s / 1.6MB / parse 7ms). Proposed: (A) skip phase/uphase unless needed [quick win], (B) multithread the loop, (C) lazy per-visible STFT. Awaiting owner gate. Profiling timers reverted; worker rebuilt+rebundled clean."
    },
    {
      "ts": "2026-07-02T04:20:00Z",
      "step": "SF7",
      "event": "planned",
      "note": "Owner added Phase 7: SF7.1 immediate tracks + Audacity-style load-progress dialog overlay (SF-D12); SF7.2 track-height resize without full recompute (SF-D13, options a/b/c - owner to choose); SF7.3 cache spectrograms across file switches (SF-D14, LRU of lazy open analyses). Awaiting owner choice on SF-D13 then go."
    },
    {
      "ts": "2026-07-02T04:30:00Z",
      "step": "SF-D13",
      "event": "decided",
      "note": "Owner chose option (b): never recompute on resize -> fixed viewBinCount decoupled from track height; height resize scales the raster only (setView no-op guard skips refetch). SF7.2 unblocked."
    },
    {
      "ts": "2026-07-03T00:20:00Z",
      "step": "SF10.4",
      "event": "fixed",
      "note": "PARTIAL/WRONG first attempt: made SpectrogramMinimap a static import (still correct + kept), but that alone did NOT fix the blank tracks."
    },
    {
      "ts": "2026-07-04T13:45:00Z",
      "step": "SF12.1",
      "event": "done",
      "note": "Fixed the TSpecFft real-FFT N/2-k mirror bug. Root cause (vs vendored Audacity RealFFTf.cpp): C SinTable[*br1+1] (adjacent cos slot) mistranslated as Pascal FSinTable[br1[1]] (=FBitReversed[i+1], a different bin's twiddle), in 3 spots (RealFFTf massage cos, center-bin conjugate, InverseRealFFTf massage cos). Fixed to br1[0]+1 / br1^+1. Added dependency-free regression (tests/SpectrumCoreFftTests.pas + scripts/build_fft_tests_x64.bat): cosine/sine sweep every bin over N {256..4096} + impulse/DC/Nyquist/two-tone all clean (<0.1% of peak); exit 0. Independent of the FFTW spectrogram path -> no rebundle. Bug report marked FIXED. Phase 12 done."
    },
    {
      "ts": "2026-07-04T12:40:00Z",
      "step": "SF12.1",
      "event": "planned",
      "note": "Owner: add to the plan the TSpecFft real-FFT mirror bug (SF-D34, Phase 12). TSpecFft.RealFFTf+ReorderToFreq (SpectrumCoreFft.pas, Audacity-hfft-derived) give a spurious N/2-k mirror peak (up to ~80% of peak), folding the spectrum about N/4; also SpectrumCoreStft.pas. INDEPENDENT of the MindWave spectrogram (FFTW path, unaffected) - a distinct SpectrumCore bug found by SoundCore. Plan step SF12.1: root-cause vs Audacity original, fix, add the 10-case assertion suite across N {256..4096} (clean = non-signal bins <0.1% of peak). Not started - planning only per owner. Ref: SpectrumCore/TSPECFFT-MIRROR-BUG.md."
    },
    {
      "ts": "2026-07-04T12:10:00Z",
      "step": "SF11.9",
      "event": "done",
      "note": "Backlog B1 done (responsiveness variant; original fast->detailed superseded by SF11.5/11.6). Client-only cross-zoom tile fallback: assembleVisibleTiles falls back to a cached covering tile from another zoom (rendered stretched, coarse-under-exact) when the exact tile isn't cached -> zoom/pan shows instant coarse content (whole-clip overview / prev zoom) instead of blank, refined in ~100ms. SpectrogramTileCache.findBest + 2 tests. vue-tsc + ui 63/0. No worker/protocol change. Owner verify = gate. Phase 11 + backlog B1/B7 done."
    },
    {
      "ts": "2026-07-04T11:30:00Z",
      "step": "SF11.8",
      "event": "done",
      "note": "Backlog B7 done: the bottom minimap now renders a whole-clip spectrogram thumbnail behind the navigator. Own useSpectrogram, fixed whole-clip view at minimap-width zoom; AudioPage passes the primary track's analysis so the server reuses its warm analysis (no extra decode/analysis; overview tile cached) -> near-free. UI-only; vue-tsc + build + ui 61/0. B7 marked done in backlog. Owner verify = gate."
    },
    {
      "ts": "2026-07-04T10:45:00Z",
      "step": "SF11.7",
      "event": "done",
      "note": "Shared decode for stereo: 1-file decode cache inside AudioLoadChannel (sndfile path) - decode all channels once, hand each to its analysis, stash siblings transiently. ch1 open 2665ms -> 1ms; stereo first-load decode ~8.3s -> ~2.9s. Memory-safe (no extra steady-state; cache holds only not-yet-taken siblings for one file). Rebuilt+rebundled; bridge+session 31/0. B2 closed obsolete. Backend restart. Owner verify = gate. Phase 11 complete."
    },
    {
      "ts": "2026-07-04T10:00:00Z",
      "step": "B2/SF11.7",
      "event": "planned",
      "note": "End-to-end profile (real SpectrogramSession): after first load everything is fast (full-view tiles 1222ms/6 tiles cold, 0ms repeat via SF11.2, 0ms after file return via warm LRU); DOMINANT first-load cost = FLAC decode (open-analysis) ~5.6s ch0 + ~2.7s ch1 (stereo decodes the file TWICE). So B2 (persistent STFT cache) targets the wrong thing (STFT already fast+cached) -> CLOSED as obsolete (see backlog). Added SF11.7 (SF-D33): decode a file's PCM once, both channel analyses reuse it so the 2nd channel skips the decode. Owner: close B2, do SF11.7."
    },
    {
      "ts": "2026-07-04T09:00:00Z",
      "step": "SF11.4-11.6",
      "event": "verified",
      "note": "Owner verified after restart: 'стало быстро и красиво' (fast AND good-looking). Phase 11 speed/quality complete: SF11.4 display-resolution STFT + SF11.5 bounded-oversample max-pool (quality) + SF11.6 base64 float32 tile transport -> overview 3949ms -> 76ms (~52x) with restored quality. Spectrogram-fixes initiative core work done; remaining items are backlog (B1/B2 optional, B5/B7 minor)."
    },
    {
      "ts": "2026-07-03T08:30:00Z",
      "step": "SF11.6",
      "event": "done",
      "note": "Binary tile transport: tile bins now a base64 float32 blob (binsB64) instead of per-bin JSON. Worker EncodeStringBase64 (no FloatToStrF); server passes it through; client decodes with atob -> Float32Array (memoized) in tileToImage (frames[] fallback kept). Profiled overview zoom=10 272ms->76ms (JSON floor gone); payload smaller than JSON too. Cumulative Phase-11: overview 3949ms->76ms (~52x) at restored quality. Protocol binsB64 added; tests updated; vue-tsc + server 31/0 + ui 61/0. Rebuilt+rebundled. Backend restart. Owner verify = gate. Phase 11 core speed/quality work COMPLETE."
    },
    {
      "ts": "2026-07-03T07:30:00Z",
      "step": "SF11.5",
      "event": "done",
      "note": "Bounded-oversample max-pool restores overview quality. New EnsureColumnsMaxPooled max-pools K=min(Factor,32) RAW magnitude sub-columns per output column (in-place, magnitude-only), so the frequency interpolation runs once per output column (a naive pool-after-interpolation was 3-5x slower). Profiled overview zoom=10 272ms (vs 3949ms old full-res, ~14x) with peak brightness restored (max~0.10). Rebuilt+rebundled; bridge+session 31/0. Backend restart. Owner verify = gate. Next: SF11.6 binary transport (JSON is still the floor)."
    },
    {
      "ts": "2026-07-03T06:40:00Z",
      "step": "SF11.4",
      "event": "feedback",
      "note": "Owner verified: speedup MINIMAL and quality visually MUCH WORSE (Audacity is both faster and better). Root cause: SF11.4's one-FFT-per-column dropped the old max-pool (256:1 info loss) -> dim/sparse overview; and the perceived-speed win is masked because JSON emit (~500ms/tile) is now the floor (compute is negligible). Agreed plan: SF11.5 bounded-oversample max-pool (K=min(Factor,32), restore quality ~free since JSON-bound) -> verify -> SF11.6 binary tile transport (kill the JSON floor = real speed) -> verify."
    },
    {
      "ts": "2026-07-03T06:10:00Z",
      "step": "SF11.4",
      "event": "done",
      "note": "Audacity-style display-resolution STFT implemented + owner-gated go. Analysis: column-based parallel STFT with a stride param + EnsureColumns (decimated frames presented as contiguous, bias 0). Worker: zoom>0 lazy magnitude/phase modes compute one FFT per emitted column (Factor=1<<zoom) instead of full-res + max-pool pyramid; zoom0/pitch/reassignment unchanged. Profiled: overview zoom=10 cold 3949ms -> 545ms (~7x); JSON emit is now the floor (~500ms; backlog B4). Rebuilt + rebundled; bridge+session 31/0. Cursor readout unaffected. Backend restart. Owner UI verify = gate. Tradeoff: overviews approximate (skip samples between columns), like Audacity."
    },
    {
      "ts": "2026-07-03T04:35:00Z",
      "step": "backlog",
      "event": "parked",
      "note": "Owner: collect the set-aside Phase-11 speed ideas in the plan under 'Backlog - требует последующего обсуждения' so nothing is lost; none committed, each requires later discussion and may be dropped/reshaped after SF11.4 is verified. Items: B1 progressive two-level rendering (fast + idle-refine, refined pass = bounded oversample 4-8x not full-res, setting toggle) [owner's idea]; B2 bounded persistent per-frame STFT cache (Audacity SpecCache-style); B3 detail-level setting granularity; B4 binary tile transport; B5 readout<->pixel consistency on deep overviews; B6 tile-cache tuning after SF11.4; B7 minimap spectrogram thumbnail (SF10.4 follow-up); B8 worker robustness/hardening (GUI-subsystem dialogs / raw-pipe drive). See plan Backlog section."
    },
    {
      "ts": "2026-07-03T04:20:00Z",
      "step": "SF11.4",
      "event": "planned",
      "note": "Studied Audacity 3.7.8 SpectrumCache.cpp: SpecCache::Populate computes ONE windowSize FFT per DISPLAY column (CalculateOneSpectrum: from=where[0]+xx*samplesPerPixel), FFT count = display width, independent of clip length -> instant zoom. Designed SF11.4 (SF-D30): add a display-resolution STFT path to our worker so overview/zoom compute ~columns FFTs not full-res. Big SpectrumCore worker change -> APPROVAL GATE; presenting design, awaiting owner go."
    },
    {
      "ts": "2026-07-03T04:00:00Z",
      "step": "SF11.3",
      "event": "feedback",
      "note": "Owner verified after restart: STILL slow - file switching recomputes, ZOOM is slow (Audacity zoom is instant), initial spectrum display is slow. So SF11.2 (final-tile cache) only helped exact re-views; the dominant pain is COLD tile compute. Confirmed by profiling: an overview computes the FULL-resolution STFT (171144 FFTs) to emit ~168 columns. => SF-D29 step 2 is now active: study Audacity 3.7.8 to learn how it computes at display resolution (hop/samplesPerPixel scaling with zoom) so overview/zoom compute ~columns FFTs, not full-res. Added SF11.4."
    },
    {
      "ts": "2026-07-03T03:20:00Z",
      "step": "SF11.3",
      "event": "done",
      "note": "Profiled the get-tile path (code analysis + LIVE harness on 1_Orientation.flac). Measured: cold overview 3949ms (whole-file STFT); worker W3 'repeat' still 863ms (re-pools full-res every call); 120s window 668/455ms; 2s window 247/218ms. Applied win = SF11.2 server tile cache -> re-fetch skips the round-trip + the 863ms re-pool + JSON (instant). Cold first-gen = whole-file STFT (already A+B+C). Deeper win (SF-D29 step 2, owner-gated) = a bounded PERSISTENT per-frame STFT cache (like Audacity SpecCache) so overlapping pans/zoom levels don't recompute the same frames. Owner verifies SF11.2 first."
    },
    {
      "ts": "2026-07-03T02:40:00Z",
      "step": "SF11.2",
      "event": "done",
      "note": "Caching fix: root cause = warm-analysis LRU worked but Opt C lazy STFT recomputes tiles on every get-tile, so file-switch-back rebuilt tiles. Added a per-warm-analysis server-side computed-tile cache (keyed zoom|timeStart|timeEnd|viewBinCount, LRU cap 64) so re-fetch/return is instant. +mock unit test (worker computes once for identical requests). server 31/0. Backend restart. Owner verify = gate. Next: SF11.3 (profile tile-gen speed)."
    },
    {
      "ts": "2026-07-03T02:05:00Z",
      "step": "SF11.1",
      "event": "done",
      "note": "Localized all field + slider labels in SpectrogramSettingsPanel (18 new audio.spectrogram* keys, en/ru); sliders now a computed for reactive labels. Technical option values + preset names left untranslated by design. vue-tsc + bun 61/0. UI-only. Next: SF11.2 (caching fix)."
    },
    {
      "ts": "2026-07-03T01:45:00Z",
      "step": "SF11",
      "event": "planned",
      "note": "Owner added Phase 11: SF11.1 i18n for the Параметры panel (SF-D27); SF11.2 fix spectrogram caching across file switches - A->B->A recomputes despite SF7.3 warm LRU (SF-D28); SF11.3 profile + speed up tile generation, then owner verifies, then (if needed) study Audacity 3.7.8 sources (SF-D29). Executing next."
    },
    {
      "ts": "2026-07-03T01:30:00Z",
      "step": "SF3.1",
      "event": "done",
      "note": "Settings panel converted to a toggleable overlay 'Параметры' (SF-D4/D5): removed the fixed 264px side column (no more plot shrink); dark absolute <aside> + backdrop + slide/fade transitions over the spectrogram section; toggle 'tune' button in the common header (aria-expanded/controls), close via button/backdrop/Escape; default closed. i18n en/ru added. vue-tsc + prod build + bun 61/0. UI-only. Last remaining planned step done -> the spectrogram-fixes plan is functionally complete (optional follow-up: minimap spectrogram thumbnail)."
    },
    {
      "ts": "2026-07-03T01:00:00Z",
      "step": "SF9-10",
      "event": "verified",
      "note": "Owner confirmed the spectrogram tracks render again after the static-SpectrogramView fix ('Исправлено!'). Phase 9 (square corners + per-track resize model) and Phase 10 (header/labels/axes/bottom minimap + cursor tooltip) are visually working. Proceeding to SF3.1 (deferred Параметры overlay)."
    },
    {
      "ts": "2026-07-03T00:45:00Z",
      "step": "SF9.2",
      "event": "fixed",
      "note": "ROOT CAUSE of the blank spectrogram tracks (regression from SF9.2's v-for). Console showed '[Vue warn] Invalid vnode type when creating vnode: null' in the spectrogram QTabPanel; a temp diagnostic proved spectrogramTracks=2 and heights=[338,338] with NO thrown error -> the tracks were in the vdom but never resolved. Cause: SpectrogramView is an ASYNC component (createAsyncAudioPanel, delay:120) that returns a NULL vnode during its loading-delay phase. In Phase 8 the tracks sat in static v-if/v-else positions (fine). SF9.2 moved them into a keyed <template v-for> -> an async component that yields null as a keyed v-for child breaks the fragment reconciliation, so the tracks never advance past null (settings panel is async too but NOT in a v-for, so it resolved fine -> the earlier 'harmless warn' read was wrong). Fix: import SpectrogramView STATICALLY (no null loading phase). vue-tsc + prod build + bun 61/0. Removed the temp diagnostic (onErrorCaptured/watchEffect)."
    },
    {
      "ts": "2026-07-02T09:50:00Z",
      "step": "SF10.4",
      "event": "done",
      "note": "Bottom minimap-overview: new SpectrogramMinimap.vue (canvas overview + draggable/resizable visible-window handle; body=pan, edges=zoom, outside=recenter) at the very bottom of the stack, driving the shared view (SF-D24). Readout moved to a cursor tooltip on the plot; SpectrogramView's q-range/readout toolbar fully removed (SF-D25). i18n spectrogramTimespan added. vue-tsc + bun 61/0. Phase 10 (chrome layout) COMPLETE. UI-only. PAUSED for owner UI verification. NOTE: minimap has no rendered spectrogram thumbnail yet (plain overview track) - possible follow-up."
    },
    {
      "ts": "2026-07-02T09:15:00Z",
      "step": "SF10.3",
      "event": "done",
      "note": "Time ruler now only above the first track and below the last (dynamic top/bottom margins; middle tracks none); frequency axis stays per-track. AudioPage passes first/last flags by index. vue-tsc clean. UI-only. Next: SF10.4 (bottom minimap-overview + cursor-tooltip readout), the big one."
    },
    {
      "ts": "2026-07-02T08:55:00Z",
      "step": "SF10.2",
      "event": "done",
      "note": "Channel labels (L/R) now render as a top-left plot overlay for every track (same place); removed the toolbar label. vue-tsc clean. UI-only. Next: SF10.3 (time axis top-of-first + bottom-of-last)."
    },
    {
      "ts": "2026-07-02T08:45:00Z",
      "step": "SF10.1",
      "event": "done",
      "note": "Common header above the stack (AudioPage) holds the zoom/fit buttons once, driving the shared view directly; removed those buttons (and dead zoom/fit helpers) from SpectrogramView. vue-tsc clean. UI-only. Next: SF10.2 (L/R labels as top-left overlays)."
    },
    {
      "ts": "2026-07-02T08:25:00Z",
      "step": "SF9.3",
      "event": "done",
      "note": "Bottom handle (below the last track) resizes ALL tracks by the same delta, clamped so none leaves [120,1200] (SF-D20). Resize handling now fully in the AudioPage stack (divider=mutual, bottom=uniform). Phase 9 (square corners + resize model) COMPLETE. vue-tsc + bun 61/0. UI-only. PAUSED for owner UI verification; next Phase 10 (SF10.1-10.4)."
    },
    {
      "ts": "2026-07-02T08:10:00Z",
      "step": "SF9.2",
      "event": "done",
      "note": "Per-track heights: AudioPage now owns spectrogramTrackHeights[] (persisted JSON), synced to the track count; a 2px divider between adjacent tracks does mutual resize (one grows / the other shrinks, total fixed). SpectrogramView's own SF5.2 resize handle retired (height is a plain prop now). vue-tsc + bun 61/0. UI-only. Next: SF9.3 (bottom handle = uniform resize of all tracks)."
    },
    {
      "ts": "2026-07-02T07:52:00Z",
      "step": "SF9.1",
      "event": "done",
      "note": "Removed border-radius:12px from .spectrogram-view -> square track corners (Audacity-like). vue-tsc clean. UI-only. Next: SF9.2 (per-track heights + 2px mutual-resize divider)."
    },
    {
      "ts": "2026-07-02T07:42:00Z",
      "step": "SF10.5",
      "event": "done",
      "note": "Owner: removed the 'Спектрограмма' subtitle (audio.spectrogramTitle) shown under the Старт/transport toolbar in the spectrogram tab. vue-tsc clean. UI-only (HMR). Remaining Phase 10: SF10.1-10.4 (header/labels/axes/bottom minimap) pending owner 'go'."
    },
    {
      "ts": "2026-07-02T07:35:00Z",
      "step": "SF10.4",
      "event": "decided",
      "note": "Owner answered the Phase 10 questions: (SF-D24) bottom selector = FULL minimap-overview like the player (canvas + draggable window), not a relocated q-range slider; (SF-D25) readout = tooltip at the cursor, not in the header/bottom bar (header holds only buttons); (SF-D23) time ruler both above the first and below the last track. SF10.4 unblocked."
    },
    {
      "ts": "2026-07-02T07:25:00Z",
      "step": "SF10",
      "event": "planned",
      "note": "Owner added Phase 10 (track chrome layout): SF10.1 common header above the stack with all buttons (SF-D21); SF10.2 L/R labels as matching top-left overlays (SF-D22); SF10.3 time axis only top-of-first + bottom-of-last (SF-D23); SF10.4 fixed bottom timespan selector player-minimap style (SF-D24, blocked on owner: minimap vs relocated slider + readout placement)."
    },
    {
      "ts": "2026-07-02T07:15:00Z",
      "step": "SF9",
      "event": "planned",
      "note": "Owner added Phase 9 (track resize model): SF9.1 remove track border-radius (SF-D18); SF9.2 per-track heights + 2px mutual-resize divider between adjacent tracks (SF-D19; owner text 'обе уменьшаются' read as one-up/one-down - to confirm); SF9.3 bottom handle uniform-resizes all tracks (SF-D20), moving resize to the AudioPage stack + retiring the per-view SF5.2 handle."
    },
    {
      "ts": "2026-07-02T07:00:00Z",
      "step": "SF8.1",
      "event": "done",
      "note": "Audacity-style multi-track chrome: stacked stereo tracks share one time window + area selection (provide/inject spectrogramShared); toolbar/icons/readout on the first (primary) track only, non-primary shows just a label overlay; per-track resize handle; inter-track gap 8->1px. Shared zoom/pan/select across tracks; area-query readout on primary via commitSeq. SpectrogramSelection type added. vue-tsc + bun 61/0. UI-only. PAUSED for owner verification. Phase 8 complete pending sign-off (SF3.1 still deferred)."
    },
    {
      "ts": "2026-07-02T06:20:00Z",
      "step": "SF7.3",
      "event": "done",
      "note": "Spectrogram cache across file switches: SpectrogramSession keeps analyses warm as an LRU (file+params key); reopen reuses -> skips re-decode. onClose keeps warm; eviction on open past caps (MAX 4 / 1.5GB samples). Contract test updated (close keeps warm) + reuse test added; server 18/0. Backend restart (no re-bundle). PAUSED for owner UI verification. Remaining: SF8.1 (multi-track chrome, big)."
    },
    {
      "ts": "2026-07-02T05:55:00Z",
      "step": "SF8.2",
      "event": "done",
      "note": "SF8.3: dropped immediate from the ensureSpectrogramPrepared watch -> opening the Audio tab with a restored selection no longer auto-builds; builds only on file click / switch to Спектрограмма. SF8.2: frequency-axis tick count now scales with plot height (~1 per 42px, clamp 3..20) -> more levels on taller tracks (Audacity-like). vue-tsc + bun 61/0. UI-only (HMR). Remaining Phase 8: SF8.1 (multi-track chrome, big). Also pending: SF7.3 (backend cache)."
    },
    {
      "ts": "2026-07-02T05:40:00Z",
      "step": "SF-D17",
      "event": "decided",
      "note": "Owner chose option (a): build only on explicit action (file click / switch to Спектрограмма sub-tab), not on mount for a restored selection -> drop immediate:true from the SF4.1 watch. SF8.3 unblocked."
    },
    {
      "ts": "2026-07-02T05:30:00Z",
      "step": "SF8",
      "event": "planned",
      "note": "Owner added Phase 8: SF8.1 Audacity-style multi-track chrome (icons on first track, per-track resize, unified span selector, no inter-track gap; SF-D15); SF8.2 adaptive frequency-axis tick density with height (SF-D16); SF8.3 don't auto-build on Audio-tab open (SF-D17, options a/b/c - owner to choose)."
    },
    {
      "ts": "2026-07-02T05:15:00Z",
      "step": "SF7.2",
      "event": "done",
      "note": "SF7.1: full-panel loading placeholder replaced by a small persistent progress dialog over the tracks screen (Audacity-style). SF7.2: viewBinCount fixed (MAX_VIEW_BINS) -> height resize no longer recomputes (view unchanged, setView guard skips; raster scales). vue-tsc clean; ui 61/0. UI-only (HMR). Next: SF7.3 (cache across file switches, backend)."
    },
    {
      "ts": "2026-07-02T04:45:00Z",
      "step": "SF6.4",
      "event": "verified",
      "note": "Owner verified SF6.4 in the UI: no errors (OOM gone). Phase 6 (performance) COMPLETE: SF6.1 profile, SF6.2 (A skip phase), SF6.3 (B multithread), SF6.4 (C lazy STFT). open-analysis 22.2s -> 2.6s and no persistent matrix / no OOM."
    },
    {
      "ts": "2026-07-02T04:00:00Z",
      "step": "SF6.4",
      "event": "done",
      "note": "Optimization C (lazy STFT) implemented -> fixes the 'open-analysis Out of memory'. No persistent full-file matrix: keep samples, compute the requested frame range on demand (parallel) into a transient buffer, biased FMagnitudeData maps onto it so readers are unchanged; reassignment stays eager. open-analysis 2.6s (decode-only); full-view tiles ~2.7s; payloads byte-identical; stereo opens with no matrix (no OOM). bridge+session 17/0. Rebuilt+rebundled. PAUSED for owner UI verification. Phase 6 (A+B+C) complete pending owner sign-off."
    },
    {
      "ts": "2026-07-02T02:40:00Z",
      "step": "SF6.3",
      "event": "done",
      "note": "Optimization B: parallelized the standard STFT frame loop across TThread.ProcessorCount workers (per-thread FFTW plan/buffers; reassignment stays serial). open-analysis 6.2s -> ~3.06s on 1_Orientation.flac (16 CPUs); cumulative A+B 22.2s -> 3.06s (~7x); now decode-bound. Tile payloads byte-identical. bridge+session 17/0. Rebuilt+rebundled. PAUSED for owner UI verification. Next: C (lazy per-visible STFT) would cut the remaining ~3s decode/eager work."
    },
    {
      "ts": "2026-07-02T02:00:00Z",
      "step": "SF6.2",
      "event": "done",
      "note": "Optimization A: skip phase/unwrapped-phase compute+store unless data mode needs them (NeedPhase). open-analysis 22.2s -> 6.2s (~3.6x) on 1_Orientation.flac; magnitude tiles byte-identical; phase mode still works; magnitude point-query phase=0 (acceptable). Nil-guarded PhaseAt/UnwrappedPhaseAt. bridge+session 17/0. Rebuilt+rebundled. PAUSED for owner UI verification. Next candidates: B (multithread) then C (lazy)."
    },
    {
      "ts": "2026-07-02T01:25:00Z",
      "step": "SF6.1",
      "event": "note",
      "note": "Owner saw an error popup 'EInOutError: File not open' during profiling — caused by my temporary StdErr timers in the GUI-subsystem worker (no console). Reverted the timers, rebuilt + re-bundled the clean worker, killed stray worker processes. LATENT ISSUE noted: the worker is a GUI app that shows a message box on unhandled exceptions (e.g. a broken stdio pipe on kill/timeout) — worth a hardening follow-up (global handler / console subsystem)."
    },
    {
      "ts": "2026-07-02T00:30:00Z",
      "step": "SF6.1",
      "event": "planned",
      "note": "Owner: main problem is generation SPEED (Audacity much faster). Suspects: single-threaded worker FFT, JSON-not-binary tile transport, repeated per-request FLAC decode. Work via plan+ledger; profile the pipeline on d:\\bin\\Presets\\1_Orientation.flac first, then optimize at a gate."
    },
    {
      "ts": "2026-07-02T00:20:00Z",
      "step": "SF5.4",
      "event": "done",
      "note": "Set default spectrogram settings to match Audacity (owner screenshot): log scale 80-4000 Hz, 0 dB gain, 120 dB range, HF boost 0, Frequencies/magnitude, window 2048 Hann, zero-pad 2; palette rainbow (closest coloured to Audacity 'Color pink'). audacity preset aligned. vue-tsc + bun 61/0. NB: existing saved settings override defaults (Reset/preset to apply). Next: SF2.1."
    },
    {
      "ts": "2026-07-02T00:05:00Z",
      "step": "SF5.3",
      "event": "fixed",
      "note": "Owner: left channel loaded but the RIGHT channel spun forever (no data, no error). Root cause: ws.ts dispatch() stops at the first handler returning true, and every spectrogram composable's onSpectrogram handler returned true -> the first-registered (left) view consumed ALL spectrogram messages (filtering by requestId), so the second (right) view's handler was never called and its open/tile responses never arrived. Fix: multicast spectrogram messages to a dedicated Set of subscribers (all views receive every spectrogram message); regular ServerEvents keep single-consumer dispatch. UI-only change (HMR / page reload; no backend restart). vue-tsc clean; ui 61/0."
    },
    {
      "ts": "2026-07-01T23:40:00Z",
      "step": "SF5.3",
      "event": "fixed",
      "note": "Owner error on the left channel: 'get-tile failed: get-tile analysisId mismatch'. The WORKER (SpectrumCoreFftwWorkerProbe) was single-analysis too (one global Current; EnsureCurrent erroring on a different id) — the server multi-analysis change alone wasn't enough. Made the worker keep a registry of analyses; EnsureCurrent(id) now SELECTS the analysis and restores its TileCacheAnalysisToken (which keys both tile caches, so caching stays correct per analysis); open registers instead of freeing the previous; close unregisters; shutdown frees all. Rebuilt the probe + re-bundled into MindWave (manifest.json updated; exe gitignored). Verified via direct stdio probe: open AAA + BBB, get-tile AAA -> ok (frames=33). Bridge integration test 15/0. Requires bun server restart to pick up the new bundled worker."
    },
    {
      "ts": "2026-07-05T00:00:00Z",
      "step": "SF16.0",
      "event": "planned",
      "note": "Owner addition (2026-07-05): Phase 16 = Audacity parity round 2. 6 items -> SF16.1..16.6 (SF-D44..D49). SF16.1 (numeric spinbox dials) and SF16.3 (zoom popover x1..x16 + %) carry OPEN questions (default-0 gain semantics; x1 fit-vs-1px/sample). Proposed order SF16.2->16.5->16.6->16.1->16.4->16.3. NOT approved yet; awaiting go + answers to the two open questions."
    },
    {
      "ts": "2026-07-05T00:40:00Z",
      "step": "SF16.2",
      "event": "done",
      "note": "Four Audacity palettes (roseus/classic/grayscale/invgrayscale); default=roseus; legacy intensity->grayscale, rainbow->classic migration; i18n labels Розовый/Классика/Оттенки серого/Инверсия. Client render only. GnauralCore a7990bd."
    },
    {
      "ts": "2026-07-05T01:10:00Z",
      "step": "SF16.1",
      "event": "done",
      "note": "5 color-group dials -> numeric spinbox (-/+ steppers, clamped). Gain switched to dB (0 dB neutral, mult=10^(dB/20)); default gain 0. GnauralCore b3e532c."
    },
    {
      "ts": "2026-07-05T01:40:00Z",
      "step": "SF16.5",
      "event": "done",
      "note": "Diagnosed: our-vs-Audacity difference is configuration not a pipeline bug. Palette (SF16.2), Gain 0->20dB & Range 120->80dB (Audacity punchy contrast), fscale log->mel, freq 80-4000->0-20000. Window/zeropad/winfunc already matched; SF11.5 max-pool preserves peaks (not the cause). Fixed the Audacity preset to true Audacity defaults. Open: adopt as global defaults? GnauralCore 72607a4."
    },
    {
      "ts": "2026-07-05T02:05:00Z",
      "step": "SF16.6",
      "event": "done",
      "note": "Audacity-style time ruler: adaptive major labels (~1/90px) + short minor ticks subdividing each major interval, top+bottom. Draw-layer only. GnauralCore 7e76b0c."
    },
    {
      "ts": "2026-07-05T02:25:00Z",
      "step": "SF16.4",
      "event": "done",
      "note": "Keyboard nav: canvas focusable; Home/End scroll to clip start/end (keep span), Left/Right pan 15%, Shift+arrow full page. GnauralCore 6eef7db."
    },
    {
      "ts": "2026-07-05T02:55:00Z",
      "step": "SF16.3",
      "event": "done",
      "note": "Right-click zoom popover: presets x1 x2 x3 x4 x8 x16 + live % field (shows+sets zoom). x1=whole clip. Right/middle mouse no longer starts drag selection. GnauralCore c1bcb3d. Phase 16 complete."
    },
    {
      "ts": "2026-07-05T03:05:00Z",
      "step": "SF16.5",
      "event": "amended",
      "note": "Owner decision: adopt Audacity defaults app-wide. DEFAULT_SPECTROGRAM_SETTINGS now = Audacity defaults (Mel 0-20kHz, Gain +20dB, Range 80dB, log, Roseus); ffmpeg preset pins its own neutral character. GnauralCore d03d5de."
    },
    {
      "ts": "2026-07-05T03:30:00Z",
      "step": "SF17.0",
      "event": "planned",
      "note": "Owner addition (2026-07-05) after Phase 16 shipped: Phase 17 = zoom popover polish (SF17.1, SF-D50) + high-zoom sharpness (SF17.2, SF-D51, discuss-first). Not approved; awaiting go. SF17.2 needs an approach decision (nearest-neighbour vs high-zoom analysis profile vs reassignment)."
    },
    {
      "ts": "2026-07-05T03:45:00Z",
      "step": "SF17.0",
      "event": "scoped",
      "note": "Owner: implement ALL sharpness variants + UI config. Split SF17.2 into SF17.2 (imageScaling smooth/sharp, render-only) + SF17.3 (high-zoom analysis profile: zoom-reactive window/reassign above threshold). New «Резкость» settings group. Awaiting go."
    },
    {
      "ts": "2026-07-05T04:15:00Z",
      "step": "SF17.1",
      "event": "done",
      "note": "Zoom popover: header with close ✕, Esc closes, % field autofocus, Применить now a full-width primary button. GnauralCore 0a29e50."
    },
    {
      "ts": "2026-07-05T04:45:00Z",
      "step": "SF17.2",
      "event": "done",
      "note": "imageScaling (smooth=bilinear|sharp=nearest) render-only setting in new Резкость group; draw() reads it. GnauralCore f7b4b0d."
    },
    {
      "ts": "2026-07-05T05:25:00Z",
      "step": "SF17.3",
      "event": "done",
      "note": "High-zoom analysis profile: AudioPage derives zoom factor from shared view+duration; above threshold (0.75x hysteresis) overrides window->highZoomWindow (re-derived hop) or data->reassign. Applies to mono+L/R. Phase 17 complete. PAUSE for owner UI verification. GnauralCore 7b690c3."
    },
    {
      "ts": "2026-07-05T05:45:00Z",
      "step": "SF17.1",
      "event": "fixed",
      "note": "Owner UI verify bug 1: Esc closed the whole view because the popover used @keyup.esc (Escape keydown still reached the global window keydown handler + enclosing q-dialog). Fixed with @keydown.esc.stop.prevent. GnauralCore a0302c2. Bug 2 (normal-zoom softness) under investigation: default path unchanged, effect tied to high-zoom re-analysis mode; asked owner for a decisive test."
    },
    {
      "ts": "2026-07-05T06:10:00Z",
      "step": "SF17.4",
      "event": "planned",
      "note": "Owner picked variant C. Verified root cause: reconfigure=close+reopen whole-track; reassign eager (FLazy=false) whole-track. Design SF-D52: per-tile windowOverload via lazy sibling analysis (reuses WriteTileResponse). Steps SF17.4a worker, b protocol/server, c client. Scope: smallWindow via C; reassign stays whole-track."
    },
    {
      "ts": "2026-07-05T07:10:00Z",
      "step": "SF17.4a",
      "event": "done",
      "note": "Worker: get-tile windowOverride via lazy sibling analysis; built+bundled+probe-verified."
    },
    {
      "ts": "2026-07-05T07:10:00Z",
      "step": "SF17.4b",
      "event": "done",
      "note": "Protocol+server thread windowOverride through get-tile."
    },
    {
      "ts": "2026-07-05T07:10:00Z",
      "step": "SF17.4c",
      "event": "done",
      "note": "Client: smallWindow high-zoom via per-tile windowOverride through view->cache key->get-tile; base analysis untouched."
    },
    {
      "ts": "2026-07-05T07:12:00Z",
      "step": "SF17.4",
      "event": "done",
      "note": "Variant C complete: per-tile windowOverride via lazy sibling analysis. smallWindow high-zoom no longer reconfigures/recomputes the whole track — only visible tiles. PAUSE for owner UI verification."
    },
    {
      "ts": "2026-07-05T07:40:00Z",
      "step": "SF17.4a",
      "event": "fixed",
      "note": "Owner: high zoom (x16) very slow again. Cause: sibling hop=window/4 gave ~4x base frames -> 4x FFTs/tile. Fix: sibling inherits base hop (clamped to window) -> same column/FFT count as base, cheaper smaller FFTs. Probe: override tile 108 cols == base. SpectrumCore a5300cf; rebundled."
    },
    {
      "ts": "2026-07-05T08:00:00Z",
      "step": "SF18.0",
      "event": "planned",
      "note": "Owner picked full preset manager. SF-D53: user presets (localStorage) + active/modified + apply/saveAs/rename/delete/duplicate/export/import; dropdown + manager dialog. Client-only. Implementing now."
    },
    {
      "ts": "2026-07-05T08:55:00Z",
      "step": "SF18.1",
      "event": "done",
      "note": "Store+model: user presets, active/modified, actions."
    },
    {
      "ts": "2026-07-05T08:55:00Z",
      "step": "SF18.2",
      "event": "done",
      "note": "UI: preset dropdown + manager dialog; bilingual."
    },
    {
      "ts": "2026-07-05T08:56:00Z",
      "step": "SF18",
      "event": "done",
      "note": "Full preset manager complete. PAUSE for owner UI verification."
    },
    {
      "ts": "2026-07-05T09:10:00Z",
      "step": "SF19.0",
      "event": "planned",
      "note": "Owner addition: Phase 19 = arrows->playhead + Alt->pan (SF19.1/D54), idle debounce for fast scroll/zoom (SF19.2/D55), seamless zoom no preparing-overlay/dim + background swap (SF19.3/D56, main). Not approved; awaiting go + open-question answers (step size, Home/End, debounce value, background indicator)."
    },
    {
      "ts": "2026-07-05T09:20:00Z",
      "step": "SF19.0",
      "event": "scoped",
      "note": "Owner answers: arrows ±1s (Shift ±5s), Alt=pan, Home/End keep view-edges; debounce ~200ms; SF19.3 tiny corner spinner (no dim/text) on view-change refetch. Decisions finalized; awaiting go."
    },
    {
      "ts": "2026-07-05T10:05:00Z",
      "step": "SF19.1",
      "event": "done",
      "note": "Arrows move playhead (seek); Alt+arrows pan; view follows playhead."
    },
    {
      "ts": "2026-07-05T10:05:00Z",
      "step": "SF19.2",
      "event": "done",
      "note": "Idle debounce raised to 200ms for fast scroll/zoom."
    },
    {
      "ts": "2026-07-05T10:05:00Z",
      "step": "SF19.3",
      "event": "done",
      "note": "Seamless zoom: no dim/overlay on view-change refetch; tiny corner spinner; background swap."
    },
    {
      "ts": "2026-07-05T10:06:00Z",
      "step": "SF19",
      "event": "done",
      "note": "Phase 19 complete. PAUSE for owner UI verification (esp. SF19.3 seamless zoom)."
    },
    {
      "ts": "2026-07-05T10:30:00Z",
      "step": "SF20.0",
      "event": "planned",
      "note": "Owner: Audio tab must not auto-load the last file; show 5 recent files for quick access. SF-D57. Implementing."
    },
    {
      "ts": "2026-07-05T10:35:00Z",
      "step": "SF20.0",
      "event": "planned",
      "note": "Owner also added SF20.3 (SF-D58): sub-second time-axis labels at high zoom (comma fraction when duplicate second labels). Phase 20 = SF20.1 recent-files store, SF20.2 recent-files UI, SF20.3 tick labels. Awaiting go."
    },
    {
      "ts": "2026-07-06T00:40:00Z",
      "step": "SF20.1",
      "event": "done",
      "note": "Store: recent files + stop auto-load."
    },
    {
      "ts": "2026-07-06T00:40:00Z",
      "step": "SF20.2",
      "event": "done",
      "note": "UI: recent-files dropdown in toolbar."
    },
    {
      "ts": "2026-07-06T00:40:00Z",
      "step": "SF20.3",
      "event": "done",
      "note": "Sub-second tick labels + comma separator."
    },
    {
      "ts": "2026-07-06T00:41:00Z",
      "step": "SF20",
      "event": "done",
      "note": "Phase 20 complete (recent-files dropdown + no auto-load + sub-second comma tick labels). PAUSE for owner UI verification."
    }
  ]
}