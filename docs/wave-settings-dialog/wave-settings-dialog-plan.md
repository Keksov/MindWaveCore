# wave-settings-dialog — шестерёнка в тайтл-бар «Общей волны» + двухпанельный диалог настроек

**Owner request (2026-07-15):** «Перенос иконки-шестерёнки из области графики дорожек общей волны в
тайтл бар этого графика. В новом диалоге, который будет открываться при клике на эту кнопку должно
быть две панели: узкая слева и широкая по центру. В левой панели отображается список из трёх
элементов-закладок "Обе канала", "Левый", "Правый". В центральной части находятся настройки для
отображения волны. Настройки применябтся в зависимости от текущей активной вкладки слева.»
*(цитата дословная, с опечатками исходника: «Обе канала», «применябтся»)*

Ledger (authoritative): [wave-settings-dialog-progress.json](wave-settings-dialog-progress.json).
Методология Plan+Ledger — как в [tooltip-placement](../tooltip-placement/tooltip-placement-plan.md) /
[menu-redesign](../menu-redesign/menu-redesign-plan.md): atomic per-step commits (префикс id шага),
`verify` перед `done`, пауза на owner-чекпоинтах. Правка целиком в **GnauralCore** (докам — коммиты
в MindWaveCore); коммитим по репозиториям раздельно, стейджим только свои файлы.

## 1. Требования владельца (дословно → нумерованно)

1. Иконка-шестерёнка **переносится из области графики** дорожек общей волны **в тайтл-бар** этого
   графика.
2. Клик по этой кнопке открывает **новый диалог** из **двух панелей**: **узкая слева**, **широкая
   по центру**.
3. В левой панели — **список из трёх элементов-закладок**: «Обе канала» *(грамматически — «Оба
   канала», см. R6)*, «Левый», «Правый».
4. В центральной части — **настройки для отображения волны**.
5. Настройки **применяются в зависимости от текущей активной вкладки** слева.

**Owner answers (2026-07-15, на вопросы WS1.3):**

6. «На форме "Оба канала" нужен **тоггл - Применить к обоим каналам**. Если он включен, то пункты
   слева "Левый" и "Правый" **становятся недоступны**.» *(заменяет прежний вопрос про индикацию
   расхождения L/R)*
7. «**Моно-файл - один пукт "Моно"**» *(→ WS-D4)*
8. «**Оба канала**» — формулировка подтверждена, R6 закрыт.

**Owner review at WS1.3 (2026-07-15): «В целом, всё хорошо, но есть ряд замечаний»** *(дефолт
тоггла ON подтверждён отдельно: «Согласен дефолт тоггла ON»)*

9. «Шестерёнку нужно **отодвинуть от правого края**, визуально она должны быть на **таком же
   расстоянии** от него, как была до этого **на графике**» *(→ WS1.4)*
10. «**Диалог колорпикера должен иметь Close иконку в заголовке**» *(→ WS-D10)*
11. «При переключении тоггла **в ON сразу нужно применять** текущие настройки **к обоим каналам**»
    *(→ WS-D3 rev.)*
12. «Настройки на вкладке "Оба канала" **не должны изменять** настроек на вкладках левого и правого
    каналов. Это **три независимых сущности**» *(→ WS-D9; отменяет прежнюю семантику «"Оба канала"
    пишет в оба канала»)*

## 2. Что уже есть (verified 2026-07-15)

### 2.1. Шестерёнка сегодня — per-track, внутри области графика (SF27)

[WaveformView.vue:35-45](../../../GnauralCore/ui/components/WaveformView.vue#L35-L45) — кнопка
`icon="settings"` в правом-верхнем углу **каждой** волновой дорожки (комментарий «SF27: per-track
colour/scale gear»), CSS [:569-577](../../../GnauralCore/ui/components/WaveformView.vue#L569-L577).
Она только эмитит `open-settings`
([:121](../../../GnauralCore/ui/components/WaveformView.vue#L121)); хост решает, что открыть.

В overall-стеке «Общая волна» ([TracksPanel.vue:382-405](../../../GnauralCore/ui/components/TracksPanel.vue#L382-L405))
на каждый канал L/R приходится своя дорожка `waveform-view`, и её гир открывает диалог **своего**
канала: `@open-settings="openWaveformSettings(wtrack.channel)"`
([:402](../../../GnauralCore/ui/components/TracksPanel.vue#L402)). Т.е. сегодня — **две** шестерёнки
(по числу каналов) в области графики; req 1 заменяет их **одной** в тайтл-баре.

### 2.2. Тайтл-бар «Общая волна» (GT11.10) — куда переезжаем

[TracksPanel.vue:348-380](../../../GnauralCore/ui/components/TracksPanel.vue#L348-L380) — fold-хедер
overall-стека волны: цветовая полоска (`wfColor(0)`), кнопка сворачивания, глаз скрытых графиков
(GT11.11, условный), заголовок `t('audio.tracksOverallWave')` («Общая волна»). Заголовок имеет
`flex: 1 1 auto` (CSS [:2942-2950](../../../GnauralCore/ui/components/TracksPanel.vue#L2942-L2950)) —
кнопка, добавленная **после** него, автоматически прижмётся к правому краю. Высота хедера 22px,
кнопки `size="xs"` там уже живут (fold, eye) — гир того же размера влезает без правок высоты.

Хедер рендерится только при `showWaveform && hasSpectrogramData && overallMixActive &&
!overallWaveHidden` ([:347](../../../GnauralCore/ui/components/TracksPanel.vue#L347)) — т.е. в
режимах `waveform`/`both`; в `overlay` его нет (см. 2.6).

### 2.3. Текущий диалог SF27 — что заменяем

[TracksPanel.vue:702-733](../../../GnauralCore/ui/components/TracksPanel.vue#L702-L733) — маленький
одностолбцовый `q-dialog`: заголовок `t('audio.waveformStyle')` + метка канала «L/R», и три
настройки:

| Настройка | Контрол | Область действия |
|---|---|---|
| Цвет | `<input type="color">` | канал |
| Шкала амплитуды | `q-btn-toggle` lin / dB | канал |
| Прозрачность наложения | `q-slider` 0.1–1 | канал; виден **только** при `viewMode === 'overlay'` |

Состояние: `waveformSettingsChannel: number | null` (null = закрыт)
[:1850-1860](../../../GnauralCore/ui/components/TracksPanel.vue#L1850-L1860); `wfDlgScale/Color/Opacity`
— computed get/set по каналу [:1861-1888](../../../GnauralCore/ui/components/TracksPanel.vue#L1861-L1888).

### 2.4. Значения per-channel + персистентность — НЕ меняются

[:1820-1849](../../../GnauralCore/ui/components/TracksPanel.vue#L1820-L1849) — массивы
`waveformScales/Colors/Opacities` (индекс = канал; дефолты L cyan `#67e8f9`, R amber `#fbbf24`),
сидируются из localStorage `mindwave-tracks-waveform`
([:1794](../../../GnauralCore/ui/components/TracksPanel.vue#L1794)) и пишутся обратно watch'ем
[:2215-2226](../../../GnauralCore/ui/components/TracksPanel.vue#L2215-L2226). Применяются к дорожкам
через props `:scale/:color` ([:391-392](../../../GnauralCore/ui/components/TracksPanel.vue#L391-L392))
и к overlay-отрисовке на спектрограмме `:waveform-*`
([:478-482](../../../GnauralCore/ui/components/TracksPanel.vue#L478-L482)). Стерео-признак —
`isSpectrogramStereo` ([:1787](../../../GnauralCore/ui/components/TracksPanel.vue#L1787)); дорожек
в стеке 1 (моно) или 2 ([:2240-2248](../../../GnauralCore/ui/components/TracksPanel.vue#L2240-L2248)).

### 2.5. Прецедент двухпанельного диалога — GT10.29 (образец для req 2/3)

[AudioPage.vue:49-86](../../../GnauralCore/ui/pages/AudioPage.vue#L49-L86) — диалог настроек
приложения ровно требуемой формы: `q-card` → `row no-wrap` → **узкий `q-list` слева** (200px,
`clickable` пункты с `active-class`) + `q-separator vertical` + **широкий контент справа**. CSS
[:1948-1975](../../../GnauralCore/ui/pages/AudioPage.vue#L1948-L1975). Новый диалог строим по этому
образцу (свои классы `tracks-panel__wf-settings-*`, левая колонка уже — ~160px: пункты короткие).

### 2.6. Смежные точки, которые НЕ переезжают (+ side-find)

- **Overlay-режим**: у `SpectrogramView` есть свой гир, видимый при `waveformOverlay ||
  showSettingsGear` ([SpectrogramView.vue:86-92](../../../GnauralCore/ui/components/SpectrogramView.vue#L86-L92),
  проп GT10.3 [:213-214](../../../GnauralCore/ui/components/SpectrogramView.vue#L213-L214)); в
  overall-стеке спектра он открывает настройки **волны** своего канала
  ([TracksPanel.vue:494](../../../GnauralCore/ui/components/TracksPanel.vue#L494)) — потому что в
  overlay волна рисуется поверх спектрограммы, а стек «Общая волна» (и его тайтл-бар) не рендерится
  вовсе. Этот гир остаётся на месте и открывает **новый** диалог с вкладкой своего канала (WS-D5).
- **Лейны gtrack**: `waveform-view` в соло-подполосе
  ([:286-300](../../../GnauralCore/ui/components/TracksPanel.vue#L286-L300)) — его гир открывает
  **per-lane** настройки (`gtrackSettingsId`, GTrackSpectrumSettings), не трогаем; inline-underlay
  ([:204-217](../../../GnauralCore/ui/components/TracksPanel.vue#L204-L217)) слушателя не имеет
  вовсе (гир перекрыт GTrackView). Значит гир из WaveformView **удалять нельзя** — только прятать
  пропом в overall-стеке (WS-D6).
- **Side-find (вне объёма фичи): в `AudioPage.vue` остался осиротевший «остров» —** ~301 строка
  мёртвого кода от выделения вкладки «Треки» в TracksPanel (GT2.4), включая копию настроек волны.
  На фичу не влияет (остров инертен). Обследован по запросу владельца 2026-07-15 и **отложен**
  (WS2.1) — полное описание, измерения и порядок чистки: **§8**.

### 2.7. Инфраструктура проверок

CI и ESLint отсутствуют; рутина — `bun run typecheck` + `bun run build` из `MindWaveCore/ui`,
`bun test ui server` из GnauralCore. Существующие тесты диалог SF27 не покрывают (grep по
`waveformSettings|wfDlg|mindwave-tracks-waveform` в `*.test.ts` — пусто), чистой вычислимой логики в
фиче нет (маппинг вкладка→каналы тривиален) — новые bun-тесты не заводим, главная проверка — прогон
реального приложения.

## 3. Решения

- **WS-D1 — одна шестерёнка на группу, в тайтл-баре.** Вместо двух per-канальных гиров в области
  графики (SF27) — одна кнопка `settings` в правом краю fold-хедера «Общая волна» (после заголовка;
  `flex:1` заголовка сам прижмёт её вправо), `@click.stop` (не задевать Ctrl-click/фолд поведение
  хедера). Открывает диалог на вкладке «Оба канала». Быстрый доступ к каналу — вкладками (req 5).
- **WS-D2 — форма диалога по образцу GT10.29, inline в TracksPanel.** `q-card` → `row no-wrap` →
  узкий `q-list` (~160px, `clickable` + `active-class`) + `q-separator vertical` + широкая панель
  контента; свои классы `tracks-panel__wf-settings-*`. В отдельный компонент не выделяем: состояние
  и персистентность живут в TracksPanel, контролов три, и все диалоги TracksPanel (SF26 minimap,
  gtrack, SF27) — inline; выделение добавило бы только props/emits-обвязку.
- **WS-D3 — REVISED (owner req 6) — модель вкладок + тоггл «Применить к обоим каналам».** Состояние:
  `waveformSettingsOpen: boolean` + `waveformSettingsTab: 'both' | 0 | 1` (вместо сегодняшнего трюка
  «channel|null») + `waveformLinkChannels: boolean` (персистится, дефолт **ON**). Тоггл — на форме
  «Оба канала», над настройками (req 6, дословно). Семантика:
  - **ON (дефолт):** форма «Оба канала» активна и правит **свою** сущность (WS-D9, req 12 —
    прежняя запись «в оба канала» отменена); пункты «Левый»/«Правый» — `:disable` (req 6).
  - **OFF:** «Левый»/«Правый» активны, каждый правит свой канал; контролы формы «Оба канала»
    **выключены** (жив только сам тоггл). Иначе форма противоречила бы своему лейблу: «применить к
    обоим» выключено, а форма всё равно писала бы в оба.
  - **REVISED (req 11): флип тоггла применяется мгновенно.** Тоггл **выбирает источник отрисовки**:
    `linked → каждый канал рисует сущность «Оба канала»`, иначе — свою. Поэтому ничего не
    копируется и не «выравнивается» — меняется источник, а L/R-сущности остаются нетронутыми
    (req 12). Флип обратно в OFF **возвращает** каждому каналу его собственные настройки — операция
    полностью обратима (это закрывает прежний риск «undo нет»).
  - **get на «Оба канала» читает свою сущность** (а не канал 0 — req 12). Вкладки «Левый»/«Правый»
    читают **сырые** массивы, а не `wfColor(ch)`: последний отвечает на вопрос «что канал
    *рисует*», а это при `link=ON` — сущность «Оба канала»; иначе три сущности перестали бы быть
    независимыми на экране.
  - Дефолт тоггла **ON** — подтверждён владельцем 2026-07-15 («Согласен дефолт тоггла ON»).
    Следствие см. R1.
- **WS-D4 — REVISED (owner req 7) — моно-файл: один пункт «Моно».** `isSpectrogramStereo === false`
  → в списке единственный пункт с ярлыком «Моно» (внутренний id вкладки остаётся `'both'` — это
  «единственная/общая» область; меняется только ярлык), «Левый»/«Правый» скрыты, **тоггл скрыт**
  (при одном канале «применить к обоим» бессмысленно), форма правит канал 0.
- **WS-D5 — overlay и лейны не переезжают.** Гир на спектрограмме в overlay-режиме остаётся (стек
  волны и его тайтл-бар в overlay не существуют — переносить некуда) и открывает новый диалог с
  вкладкой своего канала. **REVISED (req 6):** при `waveformLinkChannels === true` вкладка канала
  заперта, поэтому любой per-channel вход ведёт на «Оба канала»:
  `openWaveformSettings(ch)` → `tab = link ? 'both' : ch` (то же правило — для per-канальных гиров
  дорожек, пока они живы в WS1.1). Гиры лейнов gtrack (per-lane настройки) — без изменений.
- **WS-D6 — WaveformView: проп `showSettingsGear`, default `true`.** Через `withDefaults` (не
  «голый» type-only boolean — absent-проп кастуется в `false`, см. правило про Vue boolean): лейны
  ничего не передают и не меняются, overall-стек передаёт `:show-settings-gear="false"`. Эмит
  `open-settings` остаётся. Симметрично прецеденту GT10.3 в SpectrogramView (там же имя пропа),
  только с обратным дефолтом.
- **WS-D7 — набор настроек не меняется, персистентность +2 ключа.** Цвет / шкала lin-dB /
  прозрачность наложения (последняя, как сейчас, видна только при `viewMode === 'overlay'`). Ключ
  localStorage прежний (`mindwave-tracks-waveform`), формат получает `link: boolean` (WS-D3) и
  `both: {scale, color, opacity}` (WS-D9 — третья сущность обязана переживать перезагрузку так же,
  как две другие). Оба обратно совместимы: отсутствуют → дефолт/сид из канала 0.
- **WS-D9 — (owner req 12) три независимые сущности настроек, тоггл выбирает источник.**
  «Оба канала» — **третья** сущность (`waveformBothScale/Color/Opacity`), а не массовая запись в L и
  R. Правка любой из трёх не трогает две другие. Что **рисует** дорожка: `linked → сущность «Оба
  канала»`, иначе — своя (`wfScale/wfColor/wfOpacity` инкапсулируют правило, поэтому оверлей на
  спектрограмме и полоска в хедере подчиняются ему автоматически). Миграция: сущность «Оба канала»
  сидируется из канала 0 — именно его значения показывала вкладка «Оба канала» в WS1.1.
  **Моно игнорирует тоггл целиком**: при одном канале объединять нечего, поэтому вкладка «Моно» —
  это сущность канала 0 (WS-D4), иначе один график подчинялся бы двум разным сущностям.
- **WS-D10 — (owner req 10) свой колорпикер вместо `<input type="color">`.** Нативный инпут
  открывает пикер **браузера** — это его chrome, добавить туда иконку Close нельзя. Поэтому цвет
  выбирается кружком-образцом + `q-color` в `q-popup-proxy` с кнопкой X — **ровно тот паттерн,
  который владелец сам задал для цветов лейнов** в GT10.20 (`TracksPanel.vue`, диалог настроек
  лейна: «open to the RIGHT of the circle, with a close (X)»), с переиспользованием его же классов
  `tracks-panel__color-swatch` / `tracks-panel__color-popup`. У `QPopupProxy` нет пропа `disable`,
  поэтому в выключенном состоянии формы поповер снимается через `v-if` (+ класс `disabled` и
  `tabindex="-1"` на кружке). Правило `.audio-page__wf-color` удалено вместе с его единственным
  пользователем.
- **WS-D8 — тексты (i18n ru+en).** Новый ключ `audio.waveformSettingsTitle` («Настройки отображения
  волны» / "Waveform display settings") — заголовок диалога и тултип/aria новой кнопки; ключи
  вкладок `audio.waveformChannelsBoth/Left/Right` («Оба канала» / «Левый» / «Правый», en: "Both
  channels" / "Left" / "Right") + `audio.waveformChannelMono` («Моно» / "Mono", req 7) +
  `audio.waveformApplyBoth` («Применить к обоим каналам» / "Apply to both channels", req 6).
  Формулировка «Оба канала» подтверждена владельцем (req 8) — R6 закрыт. Существующий
  `audio.waveformStyle` остаётся — его используют гиры лейнов в WaveformView.

## 4. Шаги

### Phase 1 — диалог и перенос

- **WS1.1** — новый двухпанельный диалог на месте SF27-диалога: вкладки + тоггл «Применить к обоим
  каналам» и его семантика (WS-D3), моно-пункт «Моно» (WS-D4), персист `link` (WS-D7), i18n
  (WS-D8). Открывается пока из **старых** гиров: per-канальный гир → вкладка своего канала (при
  `link=ON` → «Оба канала», WS-D5). Приложение в рабочем состоянии на каждом шаге.
- **WS1.2** — перенос шестерёнки (WS-D1, WS-D6): кнопка в fold-хедере «Общая волна» (открывает
  вкладку «Оба канала»), `showSettingsGear` в WaveformView + `false` в overall-стеке; комментарии
  SF27 обновить (гир больше не per-track в overall-стеке). Overlay-гир спектрограммы уже открывает
  новый диалог (после WS1.1) — проверить, не сломан.
- **WS1.3** — **PAUSE**: owner-проверка. *Пройдена частично 2026-07-15: «В целом, всё хорошо» +
  4 замечания (req 9-12) → WS1.4.* Открытым остаётся вопрос: чистить ли мёртвый дубликат
  waveform-state в AudioPage (WS2.1)? *(вопросы про индикацию расхождения / моно / «Оба канала» /
  дефолт тоггла закрыты ответами req 6-8 и «Согласен дефолт тоггла ON»)*
- **WS1.4** — доработки по замечаниям владельца на WS1.3 (req 9-12): отступ шестерёнки (req 9);
  свой колорпикер с X вместо нативного инпута (WS-D10); мгновенное применение при `link=ON`
  (WS-D3 rev.) и три независимые сущности (WS-D9) — это одна правка: тоггл выбирает **источник**
  отрисовки, а не копирует значения.

### Phase 2 — ОТЛОЖЕНА владельцем (2026-07-15: «Мы вернёмся к этому позже»)

- **WS2.1** — удалить осиротевший остров из AudioPage. **Не отменён, а отложен**: обследование
  выполнено, объём и порядок известны — см. **§8**. Фазу 1 не блокирует ничем (остров инертен).

## 5. Риски

| # | Риск | Смягчение |
|---|---|---|
| R1 | **REVISED (req 11/12) — прежняя редакция («первая правка схлопывает цвета, undo нет») СНЯТА: сущности независимы, флип полностью обратим.** Осталось следствие дефолта **ON**: сразу после загрузки оба канала рисуются **одним** цветом (сущности «Оба канала», сид — cyan канала 0), т.е. привычное различение L cyan / R amber по умолчанию не видно | Ничего не разрушается: настройки L/R лежат нетронутыми и возвращаются выключением тоггла (req 12). Владелец подтвердил дефолт ON, зная о следствии; смена дефолта на OFF — одна строка |
| R2 | Убираем быстрый путь «гир прямо на дорожке» — теперь 2 клика (гир → вкладка) | Осознанно по req 1; вкладка запоминается только в рамках открытого диалога, дефолт «Оба канала» предсказуем |
| R3 | Высота хедера 22px, `overflow: hidden` — новая кнопка может не влезть/обрезаться | Кнопки `size="xs"` (fold/eye) там уже живут; тултип телепортируется (`AppTooltip`), overflow его не режет |
| R4 | Диалог открыт, а файл сменился stereo→mono: активная вкладка `0/1` теряет смысл | При открытии всегда сбрасываем вкладку (`both`), при смене файла диалог обычно закрыт; guard: mono показывает единственный пункт «Моно», прячет тоггл и приводит вкладку к `both` |
| R5 | **ПЕРЕПИСАН 2026-07-15 — прежняя редакция («однажды начнёт перезаписывать prefs тем же ключом») была ФАКТИЧЕСКИ НЕВЕРНА**: ключ другой (`mindwave-audio-waveform`), а watch не может сработать (§8). Реальный риск — не данные, а **дезинформация**: ~301 строка мёртвого кода читается как рабочая и уже разошлась с живой копией (до-WS1.1 модель диалога, до-GT11.7 ресайз-хендлеры) | Чистка WS2.1 — **отложена** владельцем. Пока не удалено, действует правило: вкладку «Треки» правим в TracksPanel; AudioPage-копию не трогаем и по ней **не ориентируемся** (§8) |
| ~~R6~~ | ~~«Обе канала» — опечатка владельца~~ | **ЗАКРЫТ** (owner req 8): «Оба канала» подтверждено |

## 6. Верификация (каждый шаг)

- `bun run typecheck` + `bun run build` из `MindWaveCore/ui` (vue-tsc тянет GnauralCore по графу
  импортов; **не** `bunx vue-tsc` — мисматч TS).
- `bun test ui server` из GnauralCore — существующие сьюты остаются зелёными (новых тестов нет:
  фича — Vue-разметка без чистой логики).
- Ручной прогон реального приложения (главная проверка): стерео-файл — тоггл **ON**: «Левый» и
  «Правый» недоступны (req 6), оба канала рисуются настройками «Оба канала», правка применяется
  сразу (req 11); тоггл **OFF**: каждый канал **возвращает** свои настройки (req 12 — они не были
  затронуты), Л/П доступны и правят только свой канал, контролы формы «Оба канала» выключены;
  цвет — свой пикер с иконкой X в заголовке (req 10); шестерёнка отстоит от правого края так же,
  как отстояла на графике (req 9); закрыть/открыть — все три сущности и состояние тоггла
  персистятся; режим overlay — гир на спектрограмме открывает диалог (при ON — на «Оба канала»),
  слайдер прозрачности виден только тут; лейны gtrack — их гир по-прежнему открывает per-lane
  настройки; моно-файл — в списке один пункт «Моно», тоггла нет; fold/eye/Ctrl-click хедера не
  сломаны.

## 7. Ссылки

- Требования: этот файл §1 (цитата дословно).
- Леджер: [wave-settings-dialog-progress.json](wave-settings-dialog-progress.json).
- Код: [WaveformView.vue](../../../GnauralCore/ui/components/WaveformView.vue),
  [TracksPanel.vue](../../../GnauralCore/ui/components/TracksPanel.vue),
  [SpectrogramView.vue](../../../GnauralCore/ui/components/SpectrogramView.vue),
  [AudioPage.vue](../../../GnauralCore/ui/pages/AudioPage.vue) (образец GT10.29 + side-find),
  i18n [ru.json](../../../GnauralCore/ui/i18n/ru.json) / [en.json](../../../GnauralCore/ui/i18n/en.json).
- Прецеденты: GT10.29 (двухпанельный диалог настроек), GT10.3 (`showSettingsGear` в
  SpectrogramView), GT11.10/GT11.11 (fold-хедер overall-групп), SF27 (per-track настройки волны —
  заменяется этим планом).

## 8. WS2.1 — осиротевший остров в `AudioPage.vue` (СДЕЛАНО 2026-07-16)

**Статус:** обследовано 2026-07-15; **вычищено 2026-07-16** по порядку из §8.5 — 525 строк, скрипт
2478 → 1959. Все три ключа-сироты из §8.5.3 забраны, плюс четвёртый, который список пропустил:
`mindwave-audio-spectrogram-track-heights`. Осталось только живое (`mindwave-audio-expanded-paths`,
`-files-panel-open`). Заодно, отдельным коммитом, ушёл клиентский буфер
`gnauralSpectrogramBuffer` в `stores/audio.ts`: его последние читатели (`isSpectrogramStereo`,
`spectrogramDuration`) были частью острова. Проверено: vue-tsc 0, quasar build, 260/260 тестов.

Обзор ниже сохранён как есть — он оказался точен, и §8.6 (метод + его ловушки) сработал именно так,
как задумывался: контроль `TracksPanel` сразу поймал в новом скане слепое пятно с kebab-case. Ещё
три ловушки метода, найденные при исполнении, дописаны в §8.6.

Найдено попутно, к настройкам волны отношения уже не имеет: это наследие **GT2.4**, самостоятельная
задача. Оставлено здесь, т.к. обнаружено в этом плане; при возврате можно вынести в свой план.

### 8.1. Что это и почему возникло

При выделении вкладки «Треки» в `TracksPanel.vue` старую инлайновую реализацию из `AudioPage.vue`
**не удалили**. Причина зафиксирована в самом коде —
[AudioPage.vue:364-366](../../../GnauralCore/ui/pages/AudioPage.vue#L364-L366): «*GT2.4 (GT-D10):
the whole new track stack lives in TracksPanel.vue (its own zoom/selection state + storage keys)*».
Шаблон AudioPage заканчивается на **427-й** строке и всю вкладку отдаёт `<tracks-panel>`
([:368](../../../GnauralCore/ui/pages/AudioPage.vue#L368)), а скрипт (**1515** строк) до сих пор
содержит копию: prefs волны, SF27-диалог, слой order/hidden, ресайз-хендлеры, зум спектра.

### 8.2. Измерение (скан по всем 211 top-level объявлениям скрипта)

**64 объявления / ~301 строка недостижимы** — ни из шаблона, ни из блоков с эффектами
(`onMounted`/`watch`). Крупнейшие непрерывные куски:

| Строки | Что лежит |
|---|---|
| [1030-1077](../../../GnauralCore/ui/pages/AudioPage.vue#L1030-L1077) | `wfScale`/`wfColor`/`wfOpacity` + весь SF27-диалог (`wfDlg*`, `openWaveformSettings`) |
| [1133-1204](../../../GnauralCore/ui/pages/AudioPage.vue#L1133-L1204) | hide/show/reorder дорожек, `hiddenTrackList`, `showWaveform`/`showSpectrogram`/`viewModeLabel` |
| [1272-1327](../../../GnauralCore/ui/pages/AudioPage.vue#L1272-L1327) | ресайз-хендлеры волны (divider + bottom) |
| [1464-1537](../../../GnauralCore/ui/pages/AudioPage.vue#L1464-L1537) | ресайз-хендлеры спектрограммы |
| [1392-1419](../../../GnauralCore/ui/pages/AudioPage.vue#L1392-L1419) | зум спектрограммы (in/out/fit) |

Остальной state (`viewMode`, массивы prefs, `waveformTracks`/`spectrogramTracks`, высоты) числится
«достижимым» **только** через инертный watch — уберёшь недостижимую часть, и он тоже станет
недостижим.

### 8.3. Доказательство инертности + ДВЕ ИСПРАВЛЕННЫЕ ОШИБКИ (важно для решения)

Две мои прежние формулировки в этом плане были **фактически неверны** — исправлены, чтобы решение
принималось по фактам:

1. ~~«пишет в **тот же** ключ localStorage»~~ — **нет**. AudioPage читает/пишет **свой** ключ
   `mindwave-audio-waveform` ([:983](../../../GnauralCore/ui/pages/AudioPage.vue#L983)); TracksPanel —
   `mindwave-tracks-waveform`. Ключи не пересекаются.
2. ~~«мина: однажды начнёт перезаписывать prefs»~~ — **нет**. Watch
   [:1205-1220](../../../GnauralCore/ui/pages/AudioPage.vue#L1205-L1220) **не может сработать**: его
   зависимости пишут только сеттеры `wfDlg*`
   ([:1059/1067/1075](../../../GnauralCore/ui/pages/AudioPage.vue#L1059-L1075)), а те недостижимы;
   `viewMode`/`minimapMode` не присваиваются нигде.

Риск был выведен из **формы** кода (watch → localStorage) без чтения ключа и без вопроса «кто
вообще может его запустить».

### 8.4. Что реально плохо

Не данные, а **дезинформация**: ~300 строк выглядят рабочим кодом и уже **разошлись** с живой
копией — в AudioPage лежит до-WS1.1 модель диалога (`waveformSettingsChannel: number | null`) и
до-GT11.7 раздельные ресайз-хендлеры (`onWaveformDividerPointerDown`), которых в TracksPanel уже
нет (их заменил унифицированный `onOverallResize*`). Правка «по аналогии» в AudioPage не даст
ничего во время работы.

**Почему не ловится автоматикой:** ESLint'а в воркспейсе нет, а `<script setup>` отдаёт все
top-level биндинги в шаблон → vue-tsc не считает их неиспользуемыми. Находится только явным
проходом по достижимости.

### 8.5. Порядок чистки, когда вернёмся

1. Удалить **недостижимую** часть (таблица §8.2) → `typecheck` + `build`.
2. Удалить ставший недостижимым **state** и инертный watch (prefs волны, order/hidden,
   `waveformTracks`/`spectrogramTracks`, высоты) → `typecheck` + `build`.
3. Забрать осиротевшие ключи, которые читает только эта копия и не пишет никто:
   `mindwave-audio-waveform`, `mindwave-audio-track-layout`, `mindwave-audio-waveform-track-heights`.
4. Smoke: вкладка «Треки» не затрагивается вовсе (её рисует TracksPanel), prefs не сбрасываются.

Ожидаемый эффект: скрипт AudioPage худеет с ~1515 строк примерно на треть.

### 8.6. Как перепроверить (метод, и где он врёт)

Скан: разбить SFC на template/script, взять top-level объявления, корни = имена из шаблона **плюс**
блоки с эффектами, дальше транзитивно. Две ловушки, на которых первый прогон **соврал** в мою
пользу — проверять обязательно:

- **нельзя** резать template по первому `</template>` — слотовые шаблоны вложены, и весь шаблон
  уедет в «скрипт» (режем по началу `<script>`);
- `onMounted`/`watch` **обязаны** быть корнями, иначе живой код (`handlePlayerKeyDown`, `$q`,
  `router`) попадёт в «мёртвый».

Проверяйте скан на заведомо живом контроле — здесь им был `TracksPanel.vue` (те же имена там
используются шаблоном).

**Ещё четыре ловушки, найденные при исполнении 2026-07-16** (каждая из первых трёх ловилась
`typecheck`, четвёртая — только контролем):

- `type` / `interface` / `enum` — **тоже объявления**. Если не считать их границами, они уезжают
  внутри диапазона соседнего мёртвого объявления (так пропали `TrackKind`, `WaveformTrack`).
- **Деструктуризация** — тоже объявление: `const { t } = useI18n()` исчезла ровно так же.
- **Блоки-эффекты обязаны быть ГРАНИЦАМИ**, а не только корнями. Иначе диапазон «от мёртвого
  объявления до следующего объявления» **проглатывает стоящий за ним `watch()`** — и вот этого
  `typecheck` уже не заметит (23 строки живых watch'ей уехали молча на первом прогоне).
- Шаблон зовёт PascalCase-компоненты в **kebab-case** (`<gnaural-editor-panel>`). Сравнение имён
  «в лоб» записывает живые компоненты в мёртвые — это поймал контроль `TracksPanel`.
