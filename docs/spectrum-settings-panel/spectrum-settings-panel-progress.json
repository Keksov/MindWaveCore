{
  "schemaVersion": 1,
  "plan": "spectrum-settings-panel",
  "planDoc": "docs/spectrum-settings-panel/spectrum-settings-panel-plan.md",
  "title": "spectrum-settings-panel: the spectrogram «Параметры» panel moves onto the universal @panel control (dockable + detachable, like «Открытие файлов» / «Список треков»), and its parameter groups become blocks laid out as TILES when two fit side by side, or as a single-column ACCORDION when they do not",
  "status": "in-progress",
  "createdAt": "2026-07-16",
  "updatedAt": "2026-07-16",
  "decisions": [
    {
      "id": "SS-D1",
      "text": "THIRD consumer of the @panel control; host = AudioPage's shared dock-wrap; the `tune` toolbar button stays in TracksPanel. New stores/spectrum-settings-panel.ts -> usePanelWindowState('spectrum-settings', {defaultMode:'floating', defaultDockSize:320}) with the DEFAULT mindwave-panel-spectrum-settings-* keys — no legacy mapping is needed (unlike file-open, which had to keep mindwave-fs-browser-*), because today's visibility is not persisted at all: it is a plain local ref (TracksPanel.vue:2649). New SpectrumSettingsDialog.vue (PanelWindow wrapper) is hosted in audio-page__dock-wrap next to FileOpenDialog + TrackListDialog (AudioPage.vue:133-151) behind the same <Teleport to=\"body\" :disabled=\"!floating\">. WHY AudioPage and not TracksPanel: PW-D9 was FORCED to host «Список треков» inside TracksPanel because gtracks = useGtrackLanes(...) is a per-call factory and a second host would spawn a second divergent instance — that constraint does NOT apply here, since the spectrogram settings live in a GLOBAL Pinia singleton store (stores/spectrogram.ts:95) that every host shares by construction. What DOES apply is PW-D10: q-tab-panels have no keep-alive, so TracksPanel is destroyed on every tab switch and a panel hosted from there cannot survive one. AudioPage hosting therefore reproduces exactly the behaviour of the two panels the owner named as the model (req 1: «по аналогии»). The `tune` button stays where the owner put it (SF-D5) and toggles panel.open of the shared state: usePanelWindowState is a module-level Map singleton (use-panel-window.ts:119), NOT Pinia, so TracksPanel's toolbar and AudioPage's host reach the same object with zero prop plumbing."
    },
    {
      "id": "SS-D2",
      "text": "REVISED 2026-07-16 — THE DECISION IS UNCHANGED, ITS JUSTIFICATION IS NOT. Originally the name was held in place by a PROHIBITION: AudioPage.vue:508 still imported the file from the dead GT2.4 island, so renaming would break vue-tsc and drag the owner-deferred WS2.1 cleanup into this feature (R6). Another session has since DELETED that island (GnauralCore 15d1960, 'WS2.1 audio: delete the orphaned pre-GT2.4 tracks island from AudioPage'; WS2.1 is now `done` in the wave-settings-dialog ledger). Re-verified 2026-07-16: the import is gone and exactly ONE consumer imports the file workspace-wide — TracksPanel.vue:1294. So renaming is now POSSIBLE and R6 is closed. The decision nevertheless stands, because it always had a second and stronger leg which is now the only one: this IS the PW5.7 precedent's naming — TrackListView (pure view) / TrackListPanel (IN-WINDOW ADAPTER) / TrackListRemote (child). SpectrogramSettingsPanel is the same in-window adapter and already carries the right name; renaming it now would break step with the only living model, for the sake of renaming. Lesson recorded on purpose: a decision that survives the loss of its stated reason must have the surviving reason written down, or the next session will 'fix' it back.\n\nSplit into View + two adapters (the PW5.7 «пульт» shape), file name SpectrogramSettingsPanel.vue PRESERVED. (a) SpectrogramSettingsView.vue — pure view: props = a snapshot (settings, presets, activePresetId, isModified) + layout, emits ONE serializable `action`; ALL block/tile/accordion markup lives here. (b) SpectrogramSettingsPanel.vue — in-window adapter over the store (same path, same name). (c) SpectrogramSettingsRemote.vue — child adapter: renders the parent's snapshot, emits `action` over the bridge, owns NO store and NO persistence. (d) applySpectrumSettingsAction(store, action) — one function applying an action on both sides, so the parent's store stays the single source of truth. (For the superseded 'the name is load-bearing because AudioPage:508 imports it' rationale, see the paragraph above — it was true when written on 2026-07-16 and stopped being true the same day.)"
    },
    {
      "id": "SS-D3",
      "text": "Detach MUST be a «пульт» (PW5.7); the naive per-child-store variant is VERIFIED BROKEN, and it fails WORSE here than it did for «Список треков». Verified 2026-07-16 by grep over GnauralCore/ui stores+composables+components: there is NO 'storage' event listener ANYWHERE, and stores/spectrogram.ts:121-123 persists three keys (mindwave-spectrogram-settings / -user-presets / -active-preset) via deep watches. Consequences for a detached child running its own Pinia (every window is its own JS realm — PW-D11): (a) SILENT NO-OP — the child edits ITS store, so the parent's renderOptions/analysisParams never change and the main window's spectrogram is never repainted, i.e. the panel's entire purpose fails; (b) CLOBBERING — both windows' deep watches write the same 3 keys, last-writer-wins. The asymmetry vs PW-D11 matters and is against us: the naive detached track list showed an EMPTY list — visibly broken, impossible to ship by accident. Here the child would look FULLY functional (the snapshot seeds from localStorage, every control moves) and simply affect nothing. A silent failure is the one that ships. Therefore allowDetach is enabled ONLY together with the remote (phase 3); until then :allow-detach=\"false\" is passed EXPLICITLY (never omitted — see SS-R5 / PW5.7c: an absent type-only Boolean prop casts to false, which is how the mode silently vanished for both existing panels)."
    },
    {
      "id": "SS-D4",
      "text": "The tiles<->accordion switch is driven by CONTENT WIDTH via ResizeObserver, not by CSS. It cannot be pure CSS (@container/media) because the switch is STRUCTURAL: accordion blocks collapse, tiles do not. ResizeObserver on the panel body, guarded by `typeof ResizeObserver !== 'undefined'` — the convention of all 6 existing call sites (FsEntryList.vue:315-320 — which is itself inside the file-open panel — GTrackView, SpectrogramView, WaveformView, GnauralScheduleView, SpectrogramMinimap). Rule: TILES when contentWidth >= 2*MIN_BLOCK + gap, else ACCORDION (owner req 3/4). Starting MIN_BLOCK = 280px (today's panel is a fixed 300px column and the controls fit it — TracksPanel.vue:3269-3284); the exact value is MEASURED against the real controls in SS2.1 and recorded here. gap = 8px -> threshold ~568px. The tile grid itself is grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)), so 2 columns at the threshold and 3-4 when wider come for free: req 3 states the CONDITION («два блока»), not a cap (confirm via Q4). Both modes are reachable by construction: minFloatW=460 and defaultDockSize=320 (left/right dock) are below the threshold -> accordion; a top/bottom dock spans the editor width -> tiles."
    },
    {
      "id": "SS-D5",
      "text": "CONFIRMED by owner req 6 («Несколько сразу, состояние запоминается»). Accordion = q-expansion-item, modelled on GTrackSpectrumSettings.vue:7,40 — the ONLY file in the entire workspace that uses that control, and conveniently a per-lane editor of the very same SpectrogramSettings type. Multiple blocks may be open at once; the open set persists (mindwave-panel-spectrum-settings-groups). Default open = the first block — after SS-D6 rev.2 that is «Масштаб» (`scale`), since presets are no longer a block."
    },
    {
      "id": "SS-D6",
      "text": "REVISED 2 (owner 2026-07-16, after seeing SS2.1) — presets leave the panel body entirely for a title-bar-invoked MODAL dialog; blocks are the four groups again. Owner: «Блок пресетов нужно вынести за рамки этого диалога. В заголовке формы Параметры добавь иконку для вызова этого диалога», form clarified as «Отдельный модальный диалог» (not a dropdown). So: (a) the blocks are exactly the four parameter groups — «Пресеты» is no longer a block; (b) a `bookmarks` icon in the «Параметры» title bar (left of the dock menu, via the new SS-D11 titlebar-actions slot) opens SpectrogramPresetsDialog.vue — a modal q-dialog with a header close X (the owner's WS-D10 taste), the preset list (✓ active, * modified in the title), «Сохранить как…» and «Управление…» (still opening the full manager); (c) «Сброс» is UNCHANGED in the window footer (not in the presets dialog); (d) the accordion's default-open block shifts from `presets` to `scale` (the first group); (e) laziness preserved — the presets dialog + manager are a defineAsyncComponent mounted on first open, so they stay OUT of the eager AudioPage chunk (as they were when the panel carried the presets), while the trigger button in the title bar is eager (must be visible immediately). The prior rev (five blocks, «Пресеты» as block #1) is SUPERSEDED; its text is kept below as history. --- SUPERSEDED rev 1 (owner req 5): the answer was a HYBRID of the two options offered, and both halves carry weight: «Пресеты - отдельный блок. Кнопка Сброс сама по себе отдельно в футере». (a) PRESETS ARE A FIFTH BLOCK, first in order — a tile in the tiled layout, a collapsible item in the accordion — holding today's preset bar (SpectrogramSettingsPanel.vue:3-34: dropdown with ✓active and *modified, «Сохранить как…», «Управление…»). (b) «Сброс» (:105-107) is NOT inside that block: it goes into PanelWindow's `footer` slot (PanelWindow.vue:46-47) «сам по себе». The distinction is functional, not cosmetic, and it is why the offered 'presets are a fifth block' option was not accepted wholesale: had «Сброс» lived inside the presets block, collapsing that block in the accordion would have hidden Reset too. In the footer it is always visible and never collapses. CONSEQUENCE for req 3: five blocks, not four — with auto-fill the last tile row is always partial (see R2). CONSEQUENCE for SS-D2: «Пресеты» is an ordinary View block, so preset gestures emit the SAME serializable `action` as field edits — no second channel."
    },
    {
      "id": "SS-D7",
      "text": "i18n: no new title key needed. The PanelWindow title and the manifest titleKey both reuse the EXISTING audio.spectrogramSettingsTitle («Параметры» / \"Parameters\", SF-D4) — it is already exactly this panel's caption; icon `tune`, the same as the toolbar button. Dock/detach menu labels are already generic and translated (panel.dockMenu/dockFloat/dockLeft/dockRight/dockTop/dockBottom/detach/close in MindWaveCore/ui/src/i18n/locales/{ru,en}.json). New keys only if Q1/Q2 introduce new captions."
    },
    {
      "id": "SS-D12",
      "text": "NEW (owner 2026-07-16, after SS2.3) — preset MANAGEMENT is folded into the presets form itself; the separate manager window and the «Управление…» button are gone. Owner: «Оформи подформу Управления пресетами как часть основной формы пресетов. Лишняя кнопка не нужна, раз уж у нас теперь есть самостоятельное окно». So SpectrogramPresetManager.vue's whole body (built-in + user preset lists with Apply + duplicate/update/rename/delete + import/export) moved DIRECTLY into SpectrogramPresetsDialog.vue; the manager file is DELETED, and both the «Управление…» button and the nested <spectrogram-preset-manager> are removed. «Сохранить как…» stays (footer, left). Three now-orphaned i18n keys (spectrogramPresetManage / spectrogramPresetManager / spectrogramPresetClose) removed from BOTH locales — verified 0 code refs workspace-wide first. The dialog remains ONE lazy chunk (7.2 KB now, was ~this + a manager chunk), still off the eager AudioPage path."
    },
    {
      "id": "SS-D11",
      "text": "NEW (owner 2026-07-16) — a `titlebar-actions` named slot added to the shared @panel control PanelWindow. The title bar is entirely PanelWindow's (drag zone + dock menu + close), with no slot for a consumer's own buttons, so «Параметры» could not host a presets icon there. Added <slot name=\"titlebar-actions\" /> LEFT of the dock-menu button. Backward-compatible: an unused named slot renders nothing, so «Открытие файлов»/«Список треков» are untouched. This is the feature's ONLY change under MindWaveCore/ui (the @panel control lives there) — cross-repo, committed separately from the GnauralCore code."
    },
    {
      "id": "SS-D8",
      "text": "REVISED by owner req 7 («Применить + «Сохранить как…»») — the detached child gets preset APPLY + «Сохранить как…»; «Управление…» stays main-window-only. Both permitted operations decompose into 'local input + one bridged action': apply emits an action directly; «Сохранить как…» runs its $q.dialog prompt child-side (a prompt is UI, and UI is local) and then bridges an action carrying the chosen name. The child still owns no store and no persistence (SS-D3). SpectrogramPresetManager.vue (rename/delete/duplicate/EXPORT/IMPORT) does NOT travel to the child, which removes the dirtiest part of phase 3: file export/import over the bridge. Implementation = an `allowPresetManage` prop on the View, declared via withDefaults with default TRUE — the main window passes nothing, SpectrogramSettingsRemote passes false to hide «Управление…». withDefaults is mandatory rather than a bare type-only boolean: an ABSENT type-only Boolean prop is cast by Vue to false (R5 / WS-D6 / PW5.7c — the exact trap that silently hid the detach mode from BOTH existing panels), so a bare prop here would silently hide «Управление…» in the MAIN window too."
    }
  ],
  "steps": [
    {
      "id": "SS0.1",
      "phase": 0,
      "title": "План + леджер (этот файл + spectrum-settings-panel-plan.md); инвентаризация кода и прецедентов — PAUSE ПРОЙДЕНА 2026-07-16: владелец ответил на Q1-Q4 (req 5-8) -> SS-D6 и SS-D8 REVISED, SS-D4/SS-D5 подтверждены",
      "status": "done",
      "dependsOn": [],
      "artifacts": [
        "docs/spectrum-settings-panel/spectrum-settings-panel-plan.md (owner reqs verbatim, inventory §2, decisions SS-D1..SS-D8, phases, risks, open questions §7)",
        "docs/spectrum-settings-panel/spectrum-settings-panel-progress.json (this ledger)"
      ],
      "verify": "DONE — owner answered all four questions 2026-07-16 (plan §1 req 5-8, §7 table): Q1 -> presets are their own block but «Сброс» sits alone in the footer (SS-D6 REVISED); Q2 -> multi-open + persisted (SS-D5 confirmed); Q3 -> apply + save-as only in the detached child (SS-D8 REVISED); Q4 -> tiles from ~568px, 3-4 columns when wider (SS-D4 confirmed). SS-D1 (host = AudioPage) and SS-D3 (detach only as a remote) drew no objection",
      "startedAt": "2026-07-16",
      "finishedAt": "2026-07-16",
      "blockedReason": null,
      "evidence": [
        "Every claim in the plan was verified by reading the code, not inferred from shape — this repo has already punished shape-based reasoning twice in the wave-settings ledger (a localStorage key claimed shared that was not, and a watch claimed dangerous that could not fire).",
        "THE FIND THAT SHAPES THE WHOLE FEATURE (SS-D3): grep for a 'storage' event listener across GnauralCore/ui stores+composables+components returns NOTHING. So a naive detached child with its own Pinia would edit its own store, never repaint the parent's spectrogram, and — via the deep watches at stores/spectrogram.ts:121-123 — race the parent on the same 3 localStorage keys. Crucially the failure is INVISIBLE (the child seeds from localStorage and looks fully alive), unlike PW-D11's empty track list, which advertised its own brokenness. That is why detach is phased behind the PW5.7 remote instead of shipped naively.",
        "THE FIND THAT SHAPED THE FILE LAYOUT (SS-D2/R6) — SUPERSEDED THE SAME DAY, see the 2026-07-16 re-check log entry: at authoring time AudioPage.vue:508 still imported SpectrogramSettingsPanel.vue from the dead GT2.4 island (template ended :426, never rendered it — inert at runtime, live to vue-tsc), which forbade renaming the file. Another session then landed WS2.1 (GnauralCore 15d1960) and deleted the island; the import is gone, R6 is closed, and SS-D2 now rests on the PW5.7 naming precedent instead. Left here as-written rather than rewritten, because it WAS the reason at the time and the ledger records what was known when.",
        "THE FIND THAT MAKES req 2 CHEAP: the panel is data-driven, not markup-driven — the groups are a computed<Group[]> (SpectrogramSettingsPanel.vue:229-280) rendered by ONE v-for (:37). 'Every group becomes a block' is therefore a change to one v-for plus CSS, not a rewrite of 17 fields. The four groups (Масштаб 3 / Цвет 7 / FFT-фильтр 5 / Резкость 4 fields) are SF-D35 + SF-D51 and are exactly what the owner calls «группы параметров».",
        "PLACEMENT is not a coin flip (SS-D1): PW-D9 hosted «Список треков» inside TracksPanel only because gtracks is a per-call factory; our state is a global Pinia singleton, so that constraint is absent, while PW-D10 (no keep-alive on q-tab-panels -> TracksPanel dies on tab switch) still applies. AudioPage hosting is what makes 'по аналогии с Открытием файлов и Списком треков' literally true.",
        "PRECEDENT FOUND FOR THE CLEANUP SS1.1 MUST DO: TracksPanel.vue:2921-2922 records that when «Список треков» became a PanelWindow it stopped using the --tp-* scroll-frame pinning, because PanelWindow owns its positioning. Our panel must leave anyOverlayOpen (:2923) for the same reason — the precedent already walked this path.",
        "Also confirmed first-hand: PanelWindow's props/emits/slots (PanelWindow.vue:68-89, footer slot at :46-47, minFloatW=460), the panel registry being a module manifest and not an enum/switch (GnauralCore/ui/module.ts:20-38 -> modules/index.ts:40 -> PanelHostPage.vue:33), the panel-state singleton being a module-level Map rather than Pinia (use-panel-window.ts:119), q-expansion-item existing in exactly one file (GTrackSpectrumSettings.vue), and all 6 ResizeObserver sites guarding on `typeof ResizeObserver !== 'undefined'`.",
        "Step ids use the SS prefix: AP/FB/BK/GT/R/MR/PW/PR/SF/SB/TT/RF/WS are taken by the 11 existing ledgers (SF = spectrogram-fixes, which owns this panel's history and stays untouched).",
        "NOT decided by me, deliberately: Q1-Q4 (§7) were put to the owner rather than settled by proposal. That paid off on Q1: the answer («Пресеты - отдельный блок. Кнопка Сброс сама по себе отдельно в футере») was a HYBRID that neither offered option contained, and the half I would have gotten wrong is the load-bearing one — with «Сброс» inside the presets block, collapsing that block in the accordion would have hidden Reset. R2/R3 remain visual calls for the SS2.2/SS1.2 PAUSEs, since no browser driver exists in this workspace (verified in WS1.1).",
        "Owner answers folded back into both documents the same day: SS-D6 and SS-D8 rewritten as REVISED (with the reason the hybrid differs from either option, so a future session does not 'simplify' Reset back into the presets block), SS-D4/SS-D5 marked confirmed, plan §7 turned from a question list into a closed-questions table, R2 strengthened (five blocks, not four -> the tile grid's last row is always partial and «Пресеты» is a 1-row block against «Цвет»'s 7 fields), and SS2.1/SS3.2 re-scoped."
      ]
    },
    {
      "id": "SS1.1",
      "phase": 1,
      "title": "Панель переезжает на @panel: stores/spectrum-settings-panel.ts + SpectrumSettingsDialog.vue (PanelWindow, :allow-detach=\"false\") + хост в audio-page__dock-wrap (AudioPage); кнопка tune переключает panel.open; старый fixed-флайаут из TracksPanel снесён (aside+backdrop+CSS+ref+Escape+anyOverlayOpen) + мёртвая копия в AudioPage. Содержимое панели НЕ меняется",
      "status": "done",
      "dependsOn": ["SS0.1"],
      "artifacts": [
        "GnauralCore/ui/stores/spectrum-settings-panel.ts (new; mirrors stores/track-list-panel.ts)",
        "GnauralCore/ui/components/SpectrumSettingsDialog.vue (new; PanelWindow wrapper, :allow-detach=\"false\"). NOTE: «Сброс» moving to the footer slot is SS2.1's work, NOT this step's — an earlier draft listed it here while the same step also said «содержимое панели не трогаем», which contradicted itself. Resolved in favour of a PURE relocation: SS1.2 is the owner's checkpoint on the FORM, and it is only a clean checkpoint if the content is byte-for-byte what it was, so anything odd there is the form and nothing else.",
        "GnauralCore/ui/pages/AudioPage.vue (host in the shared dock-wrap + Teleport)",
        "GnauralCore/ui/components/TracksPanel.vue (remove aside/backdrop/transition :1134-1164, CSS :3269-3284, local ref+toggle/close :2649-2655, Escape branch :2828-2830, drop from anyOverlayOpen :2923; the tune button now toggles panel.open)",
        "GnauralCore/ui/pages/AudioPage.vue — WIDENED 2026-07-16 (was forbidden while WS2.1 was deferred; WS2.1 is now done): delete the SECOND, DEAD copy of the same overlay — spectrogramSettingsOpen + closeSpectrogramSettings (:1000-1005), the dead Escape branch inside the LIVE handlePlayerKeyDown (:1156-1161), and the dead scoped CSS (~:1689-1752). Verified dead, not assumed: the ref is only ever declared and set to false (no toggle survives), so the Escape guard is permanently false; the CSS classes appear nowhere in AudioPage's template (which ends at :426). Verified NOT a trap: both <style> blocks are `scoped` (AudioPage :1428, TracksPanel :2969), so AudioPage's rules never styled TracksPanel's aside — the copies are independent and both die here."
      ],
      "verify": "bun run typecheck + bun run build (MindWaveCore/ui); bun test ui server (GnauralCore) green; manual: the tune button opens the panel; all 5 dock modes + floating work, drag/resize work, geometry+open state survive F5 (mindwave-panel-spectrum-settings-*), the panel survives a tab switch (PW-D10); editing any field still repaints the spectrogram immediately; presets and «Сброс» still work; the «Отдельное окно» menu item is ABSENT (SS-D3/R5 — check live, not just typecheck); the remaining fixed flyouts (diagnostics, inspector) still pin correctly after the anyOverlayOpen change; after the AudioPage deletions the OTHER player-view tabs still work and their hotkeys (Escape included) are unaffected — handlePlayerKeyDown stays live, only a permanently-false branch leaves it",
      "startedAt": "2026-07-16",
      "finishedAt": "2026-07-16",
      "blockedReason": null,
      "evidence": [
        "Verify run: vue-tsc --noEmit 0 errors; quasar build ok; GnauralCore bun test ui server 260 pass / 0 fail (879 expect()).",
        "A TRAP THE PLAN DID NOT FORESEE, caught by checking before deleting: the flyout's CSS classes are NOT its own. .audio-page__spectrogram-settings-backdrop/-header/-title/-body are REUSED by TracksPanel's diagnostics overlay (:873-888) and the point inspector (:935-1058) — deleting the block the plan pointed at (\"CSS :3269-3284\") would have stripped the chrome off two unrelated overlays. Only three rules were actually exclusive and were the only ones removed: .audio-page__spectrogram-settings-panel (the fixed 300px flyout geometry), the .spectrogram-settings-panel-* transition pair (no <transition name> left to use it), and .audio-page__async-placeholder (TracksPanel's copy, orphaned with the async helper — AudioPage keeps its own scoped copy). Verified per-rule by grepping template usages, incl. confirming the diagnostics aside uses its own .tracks-panel__voices-panel rather than the panel rule.",
        "Dead code the compiler cannot see, removed with its user: SpectrogramSettingsPanel was the ONLY consumer of TracksPanel's createAsyncPanel + AsyncPanelLoading, which in turn were the only users of six imports (defineAsyncComponent, defineComponent, h, QSpinnerHourglass, type AsyncComponentLoader, type Component). <script setup> hands every top-level binding to the template, so vue-tsc reports none of them as unused (the same blind spot WS §8.4 documents) — found by counting references per symbol.",
        "Laziness preserved, and PROVEN rather than asserted: the panel used to be lazy inside TracksPanel (createAsyncPanel), and a plain static import in the new Dialog would have pulled it — plus SpectrogramPresetManager — into AudioPage's chunk for every visit, opened or not. SpectrumSettingsDialog keeps it behind defineAsyncComponent, and the build output confirms it: assets/SpectrogramSettingsPanel-BacbNwt2.js is still its own 13.6 KB chunk, separate from AudioPage-CRm8pA4D.js.",
        "Checked what the build cannot catch (a missing i18n key renders as the key string, silently): audio.spectrogramSettingsTitle resolves in BOTH GnauralCore locales, and all 8 panel.* chrome keys (dockMenu/dockFloat/dockLeft/dockRight/dockTop/dockBottom/close/detach) resolve in both MindWaveCore locales. audio.spectrogramSettingsClose was NOT orphaned — diagnostics and the inspector still use it (3 refs).",
        "a11y: dropped aria-controls=\"tracks-panel-settings\" from the toolbar button — the id died with the aside, and an aria-controls pointing at a nonexistent element is worse than none. aria-expanded now tracks the shared panel state.",
        "MY OWN SLIP, caught before it reached a commit and worth recording because the repo is MIXED: AudioPage.vue and TracksPanel.vue are CRLF, while every panel sibling (TrackListDialog, FileOpenDialog, their stores, SpectrogramSettingsPanel) is LF. A scripted python rewrite of AudioPage silently converted the whole file to LF, turning a ~70-line diff into a 3854-line one. Caught by reading `git diff --stat` rather than trusting the edit; restored to CRLF, and the two new files were matched to their LF siblings. Rule for this feature: check the target file's endings, per file — do not assume the repo has one convention.",
        "NOT verified by me: the visual/interaction pass — no browser driver exists in this workspace (re-checked). That check is SS1.2, an owner PAUSE by design. What IS confirmed live: the owner's dev server (Mode=dev per server/app.cfg) answers 200 on :9000 and serves the new module (SpectrumSettingsDialog.vue is compiled and delivered with a Vite HMR context), so the running app has the change."
      ]
    },
    {
      "id": "SS1.2",
      "phase": 1,
      "title": "PAUSE — owner-проверка формы (req 1): док слева/справа/сверху/снизу, floating, drag, ресайз, персист, переключение вкладок; заодно подтвердить R3 (панель больше не модальная: бэкдропа нет, как у двух других)",
      "status": "done",
      "dependsOn": ["SS1.1"],
      "artifacts": [],
      "verify": "owner drives the real app and confirms the form behaves «по аналогии» with «Открытие файлов» / «Список треков»",
      "startedAt": "2026-07-16",
      "finishedAt": "2026-07-16",
      "blockedReason": null,
      "evidence": [
        "Owner approved the form («ok», 2026-07-16) with no remarks — the dockable/floating «Параметры» behaves «по аналогии» with «Открытие файлов» / «Список треков». R3 (no backdrop/Escape, like the other two panels) accepted implicitly. Proceed to SS2.1 (blocks: tiles/accordion)."
      ]
    },
    {
      "id": "SS2.1",
      "phase": 2,
      "title": "Блоки + адаптивная раскладка (req 2-4): ПЯТЬ блоков — «Пресеты» (первый, req 5) + четыре группы, «Сброс» в слот footer (SS-D6 rev.); ResizeObserver + порог ~568px (SS-D4, req 8); плитки = grid auto-fill/minmax; аккордеон = q-expansion-item с мульти-раскрытием и персистом набора (SS-D5, req 6). Измерить реальный MIN_BLOCK на группе «Цвет» (7 полей) и записать сюда",
      "status": "done",
      "dependsOn": ["SS1.2"],
      "artifacts": [
        "GnauralCore/ui/composables/spectrogram-settings-fields.ts (new; shared Field/Group/Spin types + pure clamp + the SS-D4 layout rule layoutForWidth/SPECTRUM_TILES_THRESHOLD_PX)",
        "GnauralCore/ui/composables/spectrogram-settings-fields.test.ts (new; clamp + layoutForWidth unit tests)",
        "GnauralCore/ui/components/SpectrogramSettingsFields.vue (new; the field renderer, extracted so tiles+accordion share ONE copy)",
        "GnauralCore/ui/components/SpectrogramSettingsPresets.vue (new; the preset dropdown+manager, so «Пресеты» can be block #1)",
        "GnauralCore/ui/components/SpectrogramSettingsPanel.vue (rewritten: 5 blocks, ResizeObserver tiles/accordion, persisted open-set; groups computed kept)",
        "GnauralCore/ui/components/SpectrumSettingsDialog.vue («Сброс» in PanelWindow's footer slot + scrollbar-gutter:stable on the scroll body — SS-D6, R1)"
      ],
      "verify": "DONE — vue-tsc 0 errors; quasar build ok; bun test ui server 265 pass / 0 fail (was 260 — +5 new clamp/layout assertions). Runtime-visual part is SS2.2 (owner PAUSE — no browser driver here). What the build cannot catch was checked: all 6 t() keys resolve in both locales; the emitted panel CSS carries repeat(auto-fill,minmax(280px,1fr));gap:8px;align-items:start and the dialog CSS carries scrollbar-gutter:stable + the footer; the panel is still its own lazy chunk (15.3 KB, ResizeObserver x3 + the open-set key present); the dev server serves both new child components (HTTP 200).",
      "startedAt": "2026-07-16",
      "finishedAt": "2026-07-16",
      "blockedReason": null,
      "evidence": [
        "MEASURED MIN_BLOCK (the number SS2.1 owed): set to 280px, threshold = 2*280 + 8 gap = 568px, in composables/spectrogram-settings-fields.ts. Basis: the pre-refactor panel was a fixed 300px column and its controls (dense q-select/q-input) were comfortable in it, so 280 is the floor where a block stays usable and two + a gap fit in a ~580px window. This is a starting value, not a visual measurement — no browser driver exists here — so the owner confirms the feel at SS2.2; changing it is one constant.",
        "DECOMPOSITION (why 3 new files, not one edited v-for as the plan guessed): the two layouts are STRUCTURALLY different (accordion collapses via q-expansion-item; tiles are a fixed CSS grid), so both need to render the same block bodies. Rendering the field controls inline in both would duplicate a ~50-line control block that must stay in sync — exactly the divergent-copy hazard WS §8 documents. So the field renderer became SpectrogramSettingsFields.vue and the preset bar SpectrogramSettingsPresets.vue, each referenced once per layout as a single tag. Bonus: this is the decomposition SS3.1 needs anyway (the pure View will contain these same children).",
        "A REAL BUG my own test caught before commit: clamp(0.35, {step:0.1}) returns 0.3, not 0.4, because 0.35/0.1 === 3.4999999999999996 in IEEE-754 and rounds DOWN. clamp is verbatim from the pre-SS2.1 panel, so this is behaviour the sliders always had — my test's EXPECTATION was wrong, not the code. Fixed the assertion and added an explicit test documenting the .5-boundary FP quirk, so a later reader does not 'correct' clamp and change long-standing behaviour.",
        "R1 (tiles<->accordion flapping) attacked at the source: the switch is driven by rootEl.clientWidth (the panel root, whose width is set by PanelWindow geometry, NOT by content), and the scroll container gets scrollbar-gutter:stable so the appearance of a scrollbar does not change content width. That removes the collapsed->no-scrollbar->wider->tiles->taller->scrollbar->narrower->accordion loop by construction. If SS2.2 still shows flap at the exact boundary, hysteresis is the documented next step (plan R1).",
        "ResizeObserver guarded by `typeof ResizeObserver !== 'undefined'` (the convention of all 6 existing sites); without it the panel stays in the safe single-column accordion. Also: measure() runs once on mount before observing, so the first paint is already correct.",
        "Accordion open-set (SS-D5): multi-open (independent q-expansion-item v-models, no `group` prop), persisted to mindwave-panel-spectrum-settings-groups as a string array, default = ['presets'] (first block). localStorage read/write is best-effort (try/catch), matching the @panel state persistence.",
        "«Сброс» is in the window FOOTER slot (SS-D6), NOT in the «Пресеты» block — so collapsing every accordion block leaves Reset visible. It calls store.reset() directly (the store is the global singleton); SS3.1 turns this into a bridged action for the detached child.",
        "Content otherwise unchanged: the four groups, their 17 fields, help popovers, spinbox steppers, preset dropdown and manager are the same — only their CONTAINER (block/tile/accordion) is new. i18n needed no new keys (SS-D7): «Пресеты» reuses audio.spectrogramPresets, «Сброс» reuses audio.spectrogramReset.",
        "Line endings: all 4 new files + the 2 edited ones are LF (matching every panel sibling); checked per file after writing, not assumed.",
        "Staged only my files: GTrackView.vue also shows modified in GnauralCore but it is the owner's parallel GT11.15 work (verified: last commit 725e715, references nothing I touched) — left untouched, not staged."
      ]
    },
    {
      "id": "SS2.3",
      "phase": 2,
      "title": "Пресеты ИЗ панели в модальный диалог из тайтл-бара (owner 2026-07-16): слот titlebar-actions в PanelWindow (SS-D11), иконка bookmarks в тайтл-баре «Параметры» → SpectrogramPresetsDialog (модаль, ленивая); удалён блок пресетов (осталось 4 группы) + SpectrogramSettingsPresets.vue; дефолт открытого блока → scale. «Сброс» в футере без изменений (SS-D6 rev.2)",
      "status": "done",
      "dependsOn": ["SS2.1"],
      "artifacts": [
        "MindWaveCore/ui/src/components/panel/PanelWindow.vue (new `titlebar-actions` slot, left of the dock menu — SS-D11)",
        "GnauralCore/ui/components/SpectrogramPresetsDialog.vue (new; modal q-dialog: preset list + Save as… + Manage…, header close X)",
        "GnauralCore/ui/components/SpectrumSettingsDialog.vue (title-bar `bookmarks` button in #titlebar-actions; lazy defineAsyncComponent + mount-on-first-open of the presets dialog)",
        "GnauralCore/ui/components/SpectrogramSettingsPanel.vue (presets removed from `blocks`; blocks = the four groups; default open-set = ['scale'])",
        "GnauralCore/ui/components/SpectrogramSettingsPresets.vue (DELETED — its role moved to the modal)"
      ],
      "verify": "DONE — vue-tsc 0 errors; quasar build ok; bun test ui server 265 pass / 0 fail. Runtime-visual is folded into the SS2.2 PAUSE. What the build can't catch, checked: laziness PRESERVED and improved — SpectrogramPresetsDialog is now its OWN lazy chunk (SpectrogramPresetsDialog-*.js) carrying the preset manager, off the eager AudioPage path; the `bookmarks` trigger button IS in the eager AudioPage chunk (correct — it must show immediately); the dev server serves the new dialog + the updated PanelWindow (HTTP 200 both). No new i18n keys (SS-D7 holds): the icon tooltip/aria and the dialog title reuse audio.spectrogramPresets, the close X reuses audio.spectrogramSettingsClose.",
      "startedAt": "2026-07-16",
      "finishedAt": "2026-07-16",
      "blockedReason": null,
      "evidence": [
        "The owner's Q1 answer at SS0.1 (presets as their own block) was itself superseded here by a fuller look at the running SS2.1 result: presets belong out of the parameter-blocks area entirely, reachable from the chrome. SS-D6 is now on its second revision; both prior forms are kept as history in the decision text so a later session sees the trail rather than a silent flip.",
        "SS-D11 was necessary and is the feature's only MindWaveCore/ui change: PanelWindow owns the whole title bar, so without a slot there was nowhere to put the presets icon. The slot is backward-compatible (empty named slot renders nothing) — file-open and track-list are untouched, confirmed by their unchanged typecheck/build.",
        "A REGRESSION I caught in my own first cut, before commit: statically importing SpectrogramPresetsDialog into SpectrumSettingsDialog (which AudioPage imports eagerly) pulled the presets dialog AND the heavy preset manager into AudioPage's eager chunk — whereas before SS2.3 they were lazy inside the panel. Fixed by making the dialog a defineAsyncComponent AND guarding its mount with v-if=\"presetsEverOpened\" (a plain v-model dialog is always rendered, so the async chunk would load on mount regardless). Build confirms it is now its own chunk; net it is LAZIER than before (the manager used to load with the panel body, now only on presets-open).",
        "The presets dialog keeps the owner's earlier taste (WS-D10): a Close X in the header. Applying a preset does NOT close the modal (a modal is deliberate; leaving it open lets the user audition presets and watch the ✓/* move), unlike the old dropdown which closed on click.",
        "Line endings: the new dialog + the 3 edited files are LF (panel siblings), PanelWindow stays LF — checked per file. GTrackView.vue still shows the owner's parallel GT11.15 work; not staged."
      ]
    },
    {
      "id": "SS2.4",
      "phase": 2,
      "title": "Управление пресетами свёрнуто в форму пресетов (owner 2026-07-16, SS-D12): содержимое SpectrogramPresetManager перенесено в SpectrogramPresetsDialog; менеджер и кнопка «Управление…» удалены; три мёртвых i18n-ключа убраны из обеих локалей",
      "status": "done",
      "dependsOn": ["SS2.3"],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramPresetsDialog.vue (merged: preset list + per-preset manage actions + import/export + Save as…, header close X)",
        "GnauralCore/ui/components/SpectrogramPresetManager.vue (DELETED — body absorbed into the dialog)",
        "GnauralCore/ui/i18n/ru.json + en.json (removed spectrogramPresetManage / spectrogramPresetManager / spectrogramPresetClose)"
      ],
      "verify": "DONE — vue-tsc 0 errors; quasar build ok; bun test ui server 265 pass / 0 fail. Checked beyond the build: the dialog is still ONE lazy chunk (SpectrogramPresetsDialog-*.js, 7.2 KB, carries content_copy/exportPresets/deleteConfirm), no separate manager chunk; the 3 removed keys have 0 code refs anywhere in GnauralCore+MindWaveCore; both locales still parse and have identical key sets (443/443, no drift); dev server serves the merged dialog (HTTP 200).",
      "startedAt": "2026-07-16",
      "finishedAt": "2026-07-16",
      "blockedReason": null,
      "evidence": [
        "The management UI was moved VERBATIM (same list rows, same per-preset icons, same import/export handlers) rather than rewritten — the merge is a relocation, so behaviour is unchanged and there was nothing new to test; the existing store-action tests (SF18.1) still cover apply/save-as/rename/delete/duplicate/import/export.",
        "Interaction note: rows use explicit [Apply] buttons (the manager's model), not whole-row-click, because each row now also carries manage icons — a click target that both applies and sits under action icons would be ambiguous. The active preset shows ✓ + text-primary.",
        "Orphaned-i18n cleanup done properly: confirmed 0 code refs to the 3 keys across BOTH repos (not just the edited file) before deleting, and re-checked locale parity after (443==443, no only-in-ru / only-in-en). Leaving them would have been harmless data, but they name UI that no longer exists (a 'Manager'/'Manage…'), which is the kind of stale signal that misleads a later reader.",
        "Laziness held: a static merge could have changed nothing about chunking (the dialog was already lazy), and the build confirms it stayed one lazy chunk with the manager code now inside it — the separate SpectrogramPresetManager-*.js chunk is gone.",
        "Line endings LF on the merged dialog + both i18n files; GTrackView/SpectrogramView/WaveformView remain the owner's parallel work, not staged."
      ]
    },
    {
      "id": "SS2.5",
      "phase": 2,
      "title": "Компактный вид (owner 2026-07-16): убраны большие gap вокруг блоков; соседние плитки делят ОДНУ границу вместо двух с промежутком — gapless-grid в одном фрейме (контейнер рисует внешнюю рамку, плитка — только правую+нижнюю); аккордеон так же в один фрейм с общими разделителями; уменьшены отступы тела и полей",
      "status": "done",
      "dependsOn": ["SS2.1"],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramSettingsPanel.vue (tiles = gapless collapsed-border grid; accordion = one frame with shared dividers; smaller paddings)",
        "GnauralCore/ui/composables/spectrogram-settings-fields.ts (SPECTRUM_BLOCK_GAP_PX 8 -> 0, so the tiles threshold matches the now-gapless grid: 560px)",
        "GnauralCore/ui/components/SpectrumSettingsDialog.vue (body padding 12 -> 8)",
        "GnauralCore/ui/components/SpectrogramSettingsFields.vue (field gap 10 -> 8)"
      ],
      "verify": "DONE — vue-tsc 0 errors; quasar build ok; bun test ui server 265 pass / 0 fail (the threshold test uses SPECTRUM_TILES_THRESHOLD_PX symbolically, so 568->560 needed no test edit). Emitted CSS confirmed: --tiles is display:grid + one outer border + overflow:hidden, __tile draws only border-right+border-bottom (adjacent tiles => a single shared line, no gap), --accordion is one framed stack. Visual acceptance is the SS2.2 PAUSE.",
      "startedAt": "2026-07-16",
      "finishedAt": "2026-07-16",
      "blockedReason": null,
      "evidence": [
        "The 'one border, not two + a gap' ask is a collapsed-border grid: gap:0, the CONTAINER draws the outer frame, each tile draws only its right + bottom edge. So every interior line is exactly one border (a cell's right or bottom), and the frame closes without doubling. Same pattern vertically for the accordion (each block a border-bottom, :last-child drops it so the frame's own bottom isn't doubled).",
        "CONSCIOUS trade-off vs the old R2 choice: I removed align-items:start (which let unequal-height blocks sit ragged) — a collapsed grid needs cells to stretch to equal height per row, or the shared horizontal divider would be jagged where a short block ends. So short blocks (e.g. «Масштаб», 3 fields) now stretch to their row-mate's height, trading a little empty space for the straight single borders the owner asked for. If that empty space reads badly at SS2.2, align-items is the knob.",
        "Consistency fix, not just cosmetics: with the grid now gapless, the tiles<->accordion threshold had to drop the +gap term (SPECTRUM_BLOCK_GAP_PX 8->0 => 560px), otherwise the layout would claim it needs 568px for two blocks that actually fit at 560. The unit test is symbolic so it still passes; the number is now honest.",
        "Compactness beyond the borders: block gaps removed entirely (the frame + dividers replace them), tile padding 12->8/12/10, body padding 12->8, field gap 10->8. Nothing structural — pure spacing.",
        "Line endings LF on all four; owner's GTrackView/SpectrogramView/WaveformView untouched."
      ]
    },
    {
      "id": "SS2.6",
      "phase": 2,
      "title": "Компактность-2 (owner 2026-07-16): (1) убран зазор между внешней рамкой блока и рамкой окна — во всех режимах (док/float/detached) и в аккордеоне: padding тела диалога 8→0, снят scrollbar-gutter, убран border-radius у контейнеров; против дрожания порога добавлен гистерезис в ResizeObserver. (2) из числовых полей (spin, напр. «Усиление») убраны кнопки +/-, оставлены только нативные стрелки — они дублировали и путали",
      "status": "done",
      "dependsOn": ["SS2.5"],
      "artifacts": [
        "GnauralCore/ui/components/SpectrumSettingsDialog.vue (body padding 8->0, scrollbar-gutter removed)",
        "GnauralCore/ui/components/SpectrogramSettingsPanel.vue (tiles/accordion border-radius+overflow removed -> square flush frame; measure() gains hysteresis; imports SPECTRUM_TILES_THRESHOLD_PX instead of layoutForWidth)",
        "GnauralCore/ui/components/SpectrogramSettingsFields.vue (spin field: removed the +/- prepend/append buttons and stepField; native number arrows + @blur clamp remain)"
      ],
      "verify": "DONE — vue-tsc 0 errors; quasar build ok; bun test ui server 265 pass / 0 fail. Bundle confirmed: __body has no padding and no scrollbar-gutter; --tiles/--accordion carry a plain 1px border with NO radius; the compiled fields chunk has 0 'remove'/'add' stepper icons while help_outline stays. Dev server serves the change (HTTP 200). Visual acceptance is SS2.2.",
      "startedAt": "2026-07-16",
      "finishedAt": "2026-07-16",
      "blockedReason": null,
      "evidence": [
        "Two coupled decisions behind the 'flush' fix: (a) removing the scrollbar gutter was necessary — with it reserved, padding 0 would still leave a ~15px empty strip on the RIGHT (a gap), so it had to go for the frame to be flush on all four sides; (b) but the gutter was SS2.1's R1 flap guard, so I replaced it with hysteresis (tiles at >= threshold, back to accordion only below threshold-24px). A ~15px scrollbar delta can no longer ping-pong the layout. layoutForWidth stays as the canonical threshold (still unit-tested); measure() now uses the constant + hysteresis.",
        "border-radius removed from both containers, not just padding: a rounded block frame flush to the square window content box leaves four small corner triangles of background — themselves a 'gap'. A square frame is flush with zero gap anywhere, which is what the owner asked («зазор ... не нужен»).",
        "Tiles container KEPT its full (4-side) border rather than top+left-only: a non-full last row (3 columns, 4 blocks -> row2 has 1 tile) would otherwise leave the bottom-right open. Full container border + cells' right/bottom keeps interior lines single AND closes ragged rows. I caught this mid-edit (had briefly switched to top+left) and reverted.",
        "The +/- removal is exactly what the owner described: the spin field already renders the native number-input up/down arrows, so the custom prepend/append buttons duplicated them. Removed the buttons + the now-unused stepField; the field keeps min/max/step (the native arrows honour step) and the @blur clamp, so bounded stepping still works — nothing lost, one control instead of two. Verified no global CSS hides the native spinner (so the arrows are really there).",
        "Line endings LF on all three; owner's parallel GTrackView/SpectrogramView/WaveformView not staged."
      ]
    },
    {
      "id": "SS2.7",
      "phase": 2,
      "title": "Masonry вместо регулярной сетки (owner 2026-07-16, скриншот): блоки разной высоты в регулярной grid растягивали короткий блок до высоты соседа => много пустого места. Плитки переведены на CSS multicol (column-width) — блоки складываются в сбалансированные колонки по своей высоте, без пустот; рамки/flush из SS2.5/2.6 сохранены",
      "status": "done",
      "dependsOn": ["SS2.6"],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramSettingsPanel.vue (--tiles: display:block + column-width:280px + column-gap:0 instead of the grid; tile gets break-inside:avoid)"
      ],
      "verify": "DONE — vue-tsc 0 errors; quasar build ok; bun test ui server 265 pass / 0 fail. Bundle confirms --tiles is now display:block + column-width:280px + column-gap:0 (multicol), autoprefixed, no grid; dev server HTTP 200. Visual acceptance is SS2.2.",
      "startedAt": "2026-07-16",
      "finishedAt": "2026-07-16",
      "blockedReason": null,
      "evidence": [
        "Root cause of the empty space (owner's screenshot): SS2.5 used a regular grid with equal-height rows (align-items removed), so a row's height = its tallest block. «Масштаб» (3 fields) shared a row with «Цвет» (7 fields) and was padded out to «Цвет»'s height — the big blank under «Масштаб». align-items:start would NOT fix it (the empty space is the row height, set by the tall cell), which is why masonry is the actual answer.",
        "CSS multicol (column-width) is the no-JS masonry: with column-fill:balance (default) and no height cap, the browser packs the 4 blocks into balanced columns by natural height (Масштаб+Цвет | FFT+Резкость), so the container is ~as short as possible with no stretched blocks. Reading order becomes column-major (newspaper), acceptable.",
        "A BUG I caught before building: the base .spectrogram-settings is display:flex, and CSS multicol is IGNORED on a flex container — it only applies to a block container. So --tiles must set display:block or the columns silently never form. Added display:block to --tiles; bundle confirms it.",
        "Border/flush treatment from SS2.5/2.6 kept: container full border, each block border-right+bottom, column-gap 0. Touching blocks share one line; masonry makes those interior lines ragged where heights differ, which is inherent to masonry and expected (the trade for zero empty space). column-width == SPECTRUM_BLOCK_MIN_PX (280) so the multicol column count still matches the tiles<->accordion threshold (560).",
        "break-inside:avoid keeps each block whole across column breaks (Chromium/WebView2 supports it — the prod runtime). Line endings LF; owner's parallel files not staged."
      ]
    },
    {
      "id": "SS2.8",
      "phase": 2,
      "title": "Единый вид карточек (owner 2026-07-16, два сообщения): (1) в режиме плиток заголовок блока — как в аккордеоне: текст + горизонтальная черта под ним, но БЕЗ иконки свернуть/развернуть; (2) каждый блок — карточка с рамкой 1px (уходит рваность masonry-границ SS2.7), зазор между карточками не нужен. Оба режима сведены к одному card-стилю",
      "status": "done",
      "dependsOn": ["SS2.7"],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramSettingsPanel.vue (unified .spectrogram-settings__card + __block-header[--ruled|--static] + __card-body; tiles = static card with an always-ruled header and no chevron; accordion = the same card, collapsible, header ruled only when open)"
      ],
      "verify": "DONE — vue-tsc 0 errors; quasar build ok; bun test ui server 265 pass / 0 fail. Bundle confirms: __card has its own 1px border (no container frame), block-header--ruled adds the horizontal line, --static pads the tile header, and the accordion header rule is reached via :deep. Dev server HTTP 200. Visual acceptance is SS2.2.",
      "startedAt": "2026-07-16",
      "finishedAt": "2026-07-16",
      "blockedReason": null,
      "evidence": [
        "Two owner messages in one turn, addressed together because they describe one coherent look: (msg1) tiles titles should read like the accordion header — uppercase text + a horizontal rule under it — minus the collapse chevron; (msg2) each block should be a self-bordered 1px card so masonry has no ragged seams, and no gap between cards. Both resolve to: ONE card style shared by tiles and accordion.",
        "The ragged-seam problem SS2.7 flagged is gone by construction: SS2.5-2.7 gave each block only right+bottom borders relying on a container frame, so in masonry (unequal heights) the shared lines were incomplete. Now every card draws its OWN full border, so each is a complete rectangle regardless of its neighbours' heights. The container frame is removed (the cards are the frame). Trade: adjacent cards touch as a 2px seam (two 1px borders, gap 0 per the owner) — uniform, not ragged; if the owner wants 1px seams later, a negative-margin collapse is the lever (unreliable across multicol columns, hence not done now).",
        "The header rule is shown at the right times: ALWAYS in tiles (a static card is always 'open'), and only WHEN OPEN in the accordion (a collapsed block would show a rule under a title with nothing beneath it). Done with a conditional header-class (--ruled) bound to openSet, so no rule/body mismatch.",
        "Scoped-CSS detail handled: the tile header is in THIS component's template (plain :deep-free selectors apply), while the accordion header is rendered by q-expansion-item (child), so its rule needs :deep. Both are present; the card border reaches the q-expansion-item root because a child component's root inherits the parent's scope id.",
        "Line endings LF; owner's parallel GTrackView/SpectrogramView/WaveformView not staged."
      ]
    },
    {
      "id": "SS2.2",
      "phase": 2,
      "title": "PAUSE — owner-проверка вида: единый card-вид + заголовок с чертой без шеврона (SS2.8); masonry без пустот (SS2.7); flush + только стрелки в числовых полях (SS2.6); плитки/аккордеон, порог; иконка пресетов и единый диалог со встроенным управлением (SS2.3+SS2.4)",
      "status": "pending",
      "dependsOn": ["SS2.1", "SS2.3", "SS2.4", "SS2.5", "SS2.6", "SS2.7", "SS2.8"],
      "artifacts": [],
      "verify": "owner drives the real app; the tile/accordion rule matches req 2-4 and the threshold feels right (Q4)",
      "startedAt": null,
      "finishedAt": null,
      "blockedReason": null,
      "evidence": []
    },
    {
      "id": "SS3.1",
      "phase": 3,
      "title": "Расщепление SS-D2: SpectrogramSettingsView.vue (вся разметка фазы 2 переезжает целиком) + SpectrogramSettingsPanel.vue как in-window адаптер + applySpectrumSettingsAction (+ bun-тест на него). Поведение в главном окне 1:1, detach ещё выключен",
      "status": "pending",
      "dependsOn": ["SS2.2"],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramSettingsView.vue (new; pure view: snapshot props + one serializable `action` emit)",
        "GnauralCore/ui/components/SpectrogramSettingsPanel.vue (becomes the in-window adapter; NAME/PATH UNCHANGED — SS-D2/R6)",
        "GnauralCore/ui/composables/spectrum-settings-action.ts (new; applySpectrumSettingsAction, shared by both sides)",
        "GnauralCore/ui/tests (new unit test for applySpectrumSettingsAction, mirroring SF18.1's action-semantics tests)"
      ],
      "verify": "bun run typecheck + bun run build; bun test ui server green INCLUDING the new applySpectrumSettingsAction suite; manual: the main window behaves exactly as after SS2.2 (pure refactor — every field, preset action and «Сброс» still works and still repaints live)",
      "startedAt": null,
      "finishedAt": null,
      "blockedReason": null,
      "evidence": []
    },
    {
      "id": "SS3.2",
      "phase": 3,
      "title": "Detached «пульт»: SpectrogramSettingsRemote.vue + запись в panels[] манифеста (id 'spectrum-settings', events ['action'], titleKey audio.spectrogramSettingsTitle) + :bridge-state=\"snapshot\" + снятие allow-detach=false. Пресеты в ребёнке — «Применить» + «Сохранить как…»; «Управление…» скрыто пропом allowPresetManage=false (SS-D8 rev., req 7)",
      "status": "pending",
      "dependsOn": ["SS3.1"],
      "artifacts": [
        "GnauralCore/ui/components/SpectrogramSettingsRemote.vue (new; no store, no persistence — snapshot in, action out)",
        "GnauralCore/ui/module.ts (panels[] entry)",
        "GnauralCore/ui/components/SpectrumSettingsDialog.vue (bridge-state snapshot + panel-event -> applySpectrumSettingsAction; allow-detach enabled)"
      ],
      "verify": "bun run typecheck + bun run build; bun test ui server green; manual: «Отдельное окно» appears and opens (no «Неизвестная панель» — the PW5.7c symptom of a missing manifest entry); EDITING IN THE CHILD REPAINTS THE PARENT'S SPECTROGRAM (the whole point — SS-D3); preset apply + «Сохранить как…» work from the child and land in the parent's store, while «Управление…» is absent there but STILL PRESENT in the main window (SS-D8 rev. — the withDefaults check, since a bare boolean prop would hide it in both); the child holds no state of its own (parent F5 -> child re-adopts or self-closes per PW-D2); closing the main window closes the child; detached geometry persists",
      "startedAt": null,
      "finishedAt": null,
      "blockedReason": null,
      "evidence": []
    },
    {
      "id": "SS3.3",
      "phase": 3,
      "title": "PAUSE — owner-проверка detached-окна, В ТОМ ЧИСЛЕ в AppCore (AppMain.exe), а не только в браузере: правка в отдельном окне перерисовывает спектрограмму в главном; закрытие главного закрывает ребёнка; геометрия переживает переоткрытие",
      "status": "pending",
      "dependsOn": ["SS3.2"],
      "artifacts": [],
      "verify": "owner drives the real app in AppCore (per PW-D1 the child is a default WebView2 popup there, which is the production path — the browser is dev only)",
      "startedAt": null,
      "finishedAt": null,
      "blockedReason": null,
      "evidence": []
    }
  ],
  "log": [
    {
      "at": "2026-07-16",
      "text": "SS2.8 — owner, two messages in one turn: (1) «В режиме окна заголовки блоков должны отображаться так же как в аккордеоне с горизонтальной чертой, но без иконки свернуть/развернуть»; (2) «Пусть каждый блок имеет рамочку в 1px, тогда не будет рваных границ. Зазор между карточками не нужен». Handled together as one card style: every block is a self-bordered 1px card (no container frame), so masonry has no ragged seams (each card is a complete rectangle); no gap (2px seams where cards touch). The header shows an uppercase title + a horizontal rule — always in tiles (no chevron), only-when-open in the accordion (a collapsed block would otherwise show a rule under an empty title). Scoped-CSS detail: tile header is local (plain selectors), accordion header is q-expansion-item's (needs :deep). verify: vue-tsc 0, quasar build ok, bun 265/0; bundle confirms the card + header-rule CSS."
    },
    {
      "at": "2026-07-16",
      "text": "SS2.7 — owner (with a screenshot): «Из-за разного размера блоков сейчас образуется много пустого места ... Блоки должны располагаться не в регулярной сетке, а в сетке адаптивного размера чтобы занимать [место]». The empty space came from SS2.5's equal-height grid rows (short «Масштаб» stretched to tall «Цвет»). Fixed by switching tiles to CSS multicol (column-width) masonry: blocks pack into balanced columns by natural height, no stretching, no empty cells. Kept the SS2.5/2.6 borders (container frame + block right/bottom, gap 0, flush). Gotcha caught before building: multicol is ignored on a display:flex container, so --tiles had to set display:block. verify: vue-tsc 0, quasar build ok, bun 265/0; bundle confirms the multicol CSS."
    },
    {
      "at": "2026-07-16",
      "text": "SS2.6 — owner, two asks: (1) «Между внешей границей блока и рамкой диалога зазор тоже не нужен. Включая отдельное окно и аккордеон» -> body padding 8->0, scrollbar-gutter removed, container border-radius removed so a square frame is flush on all sides in every mode. Removing the gutter re-opened SS2.1's R1 flap risk, so I added hysteresis to the ResizeObserver (band = 24px) — that is the documented R1 fallback. (2) «...оставить только стрелочки в подобных контролах, т.е. плюс/минус вводят в заблуждение и дублируют стрелочки» -> removed the +/- prepend/append buttons from spin fields; the native number arrows + @blur clamp remain, so bounded stepping is unchanged. One gotcha handled: kept the tiles container's FULL border (not top+left) so a ragged last row still closes. verify: vue-tsc 0, quasar build ok, bun 265/0; bundle confirms no padding/gutter/radius and 0 stepper icons."
    },
    {
      "at": "2026-07-16",
      "text": "SS2.5 — owner: «Сделай форму парметров спектра более компактной, сейчас очень большие gap вокруг блоков. Если два блока находятся рядом, то между ними должна быть только одна рамочка, а не две с промежутком как сейчас». Done as a collapsed-border grid: tiles are now a gapless grid in one frame (container = outer border, tile = right+bottom only), so adjacent blocks share ONE line; the accordion is the same idea vertically. Paddings/gaps trimmed. One real consequence recorded: a collapsed grid needs equal-height cells, so I dropped align-items:start (the old R2 ragged-height choice) — short blocks now stretch to their row-mate's height; if that empty space bothers, align-items is the single knob. Also made the tiles threshold honest (gap term 8->0 => 560px). verify: vue-tsc 0, quasar build ok, bun 265/0."
    },
    {
      "at": "2026-07-16",
      "text": "SS2.4 — owner: «Оформи подформу Управления пресетами как часть основной формы пресетов. Лишняя кнопка не нужна, раз уж у нас теперь есть самостоятельное окно». So the whole SpectrogramPresetManager body (built-in/user lists, per-preset manage icons, import/export) moved into SpectrogramPresetsDialog; the manager file and the «Управление…» button are deleted (SS-D12). The merge is a verbatim relocation, so behaviour is unchanged and the store-action tests still cover it. Also removed 3 now-orphaned i18n keys (Manage/Manager/Close) from both locales — 0 code refs workspace-wide, verified before deletion and locale parity re-checked after (443==443). Still one lazy chunk (7.2 KB), no separate manager chunk. verify: vue-tsc 0, quasar build ok, bun 265/0."
    },
    {
      "at": "2026-07-16",
      "text": "SS2.3 — owner change after seeing SS2.1: «Блок пресетов нужно вынести за рамки этого диалога. В заголовке формы Параметры добавь иконку для вызова этого диалога», form = «Отдельный модальный диалог». So presets left the parameter-blocks area for a title-bar-invoked modal, and the panel is back to four blocks. This is SS-D6's SECOND revision; both earlier forms (block #1; and before that, presets-as-a-strip) are kept as history in the decision, so the trail is visible.\n\nThree things worth carrying: (1) SS-D11 — PanelWindow gained a `titlebar-actions` slot; it owns the whole title bar, so there was nowhere else to put the icon. Only MindWaveCore/ui change in the feature, backward-compatible, committed separately. (2) I caught a laziness regression in my own first cut: a static import of the presets dialog would have dragged it + the preset manager into AudioPage's EAGER chunk (they were lazy inside the panel before). Fixed with defineAsyncComponent + a v-if=\"presetsEverOpened\" mount guard (a plain v-model dialog renders on mount, defeating the async defer); build confirms a separate lazy chunk, so it is now lazier than before. (3) The default-open accordion block moved presets->scale. verify: vue-tsc 0, quasar build ok, bun 265/0."
    },
    {
      "at": "2026-07-16",
      "text": "RE-CHECK after the owner reported that another session deleted the dead island (GnauralCore 15d1960 «WS2.1 audio: delete the orphaned pre-GT2.4 tracks island from AudioPage»; WS2.1 is now `done` in the wave-settings-dialog ledger). The plan changes in one decision's REASONING, one risk closes, and SS1.1 gains a small deletion. Implementation still not started.\n\nWHAT I RE-VERIFIED RATHER THAN ASSUMED — every line citation in the plan, because another session's commits move line numbers and a plan citing wrong lines is exactly the «дезинформация» WS §8 warns about. Result: TracksPanel, SpectrogramSettingsPanel, stores/spectrogram.ts, the @panel control, module.ts and both consumer examples are ALL still accurate to the line — that session touched only AudioPage. Even the two AudioPage citations the plan still needs (dock-wrap :133-151, template end :426) survive.\n\nWHAT CHANGED: (1) R6 is CLOSED — the import at AudioPage:508 is gone and exactly one consumer imports the file workspace-wide (TracksPanel.vue:1294), so renaming SpectrogramSettingsPanel.vue is now possible and breaks nothing. (2) SS-D2 is REVISED but NOT reversed: it keeps the name because that is the PW5.7 precedent's naming (TrackListPanel is likewise the in-window adapter) — the leg it always had, and now the only one. A decision whose stated reason evaporates must have its surviving reason written down, or a later session 'fixes' it back; that is why SS-D2 now spells this out instead of quietly still saying 'preserved'.\n\nWHAT THE CLEANUP DID NOT TAKE (found by re-checking, not reported): WS2.1 removed the TRACKS island, but the AudioPage residue belonging to OUR panel survived it — spectrogramSettingsOpen + closeSpectrogramSettings (:1000-1005), an Escape branch (:1156-1161) inside the LIVE handlePlayerKeyDown (registered :1384), and the overlay CSS (~:1689-1752). All three are provably dead rather than presumed: the ref is only declared and set to false — no toggle survives — so the Escape guard is permanently false, and the CSS classes appear nowhere in the template (ends :426). I also checked the trap that would have made deleting them dangerous: both <style> blocks are `scoped` (AudioPage :1428, TracksPanel :2969), so AudioPage's rules never styled TracksPanel's live aside — the two copies are independent. SS1.1 therefore widens to delete this residue: the old rule 'do not touch AudioPage, WS2.1 is deferred' no longer exists, and leaving a dead Escape guard + dead CSS for a panel that no longer exists in that shape anywhere would recreate, in miniature, the very island that was just removed."
    },
    {
      "at": "2026-07-16",
      "text": "SS0.1 PAUSE passed — owner answered Q1-Q4 (plan §1 req 5-8; §7 is now a closed-questions table). Two decisions REVISED, two confirmed; implementation still not started, SS1.1 is next.\n\nQ1 is the one worth recording, because the answer was a HYBRID of the two options offered and NEITHER was right: «Пресеты - отдельный блок. Кнопка Сброс сама по себе отдельно в футере». I had offered (a) presets as a strip + Reset in the footer, or (b) presets as a fifth block with Reset inside it. The owner took the block from (b) and the footer from (a) — and that combination is not cosmetic: had «Сброс» lived inside the presets block, collapsing that block in the accordion would have hidden Reset with it. In the footer it is always visible and never collapses. SS-D6 now records that reason explicitly, so a later session does not 'tidy' Reset back into the block it visually belongs to.\n\nConsequences folded through both documents rather than left implicit: five blocks instead of four means the tile grid's last row is always partial and the height spread grows («Пресеты» = 1 row vs «Цвет» = 7 fields) -> R2 strengthened, with 'presets full-width on the first row' noted as a fallback for SS2.2; «Пресеты» being an ordinary View block means preset gestures ride the SAME serializable `action` as field edits (SS-D2), so no second bridge channel is needed.\n\nQ3 («Применить + «Сохранить как…»») shrinks SS3.2 usefully: the child never needs file export/import over the bridge — the dirtiest part of the remote. It becomes an allowPresetManage prop, and it MUST be declared via withDefaults(default true): an absent type-only Boolean casts to false in Vue (WS-D6 / PW5.7c — the trap that already hid the detach mode from both existing panels), so a bare prop would hide «Управление…» in the MAIN window too. That check is written into SS3.2's verify rather than trusted to memory.\n\nQ2 (multi-open + persisted) and Q4 (tiles from ~568px, 3-4 columns wider) confirmed as proposed — SS-D5/SS-D4 unchanged apart from being marked confirmed."
    },
    {
      "at": "2026-07-16",
      "text": "Plan + ledger created (owner req 2026-07-16): the spectrogram settings panel moves onto the dockable/detachable @panel control «по аналогии» with «Открытие файлов» / «Список треков», and its parameter groups become blocks — tiles when two fit side by side, a single-column accordion when they do not.\n\nThe inventory turned three facts into the plan's spine, each verified by reading code rather than inferred:\n\n1. DETACH CANNOT BE NAIVE, AND FAILS SILENTLY IF IT IS (SS-D3). There is no 'storage' event listener anywhere in GnauralCore/ui, and stores/spectrogram.ts:121-123 deep-watches three localStorage keys. A detached child with its own Pinia (every window is its own JS realm — PW-D11) would edit its own store, never repaint the PARENT's spectrogram, and race the parent on the same keys. The asymmetry vs PW-D11 is what matters: the naive detached track list showed an EMPTY list and advertised its brokenness; this panel would look perfectly alive and simply do nothing. Silent failures are the ones that ship, so detach is phased behind the PW5.7 remote («пульт»), with :allow-detach=\"false\" passed EXPLICITLY until then (absent type-only Boolean casts to false — the PW5.7c trap that already hid the mode from both existing panels).\n\n2. THE FILE MAY NOT BE RENAMED (SS-D2/R6). AudioPage.vue:508 still imports SpectrogramSettingsPanel.vue from the dead GT2.4 island; the template (ends :426) never renders it, so it is inert at runtime but live to vue-tsc. Renaming would break typecheck and drag WS2.1 — a cleanup the OWNER DEFERRED — into this feature. So the new file is …View.vue; the old name keeps its path and becomes the in-window adapter.\n\n3. req 2 IS CHEAP, BECAUSE THE PANEL IS DATA-DRIVEN. The groups are a computed<Group[]> (:229-280) rendered by ONE v-for (:37) — 'every group becomes a block' is one v-for + CSS, not 17 fields rewritten.\n\nHosting is likewise not a coin flip: PW-D9 kept «Список треков» inside TracksPanel only because gtracks is a per-call factory; our store is a global Pinia singleton, so that constraint is gone, while PW-D10 (no keep-alive -> TracksPanel dies on tab switch) still applies -> host in AudioPage's shared dock-wrap, which is what makes «по аналогии» literally true. And TracksPanel.vue:2921-2922 already records the cleanup SS1.1 owes: a PanelWindow owns its positioning, so the panel must leave anyOverlayOpen/--tp-*, exactly as PW5.2 did.\n\nThe tiles<->accordion switch needs JS (ResizeObserver, guarded like all 6 existing sites), because it is STRUCTURAL — accordion blocks collapse, tiles do not — so CSS @container cannot express it. Its real risk is not the threshold but FLAPPING at it: collapsed accordion -> no scrollbar -> wider -> tiles -> taller -> scrollbar -> narrower -> accordion (R1); mitigated by measuring a container whose width does not depend on content, scrollbar-gutter: stable, and hysteresis if needed.\n\nFour questions are left OPEN for the owner rather than silently decided (§7): Q1 presets/«Сброс» — strip+footer or a fifth block; Q2 accordion — multi-open+persisted or exactly one; Q3 detached preset manager — full or apply+save-as only (this one materially sizes SS3.2); Q4 the tile threshold and 3-4 columns on a wide dock. Implementation not started — owner review of the plan, then SS1.1."
    }
  ]
}
