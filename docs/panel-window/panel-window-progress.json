{
  "schemaVersion": 1,
  "plan": "panel-window",
  "planDoc": "docs/panel-window/panel-window-plan.md",
  "title": "panel-window: universal panel control (floating / docked / detached OS window); first consumer = file-open panel",
  "status": "in-progress",
  "createdAt": "2026-07-13T00:00:00Z",
  "updatedAt": "2026-07-13T16:50:00Z",
  "decisions": [
    {
      "id": "PW-D1",
      "text": "Detached mechanism = window.open() of the same SPA, NO second AppCore process (answers owner's question, 2026-07-13). AppCore (c:\\projects\\Games\\AppCore, FreePascal) hosts one WebView2 window via the webview/webview library (libwebview.dll); that library does NOT subscribe to NewWindowRequested (verified by grep over webview/core) -> WebView2's DEFAULT behavior applies: the runtime itself creates a child top-level popup window for window.open() — same browser process, same profile, same origin (shared localStorage + BroadcastChannel), and it dies with the host process. The child window opens '#/panel/<panelId>' (hash router) resolved against the current origin, so the SAME built SPA serves both windows in dev (quasar dev, plain browser popup) and prod (AppCore). Zero Pascal changes. A second AppCore process was REJECTED as heavy: AppMain.exe is built around launching bun + one window; it would need a window-only config mode plus a parent-PID watchdog, all in another repo. Fallback: if window.open returns null (popup blocker), stay floating and show a Quasar Notify."
    },
    {
      "id": "PW-D2",
      "text": "Child-depends-on-parent (owner req. 2) is enforced on three levels: (a) the parent's pagehide handler broadcasts 'parent-gone'; the child then waits a grace period (~5s) for a fresh 'parent-hello' — this SURVIVES F5/hot-reload of the main window (the reloaded parent re-adopts the child) — and self-closes otherwise (window.close() is permitted for script-opened windows); (b) the parent heartbeats over the BroadcastChannel every ~2s; child silence-timeout (~6-8s) self-close covers a crashed/killed main tab where pagehide never fired; (c) the platform itself: when the AppCore host process exits, the WebView2 browser process and all its popup windows die. No server involvement."
    },
    {
      "id": "PW-D3",
      "text": "Universal control (owner req. 3) = PanelWindow.vue + usePanelWindowState(panelId, opts). Generic state {open, mode: 'floating'|'left'|'right'|'top'|'bottom'|'detached', floatRect, dockSize, prevMode} persisted per panel under 'mindwave-panel-<id>-*' localStorage keys; opts lets a panel pass LEGACY key names so the file-open panel keeps its existing 'mindwave-fs-browser-*' keys (windowMode/dockSize/float/open) — backward compatible, no migration. The window chrome currently inlined in FileOpenDialog.vue moves into PanelWindow: titlebar (icon + title + drag), the collapsed dock-menu button (gains an 'Отдельное окно' item), close button, float corner-resizer and dock edge-resizer, plus the floating/docked mode classes and geometry style. Panel content and footer are slots. The fs-browser store keeps only browser domain state; its window-model fields move behind the shared composable."
    },
    {
      "id": "PW-D4",
      "text": "Child-window content = a top-level route '/panel/:panelId' OUTSIDE MainLayout (no nav/header) rendering PanelHostPage.vue, which resolves the panel component from a PANEL REGISTRY contributed via module manifests — gnauralModule gains panels: [{id:'file-open', component, titleKey, icon}], collected in MindWaveCore/ui/src/modules/index.ts exactly like routes/settingsTabs. To make the same content usable in both windows, FileOpenDialog.vue splits into: PanelWindow (chrome, main window only) + FileOpenPanel.vue (body + footer: the browser UI, emits open/close) hosted by both FileOpenDialog-in-main and PanelHostPage-in-child. PanelHostPage sets document.title from titleKey so the OS window gets a proper caption."
    },
    {
      "id": "PW-D5",
      "text": "Parent<->child bridge = BroadcastChannel('mw-panel:<id>') — same-origin is guaranteed by PW-D1. Message protocol: parent -> 'parent-hello' (on open + on reload), 'heartbeat', 'parent-gone' (pagehide), 'close-panel'; child -> 'child-ready', 'child-closed' (its own pagehide), 'panel-event' {name, payload}. Composables usePanelBridgeParent(id)/usePanelBridgeChild(id) own channel lifecycle + timers. Panel events carry domain actions: the file-open panel emits 'open' {path, kind}; the parent side re-emits it to AudioPage.handleExternalFileOpen — identical code path to an in-window open. Pinia stores are per-window instances (the child is a separate app instance); shared localStorage keeps preferences/favorites consistent, and navigation state is intentionally independent per window."
    },
    {
      "id": "PW-D6",
      "text": "Detached lifecycle: choosing 'Отдельное окно' stores prevMode=current mode, sets mode='detached', calls window.open('#/panel/<id>', 'mw-panel-<id>', features from the persisted detached rect) and stops rendering the panel content in the main window (PanelWindow renders nothing while detached; the dock reflow releases the edge). User closes the CHILD window -> 'child-closed' -> panel returns to prevMode in the main window (panel stays open). Closing from the MAIN side (close-panel or open=false) closes the child too. On app start with open && mode='detached', auto-reopen is attempted ONCE (WebView2 default popup may be allowed without a user gesture; a browser blocker may return null) -> on failure fall back to floating + Notify (per PW-D1)."
    },
    {
      "id": "PW-D7",
      "text": "Detached window geometry persists like float/dock geometry: the child tracks resize events and polls screenX/screenY on its heartbeat tick (there is no move event) and writes 'mindwave-panel-<id>-detached-rect' to the shared localStorage; the next detach/auto-reopen passes width/height/left/top window features from it. Best-effort: if the platform ignores position features, the window still opens at its default spot."
    },
    {
      "id": "PW-D8",
      "text": "Code placement: the universal control lives in the core repo — MindWaveCore/ui/src/components/panel/ (PanelWindow.vue, use-panel-window.ts, use-panel-bridge.ts, panel-registry.ts types) — exposed to module repos via a new '@panel' alias in quasar.config.ts + tsconfig.json paths, mirroring the existing '@protocol' alias. Module repos (GnauralCore) import via the alias, never via ../../ relative paths into another repo. Commits: MindWaveCore (core control, router, modules/index, aliases, docs) and GnauralCore (FileOpenDialog split, module manifest, store slimming) are staged separately per repo per the multi-repo rule."
    }
  ],
  "steps": [
    {
      "id": "PW0.1",
      "phase": 0,
      "title": "Plan + ledger authored (this file + panel-window-plan.md)",
      "status": "done",
      "dependsOn": [],
      "artifacts": [
        "docs/panel-window/panel-window-plan.md (owner reqs verbatim, mechanism recommendation PW-D1, architecture, phases)",
        "docs/panel-window/panel-window-progress.json (decisions PW-D1..PW-D8, steps)"
      ],
      "verify": "owner reads the plan; esp. PW-D1 (window.open instead of a second AppCore) matches the owner's intent",
      "startedAt": "2026-07-13T00:00:00Z",
      "finishedAt": "2026-07-13T00:00:00Z",
      "blockedReason": null,
      "evidence": []
    },
    {
      "id": "PW1.1",
      "phase": 1,
      "title": "Extract usePanelWindowState + PanelWindow.vue (floating/docked chrome) into MindWaveCore/ui/src/components/panel/ + '@panel' alias",
      "status": "done",
      "dependsOn": [
        "PW0.1"
      ],
      "artifacts": [
        "MindWaveCore/ui/src/components/panel/use-panel-window.ts: reactive per-panel {open, mode, prevMode, dockSize, floatRect} + setMode/setDockSize/setFloatRect, per-field localStorage persist, per-panelId instance cache; opts.storageKeys for legacy key names; PanelMode includes 'detached' (chrome renders nothing in that mode until PW3.1)",
        "MindWaveCore/ui/src/components/panel/PanelWindow.vue: titlebar (icon/title/drag), collapsed dock-menu (floating/left/right/top/bottom; detached item comes in PW3.1), close, float corner-resizer + dock edge-resizer, mode classes/geometry styles; #default + #footer slots; keydown reaches the root via attrs fallthrough (tabindex=0)",
        "quasar.config.ts (build.alias) + tsconfig.json (paths '@panel/*'): '@panel' alias (PW-D8)",
        "core i18n en/ru: 'panel' section (dockMenu/dockFloat/dockLeft/dockRight/dockTop/dockBottom/detach/close) — wording mirrors the fsBrowser dock labels"
      ],
      "verify": "bun run typecheck + build in MindWaveCore/ui",
      "startedAt": "2026-07-13T00:00:00Z",
      "finishedAt": "2026-07-13T00:00:00Z",
      "blockedReason": null,
      "evidence": [
        "vue-tsc --noEmit: 0 errors; quasar build: success (2026-07-13)"
      ]
    },
    {
      "id": "PW1.2",
      "phase": 1,
      "title": "FileOpenDialog migrates onto PanelWindow: split into chrome (PanelWindow) + FileOpenPanel.vue content; behavior 1:1",
      "status": "done",
      "dependsOn": [
        "PW1.1"
      ],
      "artifacts": [
        "GnauralCore/ui/components/FileOpenPanel.vue: panel content (unavailable banner, accordion/two-pane body, footer, selection/activate/favorites/init-on-mount, navigateToDir tree-reveal); emits open/close, no window logic",
        "GnauralCore/ui/components/FileOpenDialog.vue: thin wrapper = PanelWindow (@panel) + FileOpenPanel; floating-only auto-close after open; Ctrl-S filter shortcut via attrs-fallthrough keydown",
        "GnauralCore/ui/stores/file-open-panel.ts: useFileOpenPanelState(FILE_OPEN_PANEL_ID) with legacy mindwave-fs-browser-* keys (PW-D3, no migration)",
        "fs-browser store slimmed: FsWindowMode/FsFloatRect/windowMode/dockSize/floatRect/open + setters + their persist keys removed; FsEntryList isColumn reads panel.mode",
        "AudioPage: filePanel = useFileOpenPanelState(); v-if/teleport/dockWrapClass/openFileDialog on the panel state; v-model dropped",
        "GnauralCore i18n en/ru: unused fsBrowser dock* labels removed (chrome uses core panel.* keys)"
      ],
      "verify": "typecheck+build; manual: float drag/resize, all 4 dock sides + resize, open-state persists across reload — identical to before",
      "startedAt": "2026-07-13T12:30:00Z",
      "finishedAt": "2026-07-13T13:00:00Z",
      "blockedReason": null,
      "evidence": [
        "vue-tsc --noEmit: 0 errors; quasar build: success (2026-07-13)",
        "GnauralCore commit 3c4c269"
      ]
    },
    {
      "id": "PW2.1",
      "phase": 2,
      "title": "Child-window infrastructure: /panel/:panelId top-level route + PanelHostPage + panels[] in module manifests",
      "status": "done",
      "dependsOn": [
        "PW1.2"
      ],
      "artifacts": [
        "@panel/panel-registry.ts: PanelContribution {id, titleKey, icon, lazy component, events[]}",
        "modules/index.ts: optional panels[] on CoreModuleDescriptor + modulePanels aggregate",
        "routes.ts: top-level /panel/:panelId OUTSIDE MainLayout -> ui/pages/PanelHostPage.vue (page-loaders loadPanelHostPage)",
        "PanelHostPage.vue: resolves the content component by route param, full-window flex render, document.title from titleKey (locale-aware), close -> window.close(), declared events stubbed until PW2.2; panel.unknown banner for bad ids",
        "GnauralCore module.ts: panels [{id file-open, titleKey fsBrowser.title, icon folder_open, lazy FileOpenPanel, events [open]}]"
      ],
      "verify": "typecheck+build; manual: open '#/panel/file-open' directly in a browser tab -> the file panel renders full-window and browses",
      "startedAt": "2026-07-13T13:10:00Z",
      "finishedAt": "2026-07-13T13:40:00Z",
      "blockedReason": null,
      "evidence": [
        "vue-tsc --noEmit: 0 errors; quasar build: success; PanelHostPage-*.js + FileOpenPanel-*.js chunks present in server/public/assets (2026-07-13)",
        "commits: MindWaveCore 286d46c, GnauralCore b2ae516"
      ]
    },
    {
      "id": "PW2.2",
      "phase": 2,
      "title": "BroadcastChannel bridge: usePanelBridgeParent/Child (hello/heartbeat/parent-gone/child-ready/child-closed/panel-event/close-panel) + child self-close",
      "status": "done",
      "dependsOn": [
        "PW2.1"
      ],
      "artifacts": [
        "@panel/use-panel-bridge.ts (PW-D5): createPanelBridgeParent (hello on create + on child-ready, 2s heartbeat, parent-gone on pagehide, requestClose) and createPanelBridgeChild (child-ready, child-closed on pagehide, sendEvent panel-event, obeys close-panel)",
        "child self-close watchdog (PW-D2): parent-gone + 5s grace (F5 of the main window re-adopts in time via the new parent-hello) OR 7s heartbeat silence after adoption; a never-adopted (hand-opened debug) tab is left alone",
        "PanelHostPage: creates the child bridge per window, forwards the declared panel events over it, disposes on unmount; close still = window.close() (pagehide notifies the parent)"
      ],
      "verify": "typecheck+build; manual: open child tab, kill parent tab -> child closes itself after timeout; F5 parent -> child stays",
      "startedAt": "2026-07-13T13:50:00Z",
      "finishedAt": "2026-07-13T14:10:00Z",
      "blockedReason": null,
      "evidence": [
        "vue-tsc --noEmit: 0 errors; quasar build: success (2026-07-13)",
        "commit: MindWaveCore a1e5ee0",
        "NOTE: parent side is implemented but wired only in PW3.1 (detach opens the child) — the kill-parent/F5-parent scenarios are manually verified there"
      ]
    },
    {
      "id": "PW3.1",
      "phase": 3,
      "title": "Detached mode in PanelWindow: dock-menu item, window.open, prevMode return, main-side close, popup-blocked fallback",
      "status": "done",
      "dependsOn": [
        "PW2.2"
      ],
      "artifacts": [
        "PanelWindow: detached item in the mode menu (open_in_new, panel.detach); onModeSelect opens the child FIRST inside the click user-activation (#/panel/<id>, named mw-panel-<id>, sized from floatRect) and flips the mode only on success — blocked popup = stay put + Notify panel.detachBlocked (PW-D1 fallback)",
        "parent bridge in PanelWindow: onChildClosed -> return to prevMode (1s debounce cancelled by child-ready so a child F5 does not yank the panel back); onPanelEvent -> re-emitted as @panel-event to the host",
        "open=false while detached -> requestClose() (main-side close, PW-D6); onBeforeUnmount -> clean requestClose, mode/open persist so remount reopens",
        "onMounted auto-reopen of a persisted detached panel with in-window fallback + Notify (PW-D6)"
      ],
      "verify": "typecheck+build; manual: detach -> OS window opens, main reflows; close child -> panel returns docked/floating; close main -> child dies",
      "startedAt": "2026-07-13T14:20:00Z",
      "finishedAt": "2026-07-13T14:50:00Z",
      "blockedReason": null,
      "evidence": [
        "vue-tsc 0 errors; quasar build ok (2026-07-13)",
        "commit MindWaveCore 70b4237"
      ]
    },
    {
      "id": "PW3.2",
      "phase": 3,
      "title": "File-open events over the bridge: selecting a file in the detached window opens it in the main editor; auto-reopen on start",
      "status": "done",
      "dependsOn": [
        "PW3.1"
      ],
      "artifacts": [
        "FileOpenDialog @panel-event handler: bridged open {path, kind} goes through the same onOpen path as an in-window pick -> AudioPage.handleExternalFileOpen; detached keeps the panel open (like docked)",
        "startup auto-reopen lives in PanelWindow onMounted (PW3.1) — fires when the hosting page mounts with a persisted open+detached panel"
      ],
      "verify": "typecheck+build; manual: pick a file in the detached window -> it loads in the main editor; restart with detached open -> window reappears (or falls back with Notify)",
      "startedAt": "2026-07-13T14:50:00Z",
      "finishedAt": "2026-07-13T15:00:00Z",
      "blockedReason": null,
      "evidence": [
        "vue-tsc 0 errors; quasar build ok (2026-07-13)",
        "commit GnauralCore 3f5dc2a"
      ]
    },
    {
      "id": "PW4.1",
      "phase": 4,
      "title": "Detached geometry persist + polish (document.title, focus existing child instead of duplicates)",
      "status": "todo",
      "dependsOn": [
        "PW3.2"
      ],
      "artifacts": [
        "child tracks resize + polls screenX/Y on heartbeat, writes 'mindwave-panel-<id>-detached-rect' (PW-D7); open features use it",
        "re-detach while a child exists focuses it (named window target) instead of spawning another"
      ],
      "verify": "typecheck+build; manual: move/resize child, close+reopen -> same place/size",
      "startedAt": null,
      "finishedAt": null,
      "blockedReason": null,
      "evidence": []
    },
    {
      "id": "PW4.2",
      "phase": 4,
      "title": "AppCore verification: default WebView2 popup behavior under AppMain.exe (incl. owner req. 6: NO address bar in the child window)",
      "status": "in-progress",
      "dependsOn": [
        "PW4.1"
      ],
      "artifacts": [
        "manual run of AppMain.exe: detach opens a popup, closes with the main window, geometry restores, file-open round-trips",
        "owner req. 6: verify the default popup has NO address bar (expected: a bare WebView2 window with just a title bar). In a plain dev browser the bar cannot be hidden (anti-spoofing; location=no is ignored) — dev-only limitation, not a prod concern",
        "if the default popup turns out crippled (address bar present / features ignored): record findings + decide (owner) whether a minimal AppCore NewWindowRequested handler (own chromeless window) or plan B (second process) is needed"
      ],
      "verify": "owner checkpoint on the real AppCore build",
      "startedAt": "2026-07-13T15:30:00Z",
      "finishedAt": null,
      "blockedReason": null,
      "evidence": [
        "owner verified in AppMain.exe (2026-07-13): the WebView2 DEFAULT popup DOES show an address bar — the PW-D1 assumption (bare title-bar-only window) is wrong on this point; owner req. 6 (no address bar) is NOT met by the default popup -> plan B triggered",
        "finding: AppCore vendored webview/ is gitignored (pristine upstream, 0 tracked files) — a NewWindowRequested patch must be pinned either by committing the patched backend or by keeping a .patch applied in build_webview.bat (owner call)",
        "assessment: a win32_edge.hh add_NewWindowRequested handler (own chromeless Win32 window + CreateCoreWebView2Controller from the SAME environment, put_NewWindow + Handled, WindowFeatures size/pos, WM_SIZE bounds, WindowCloseRequested -> DestroyWindow, DocumentTitleChanged -> caption) keeps the same profile/browser process, so BroadcastChannel/localStorage/close-with-host all keep working and NO UI code changes; a second AppCore process would BREAK BroadcastChannel+localStorage across windows (separate WebView2 environments) and require a WS-based bridge redesign",
        "plan B IMPLEMENTED (2026-07-13): AppCore vendored-webview patch (win32_edge.hh NewWindowRequested -> bare native popup from the same environment; no address bar, document.title caption, opener icons, window.close support, default-popup fallback on early failure). AppCore repo: docs/popup-window/ (plan+ledger POP1.1 done, commits ccd1d0c + 9d00d7c; patch pinned on nested-repo branch appcore-patches b937913 + exported .patch). libwebview.dll + AppMain.exe rebuilt; MindWaveCore/server copy was blocked by the running exe — ExeAutoUpdate swaps binaries on next launch. Owner verification pending (restart MindWave, detach)."
      ]
    }
  ]
}
