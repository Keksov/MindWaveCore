{
  "schemaVersion": 1,
  "plan": "panel-window",
  "planDoc": "docs/panel-window/panel-window-plan.md",
  "title": "panel-window: universal panel control (floating / docked / detached OS window); first consumer = file-open panel",
  "status": "in-progress",
  "createdAt": "2026-07-13T00:00:00Z",
  "updatedAt": "2026-07-14T18:45:00Z",
  "decisions": [
    {
      "id": "PW-D1",
      "text": "Detached mechanism = window.open() of the same SPA, NO second AppCore process (answers owner's question, 2026-07-13). AppCore (c:\\projects\\Games\\AppCore, FreePascal) hosts one WebView2 window via the webview/webview library (libwebview.dll); that library does NOT subscribe to NewWindowRequested (verified by grep over webview/core) -> WebView2's DEFAULT behavior applies: the runtime itself creates a child top-level popup window for window.open() — same browser process, same profile, same origin (shared localStorage + BroadcastChannel), and it dies with the host process. The child window opens '#/panel/<panelId>' (hash router) resolved against the current origin, so the SAME built SPA serves both windows in dev (quasar dev, plain browser popup) and prod (AppCore). Zero Pascal changes. A second AppCore process was REJECTED as heavy: AppMain.exe is built around launching bun + one window; it would need a window-only config mode plus a parent-PID watchdog, all in another repo. Fallback: if window.open returns null (popup blocker), stay floating and show a Quasar Notify."
    },
    {
      "id": "PW-D2",
      "text": "Child-depends-on-parent (owner req. 2) is enforced on three levels: (a) the parent's pagehide handler broadcasts 'parent-gone'; the child then waits a grace period (~5s) for a fresh 'parent-hello' — this SURVIVES F5/hot-reload of the main window (the reloaded parent re-adopts the child) — and self-closes otherwise (window.close() is permitted for script-opened windows); (b) the parent heartbeats over the BroadcastChannel every ~2s; child silence-timeout (~6-8s) self-close covers a crashed/killed main tab where pagehide never fired; (c) the platform itself: when the AppCore host process exits, the WebView2 browser process and all its popup windows die. No server involvement."
    },
    {
      "id": "PW-D3",
      "text": "Universal control (owner req. 3) = PanelWindow.vue + usePanelWindowState(panelId, opts). Generic state {open, mode: 'floating'|'left'|'right'|'top'|'bottom'|'detached', floatRect, dockSize, prevMode} persisted per panel under 'mindwave-panel-<id>-*' localStorage keys; opts lets a panel pass LEGACY key names so the file-open panel keeps its existing 'mindwave-fs-browser-*' keys (windowMode/dockSize/float/open) — backward compatible, no migration. The window chrome currently inlined in FileOpenDialog.vue moves into PanelWindow: titlebar (icon + title + drag), the collapsed dock-menu button (gains an 'Отдельное окно' item), close button, float corner-resizer and dock edge-resizer, plus the floating/docked mode classes and geometry style. Panel content and footer are slots. The fs-browser store keeps only browser domain state; its window-model fields move behind the shared composable."
    },
    {
      "id": "PW-D4",
      "text": "Child-window content = a top-level route '/panel/:panelId' OUTSIDE MainLayout (no nav/header) rendering PanelHostPage.vue, which resolves the panel component from a PANEL REGISTRY contributed via module manifests — gnauralModule gains panels: [{id:'file-open', component, titleKey, icon}], collected in MindWaveCore/ui/src/modules/index.ts exactly like routes/settingsTabs. To make the same content usable in both windows, FileOpenDialog.vue splits into: PanelWindow (chrome, main window only) + FileOpenPanel.vue (body + footer: the browser UI, emits open/close) hosted by both FileOpenDialog-in-main and PanelHostPage-in-child. PanelHostPage sets document.title from titleKey so the OS window gets a proper caption."
    },
    {
      "id": "PW-D5",
      "text": "Parent<->child bridge = BroadcastChannel('mw-panel:<id>') — same-origin is guaranteed by PW-D1. Message protocol: parent -> 'parent-hello' (on open + on reload), 'heartbeat', 'parent-gone' (pagehide), 'close-panel'; child -> 'child-ready', 'child-closed' (its own pagehide), 'panel-event' {name, payload}. Composables usePanelBridgeParent(id)/usePanelBridgeChild(id) own channel lifecycle + timers. Panel events carry domain actions: the file-open panel emits 'open' {path, kind}; the parent side re-emits it to AudioPage.handleExternalFileOpen — identical code path to an in-window open. Pinia stores are per-window instances (the child is a separate app instance); shared localStorage keeps preferences/favorites consistent, and navigation state is intentionally independent per window."
    },
    {
      "id": "PW-D6",
      "text": "Detached lifecycle: choosing 'Отдельное окно' stores prevMode=current mode, sets mode='detached', calls window.open('#/panel/<id>', 'mw-panel-<id>', features from the persisted detached rect) and stops rendering the panel content in the main window (PanelWindow renders nothing while detached; the dock reflow releases the edge). User closes the CHILD window -> 'child-closed' -> panel returns to prevMode in the main window (panel stays open). Closing from the MAIN side (close-panel or open=false) closes the child too. On app start with open && mode='detached', auto-reopen is attempted ONCE (WebView2 default popup may be allowed without a user gesture; a browser blocker may return null) -> on failure fall back to floating + Notify (per PW-D1)."
    },
    {
      "id": "PW-D7",
      "text": "Detached window geometry persists like float/dock geometry: the child tracks resize events and polls screenX/screenY on its heartbeat tick (there is no move event) and writes 'mindwave-panel-<id>-detached-rect' to the shared localStorage; the next detach/auto-reopen passes width/height/left/top window features from it. Best-effort: if the platform ignores position features, the window still opens at its default spot."
    },
    {
      "id": "PW-D8",
      "text": "Code placement: the universal control lives in the core repo — MindWaveCore/ui/src/components/panel/ (PanelWindow.vue, use-panel-window.ts, use-panel-bridge.ts, panel-registry.ts types) — exposed to module repos via a new '@panel' alias in quasar.config.ts + tsconfig.json paths, mirroring the existing '@protocol' alias. Module repos (GnauralCore) import via the alias, never via ../../ relative paths into another repo. Commits: MindWaveCore (core control, router, modules/index, aliases, docs) and GnauralCore (FileOpenDialog split, module manifest, store slimming) are staged separately per repo per the multi-repo rule."
    },
    {
      "id": "PW-D9",
      "text": "Фаза 5 — «Список треков» (renamed from «Голоса расписания» in PW5.1) is the SECOND consumer of the @panel control. PLACEMENT: the panel is hosted INSIDE TracksPanel.vue, NOT in AudioPage like file-open, because gtracks = useGtrackLanes(schedule, filePath) is a per-call factory created in TracksPanel setup (not a singleton Pinia store) — hosting the PanelWindow there lets floating+docked share the exact same reactive gtrack/voice state with zero plumbing; hosting elsewhere would spawn a second useGtrackLanes instance with divergent in-memory state. TOOLBAR: the toggle button stays in TracksPanel own header (audio-page__spectrogram-header — the toolbar the owner meant), moved to leftmost BEFORE the zoom_in / Zoom-plus button and un-gated (drop v-if displayMode==='gnaural') so it is always visible incl. plain wav/flac. LIST ROWS: add Общая-волна / Общий-спектр rows reusing existing i18n tracksOverallWave/tracksOverallSpectrum, name + a visibility eye — names-only rows, NOT the graphs (owner 2026-07-14: 'речь идёт только о СПИСКЕ треков, а не о графике'). DETACHED (PW5.5, owner checkpoint, deferred): unlike file-open — which only needs the filesystem, re-fetchable by the child over HTTP — the track list is driven by the in-memory loaded schedule + spectrogram analysis, which a detached child app instance does NOT have (separate Pinia, no loaded file), so a detached window shows an empty voice list until a snapshot is bridged or the file is re-analyzed child-side. Float+dock deliver the value immediately; detached is a separate decision."
    },
    {
      "id": "PW-D10",
      "text": "PW5.6 (owner 2026-07-14): «Список треков» must dock at the FULL editor height, in the SAME dock as «Открыть файл» — i.e. hosted at the AudioPage level, NOT inside the Треки tab (PW5.2's placement). The player-view q-tab-panels have NO keep-alive, so TracksPanel is destroyed on every tab switch; a panel hosted/teleported from there cannot persist cross-tab. Owner picked 'like Открыть файл (cross-tab)' over a lighter tab-scoped teleport, so the list content is EXTRACTED into a standalone TrackListPanel.vue hosted by AudioPage beside FileOpenDialog. Enablers: PW5.6a — gtracks becomes a shared singleton useSharedGtrackLanes() (detached effectScope; useGtrackLanes has no lifecycle hooks) so the tab + the panel edit ONE instance. PW5.6b — 'save unsaved curve edits before a voice-patch reload' moves into gtracks.saveEdits() and a central guard in AudioPage.applyScheduleVoiceStatePatches (covers all voice-patch callers), so muting from the panel is safe even with the tab unmounted; useAudioStore is read at CALL time to keep the composable Pinia-free at creation (unit tests). PW5.6c — extract TrackListPanel + TrackListDialog (PanelWindow wrapper), host in the AudioPage dock-wrap, emit patch-voice-state/-batch, REVERT PW5.2's in-TracksPanel dock-wrap/dock-inner + root-scroll move, keep the toolbar button toggling the shared tracksListPanel.open. TracksPanel keeps its own saveGtrackEdits for the explicit save button (small duplication with gtracks.saveEdits — flagged, dedupe later)."
    },
    {
      "id": "PW-D11",
      "text": "PW5.6c (owner found 2026-07-14): detached (Отдельное окно) for «Список треков» is UNSOUND as an independent app instance. NOT about processes — per PW-D1 the popup is a lightweight same-process WebView2 window (shared localStorage/BroadcastChannel). 'Separate app instance' means the JS level: every window is its own JS realm, so the popup re-executes the SPA bundle — its own Vue runtime copy, own Pinia, own composables (PW-D5 fixed this for file-open already). Direct window.opener sharing of the parent's gtracks does not work: reactive proxies created by the parent's Vue copy register dependencies in the PARENT effect tracker, so the child's components never subscribe (no re-renders), and all references die on parent F5 — hence the event bridge. The child window (window.open '#/panel/track-list') is a separate Vue/Pinia app: (a) it has no loaded gnaural file — the app deliberately does NOT restore the last file on boot (stores/audio.ts SF20) — so the voice list is empty; (b) its own useSharedGtrackLanes() singleton writes the SAME per-file localStorage keys (mindwave-gtrack-*) as the main window, so structural edits (mode/visibility/in-mix/merge/spread) in the two windows clobber each other; (c) only voice mutes could bridge back (patch-voice-state), the rest can't live-sync. The bug surfaced because the detached menu item was exposed without the panel being registered in module.ts -> 'Неизвестная панель: track-list'. Fix: PanelWindow gains an allowDetach prop (default true; file-open keeps detach) and TrackListDialog sets it false + clears any stuck persisted detached mode. A TRUE detached «Список треков» would need a remote-control redesign — the child renders from a snapshot pushed by the parent and emits EVERY action over the bridge to the parent's authoritative gtracks (no child-side gtracks/persistence) — owner 2026-07-14: approved, registered as PW5.7."
    }
  ],
  "steps": [
    {
      "id": "PW0.1",
      "phase": 0,
      "title": "Plan + ledger authored (this file + panel-window-plan.md)",
      "status": "done",
      "dependsOn": [],
      "artifacts": [
        "docs/panel-window/panel-window-plan.md (owner reqs verbatim, mechanism recommendation PW-D1, architecture, phases)",
        "docs/panel-window/panel-window-progress.json (decisions PW-D1..PW-D8, steps)"
      ],
      "verify": "owner reads the plan; esp. PW-D1 (window.open instead of a second AppCore) matches the owner's intent",
      "startedAt": "2026-07-13T00:00:00Z",
      "finishedAt": "2026-07-13T00:00:00Z",
      "blockedReason": null,
      "evidence": []
    },
    {
      "id": "PW1.1",
      "phase": 1,
      "title": "Extract usePanelWindowState + PanelWindow.vue (floating/docked chrome) into MindWaveCore/ui/src/components/panel/ + '@panel' alias",
      "status": "done",
      "dependsOn": [
        "PW0.1"
      ],
      "artifacts": [
        "MindWaveCore/ui/src/components/panel/use-panel-window.ts: reactive per-panel {open, mode, prevMode, dockSize, floatRect} + setMode/setDockSize/setFloatRect, per-field localStorage persist, per-panelId instance cache; opts.storageKeys for legacy key names; PanelMode includes 'detached' (chrome renders nothing in that mode until PW3.1)",
        "MindWaveCore/ui/src/components/panel/PanelWindow.vue: titlebar (icon/title/drag), collapsed dock-menu (floating/left/right/top/bottom; detached item comes in PW3.1), close, float corner-resizer + dock edge-resizer, mode classes/geometry styles; #default + #footer slots; keydown reaches the root via attrs fallthrough (tabindex=0)",
        "quasar.config.ts (build.alias) + tsconfig.json (paths '@panel/*'): '@panel' alias (PW-D8)",
        "core i18n en/ru: 'panel' section (dockMenu/dockFloat/dockLeft/dockRight/dockTop/dockBottom/detach/close) — wording mirrors the fsBrowser dock labels"
      ],
      "verify": "bun run typecheck + build in MindWaveCore/ui",
      "startedAt": "2026-07-13T00:00:00Z",
      "finishedAt": "2026-07-13T00:00:00Z",
      "blockedReason": null,
      "evidence": [
        "vue-tsc --noEmit: 0 errors; quasar build: success (2026-07-13)"
      ]
    },
    {
      "id": "PW1.2",
      "phase": 1,
      "title": "FileOpenDialog migrates onto PanelWindow: split into chrome (PanelWindow) + FileOpenPanel.vue content; behavior 1:1",
      "status": "done",
      "dependsOn": [
        "PW1.1"
      ],
      "artifacts": [
        "GnauralCore/ui/components/FileOpenPanel.vue: panel content (unavailable banner, accordion/two-pane body, footer, selection/activate/favorites/init-on-mount, navigateToDir tree-reveal); emits open/close, no window logic",
        "GnauralCore/ui/components/FileOpenDialog.vue: thin wrapper = PanelWindow (@panel) + FileOpenPanel; floating-only auto-close after open; Ctrl-S filter shortcut via attrs-fallthrough keydown",
        "GnauralCore/ui/stores/file-open-panel.ts: useFileOpenPanelState(FILE_OPEN_PANEL_ID) with legacy mindwave-fs-browser-* keys (PW-D3, no migration)",
        "fs-browser store slimmed: FsWindowMode/FsFloatRect/windowMode/dockSize/floatRect/open + setters + their persist keys removed; FsEntryList isColumn reads panel.mode",
        "AudioPage: filePanel = useFileOpenPanelState(); v-if/teleport/dockWrapClass/openFileDialog on the panel state; v-model dropped",
        "GnauralCore i18n en/ru: unused fsBrowser dock* labels removed (chrome uses core panel.* keys)"
      ],
      "verify": "typecheck+build; manual: float drag/resize, all 4 dock sides + resize, open-state persists across reload — identical to before",
      "startedAt": "2026-07-13T12:30:00Z",
      "finishedAt": "2026-07-13T13:00:00Z",
      "blockedReason": null,
      "evidence": [
        "vue-tsc --noEmit: 0 errors; quasar build: success (2026-07-13)",
        "GnauralCore commit 3c4c269"
      ]
    },
    {
      "id": "PW2.1",
      "phase": 2,
      "title": "Child-window infrastructure: /panel/:panelId top-level route + PanelHostPage + panels[] in module manifests",
      "status": "done",
      "dependsOn": [
        "PW1.2"
      ],
      "artifacts": [
        "@panel/panel-registry.ts: PanelContribution {id, titleKey, icon, lazy component, events[]}",
        "modules/index.ts: optional panels[] on CoreModuleDescriptor + modulePanels aggregate",
        "routes.ts: top-level /panel/:panelId OUTSIDE MainLayout -> ui/pages/PanelHostPage.vue (page-loaders loadPanelHostPage)",
        "PanelHostPage.vue: resolves the content component by route param, full-window flex render, document.title from titleKey (locale-aware), close -> window.close(), declared events stubbed until PW2.2; panel.unknown banner for bad ids",
        "GnauralCore module.ts: panels [{id file-open, titleKey fsBrowser.title, icon folder_open, lazy FileOpenPanel, events [open]}]"
      ],
      "verify": "typecheck+build; manual: open '#/panel/file-open' directly in a browser tab -> the file panel renders full-window and browses",
      "startedAt": "2026-07-13T13:10:00Z",
      "finishedAt": "2026-07-13T13:40:00Z",
      "blockedReason": null,
      "evidence": [
        "vue-tsc --noEmit: 0 errors; quasar build: success; PanelHostPage-*.js + FileOpenPanel-*.js chunks present in server/public/assets (2026-07-13)",
        "commits: MindWaveCore 286d46c, GnauralCore b2ae516"
      ]
    },
    {
      "id": "PW2.2",
      "phase": 2,
      "title": "BroadcastChannel bridge: usePanelBridgeParent/Child (hello/heartbeat/parent-gone/child-ready/child-closed/panel-event/close-panel) + child self-close",
      "status": "done",
      "dependsOn": [
        "PW2.1"
      ],
      "artifacts": [
        "@panel/use-panel-bridge.ts (PW-D5): createPanelBridgeParent (hello on create + on child-ready, 2s heartbeat, parent-gone on pagehide, requestClose) and createPanelBridgeChild (child-ready, child-closed on pagehide, sendEvent panel-event, obeys close-panel)",
        "child self-close watchdog (PW-D2): parent-gone + 5s grace (F5 of the main window re-adopts in time via the new parent-hello) OR 7s heartbeat silence after adoption; a never-adopted (hand-opened debug) tab is left alone",
        "PanelHostPage: creates the child bridge per window, forwards the declared panel events over it, disposes on unmount; close still = window.close() (pagehide notifies the parent)"
      ],
      "verify": "typecheck+build; manual: open child tab, kill parent tab -> child closes itself after timeout; F5 parent -> child stays",
      "startedAt": "2026-07-13T13:50:00Z",
      "finishedAt": "2026-07-13T14:10:00Z",
      "blockedReason": null,
      "evidence": [
        "vue-tsc --noEmit: 0 errors; quasar build: success (2026-07-13)",
        "commit: MindWaveCore a1e5ee0",
        "NOTE: parent side is implemented but wired only in PW3.1 (detach opens the child) — the kill-parent/F5-parent scenarios are manually verified there"
      ]
    },
    {
      "id": "PW3.1",
      "phase": 3,
      "title": "Detached mode in PanelWindow: dock-menu item, window.open, prevMode return, main-side close, popup-blocked fallback",
      "status": "done",
      "dependsOn": [
        "PW2.2"
      ],
      "artifacts": [
        "PanelWindow: detached item in the mode menu (open_in_new, panel.detach); onModeSelect opens the child FIRST inside the click user-activation (#/panel/<id>, named mw-panel-<id>, sized from floatRect) and flips the mode only on success — blocked popup = stay put + Notify panel.detachBlocked (PW-D1 fallback)",
        "parent bridge in PanelWindow: onChildClosed -> return to prevMode (1s debounce cancelled by child-ready so a child F5 does not yank the panel back); onPanelEvent -> re-emitted as @panel-event to the host",
        "open=false while detached -> requestClose() (main-side close, PW-D6); onBeforeUnmount -> clean requestClose, mode/open persist so remount reopens",
        "onMounted auto-reopen of a persisted detached panel with in-window fallback + Notify (PW-D6)"
      ],
      "verify": "typecheck+build; manual: detach -> OS window opens, main reflows; close child -> panel returns docked/floating; close main -> child dies",
      "startedAt": "2026-07-13T14:20:00Z",
      "finishedAt": "2026-07-13T14:50:00Z",
      "blockedReason": null,
      "evidence": [
        "vue-tsc 0 errors; quasar build ok (2026-07-13)",
        "commit MindWaveCore 70b4237"
      ]
    },
    {
      "id": "PW3.2",
      "phase": 3,
      "title": "File-open events over the bridge: selecting a file in the detached window opens it in the main editor; auto-reopen on start",
      "status": "done",
      "dependsOn": [
        "PW3.1"
      ],
      "artifacts": [
        "FileOpenDialog @panel-event handler: bridged open {path, kind} goes through the same onOpen path as an in-window pick -> AudioPage.handleExternalFileOpen; detached keeps the panel open (like docked)",
        "startup auto-reopen lives in PanelWindow onMounted (PW3.1) — fires when the hosting page mounts with a persisted open+detached panel"
      ],
      "verify": "typecheck+build; manual: pick a file in the detached window -> it loads in the main editor; restart with detached open -> window reappears (or falls back with Notify)",
      "startedAt": "2026-07-13T14:50:00Z",
      "finishedAt": "2026-07-13T15:00:00Z",
      "blockedReason": null,
      "evidence": [
        "vue-tsc 0 errors; quasar build ok (2026-07-13)",
        "commit GnauralCore 3f5dc2a"
      ]
    },
    {
      "id": "PW4.1",
      "phase": 4,
      "title": "Detached geometry persist + polish (document.title, focus existing child instead of duplicates)",
      "status": "done",
      "dependsOn": [
        "PW3.2"
      ],
      "artifacts": [
        "use-panel-window.ts: PanelScreenRect {x,y,w,h} + detachedRectKey(panelId) (always the default mindwave-panel-<id>-detached-rect scheme so parent+child agree) + read/writePanelScreenRect (shared localStorage)",
        "PanelHostPage (child): captureGeometry writes screenX/screenY + innerWidth/innerHeight on resize and a 1s poll (no move event exists), guarded by a change signature; listeners/timer torn down on unmount",
        "PanelWindow (parent): openChildWindow reads the saved rect -> left/top/width/height window features; first detach falls back to floatRect size; named target mw-panel-<id> reuses a still-open child (dedup) instead of spawning a duplicate",
        "AppCore popup patch updated in tandem: features left/top anchor the CONTENT top-left (frame offset by non-client margins) so the persisted rect round-trips drift-free (AppCore d4df8d6)"
      ],
      "verify": "typecheck+build; manual: move/resize child, close+reopen -> same place/size",
      "startedAt": "2026-07-13T20:40:00Z",
      "finishedAt": "2026-07-13T21:05:00Z",
      "blockedReason": null,
      "evidence": [
        "vue-tsc --noEmit: 0 errors; quasar build: success (2026-07-13)",
        "commit MindWaveCore b8a49d8; AppCore popup patch rebuild d4df8d6",
        "owner manual verification pending: move/resize the detached window, close + reopen -> same place/size"
      ]
    },
    {
      "id": "PW4.2",
      "phase": 4,
      "title": "AppCore verification: default WebView2 popup behavior under AppMain.exe (incl. owner req. 6: NO address bar in the child window)",
      "status": "done",
      "dependsOn": [
        "PW4.1"
      ],
      "artifacts": [
        "manual run of AppMain.exe: detach opens a popup, closes with the main window, geometry restores, file-open round-trips",
        "owner req. 6: verify the default popup has NO address bar (expected: a bare WebView2 window with just a title bar). In a plain dev browser the bar cannot be hidden (anti-spoofing; location=no is ignored) — dev-only limitation, not a prod concern",
        "if the default popup turns out crippled (address bar present / features ignored): record findings + decide (owner) whether a minimal AppCore NewWindowRequested handler (own chromeless window) or plan B (second process) is needed"
      ],
      "verify": "owner checkpoint on the real AppCore build",
      "startedAt": "2026-07-13T15:30:00Z",
      "finishedAt": "2026-07-13T21:30:00Z",
      "blockedReason": null,
      "evidence": [
        "owner verified in AppMain.exe (2026-07-13): the WebView2 DEFAULT popup DOES show an address bar — the PW-D1 assumption (bare title-bar-only window) is wrong on this point; owner req. 6 (no address bar) is NOT met by the default popup -> plan B triggered",
        "finding: AppCore vendored webview/ is gitignored (pristine upstream, 0 tracked files) — a NewWindowRequested patch must be pinned either by committing the patched backend or by keeping a .patch applied in build_webview.bat (owner call)",
        "assessment: a win32_edge.hh add_NewWindowRequested handler (own chromeless Win32 window + CreateCoreWebView2Controller from the SAME environment, put_NewWindow + Handled, WindowFeatures size/pos, WM_SIZE bounds, WindowCloseRequested -> DestroyWindow, DocumentTitleChanged -> caption) keeps the same profile/browser process, so BroadcastChannel/localStorage/close-with-host all keep working and NO UI code changes; a second AppCore process would BREAK BroadcastChannel+localStorage across windows (separate WebView2 environments) and require a WS-based bridge redesign",
        "plan B IMPLEMENTED (2026-07-13): AppCore vendored-webview patch (win32_edge.hh NewWindowRequested -> bare native popup from the same environment; no address bar, document.title caption, opener icons, window.close support, default-popup fallback on early failure). AppCore repo: docs/popup-window/ (plan+ledger POP1.1 done, commits ccd1d0c + 9d00d7c; patch pinned on nested-repo branch appcore-patches b937913 + exported .patch). libwebview.dll + AppMain.exe rebuilt; MindWaveCore/server copy was blocked by the running exe — ExeAutoUpdate swaps binaries on next launch. Owner verification pending (restart MindWave, detach).",
        "owner verified in the exe (2026-07-13): detached window has NO address bar, correct title/icon; file picks reach the editor; close-main closes child; move/resize + reopen restores place & size drift-free — \"Всё работает как и ожидалось\""
      ]
    },
    {
      "id": "PW5.1",
      "phase": 5,
      "title": "i18n rename «Голоса расписания» → «Список треков» (key gtrackVoicesPanel → tracksListPanel; en 'Track list' / ru 'Список треков'); 4 refs in TracksPanel updated",
      "status": "done",
      "dependsOn": [
        "PW4.2"
      ],
      "artifacts": [
        "GnauralCore/ui/i18n/{en,ru}.json: key gtrackVoicesPanel renamed to tracksListPanel (en 'Track list', ru 'Список треков')",
        "GnauralCore/ui/components/TracksPanel.vue: 4 t('audio.gtrackVoicesPanel') → t('audio.tracksListPanel') (toggle button aria-label + tooltip; voices aside aria-label + title)"
      ],
      "verify": "bun run typecheck + build in MindWaveCore/ui; grep shows zero remaining gtrackVoicesPanel in code",
      "startedAt": "2026-07-14T11:30:00Z",
      "finishedAt": "2026-07-14T11:45:00Z",
      "blockedReason": null,
      "evidence": [
        "vue-tsc --noEmit: 0 errors; quasar build: success (2026-07-14)",
        "grep gtrackVoicesPanel: no matches in *.vue/ts/json code",
        "GnauralCore commit 2052804"
      ]
    },
    {
      "id": "PW5.2",
      "phase": 5,
      "title": "«Список треков» migrates onto PanelWindow (floating/docked): slide-over <aside> → <PanelWindow> + useTracksListPanelState('track-list'); hosted inside TracksPanel (shares gtracks); button toggles state.open; Esc/backdrop/anyOverlay rewired",
      "status": "done",
      "dependsOn": [
        "PW5.1"
      ],
      "artifacts": [
        "GnauralCore/ui/stores/track-list-panel.ts: useTracksListPanelState(TRACK_LIST_PANEL_ID='track-list') via usePanelWindowState — new panel, default mindwave-panel-track-list-* keys (no legacy mapping)",
        "TracksPanel.vue: the voices <aside> (currently :787-924) becomes <PanelWindow :state :title=t('audio.tracksListPanel') icon='queue_music'> with the list as default slot; dock-wrap flex container + Teleport-to-body-when-floating (mirror the AudioPage file-open host); remove the bespoke backdrop + gtrack-voices-panel transition + Esc handler; voicesPanelOpen ref replaced by the panel store open",
        "toggle button @click switches from voicesPanelOpen to tracksListPanel.open; :color reflects .open; anyOverlayOpen updated"
      ],
      "verify": "typecheck+build; manual: open the list, drag/resize floating, dock left/right/top/bottom + resize, open-state persists across reload; live voice edits still work (same app instance)",
      "startedAt": "2026-07-14T12:15:00Z",
      "finishedAt": "2026-07-14T13:00:00Z",
      "blockedReason": null,
      "evidence": [
        "vue-tsc --noEmit: 0 errors; quasar build: exit 0; 159/159 GnauralCore UI tests pass (2026-07-14)",
        "GnauralCore commit 132be4f (new stores/track-list-panel.ts; TracksPanel voice-list <aside> -> PanelWindow + dock-wrap; root scroll container moved to .tracks-panel__dock-inner so a docked panel reflows instead of scrolling away)",
        "owner manual verification pending: float drag/resize; dock all 4 sides + resize + content reflow; open persists across reload; live voice edits reflect in the tracks (same app instance)"
      ]
    },
    {
      "id": "PW5.3",
      "phase": 5,
      "title": "Toolbar button: move to RIGHTMOST (owner corrected 2026-07-14: far right of the Треки toolbar, after the settings gear — NOT leftmost before Zoom+) + remove the v-if displayMode==='gnaural' gate → always visible incl. plain wav/flac",
      "status": "done",
      "dependsOn": [
        "PW5.6c"
      ],
      "artifacts": [
        "TracksPanel.vue audio-page__spectrogram-header: relocate the track-list toggle q-btn to the LAST position (after the tune/settings button) so it is the rightmost control; drop v-if=\"audio.displayMode === 'gnaural'\" so it renders for decoded wav/flac too (the outer header already renders for both via showGtracks || hasSpectrogramData)"
      ],
      "verify": "typecheck+build; manual: button is the rightmost toolbar control; open a wav/flac -> button present and opens the list",
      "startedAt": "2026-07-14T16:15:00Z",
      "finishedAt": "2026-07-14T16:30:00Z",
      "blockedReason": null,
      "evidence": [
        "vue-tsc 0; quasar build exit 0; 159/159 UI tests pass (2026-07-14)",
        "GnauralCore commit a5debc0"
      ]
    },
    {
      "id": "PW5.4",
      "phase": 5,
      "title": "Add overall-waveform + overall-spectrum rows to the list (name + visibility eye); the list represents ALL tracks (voices + overall wave + overall spectrum)",
      "status": "done",
      "dependsOn": [
        "PW5.3"
      ],
      "artifacts": [
        "GnauralCore/ui/composables/use-overall-graphs.ts: useOverallGraphs() singleton (waveFolded/spectrumFolded + toggles, persisted to mindwave-tracks-overall-folded) so the tab + the AudioPage-hosted list toggle ONE source",
        "TrackListPanel.vue: two rows at the top of the list — 'Общая волна' (tracksOverallWave) + 'Общий спектр' (tracksOverallSpectrum), name + a visibility eye; separator before the voices",
        "TracksPanel.vue: overallWaveFolded/overallSpectrumFolded + toggles now alias the singleton (template unchanged); local refs + persist removed",
        "owner-flag: eye = FOLD (collapse) of the overall graph in the tab, NOT a view-mode remove — fold is an independent per-graph boolean (viewMode is a coupled 4-way enum with no 'none'). Say if full view-mode hide is wanted.",
        "audio-file (no voices) case: the list shows these two rows, so it is useful for plain wav/flac"
      ],
      "verify": "typecheck+build+tests; manual: rows appear with correct names; toggling the eye folds/unfolds the overall wave/spectrum in the tab; state shared with the tab's fold headers; works for gnaural + wav/flac",
      "startedAt": "2026-07-14T16:40:00Z",
      "finishedAt": "2026-07-14T17:00:00Z",
      "blockedReason": null,
      "evidence": [
        "vue-tsc 0; quasar build exit 0; 159/159 UI tests pass (2026-07-14)",
        "GnauralCore commit 16d953d",
        "fix (owner 2026-07-14): for audio files (wav/flac) hide the gnaural-only controls (bulk lane actions + point-drag mode) — only the overall wave/spectrum rows remain; isGnaural gate. GnauralCore commit 279f7dd"
      ]
    },
    {
      "id": "PW5.5",
      "phase": 5,
      "title": "Detached mode for «Список треков» — DISABLED (unsound as a separate app instance, PW-D11); a true version needs a remote-control redesign. OWNER CHECKPOINT",
      "status": "blocked",
      "dependsOn": [
        "PW5.6c"
      ],
      "artifacts": [
        "GnauralCore/ui/components/TrackListPanel.vue: standalone chrome-free content (mirror FileOpenPanel) reusable by both the in-window PanelWindow slot and PanelHostPage in the child window",
        "GnauralCore module.ts panels[]: add {id:'track-list', titleKey:'audio.tracksListPanel', icon:'queue_music', component: () => import('./components/TrackListPanel.vue'), events:[...]}",
        "cross-window data (PW-D9): a detached child has no loaded schedule/analysis — decide with owner: (a) bridge a voice-list snapshot + route each edit (visibility/mute/mode/in-mix) back over the channel like file-open's 'open' event, or (b) ship detached with a documented empty/limited list, or (c) leave detached out for this panel"
      ],
      "verify": "owner checkpoint: typecheck+build; manual: 'Separate window' opens the list; decide required detached fidelity",
      "startedAt": null,
      "finishedAt": null,
      "blockedReason": "Detached is unsound for this shared-state live panel (PW-D11): the child window is a separate app instance (same process, own JS realm) with no loaded file + its own gtracks clobbering the same localStorage. Hidden via PanelWindow allowDetach=false (MindWaveCore b7813f6, GnauralCore fa716a7). SUPERSEDED by PW5.7 (remote-control redesign, owner-approved 2026-07-14) — this step stays closed.",
      "evidence": [
        "owner 2026-07-14: 'Отдельное окно' opened a child window showing 'Неизвестная панель: track-list' (panel not registered in module.ts) -> root-caused to the deeper unsoundness (PW-D11); detach hidden for this panel via allowDetach"
      ]
    },
    {
      "id": "PW5.6a",
      "phase": 5,
      "title": "Shared gtracks singleton (useSharedGtrackLanes) — the Треки tab + the AudioPage-hosted panel edit ONE instance (re-host groundwork, PW-D10)",
      "status": "done",
      "dependsOn": [
        "PW5.2"
      ],
      "artifacts": [
        "use-gtrack-lanes.ts: useSharedGtrackLanes() — a detached effectScope(true) singleton of useGtrackLanes (no lifecycle hooks -> safe), fed the audio store schedule + displayFilePath; imports effectScope + useAudioStore",
        "TracksPanel.vue: const gtracks = useSharedGtrackLanes() (was useGtrackLanes(...)); no behaviour change"
      ],
      "verify": "typecheck+build+tests; no behaviour change",
      "startedAt": "2026-07-14T13:15:00Z",
      "finishedAt": "2026-07-14T13:40:00Z",
      "blockedReason": null,
      "evidence": [
        "vue-tsc 0; quasar build exit 0; 159/159 UI tests pass (2026-07-14)",
        "GnauralCore commit 2720dc0"
      ]
    },
    {
      "id": "PW5.6b",
      "phase": 5,
      "title": "Shared gtracks.saveEdits() + central save-first guard in AudioPage.applyScheduleVoiceStatePatches (PW-D10)",
      "status": "done",
      "dependsOn": [
        "PW5.6a"
      ],
      "artifacts": [
        "use-gtrack-lanes.ts: gtracks.saveEdits() (persist curve edits to the .gnaural; throws on error; reloads on success) + gtracks.saving; patchGnauralXml import; useAudioStore read at CALL time (Pinia-free creation for the unit tests)",
        "AudioPage.vue: const gtracks = useSharedGtrackLanes(); applyScheduleVoiceStatePatches saves dirty curve edits first (guard) before the voice-state patch reloads — covers every voice-patch caller (no-op when clean)"
      ],
      "verify": "typecheck+build+tests; existing mute/save behaviour preserved",
      "startedAt": "2026-07-14T13:45:00Z",
      "finishedAt": "2026-07-14T14:20:00Z",
      "blockedReason": null,
      "evidence": [
        "vue-tsc 0; quasar build exit 0; 159/159 UI tests pass (2026-07-14)",
        "GnauralCore commit 0d83a76"
      ]
    },
    {
      "id": "PW5.6c",
      "phase": 5,
      "title": "Extract TrackListPanel.vue + TrackListDialog.vue, host at AudioPage (full-height dock beside FileOpenDialog); revert PW5.2 in-TracksPanel dock-wrap",
      "status": "done",
      "dependsOn": [
        "PW5.6b"
      ],
      "artifacts": [
        "utils gtrack-voice-color.ts: voiceDotColor + fallback palette extracted (shared by TracksPanel + TrackListPanel)",
        "TrackListPanel.vue: voice-list body (bulk actions + per-voice rows + point-drag mode) using useSharedGtrackLanes + audio store; emits patch-voice-state/-batch (+ close); mute just emits (AudioPage guard saves-first)",
        "TrackListDialog.vue: <PanelWindow :state=tracksListPanel><TrackListPanel .../></PanelWindow> (mirror FileOpenDialog)",
        "AudioPage.vue: host TrackListDialog in the dock-wrap beside FileOpenDialog (Teleport when floating) + @patch-voice-state(-batch); tracksListPanel = useTracksListPanelState(); dockWrapClass covers both panels",
        "TracksPanel.vue: REVERT PW5.2 dock-wrap/dock-inner + root-scroll override; remove the PanelWindow + list body (now in TrackListPanel) + onToggleVoiceMuted; keep the header button toggling tracksListPanel.open (shared store)"
      ],
      "verify": "typecheck+build+tests; manual: open the list, dock left/right -> spans FULL editor height like Открыть файл; switch tabs -> panel persists; mute/mode/in-mix from the panel reflect in the tracks; mute with unsaved curve edits saves first",
      "startedAt": "2026-07-14T14:35:00Z",
      "finishedAt": "2026-07-14T15:00:00Z",
      "blockedReason": null,
      "evidence": [
        "vue-tsc 0; quasar build exit 0; 159/159 UI tests pass (2026-07-14)",
        "GnauralCore commit f70bb1b (new gtrack-voice-color.ts + TrackListPanel.vue + TrackListDialog.vue; AudioPage hosts it in the dock-wrap beside FileOpenDialog; TracksPanel reverted to no in-tab dock)",
        "owner manual verification pending: header button opens «Список треков»; dock left/right spans FULL editor height like «Открыть файл»; switch tabs -> panel stays; mute/mode/visibility/in-mix reflect in the tracks; mute with unsaved curve edits saves first"
      ]
    },
    {
      "id": "PW5.7",
      "phase": 5,
      "title": "True detached «Список треков» via remote-control: the child renders a parent-pushed snapshot and bridges every action back (no child-side gtracks/persistence); re-enable detach for the panel",
      "status": "in-progress",
      "dependsOn": [
        "PW5.4"
      ],
      "artifacts": [
        "DECOMPOSED (2026-07-14): PW5.7a bridge state-push infra = DONE (MindWaveCore 6b68680); PW5.7b = extract TrackListView (pure snapshot view) + TrackListPanel becomes the in-window adapter (parity); PW5.7c = TrackListRemote child adapter + TrackListDialog snapshot push & @panel-event mapping + module.ts registration + re-enable detach",
        "@panel/use-panel-bridge.ts: new parent->child 'panel-state' message (parent.sendState(payload) on child-ready + on change, child.onState) alongside hello/heartbeat — the snapshot channel; file-open unaffected (never sends state) [PW5.7a DONE]",
        "TrackListPanel split into a pure view + two adapters: TrackListView.vue (dumb: props = serializable snapshot {voices:[{id,label,type,preparse,mode,visible,muted,inMix}], allLanesHidden, pointDragMode, overallRows}, one emit per action) + in-window adapter (binds useSharedGtrackLanes directly — current behaviour) + child adapter TrackListRemote.vue (renders ONLY the bridged snapshot; every action -> bridge panel-event; NO gtracks, NO mindwave-gtrack-* localStorage writes; fix-preparse confirm dialog runs child-side, the confirmed action bridges)",
        "parent side (TrackListDialog): watch shared gtracks + audio.gnauralSchedule -> debounced snapshot push; @panel-event maps each action onto the authoritative gtracks (mute via AudioPage applyScheduleVoiceStatePatches with the PW5.6b save-first guard)",
        "GnauralCore module.ts panels[]: register {id:'track-list', titleKey:'audio.tracksListPanel', icon:'queue_music', component: TrackListRemote, events:[voice-mode, voice-visible, voice-mute, voice-inmix, merge-all, spread-all, all-modes, hide-all, all-in-mode, drag-mode, fix-preparse, overall-row toggles]}",
        "TrackListDialog: drop :allow-detach=\"false\" + the stuck-detached-mode reset (PW-D11 unsoundness resolved by design: the child holds no state)",
        "dependsOn PW5.4 because the snapshot schema includes the overall wave/spectrum rows added there (avoid a schema break right after shipping)"
      ],
      "verify": "typecheck+build+tests; manual: detach opens the OS window with the LIVE list; child edits reflect in the main tracks instantly and vice versa; close child -> panel returns to prevMode; parent F5 -> child re-adopts and re-receives the snapshot; devtools: zero mindwave-gtrack-* writes from the child window",
      "startedAt": "2026-07-14T18:30:00Z",
      "finishedAt": null,
      "blockedReason": null,
      "evidence": [
        "PW5.7a DONE (2026-07-14): @panel bridge sendState/onState + PanelWindow bridgeState prop + PanelHostPage passes :bridge-state to the content; file-open unaffected. vue-tsc 0, quasar build ok, 180/180 tests. MindWaveCore commit 6b68680"
      ]
    },
    {
      "id": "PW5.8",
      "phase": 5,
      "title": "Full-hide for the OVERALL waveform/spectrogram (incl. header bars), gtrack-analogous: Ctrl-click ANY L/R channel eye (owner-corrected 2026-07-14: NOT the fold header) + the list eye hide FULLY (not just fold); restore via the hidden-tracks dropdown",
      "status": "done",
      "dependsOn": [
        "PW5.4"
      ],
      "artifacts": [
        "use-overall-graphs.ts: add waveHidden/spectrumHidden (+ setters), persisted alongside folded in mindwave-tracks-overall-folded ({wave,spectrum,waveHidden,spectrumHidden}; back-compatible — old data has only wave/spectrum)",
        "TracksPanel.vue: gate the overall waveform/spectrum <template v-if> with && !overallWaveHidden / && !overallSpectrumHidden so the WHOLE stack INCL. the fold header bar is removed; the fold chevron @click -> handler: Ctrl/Cmd-click sets hidden=true, plain click toggles fold; add the hidden overall graphs to hiddenTrackList (top 'Скрытые дорожки' dropdown) with restore=setHidden(false), like hidden lanes",
        "TrackListPanel.vue: the overall-row eye toggles HIDDEN (full hide) instead of folded; icon reflects hidden (supersedes the PW5.4 fold-eye)",
        "point 1 (owner): Ctrl-click the overall wave/spectrum fold control -> full hide incl. header (plain click still just folds). point 2 (owner): the list eye fully hides these graphs, not just folds. Full analogy with the gtrack lane hide."
      ],
      "verify": "typecheck+build+tests; manual: Ctrl-click the overall fold header -> the whole graph incl. header disappears + shows in the 'Скрытые дорожки' dropdown -> restore works; the list eye fully hides/shows (not fold); plain click on the fold header still only folds",
      "startedAt": "2026-07-14T17:45:00Z",
      "finishedAt": "2026-07-14T18:00:00Z",
      "blockedReason": null,
      "evidence": [
        "vue-tsc 0; quasar build exit 0; 180/180 UI tests pass (2026-07-14)",
        "GnauralCore commit 228063c (use-overall-graphs waveHidden/spectrumHidden; hiddenTrackList restore + template gates; TrackListPanel eye -> full-hide)",
        "trigger correction (owner 2026-07-14): the hide trigger is Ctrl/Cmd-click on a CHANNEL eye (WaveformView/SpectrogramView now pass the MouseEvent with 'hide') -> hides the whole overall graph; the Ctrl-on-fold-header logic was reverted (the chevron just folds). GnauralCore commit f87eec6"
      ]
    }
  ]
}
