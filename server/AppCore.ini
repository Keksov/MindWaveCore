# AppCore branding (UTF-8).
# AppTitle = taskbar window title. Custom icon: place AppCore.ico next to the exe,
# or set AppIcon=<base64 of an .ico> here (used only when AppCore.ico is absent).
AppTitle=MindWave
